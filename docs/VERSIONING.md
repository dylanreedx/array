# Versioning

How Array is versioned and the ledger of every shipped release. The release
runbook is `RELEASE.md` (repo root) — follow it exactly when shipping; this
file is the scheme, the rules, and the history. Program history and rationale:
`docs/38-tickets/95-go-live.md`.

## Scheme

Two numbers, two audiences:

- **Marketing version** `X.Y.Z` (`CFBundleShortVersionString`) — for humans:
  release titles, `Array-X.Y.Z.dmg` filenames, the website, release notes.
- **Build number** `N` (`CFBundleVersion`) — a monotonic integer, for Sparkle.
  **This is the field Sparkle compares** to decide whether an installed app
  should update. It increases by exactly 1 every shipped release and is never
  reused or decreased — a repeated or lower build number silently breaks
  auto-update for every installed copy already in the field.

The two move together: every shipped `X.Y.Z` gets the next build number
(0.2.2 was build 4, 0.3.0 was build 5; the next release is build 6).

## Rules

- **Never commit a version bump to `Packaging/Info.plist`.** It stays at its
  dev defaults (0.1.0 / build 1); real versions are stamped at release time by
  `scripts/release-app.sh --set-version X.Y.Z --set-build N`. Why: the repo
  has one source of truth for what shipped (this ledger + GitHub releases),
  and dev builds stay visibly distinct from releases.
- **Both DMG assets on every GitHub release** (`dylanreedx/array-releases`):
  `Array.dmg` — constant name, the site's
  `releases/latest/download/Array.dmg` URL depends on it — and
  `Array-X.Y.Z.dmg` — the permalink the appcast enclosure points at.
- **Regenerate and ship the appcast after every release:**
  `scripts/generate-appcast.sh` rewrites `website/public/appcast.xml` (signed
  with the Keychain EdDSA key); commit it and push to `main`. Why: Vercel
  deploys arrayapp.dev from `main`, and installed apps poll
  `https://arrayapp.dev/appcast.xml` — until it deploys, no one sees the
  update.
- **Archive every shipped DMG in `releases/`** (gitignored, repo root). Why:
  the appcast is generated from it.
- **Never commit:** the Sparkle private EdDSA key (Keychain-only — back it
  up), `releases/`, `.env`, QA stores, signing material.
- **Append a row to the ledger below on every release.** Why: this table is
  how future sessions know which build number is next.
- **Branch topology:** work lands on `array/integration`, fast-forwards to
  `main`.
- **Identity:** the app is Array, bundle id `dev.arrayapp.macos`. Internal
  module names (`ContinuumRevived*`) are deliberately unchanged — never rename
  them; legacy fixtures and historical docs keep saying Continuum. Why: the
  go-live shipped the *external* identity cut only; the source migration is a
  separate, careful program (see the go-live doc's rename history).
- **Git identity:** commits only under Dylan's identity — no AI-attribution
  trailers, no Co-Authored-By.

## Release history

One row per shipped release. Appending here is part of every release.

| Version | Build | Date       | Tag      | Notes |
|---------|-------|------------|----------|-------|
| 0.1.0   | 1     | —          | —        | Dev default in `Packaging/Info.plist`. Never shipped. |
| 0.2.0   | 2     | 2026-08-09 | `v0.2.0` | First public release: notarized Developer ID DMG, arrayapp.dev download button. Pre-Sparkle — these installs cannot self-update and make one final manual download. |
| 0.2.1   | 3     | 2026-08-09 | `v0.2.1` | First appcast-backed release: Sparkle auto-update live (`arrayapp.dev/appcast.xml`), thin-GUI-PATH fix for claude/codex resolution, first-run Environment Setup panel, Help menu (Environment Setup…, Report a Problem…). |
| 0.2.2   | 4     | 2026-08-09 | `v0.2.2` | Provider>model picker (two-pane, t3-ported) in tiles + Settings with display names from pi's catalog; live model catalogue with throttled re-probes; onboarding pi + per-provider auth rows (CLI login only); dev/prod channel split; Settings ▸ Agents default wording. First release colleagues are pointed at. |
| 0.3.0   | 5     | 2026-08-10 | `v0.3.0` | Agent-harness picker (Settings ▸ Agents): managed agents run on Claude Code, Codex, or pi — model list filters to the choice, all on the CLI's own login (never API keys). Codex + Claude Code CLI backends (own-subscription, pi-free); transcript resume rehydrates a tile's prior conversation on relaunch; onboarding "at least one harness" fresh-setup. Fixes: markdown tables render (no more "Unsupported content"), context meter shows token counts for claude/codex, rehydrated tool cards show the command, custom-folder Home updates the header. |
| 0.4.0   | 6     | 2026-08-10 | `v0.4.0` | Closing an agent tile parks the agent in **History** (collapsed sidebar section) instead of leaving an "Unconfirmed" row forever; clicking a History row reopens and resumes the agent. A working agent stays in the live list when its tile closes. Archive became the reversible verb (parks + takes the tile) and Delete the only destructive one. Live status moved onto the prism gyro with a running clock; the footer keeps only attention states and is silent otherwise. The context ring now fills from real per-provider token usage. Drag/paste md/pdf/txt/code onto a composer as `@/path` references. Fixes: an agent whose tile was gone had an unclickable sidebar row. |
| 0.4.1   | 7     | 2026-08-10 | `v0.4.1` | Context meter fix for codex tiles. The ring divided codex's CUMULATIVE session input by pi's catalogue window and read up to 237%; occupancy now comes from codex's per-request `last_token_usage` and the window from the `model_context_window` codex itself reports (258,400 for gpt-5.6-sol, where the catalogue said 272,000). The `token_count` event that carries both arrives repeatedly DURING a turn, so the ring fills while the agent works instead of jumping at turn end. claude/pi unchanged. |
| 0.4.2   | 8     | 2026-08-11 | `v0.4.2` | Completes the 0.4.1 codex context-meter fix. 0.4.1 read codex's `token_count`, which exists only in codex's rollout log — `codex exec --json` emits thread.started/turn.started/item.completed/turn.completed and nothing else — so the handler never fired and the ring lost its fill entirely (bare "15.1k"). `turn.completed.usage` is cumulative for the session (measured 15,005 then 30,026 across two turns), so occupancy is now the per-turn DELTA, with the baseline carried on the record so it survives a runner process that lives for one turn. A resumed thread with no stored reading publishes no occupancy rather than the unbounded figure. |
| 0.4.3   | 9     | 2026-08-11 | `v0.4.3` | **⌘K is one canonical floating command center.** Entity-first titles instead of raw enum copy, categorized results (Recent, Agents & Tiles, Workspaces & Projects, Actions, Create, Developer), a curated 12-item empty-query home, and typed search that reaches model/status/attention metadata (`gpt 5.6` finds the named session) without that metadata becoming command identity. Needs You comes from the supervisor's real unresolved approval/input request, not an unread heuristic. Recents are outcome-aware: a refused or cancelled selection writes nothing. New Agent drills forward in-card to the live backend-filtered exact model catalogue; Escape restores the root query. The surface is a 660-point floating panel with Solid/Frosted/Glass/Custom appearance (Frosted at 84% by default, bounded custom opacity in Settings), Reduce Transparency forcing Solid and Increase Contrast strengthening the scrim. Dispatch identity is unchanged. **Managed-agent composer completions:** semantic `@` file navigation over a bounded checkout index — fuzzy match, directory navigation, ignore/symlink/deletion handling — accepted as structured file references rather than inserted strings. **Codex context occupancy stops being an estimate.** 0.4.2's per-turn DELTA of the cumulative `turn.completed.usage` is replaced by the exact figure read from codex's rollout log: `event_msg/token_count` carries `last_token_usage.total_tokens` (the prompt on the last request) and `model_context_window` (that request's provider-stated limit), so both halves of the ring come from codex instead of from arithmetic plus pi's catalogue. The runner joins the reading after process exit from a bounded tail read at the byte offset the run started at, and orders it ahead of the terminal event so one coherent final state persists. `codexTurnUsage` is now never occupancy — it stays the row's accounting total only — and records still carrying the old bogus used/max pair are repaired from the rollout on relaunch, or stripped when it is gone. Also: panning the canvas no longer repaints tile chrome. `layoutTile` re-assigns `view.tile` every camera event, which unconditionally marked the title bar dirty, so a trackpad pan re-rasterized every tile's title text on every frame; invalidation is now gated on the drawn value changing (witnessed at 0 redraws across 90 pan steps, while a rename and a zoom past the chrome floor still repaint). |
| 0.4.4   | 10    | 2026-08-11 | `v0.4.4` | **A restored Codex agent shows its transcript again.** Rehydration shipped for Claude and Pi in 0.3.0; the Codex backend landed right after and never got a reader, so `ManagedTranscriptRehydrator` checked only the Claude UUID file and the Pi derived-session file. A native Codex agent matched neither, so the tile rendered the bare "Previous session — send a prompt to continue." notice with no transcript behind it — while the whole conversation sat in codex's rollout log the entire time. Adds exact rollout location by persisted thread id (archived sessions included, ambiguity abstains), rollout JSONL normalized into the existing provider-neutral message model, bounded tail reading behind the existing "earlier history not shown" disclosure, and reconstruction of user, assistant, reasoning-summary and tool-lifecycle items. Display-only like the Claude/Pi path, and the next prompt still resumes the same provider thread. Also: **closed agents are now reachable from ⌘K.** A closed agent has no tile, so the command center's tile-derived rows could never represent one and History existed only in the workspace sidebar; there is now a History section keyed by agent id, ordered last and never volunteered onto the empty-query home, whose membership comes from the same `InboxSort` rule the sidebar's own History header uses. Selecting one reuses the inbox's reveal path whole, so it reopens the record, gives the agent a tile again, clears the unread mark and arms focus in that order. |
| 0.4.5   | 11    | 2026-08-11 | `v0.4.5` | **The workspace sidebar is gone; ⌘K is the only navigation surface.** It could not simply be deleted: its agent inbox was a CHILD of it, and the inbox was the only way to reach an agent with no canvas tile — so removing it first would have stranded those agents' records, transcripts and worktrees. ⌘K's agent rows came from tiles ON THE CANVAS, and "closed" is not the only tile-less lifecycle (`AgentInventory` unions tiled AND headless records; the row builder's locked decision is that `displayName` survives the tile because the agent is the entity). So the command center now offers every agent without a canvas tile, keyed by agent id: closed ones in a **History** section ordered last and never volunteered onto the home, and headless/snoozed/settled ones alongside the other agents — promoted to **Needs You**, default-visible, when they are blocked on you. Membership is asked of `InboxSort.section(for:now:)`, the same function the old inbox asked, so the two can never disagree. Opening one reuses the inbox's reveal path whole. View ▸ Show Workspace Sidebar (⌘⇧S) and the palette's "Activity dock" entry are gone rather than left pointing at nothing; three witnesses were retired **by inversion** (the mount must produce no sidebar, the toggle must not be palette-discoverable, the menu must not offer the item) so re-introducing it fails a check instead of shipping. **A new managed agent inherits the Home you are working in:** spawning read `activeProject.rootPath` unconditionally, so setting an agent's Home never carried to the next one; precedence is now explicit action → selected agent → context gravity → active project, via a pure resolver with no process-cwd rung, and an unusable Home still refuses rather than falling back. |
| 0.4.6   | 12    | 2026-08-11 | `v0.4.6` | **⌘K is finally a floating menu, and the model list names its providers.** Two user-visible defects that survived 0.4.5 because their witnesses never drove production. (1) ⌘K rendered full-height against the window edge — reported as "the sidebar still opens with ⌘K", and the reason removing the real sidebar changed nothing: `makeWorkspaceContentView` returned the **NSSplitView** as the window's `contentView`, and every overlay adds itself to `contentView`, so the split view adopted the palette as a *pane* and overwrote its frame on the next layout pass. The redesign's centred 660×520 float was computed correctly and then discarded. The mount now returns a plain `NSView` container holding the split view; a plain view does not lay out its children, so an overlay keeps the frame it asks for. `--palette-duplicate-root-check` had passed throughout because it builds its own plain host and never touched the real content view — the inverted mount check now also asserts the content view is **not** an `NSSplitView`. (2) "Only Anthropic models" was never a filter: the backend resolves to pi with `allowedProviders: nil` and the catalogue holds 16 anthropic + 7 openai-codex, but `pi --list-models` returns every anthropic row before the first codex one, so a single "Choose Model" header showed a screenful of Claude with no evidence anything followed. Each provider's block is now headed in catalogue order (a single-provider list keeps the plain header). Also removed the top bar's ☰ "Toggle workspace sidebar" button — the last visible control still pointing at the unmounted surface. |
| 0.4.7   | 13    | 2026-08-11 | `v0.4.7` | **Settings' model picker lists every provider.** 0.4.6 added provider headings to ⌘K's model step, but Settings ▸ Agents ▸ Default Model is a different control — `ProviderModelButton` → the two-pane `ProviderModelPickerView` — which still showed ONLY the selected model's provider, with the rest reachable only by clicking a 36-point rail button whose rows carry no provider text at all. With an anthropic default that opens on Anthropic, so "only Anthropic models" survived 0.4.6 on that surface. Nothing was ever filtered: the backend resolves to `.pi` with `allowedProviders: nil`, the catalogue holds 16 anthropic + 7 openai-codex, and `pi --list-models` returns both under the app's augmented PATH — a codex user simply could not see their models. The right pane now lists every provider's models, each block behind a disabled header row (`ChoiceListView` already skips disabled rows for focus and keyboard movement, so a header reads as a label and can never be committed as a model). The rail stays but JUMPS — picking a provider focuses its first model instead of hiding the others. The pane is sized for that one list with a 420pt height cap, so a large catalogue scrolls instead of growing past the screen; a single-provider catalogue gets no headers. |
| 0.4.8   | 14    | 2026-08-11 | `v0.4.8` | **A managed agent tile is named after the model it runs.** Every managed tile's chrome read "Agent · GPT-5.6" no matter which model was chosen: the title was the literal string `"GPT-5.6"` in `TileSpawner`, as was the bootstrap line "Ready. Type a prompt below to run GPT-5.6 in this tile." The composer's picker was correct and the header contradicted it. The title now comes from the model the tile will actually run, resolved from the same defaults the spawn itself uses so the header cannot disagree with what gets routed; ⌘K's model drill-down applies its choice after the tile exists, so that path renames through one shared helper instead of leaving the default model's name in the chrome. Also adds the session handoff (`.plans/11-session-handoff-2026-08-11.md`) recording the four ways a green check coexisted with a broken app across 0.4.3–0.4.7. |
| 0.4.9   | 15    | 2026-08-11 | `v0.4.9` | **Fixes the frozen model picker shipped in 0.4.7.** 0.4.7 put every provider's models in one pane behind per-provider headers — but `ChoiceListView` is a plain view sized by `intrinsicContentSize` and does **not** scroll, so a list taller than its 420pt-capped pane drew rows nobody could reach; and because the same change turned the provider rail from *filtering* into *focusing*, clicking a provider looked like it did nothing either. Two defects from one edit, and no check caught either. Reverted to the per-group rail, which is the behaviour that worked: the rail filters, every row fits, GPT models are one rail click away. Adds the witness that would have caught it — the list's intrinsic height must fit the pane the picker computed, so any future "show more in one pane" change has to add real scrolling first. Codex models remain plainly visible in ⌘K's New Agent step, which got provider headings in 0.4.6 and is unaffected. Also carries the dogfood playbook (`.plans/12-dogfood-playbook.md`). |
| 0.4.10  | 16    | 2026-08-11 | `v0.4.10` | **The workspace sidebar is back.** 0.4.5 removed it in favour of ⌘K alone; this restores the mount, the top bar's ☰ toggle, the palette's "Activity dock" command, and the four witnesses that had been inverted to assert its absence. The reachability work done to make the removal safe is **kept**: ⌘K still offers every agent with no canvas tile — closed ones under **History**, headless/snoozed/settled alongside the rest, and any agent blocked on you promoted to **Needs You** — so agents are now reachable from both surfaces and nothing depends on the sidebar to be found. Also carries the ⌘K float fix, provider headings in ⌘K's model step, model-named agent tiles, and the reverted-and-witnessed Settings picker from 0.4.6–0.4.9. |
| 0.4.11  | 17    | 2026-08-12 | `v0.4.11` | **The app no longer freezes when a restored Codex agent's tile comes back.** Rehydrating a Codex agent's previous transcript had to find its rollout in `~/.codex/sessions`, and it read the first line of EVERY rollout there one byte per `read()` syscall. That line is ~18 KB and a working corpus is hundreds of files: 688 rollouts here total 15,513,682 bytes of first lines, so one lookup cost 15.5 million syscalls — measured at 97% CPU for 90 seconds with the main thread blocked, and macOS filed five `cpu_resource` reports for Array on 2026-08-11 all naming that stack (`rehydratePreviousSessionOrNotice` → `ManagedTranscriptRehydrator.rehydrate` → `CodexRolloutLocator.locate` → `readSessionMeta` → `NSFileHandle.read(upToCount:)`). Now read in one bounded chunk, with candidates narrowed by filename first (Codex names a rollout after the session id it carries) while the id is still confirmed from the file's own `session_meta`, so a coincidental name cannot win and an archived duplicate still fails closed. `CodexAgentStateReader` carried the same loop over `session_index.jsonl`, re-read whole on every status poll per Codex tile; it reads once now. Witnessed in `ContinuumRevivedCoreChecks`: 401 padded rollouts whose filenames deliberately omit the id, forcing the full scan — per-byte loop measured 3.32 s at 201 files, the chunked read does 401 in 0.107 s against a 1 s budget. |
| 0.4.12  | 18    | 2026-08-12 | `v0.4.12` | **Pasted and dropped images reach the composer again — every one had been silently discarded.** The AppKit side identifies image data the way ImageIO does, with a UTType identifier (`CGImageSourceGetType` → `public.png`, which is also what `ComposerImagePasteboardDecoder.acceptedContentTypes` gates on). Core's managed attachment store is platform-neutral and validates an `image/*` MIME type instead. `AgentComposerView.importImages` passed `item.contentType` — the UTI — straight into `AgentComposerImageValidation`, which threw `imageInputNotValidated` inside the `do` block whose `catch` increments the failure count, so the refusal happened before any store call and every pasted or dropped image was counted as `attachment-store-rejected` and dropped with nothing shown. The composer was the only caller feeding that seam a UTI; the other six hardcode `image/png`/`image/jpeg`. Fixed with one translation at the boundary (`managedContentType` = `UTType(contentType)?.preferredMIMEType ?? contentType`), leaving the decoder's UTI contract untouched. Also repairs the rail chip's type label, which read a generic "Image" for stored attachments because it only understood UTIs — unreachable until images could land at all. `--composer-image-components-check` is now registered in `scripts/run-matrix.sh`; it had never been in the gate, which is why it could fail from the day it was written. |
| 0.4.13  | 19    | 2026-08-12 | `v0.4.13` | **⌘K resolves the agent model once, zone jumps stop stranding the keyboard, and a reveal stops zooming out.** ⌘K created the tile on the *default* model and corrected the record after attach, so a tile could read "GPT-5.6 Sol" while its composer said "Claude Opus 5"; the choice now threads into tile, record and first runner before attach, through one seam on `TileSpawner` that the witness drives by behavior rather than by scanning source for a call-site string (a reviewer defeated the old scan with `?? resolvedFromDefaults()` while it stayed green). A model that has left the live catalogue — `AgentModelCatalog.apply(listModelsOutput:)` replaces `liveOptions` on a background probe while the palette holds a snapshot — is now REFUSED out loud before any tile exists, restoring the P0.10 validation that `setProviderSettings` enforced and `spawn`/`makeAgent` never had. Inbox reveal titles its tile from the agent's own record (the path left with no in-app remedy once `renameManagedAgentTileForModel` was deleted), and a duplicate model id from pi can no longer trap ⌘K. Navigation: a zone jump left input scope on the pre-jump tile, which the new camera then moved off screen — keystrokes went to a tile the user could not see; every zone route now lands on canvas navigation, except `fitNavZone` (ordinal/next-previous), which keeps nav mode's keyboard because nav mode only receives keys while it owns `activeSurface`. `revealWorkViewport` clamped the *current* zoom to `maxJumpZoom`, so revealing or Space-previewing from closer than 1.25× zoomed OUT; the clamp now bounds only the zoom the policy asks for. Hold-⌥+Return and numpad Enter both activate the current tile. Verification: `scripts/run-matrix.sh` was bare calls under `set -e`, so the first documented KNOWN-RED aborted it — a real run reached **4 of 135 app legs**, which is how `--composer-image-components-check` failed from the day it was written unnoticed; it now classifies and continues, and this release is gated by **148 legs, 7 KNOWN-RED, 0 regressions**. |
| 0.4.14  | 20    | 2026-08-12 | `v0.4.14` | **A screenshot pastes into an agent composer.** ⌘V beeped for a screenshot while an image copied from a browser pasted fine — same code, different pasteboard. `ComposerTextView` overrode `paste(_:)` but not its VALIDATION: AppKit asks the responder whether `paste:` is valid before sending it, and a plain-text NSTextView (`isRichText = false`, `importsGraphics = false`) answers by checking whether the board holds a type IT can read. Probing the real pasteboard, a macOS screenshot is one item carrying `public.png` and nothing else, so the action was disabled, ⌘V beeped as an unhandled key equivalent, and `paste(_:)` — with the image intake inside it — never ran; a browser's "copy image" also writes HTML/text flavours, so validation passed there. The decoder was never at fault: driven against the actual screenshot bytes it reported `public.png`, 448x182, accepted, supported, `image/png`. Overrides `validateUserInterfaceItem(_:)` to enable `paste:` whenever `ComposerPasteboardIntake` finds something and defer to super otherwise, with the action and its validation routed through one injectable pasteboard so they cannot disagree. Witnessed RED ("Paste stayed DISABLED for an image-only pasteboard, so a pasted screenshot only beeps") against a fixture pinned to the real shape — one item, `[.png]`, no text companion. The first negative assertion written for it drove `paste:` on an empty board, where the override defers to super and super reads the process-global pasteboard; it passed by hand and failed in the matrix minutes later because the clipboard had changed. The reworked gate caught that flake — the first defect it found that was not already known — and it was replaced with two deterministic halves. |
| 0.4.15  | 21    | 2026-08-12 | `v0.4.15` | **Opening a project file lands in the project you are looking at, Markdown renders natively, and an agent's file links open beside it.** Opening a file resolved nothing at invocation time and three faults compounded. `ZoneRuntimeController.tileSpawner` was `weak` while `WorkspaceRuntime.install`/`switchWorkspace` built the arriving project's spawner as a LOCAL — it deallocated before the switch returned, so after an in-process workspace switch the active controller had no spawner at all. `AppDelegate` held the boot spawner forever, so every app-level spawn addressed the project the app launched with. And `setZones` leaves the flat `canvasState.tiles` holding the DEPARTED project's tiles with `activeZone` (a `let`) pinned to its placement, so `install(tileView:for:)` appended there, framed the tile against a zone no longer on screen, and persisted it through the boot project's store. The controller now owns its spawner strongly, `AppDelegate.tileSpawner` resolves the active one, and `CanvasNSView.installProjectTile` routes a new project tile into whichever model owns the active project — the `ZoneLayer` when one does (ZONE-LOCAL frames), the flat path at boot (world frames) — persisting through that project's own store. Command Center, file-tree activation and canvas drop share one route, `WorkspaceRuntime.openProjectFile`, which focuses on success, reveals an already-open file rather than duplicating it, and surfaces failures instead of beeping. `.md`/`.markdown` open in a native rendered Preview built on `MarkdownAgentMarkupParser` plus the existing block renderers with a tile-local Preview/Source switch — no WebKit, no second Markdown dependency, no persisted mode; other files keep the monospaced horizontally scrolling source view. An explicit Markdown link to a local file in a managed agent's answer opens that file 24pt right of the responding tile, top-aligned, revealing `:line:column`; `AgentLinkPolicy` gained `.openLocalFile`, a syntactic classification that authorizes nothing, and the RAW destination is resolved against that agent's live `cwd` (never the process cwd, never the active project) with navigation metadata stripped, both sides canonicalized, path-COMPONENT containment and an existing-regular-file requirement, so a worktree agent opens its own copy and cannot reach outside its checkout. Witnessed by `--file-open-active-context-check` (RED before the repair on BOTH the spawner ownership and the install target — verified by reverting each in turn), `--file-markdown-preview-check`, `--agent-local-file-link-check`, `runAgentLocalFileLinkChecks` in CoreChecks, and a rewritten `LinkPolicyChecks` negative witness that mutates the local-file branch to `.openExternally` in an isolated package and requires exit 1. Gate: 149 legs, 5 KNOWN-RED (`--component-lab-check` and `--ui-baseline-check` skipped as display-dependent), 0 regressions. The first gate run found two defects in the new checks, one of them a real resolver bug: a checkout root URL built without `isDirectory: true` has no trailing slash, so relative resolution dropped its last component and every file read as outside the checkout. Terminal, note, browser and agent spawns still call `install` + `saveCanvas` and carry the original bug after a switch — recorded in the backlog and as AGENTS.md hazard 9. |
| 0.4.16  | 22    | 2026-08-12 | `v0.4.16` | **Fixes the freeze-then-death 0.4.15 shipped: one AppKit view per semantic block.** Reusing the transcript renderers for the file Markdown document means one AppKit view with its own TextKit stack per block — right for a transcript, ruinous for a file `FilePreview` admits at up to 1 MB. Measured on the real renderer: `docs/VERSIONING.md` (28 KB) is 11 blocks at 0.030s build / 0.021s layout and `docs/09-decisions.md` (84 KB) is 293 blocks at 0.137s / 0.089s, but a generated 546 KB document parses to **12,000 blocks costing 5.147s to build, 5.145s to lay out and 1.39 GB resident**; with the budget removed again the perf check's own process dies with **exit 133** before it can report, which is the crash rather than a slow render. Preview now renders at most 400 blocks and appends a rendered notice naming how many it held back and pointing at Source — nothing is lost, because Source is one `NSTextView` holding the whole file, which TextKit is built for. The same 546 KB file renders in 0.509s at 126 MB. Second defect in the same view: `layout()` re-measured every block on every pass, and measuring is not cheap — prose rebuilds an attributed string per row and a fenced block (where a GFM table lands, since the parser renders tables as a monospace fallback) measures its entire source at unbounded width via `CodeTextView.measuredCodeSize`. Heights are cached per width now, so 30 same-width relayouts measure **0** blocks where they measured **5,490**; a frame is assigned only when it actually changed, since assigning an unchanged frame still marks a prose block as needing layout and it re-measures its rows. `--file-markdown-perf-check` holds both and asserts the zero-measurement property rather than only a stopwatch. Gate: 150 legs, 5 KNOWN-RED, 0 regressions. Left open and recorded in the backlog: Preview is budgeted, not virtualized (`AgentTranscriptListView` already virtualizes transcripts), and `AssistantProseView.layout()` still re-measures its rows on every layout pass — ~16ms for a forced full-subtree relayout of 183 blocks, a cost every agent transcript pays too. |
| 0.4.17  | 23    | 2026-08-12 | `v0.4.17` | **The Markdown tile stopped pinning a core — the cost was in the shared prose renderer, not the document view.** 0.4.16 bounded the document but Dylan's tile still burned **90s of CPU over 94s at 96%**. Two OS reports name the difference, both taken with a Markdown tile open: `Array_2026-08-12-165030.hang` (0.4.15) is 75.48s hung with the main thread in `FileMarkdownDocumentView.BodyView.layout()` → `AssistantProseRenderer.measure` → `AgentTextStyleResolver.append` → `replacingOccurrences`, which is the un-budgeted shape 0.4.16 addressed; `Array_2026-08-12-174301.cpu_resource.diag` (0.4.16) is still inside window layout but with **20 of 34 samples in `AssistantProseView.layout()`** → `NSTextView.setFrameSize` → `_boundingRectForGlyphRange` and only 8 in `BodyView.layout()`. The remaining cost was in the SHARED transcript renderer — which every agent transcript pays too — re-measuring every row on every layout pass (each measurement rebuilds an attributed string, with five `replacingOccurrences` passes inside `escapeMarkdown`) and re-assigning every text-view frame, which costs a TextKit glyph-bounds pass and re-dirties the view. Row heights are cached per width now and an unchanged frame is left alone. Two hardening changes in the file document: height comes from `intrinsicContentSize` rather than `setFrameSize` inside `layout()`, so AppKit cannot re-enter layout on that subtree, and `scrollerStyle` is pinned to `.overlay` so a legacy scroller cannot take clip width, change the document width and restart the cycle — macOS picks legacy scrollers whenever a mouse is connected, a difference no check on this laptop would have surfaced. Witness: `--file-markdown-perf-check` drives a real window and asserts 20 relayouts at an unchanged width cost NO prose row measurement — RED at 241, GREEN at 0. **Not** witnessed: the runaway loop itself, which could not be staged with or without legacy scrollers at the pre-fix shape; recorded in `.plans/15` and the backlog rather than implied. Corrects 0.4.16's ledger row: the `exit 133` cited there as the app dying under load was the perf check trapping on `Int.max + 1` in its own assertion during a teeth run, and the three `Array-2026-08-12-17*.ips` reports from that hour are those check runs, not Array — the 1.39 GB measurement is real and still justifies the budget, but it is not what took the app down. Gate: 150 legs, 5 KNOWN-RED, 0 regressions. |
| 0.4.18  | 24    | 2026-08-12 | `v0.4.18` | **A managed agent owns its harness, and a prompt that cannot be sent now says why.** Every agent record persists which harness runs it — Claude Code, Codex, or Pi — and the RECORD drives the runner, model catalogue, readiness, transcript format, auth and provider side channels; Settings only seed a NEW agent, and selection never falls back to another CLI. The composer footer gained per-agent harness/model/effort controls, and `--strict-agent-harness-check` pins ownership, catalogue isolation, the Pi argv and migration ambiguity. Shell tiles stop mirroring in the same release: each opens a tile-keyed grouped view onto the workspace session instead of attaching to a shared pane, so focusing one tile can no longer redirect another (`--terminal-tmux-no-mirror-check`). Strict ownership added four states that REFUSE a send and every one was invisible — the supervisor returned false, the caller returned, the draft survived and nothing said why nothing happened. Three are reachable in ordinary use: a legacy record whose harness could not be inferred, a logged-out CLI, and the seconds after launch while the catalogue probe is still out, when readiness is `.checking` for every harness. The rule lives once in `AgentSupervisor.sendRefusal`, which `send` asks rather than deciding again, and the tile appends it as a notice keyed by the reason. Migration no longer mints dead agents: the stored preference decides what a NEW agent gets and is not evidence about an existing one, so a settings default of Claude Code meeting an `openai-codex/*` record produced a record whose every send was refused — Pi takes those back, being the multi-provider harness they ran under before ownership existed. Two canvas paths broke where the branch routed agent spawns into the active project's `ZoneLayer`: `deleteTile` looked its target up in the flat `canvasState.tiles`, found nothing and returned silently, so closing an agent tile did nothing at all and its inbox row stayed forever; `removeTile` had the same blind spot from the other side, taking the VIEW away and leaving the model entry to be persisted and rehydrated. Both span the layers now, and managed-agent spawns go back to flat placement for this release — the layer is the right destination (hazard 9) but it is REBUILT on a workspace switch and rebuilds a `.managedAgent` tile as a plain descriptor view, so a live agent tile came back as a dead placeholder; agents need zone re-installation to reconstruct their views first. The composer footer's third control starved the second: harness and effort resist compression outright, so at 320pt the whole deficit fell on the model button and drove it to ZERO width — the harness yields now, by hiding rather than truncating to an unusable name. Verification found more than the feature: Core stopped building for **iOS** the moment the catalogue grew per-harness probes, because `AgentModelCatalog.ProbeExecutor` names `PiAgentRunner.ResolvedCommand`, which exists only on macOS — invisible to a macOS `swift build`, and the matrix's iOS leg halted on it. `AgentModelConfig`, `AgentModelCatalog` and `RoleRegistry` checks read the AMBIENT harness, so their verdict followed the operator's stored `continuum.agents.backend`: green on a machine storing `pi (all providers)`, red in four places on a clean one — each names its harness now, and the per-harness option sweep asserts non-empty so it cannot pass vacuously. Pi's own `Config` defaulted from whichever harness settings seeded, so it could hand `pi --model` an `anthropic/*` id — a PATTERN, and exactly the fuzzy match P0.10 exists to prevent; Pi resolves from Pi. And the tmux hazard that killed the running app twice on 2026-08-12 is closed properly: **three** CoreChecks sections plus the app's live tmux leg drive a real server, not the one that was guarded, and they now share one fail-closed gate (`TmuxIsolation`) that refuses unless `TMUX_TMPDIR` is disposable AND tmux itself reports a socket inside it. `run-matrix.sh` exports such a namespace before any leg runs, and the loopback-ssh proof forces it across the hop with `SetEnv`, because sshd sanitizes the environment and the remote `tmux new-session` was landing on the DEFAULT socket. Two more defects surfaced there: the I2 no-mirror check ran the production grouped-view argv with inherited stdio, so headless it threw `open terminal failed: not a terminal` and took the whole executable down (it gets a pty of its own now, as the neighbouring ssh proof does), and `RemoteSession` treated `error connecting to <socket>` as a probe failure rather than an empty topology — which is what any machine with no tmux server running actually reports. Gate: CoreChecks green twice under isolation, the iOS build green, and `--strict-agent-harness-check`, `--agent-inbox-check`, `--provider-model-picker-check`, `--ui-geometry-check`, `--agent-restore-check` and `--managed-agent-model-spawn-check` green individually; the full matrix was NOT run to completion for this release at Dylan's direction — its last complete run before the final three commits was 154 legs, 7 KNOWN-RED, and the three regressions it named are the ones fixed above. |
| 0.5.0   | 25    | 2026-08-17 | `v0.5.0` | **The agent sidebar becomes an operator console, and the canvas camera becomes one coherent system.** The sidebar now has purpose-built dense cards, truthful attention/history/lifecycle treatment, hierarchy and folding, inline rename, hover detail, selection and bulk actions, native context menus, keyboard jump hints, provider marks, responsive widths, and a production-corpus screenshot program across Aqua/Dark Aqua. Its command menus use semantic template icons and separate destructive actions. Pan, pinch, command-scroll zoom, glide, and pinch→pan handoff now share `CanvasCameraDriver`; input is submitted at display cadence, persistence/cursor/hydration work settles once, zoom-dependent chrome is bucketed, and canvas SF Symbols are cached as shared template-preserving bitmaps. The live frame HUD reports active-gesture FPS/late share/p95 without letting the quiet settle tail flatter the result. Profiling and 5/10/25/50-agent probes establish the remaining boundary honestly: pan is flat, while each native bounds-size zoom step triggers AppKit backing-property propagation, nested scroll tiling, layout and raster through every mounted agent subtree. Synthetic low-detail shells were rejected; full-detail experiments and bounded-residency work continue from `.plans/28`. Release gate: optimized release build, 48-image sidebar check, signed/notarized/stapled app and DMG, prod bundle check, and a 170-leg matrix with 11 documented KNOWN-RED and zero regressions. |
| 0.5.1   | 26    | 2026-08-19 | `v0.5.1` | **The 89-tile canvas stops flickering, stuttering, and freezing — eleven measured causes, one at a time.** The tile-surface-residency slice (still default-OFF; `continuum.tileSurfaceResidency.enabled`) gained the behaviours that made it feel right on a real 83-tile workspace, every one driven by a session log or a 20 s `sample` of Dylan actually gesturing, and every one witnessed RED-first or teeth-checked. The gesture hold: NOTHING crosses residency while the camera moves (was 738 crossings in one 2m45s session — the flicker), and the flaky `zoomingIn` delta-derivation is deleted rather than repaired. The settle edge sharpens the whole viewport in ONE beat, anchor-outward, under a 12-bake visible budget — reversing the 'a storm moved to the settle edge is still a storm' clause whose 2-per-pass rationing WAS the blur→sharp pop parade — and a one-chrome-bucket (~19%) sharpness band stops a zoom nudge from re-baking the world. Density follows visibility through ONE shared `requiredSurfaceScale` (viewport+25% lead at full zoom density, rest density off-screen, 24 MB whale cap) that the bake, admission gate, catch-up and sweep all judge by, because any two disagreeing is a permanent flap — witnessed. Under byte pressure a bake DEGRADES to rest density instead of the tile being refused (stranded natives at ~4.5 ms per camera step each were the 104–155 ms frames), an in-place slim pass reclaims over-dense bytes with zero flips, and `bakeWouldFit` credits back the surface a re-bake replaces (a tile's own picture was blocking its own re-bake). A geometry corruption is fixed (the host's `layout()` wrote the root layer's frame in superlayer space — every surfaced picture shoved up 24 pt, intermittently); a pre-scroll bake can no longer be shown after a scroll; passive AX tree-walkers (Raycast, Rectangle) no longer demote the whole canvas — the hand-back rule fires only for VoiceOver/Switch Control, teeth-checked; the surface park is HIDDEN, not merely clipped, after a sample showed AppKit's tracking-area/constraint/layout machinery recursing through ~80 parked transcript trees every display cycle; and app activation no longer spawns git on the main thread (two `rev-parse` per agent repo per switch, ~0.4 s frozen each edge) — branch chips are stale-while-revalidate with an off-main warm, witnessed at zero spawns cold. Residency logging now prints every crossing with attribution (`why: stale/softDeferred/suppressedDemotes/evictions/refusedMemory/degraded/slims/refusedBudget`) — the previous log skipped passes whose promotes and demotes cancelled, blind to exactly the flicker case. Check fixture windows order front off-screen so a matrix run never takes the display. Methodology note recorded in the suite: a new witness baking ~100 MB before the timing legs shifted the cost gate +0.5 ms through heap state alone — witnesses that allocate heavily run AFTER timing legs now. Gate: 166 legs, 9 KNOWN-RED expected, 0 regressions; release bundle spctl-verified. |
| 0.5.2   | 27    | 2026-08-19 | `v0.5.2` | **The component-lab sidebar replaces the production sidebar completely.** Production now ships the stacked, centred search header; dense proposal-A rows; native hover detail with high-contrast black/white provider marks; project favicons resolved locally and asynchronously; pointing-hand cursors; an extensive icon-driven contextual action menu; and the bounded, Reduce-Motion-safe “All set?” settle nudge. Status is no longer inferred from one completion timestamp: a durable sequenced terminal event distinguishes success, failure, interruption, cancellation and runtime error, with an acknowledgement watermark for unread state. That exact optional outcome crosses the companion event boundary, drives the phone chip/timeline, and prevents stopped or cancelled turns from masquerading as successful push notifications. The sidebar uses one shared elapsed clock and one settle clock, reloads only materialized rows, and keeps favicon decoding bounded and off the main thread. Gate: the full 166-leg release matrix passed with its 9 documented known-red legs and zero unexpected regressions; macOS and iOS builds, CoreChecks, AgentUI, inbox, production corpus, live-status, sync, live tmux integration and the 48-image sidebar screenshot checks are green. |
| 0.5.3   | 28    | 2026-08-20 | `v0.5.3` | **Agent tiles identify the conversation, and sidebar timers tell the truth.** The draggable tile chrome now uses the same canonical conversation name as the sidebar instead of repeating the provider/model, with the bundled high-contrast provider mark beside it. Sidebar elapsed time is now a running-only signal driven by one visible-row clock: it advances without hover and disappears as soon as the agent stops, so an idle row can no longer retain a stale bare duration. Gate: two 172-leg matrix sweeps covered every leg; the corrected-terminal sweep cleared the real tmux/Core regression from the first, while its sole browser network-lite flake was green in the first sweep and an immediate exact isolated rerun. CoreChecks, AgentUI, inbox, production corpus, UI geometry, the 48-image sidebar check, iOS build and live tmux integration are green. The release app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.4   | 29    | 2026-08-20 | `v0.5.4` | **The sidebar identifies an agent's working directory, and transcript internals are observable without losing your place.** Rows now prefer the privacy-safe final component of the agent's real working directory, so a missing project join no longer leaves the placement line blank; the full host path never crosses the context/row boundary, and hover detail keeps project and directory distinct when both exist. Tool calls expose one sanitized summary line while collapsed and up to four when expanded; top-level expansion now invalidates and remeasures its owning transcript row instead of persisting an invisible state change. Opening completed thinking or tool detail preserves the reader's anchor even near the bottom. Accessibility treats disclosure/copy/jump buttons as leaves, removes duplicate controls, and no longer announces a completed code block's hidden Streaming label. Gate: the 172-leg matrix covered the full app and every new Core/UI geometry/sidebar/privacy/accessibility/pixel/corpus gate is green. Core and live tmux pass in an isolated namespace with a capable `TERM`; the matrix's `TERM=dumb` failures were reproduced and attributed to pty capability. The display-dependent residency timing gate measured ~5.0 ms here, but an A/B build of the already shipped 0.5.3 commit measured the same 4.90 ms on this host, proving no release delta; its correctness witnesses remained green. The production bundle harness passed, and the app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.5   | 30    | 2026-08-20 | `v0.5.5` | **Transcript details can be inspected and selected, and agent state stays truthful across the tile and sidebar.** Expanded read/write tool calls retain privacy-safe file targets instead of collapsing to generic verbs, while disclosure remeasurement preserves the reader's anchor. Transcript prose and Markdown file previews support native multi-line click-drag selection. Pi GPT-5.6 Luna occupancy uses the provider-published context window rather than a mismatched fallback. The sidebar's directory/status geometry no longer overlaps, and tile focus/click acknowledgement clears completed state through the same path as sidebar navigation. Provider-setting updates preserve restored off-catalog models when only effort changes, and naming refuses identifier-shaped prompts before path normalization can turn them into plausible titles. Gate: the 172-leg matrix covered the full app; its two `TERM=dumb` tmux failures passed immediately in isolated namespaces with a capable `TERM`, and all remaining red legs were documented known-red baselines/performance checks. CoreChecks, supervisor, inbox, sidebar UX/corpus/48-image screenshots, transcript/Markdown, context occupancy, iOS build and app-level live tmux integration are green. The release bundle harness passed; the app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.6   | 31    | 2026-08-21 | `v0.5.6` | **Agent prompts appear immediately, documents understand their directories, and canvas changes can be undone.** Prompt echo and spawn-state handling remove first-paint dead air while keeping sidebar completion state accurate; provider and model commands are integrated into the composer. Markdown documents now resolve local file links and document relationships relative to their directories. Canvas mutations participate in transactional undo and redo, and sidebar search, directory alignment, relative completion ages, concurrent harness readiness and transcript launch paths are hardened. Gate: the 174-leg matrix covered the full app with deterministic release checks green. Two display-dependent wall-clock probes exceeded their budgets under sustained host load; the gesture probe passed its isolated rerun, the residency probe recorded a passing first matrix sample, and their structural and correctness witnesses remained green. CoreChecks, iOS, transcript, document, sidebar, composer, agent lifecycle and live tmux coverage are green. The release bundle passed and the app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.7   | 32    | 2026-08-21 | `v0.5.7` | **The complete document, Jelly auto-layout, undo, invocation, and transcript renovation ships together.** Document schema v5 establishes canonical locations and migration, Markdown editing/note conversion, durable agent↔document relationships and reveal overlays, with agent deletion explicitly preserving document tiles. Auto-layout schema v6 lands the pure solver/model/persistence layer and separate tile/zone gesture integration, settings and per-zone overrides, Tidy, animation, blue resistance feedback, Reduce Motion behavior, and performance gates. Move, resize, auto-layout displacement, Tidy and zone operations share one native undo/redo transaction contract: one pointer gesture is one Undo entry, text editors retain typing history, and the temporary auto-layout toast invokes the same inverse as Edit → Undo. The bounded `@` and slash interface covers stack invocation, approvals, background jobs, provider adapters, and provider/model commands. Semantic transcripts and subagents gain durable persistence, responsive macOS rendering and optimistic submission, lifecycle tiles and contextual lineage, shared document relationship/reveal abstractions, encrypted per-pairing sync, and an iPhone transcript with permitted controls. Gate: the 175-leg matrix ran with its 10 documented known-red legs; all six unexpected integration regressions were either fixed and rerun green or attributed to environment. CoreChecks, SyncChecks, AgentContent's 10,000-entry workload, macOS/iOS builds, provider fixtures and installed-CLI smoke tests, schema v4→v5→v6 migration, canvas undo/Jelly correctness, 50-entity solver performance, transcript encryption/scope/compatibility, and production bundle checks are green. The display-dependent residency probe measured 4.73 ms/live tile; an A/B build of shipped 0.5.6 measured 4.03 ms on the same host, already above the current 3.5 ms gate, while structural gesture/culling/eviction witnesses remained green. The app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.8   | 33    | 2026-08-21 | `v0.5.8` | **Array now teaches the workspace, scopes creation to the place you are working, and turns transcript file references into durable canvas relationships.** Guided first-run setup creates a starter layout and Getting Started checklist, introduces the core shortcuts in context, and remains replayable from Help. The Home/project chooser has clearer hierarchy, keyboard navigation and screen-aware placement; creation follows the active project/Home context. Transcript file references open or reveal documents beside their agent and persist stable relationship connectors. Settings and shortcut navigation are consolidated, the optional canvas shortcut guide is subtler and monochrome, and nested code blocks scroll correctly. Migration, persistence, sync, transcript rendering and relationship coverage were expanded. Gate: all 175 matrix legs completed with exactly 10 documented known-red checks and no unexpected failures; macOS and iOS builds, onboarding, project switching, transcript/file linkage, schema migration, sync, provider, terminal and production-bundle checks are green. Exact 0.5.7 A/B runs attribute the two newly inventoried wall-clock probes to host/SDK calibration while their structural budgets pass. The 0.5.8 (33) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.9   | 34    | 2026-08-22 | `v0.5.9` | **Switching an existing agent's harness now changes what its next turn actually runs.** A harness choice atomically persists a complete harness/model pair instead of changing only temporary footer state. When Pi and Claude Code share the same visible Anthropic model—such as Claude Opus—the model is preserved while ownership moves to Claude Code; when the prior model is incompatible, Array selects the new harness's first valid model. Rejected changes restore the prior visible selection. Gate: the focused hotfix suite passed the exact Pi→Claude Code shared-Opus UI→supervisor→disk regression, strict harness ownership, provider/model picker, full UI geometry, debug build and diff checks. The 0.5.9 (34) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.10  | 35    | 2026-08-22 | `v0.5.10` | **Workspaces switch their real canvases again, active focus borders remain visible, and transcript file references open in the right project.** The production workspace runtime now loads registered project controllers instead of failing through a placeholder factory, removes the departed flat compatibility scene on the first switch, and activates the owning workspace/project when a document reference is opened. Focus and attention overlays resolve tiles from layered workspace scenes, restoring marching-ants borders there. Gate: the 175-leg matrix completed with nine documented known-red checks; its sole unexpected browser-inspector timing failure passed three consecutive isolated reruns. The changed workspace switching, runtime installation, active-context file opening, local-file linkage and focus-border checks are green, as are macOS/iOS builds and the production bundle audit. One formerly known-red gesture-transition performance check passed and should be removed from the matrix allowlist. The 0.5.10 (35) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.11  | 36    | 2026-08-23 | `v0.5.11` | **Switching a workspace switches the canvas, and new tiles land in the zone you are working in.** `WorkspaceRuntime.canvasView` had exactly one writer — `install(into:appRegistry:)` — and no production code had ever called it since the method was introduced in June, so every canvas operation inside `switchWorkspace` optional-chained through nil: the document, the registry and the toolbar header changed while the previous workspace's tiles stayed on screen. Production now adopts the canvas from `mountWorkspaceSceneAtBoot` (extracted from `applicationDidFinishLaunching` for exactly this purpose) and a runtime with no canvas THROWS rather than silently doing the document half. That one wire also turned on a milestone that had been unreachable behind it, green only because each of its witnesses called `install(into:)` itself: per-project canvas persistence merged over disk, live hydration instead of `DescriptorTileNSView` placeholders, one runtime per tile across a round trip with orphan retirement, a spawner for every live zone, and a membership repair that rescues tiles stamped with another project's zone by world-centre containment — never moving a tile, never crossing a project boundary — after one real store was found with all 89 of a project's tiles stamped to another's. Four hazards were closed before layers could go live: `liveZones` is the zone INTERACTION model and `setZones` never wrote it; chrome orphaned and double-drew; every `canvas.json` in the field holds WORLD frames while a layer holds ZONE-LOCAL; and foreign `zoneId` stamps are ordinary rather than exotic. Creation targeting is the second half. `CanvasNSView.activeProjectZoneId` was the only input to the creation scope's `.zone` candidate and no user action ever re-pointed it, so making a second zone moved the controller but not the canvas, and because `.zone` outranks `.recentExplicit` the scope picker's correction was overruled on the very next spawn and every spawn after. `WorkspaceRuntime.setActiveZone(_:reason:)` is now the one writer of both halves; a zone arms on create, focus, click and camera settle, an explicit pick arms it too (removing the precedence conflict rather than re-ranking a locked resolver), a project-less zone never arms because `activeController` is nil for one, and camera arming rides the existing hydration debounce reading the RENDERED zone set because `liveZones` and the document diverge when auto-layout grows a zone. Turning that on exposed a frame/install split in every spawn path — agent, terminal and file tree framed against the armed zone while installing into the scope's, notes and browsers ignored the scope outright — so all five now thread one target through placement, siblings, z-order and install, resolved through `installedZonePlacement` so frame and install agree on the MODEL and not merely the zone. Zones grow when a tile lands, on both models and regardless of auto-layout, compensating layer-local frames when growth moves an origin. Stop records as `.interrupted` rather than a failure and kills the whole process group via a new `ProcessGroupChild` (`POSIX_SPAWN_SETPGROUP` at spawn time, every original descriptor closed in the child). The agent-to-document connector was painted at `worldOrigin + W` because the overlay's frame was `worldPlane.bounds` — whose origin IS the camera, pan being `setBoundsOrigin` — while its own bounds origin stayed zero; it converts now, and `refreshDocumentRelationships()` runs after the boot install walk so connectors exist on a cold launch at all. A file opened by an agent lands beside it in its zone instead of belonging to no zone, an explicit drop point beats the anchor, choosing a zone's Home is one click, an empty workspace offers Add Project instead of refusing silently, and the membership repair runs at boot so a canvas already in that state fixes itself. Five of these defects shared one shape — a witness asserting against a model the user cannot see (document geometry vs rendered, layer presence vs the document's armed zone, zone existence vs frame space, a camera at the origin vs a camera anywhere real) — and the doctrine recorded in `.plans/47` is now: drive the production entry point AND assert on what reaches the screen, in the space it is stored, from a fixture where the wrong answer would differ. **The transcript UI is unchanged**; the overhaul is the next release. Gate: 180 legs, 7 documented KNOWN-RED, zero regressions; two new legs (`--zone-arming-check`, `--relationship-geometry-check`) plus a CoreChecks section, every fix teeth-verified by reverting it. `--perf-budget-gesture-transition-check` passed and the matrix asked for its removal from the allowlist; it has flapped before and was deliberately left. The 0.5.11 (36) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.12  | 37    | 2026-08-26 | `v0.5.12` | **The transcript overhaul and agent-awareness signals ship together.** Transcript links can open web pages in an Array browser tile while local file references retain project-aware document reveal. Claude Code, Codex and Pi delegations now converge on one durable, provider-neutral agent-reference model, including nested children, late completion and relaunch recovery; their centred, status-aware chips share one renderer and open the surfaced child. Focused parent/child lineage is prioritized above tiles with a softened, click-through marching-ants connector, one bounded animation and Reduce Motion support. Transcript rhythm, streaming, compaction, queued messages, copying, accessibility and provider session continuity were hardened. Configurable visual and sound signals cover approvals, input, completion and failure with privacy-safe classification and deduplication. Integration preserved both feature contracts at their shared seams: focus acknowledgement updates lineage and sound state, Claude forwards both hook and subagent events, and Codex app-server launches retain explicit no-approval/workspace-write policy. Gate: all 182 matrix legs completed with four documented known-red navigation/performance probes and no unexpected failure; the allowlisted gesture-transition probe passed. Core, supervisor, transcript, provider fixtures, geometry, appearance, accessibility, performance structure, macOS/iOS builds and production bundle checks are green. The 0.5.12 (37) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.13  | 38    | 2026-08-26 | `v0.5.13` | **Workspace state and agent conversations survive switching exactly, and Markdown notes and files gain one shared document surface.** `WorkspaceRuntime` now owns atomic scene persistence and captures the departing workspace before a validated switch; zones retain their custom names, geometry, color, collapse state, viewport and focus across round trips and relaunch. Projects have one explicit workspace owner, duplicate legacy membership is reported instead of silently repaired, and explicit moves transfer the project with its zones. Agent tiles restore a complete semantic transcript snapshot before tailing live events, so closing a tile or switching away during a response loses and duplicates zero text beyond the lightweight replay cap. The compact provider selector remains reachable. Markdown notes and files share native Preview, Split and Edit modes with live draft preview and persisted mode. Gate: focused production-path checks, full CoreChecks, AgentSupervisorChecks, UI geometry, workspace switching, Markdown, note focus, tile residency, release build and the production bundle audit are green. The 0.5.13 (38) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.14  | 39    | 2026-08-26 | `v0.5.14` | **Hotfix: legacy duplicate workspace membership no longer prevents Array from opening.** A project’s durable `workspaceId` declaration is authoritative when an older registry retains a stale second membership. Array mounts the declared owner, leaves both workspace documents and all registry memberships untouched for explicit repair, and rejects only a conflicting mounted copy rather than globally blocking launch and safe workspace switches. The strict integrity audit still reports the duplicate. Gate: the exact declared-owner-plus-stale-membership regression, full CoreChecks, production workspace switching, debug and optimized release builds, and the production bundle audit are green. The 0.5.14 (39) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.15  | 40    | 2026-08-26 | `v0.5.15` | **Workspace creation escapes legacy ownership conflicts, and an already-owned project now leads somewhere useful.** Array validates the workspace being entered but no longer lets a stale foreign zone in the workspace being left trap the user after they close it; the legacy zone remains preserved on disk for explicit repair. Once a new empty workspace and registry entry are durable, creation is reported separately from navigation so a switch error cannot claim the workspace was never made. Adding a project owned by another workspace offers to switch there by project and workspace name instead of exposing raw UUIDs, and neither accepting nor cancelling transfers or duplicates the project. Safe targeted assignments are no longer disabled by an unrelated project’s legacy duplicate membership, while the strict global integrity audit still reports that duplicate. Gate: full CoreChecks, production workspace switching, the real add-zone/AppDelegate route, debug and optimized release builds, and the production bundle audit are green. The 0.5.15 (40) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.16  | 41    | 2026-08-26 | `v0.5.16` | **Workspaces mount completely separate canvases, and active-agent clocks no longer reset.** Creating a workspace now has a production witness proving it installs zero zones and zero tiles from the departed canvas; adding an unowned project gives the new workspace its own zone without touching the old project canvas. Legacy foreign zones are preserved in their original workspace files for explicit repair but omitted from the mounted scene in both switch directions, so stale pre-exclusive-ownership data cannot make two workspaces look identical or trap navigation. Agent elapsed time is anchored to the supervisor’s original active-turn timestamp across Claude Code, Codex and Pi; repeated provider/subagent start boundaries preserve the oldest stamp, and tile replay or workspace reattachment cannot replace it with a view-local clock. Gate: full CoreChecks, production workspace switching, empty-workspace creation, add-zone ownership handling, debug and optimized release builds are green. The broader supervisor gate reaches a pre-existing injected Home-persistence assertion before its timer section; the focused supervisor and presenter regressions compile into that gate. The 0.5.16 (41) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.17  | 42    | 2026-08-26 | `v0.5.17` | **A workspace switch now durably records the selected workspace before mounting its isolated canvas.** The switch transaction writes `lastActiveWorkspaceId` only after every throwing target-preparation step succeeds and before changing the mounted scene, so a failed registry save leaves the departing canvas intact. Superseded by 0.5.18: startup still let the boot project overwrite this saved selection. The 0.5.17 (42) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.18  | 43    | 2026-08-26 | `v0.5.18` | **Startup honors the selected workspace instead of blindly returning to the last project’s workspace.** Superseded by 0.5.19: the compatibility boot controller could still inject its foreign project zone into a selected empty workspace after restoring it. The 0.5.18 (43) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.19  | 44    | 2026-08-26 | `v0.5.19` | **An empty workspace stays truly empty across launch and relaunch.** Startup no longer attaches a compatibility project controller, its flat project tiles, or an auto-created zone when that project belongs to another workspace. If the selected workspace has projects, project-root resolution prefers an authoritatively owned project from that workspace before the globally last-used project. The boot gate asserts a foreign compatibility canvas contributes zero tiles and no controller to an empty workspace, CoreChecks cover workspace-scoped project resolution, and the switch/empty-creation gates remain green. Includes durable workspace selection, isolated workspace switching and monotonic agent elapsed time. The 0.5.19 (44) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.5.20  | 45    | 2026-08-26 | `v0.5.20` | **A saved workspace canvas no longer disappears after switching away and back.** The project/Home picker previously committed a zone before checking whether its selected project belonged to the mounted workspace. The zone was safely written to that workspace document but filtered as foreign on the next mount, producing apparent data loss. Foreign selections now remain provisional, are removed before the existing friendly switch-to-owner helper runs, and never reach the runtime document or disk; an unowned project is assigned atomically before its zone is saved. The runtime persistence boundary independently enforces the same invariant. The affected live registry was repaired from its preserved workspace documents without deleting or moving any zone, tile, project file or transcript, and a real installed-app work → personal → work round trip restored both canvases with Saved status. Gate: full CoreChecks, production workspace switching, empty-workspace creation, debug and optimized release builds, the production bundle audit, signing, notarization, stapling and Gatekeeper verification are green. The 0.5.20 (45) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.6.0   | 46    | 2026-08-26 | `v0.6.0` | **File and note tiles become trustworthy native document surfaces.** Notes and Markdown files share persisted Preview, Split and Edit modes with live rendering, stable focus and scroll state, explicit saving, external-change conflict handling, atomic recovery drafts, formatting commands and Markdown-aware editing. Source files gain language-aware highlighting, line numbers and clean live refresh when agents edit them externally; file trees gain fuzzy multi-term search, file-type icons, collapse controls, Command-F focus and direct open-as-tile actions. Vim motions remain off by default and are not exposed until a real manual opt-in exists. A release-gate workspace repair also ensures opening a headless agent switches to its project’s owning workspace before creating the tile, preventing foreign agent tiles from leaking into the current canvas; low-zoom title-bar hit routing remains draggable. Gate: the full 188-leg matrix passed with seven documented known-red gates and no unexpected regressions; macOS and iOS builds, Core, agent supervisor/content/UI, workspace ownership/switching, transcript restoration, file/note/file-tree production paths and the release bundle audit are green. The optimized Swift 6 build caught and fixed one strict-concurrency capture in syntax highlighting before publication. The 0.6.0 (46) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.6.1   | 47    | 2026-08-26 | `v0.6.1` | **Hotfix: file tiles reliably display external agent and editor changes.** File monitoring no longer treats modification time as the complete change identity: it compares timestamp, byte count and file number, so atomic same-size replacements with preserved or colliding timestamps are detected. The production mounted-tile timer path is covered directly, alongside manual refresh. Gate: focused file/Markdown checks, optimized Swift 6 release build, production bundle audit, signing, notarization, stapling and Gatekeeper verification are green. The 0.6.1 (47) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.6.2   | 48    | 2026-08-26 | `v0.6.2` | **File refresh and responsive `@` file finding ship together.** File tiles detect atomic external edits even when timestamps and byte counts collide. File mentions no longer repeatedly abandon and restart the same repository scan as each character supersedes the previous query: an in-flight scan finishes, populates the scoped cache and serves the replacement query, while cancelled requests still return no actionable rows. The picker now names the project root in its breadcrumb and explains fuzzy finding and folder navigation accurately. Gate: focused file/Markdown, Core completion cancellation and production composer semantic checks, optimized Swift 6 release build, production bundle audit, signing, notarization, stapling, Gatekeeper and public asset downloads are green. The 0.6.2 (48) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.6.3   | 49    | 2026-08-26 | `v0.6.3` | **Hotfix: tile snap guides and resize snapping work on real workspace canvases again.** The snap path no longer searches the departed flat `canvasState.tiles` model after `setZones`; it resolves every visible installed workspace tile in world coordinates, performs move and resize matching there, converts the committed target back to the dragged tile's owning zone-local coordinates and paints the guide at the world destination. The regression witness mounts non-zero-origin ZoneLayers and asserts both the visible guide frame and layered resize result, while the original flat drag path remains green. Gate: layered multi-zone rendering/snapping, legacy drag magnetization, optimized Swift 6 release build, production bundle audit, signing, notarization, stapling, Gatekeeper and anonymous public downloads are green. The 0.6.3 (49) app and DMG are Developer ID signed, notarized, stapled and Gatekeeper-verified. |
| 0.6.4   | 50    | 2026-08-26 | `v0.6.4` | **Hotfix: agent delegation no longer stops at a false four-child lifetime ceiling.** Provider-owned Claude and Pi children are always adopted because refusing them after the provider started them only hid live work and stranded its transcript. Array-managed admission defaults to Unlimited; the optional Advanced limit counts only child turns currently running, so completed, stopped and merely durable child records never consume a slot. Refusal UI uses the provider's verb, and the independent four-item human queue fan-out batch remains bounded. Gate: focused production spawn-limit, settings, fan-out and lineage checks, debug and optimized release builds, production bundle audit, Developer ID signing, app and DMG notarization/stapling, and Gatekeeper verification are green. |
| 0.7.0   | 51    | 2026-08-27 | `v0.7.0` | **Array Companion friends alpha is built into the Mac app.** A native Companion section in Settings provisions the hosted relay, creates expiring one-time iPhone invitations, presents their QR codes, reports connection and notification health, and manages paired-device revocation. The production relay is split into portable protocol/client, durable GRDB core, and SwiftNIO service layers with hashed credentials, per-Mac isolation, snapshot-plus-tail replay, private administration, bounded delivery and encrypted push registrations; the legacy long-poll service remains development-only. Desktop publishing uses opaque agent identity and privacy-safe workspace/status projections, with approval and stop capability but no companion terminal access. The hosted one-QR journey, Array-visible iOS product, widget/Live Activity extension foundation, shared gyro phases, App Group snapshots, Associated Domains, APNs taxonomy and mandatory security notifications are included for the companion build. Gate: all 191 matrix legs completed with seven documented known-red checks and no unexpected failures; hardened protocol/core/real-subprocess relay suites, legacy compatibility, Linux container, macOS and iOS builds, privacy/APNs/companion settings, production bundle audit, Developer ID signing, app and DMG notarization/stapling, Gatekeeper verification, and matching public assets are green. |
