Spec: docs/2026-06-15-overnight-sprint-workspaces/T08-add-zone-controllers.md. Builder: cheap model (claude-sonnet-4-6). Branch: overnight/workspaces-zones. Files in scope: Sources/ContinuumRevivedCore/AmbientZoneHome.swift (NEW), Sources/ContinuumRevivedCore/SettingsSchema.swift (append one field to general section), Sources/ContinuumRevivedCoreChecks/main.swift (extend Settings-schema block + new resolver round-trip suite), Sources/ContinuumRevived/App/WorkspaceRuntime.swift (add addZone(projectId:) method), Sources/ContinuumRevived/App/ContinuumApp.swift (replace runAddZoneSelfCheck body; thin-forward addProjectZone; add addGroupZone forwarder; remove orphaned body). The check replaces the current bypass body of runAddZoneSelfCheck so it drives the real addProjectZone→workspaceRuntime.addZone path and asserts registry contents, installed canvas layers, and on-disk JSON — Parts A (project zone), B (group zone), and C (AmbientZoneHome resolver round-trip).
