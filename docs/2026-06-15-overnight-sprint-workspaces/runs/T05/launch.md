Spec: docs/2026-06-15-overnight-sprint-workspaces/T05-mutable-canvas-zonelayer.md. Builder: cheap implementation model (claude-sonnet-4-6). Branch: overnight/workspaces-zones. Files in scope: Sources/ContinuumRevived/Canvas/CanvasNSView.swift (the only file the spec names for this task). The task adds a ZoneLayer reference type and mutable canvas API (setZones, upsertZoneLayer, removeZoneLayer, setZonePlacement, installedZoneLayerIds, tileIds(inZone:)) and extends runMultiZoneRenderSelfCheck with assertions 1–12 covering multi-layer install, per-layer layout, per-layer hit-test, cross-layer z-order, adapter register-on-add, overlap topmost-layer-wins, and unregister-on-remove (the T09 contract). Storage shape B (additive over single-zone) is the chosen approach per the spec default. No other files are touched.
