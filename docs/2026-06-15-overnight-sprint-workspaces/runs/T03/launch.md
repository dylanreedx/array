Spec: docs/2026-06-15-overnight-sprint-workspaces/T03-zone-hydration-orchestrator.md. Builder: cheap model (claude-sonnet-4-6). Branch: overnight/workspaces-zones. Files to touch: (1) NEW Sources/ContinuumRevivedCore/ZoneHydrationOrchestrator.swift — ZoneHydrationPlan struct + ZoneHydrationOrchestrator enum with plan(...); (2) NEW Sources/ContinuumRevivedCore/ZoneHydrationBudgetConfig.swift — config namespace with maxLiveZonesKey, defaultMaxLiveZones, maxLiveZones(defaults:); (3) EXTEND Sources/ContinuumRevivedCore/SettingsSchema.swift — append one .text field for maxLiveZonesKey to the general section; (4) EXTEND Sources/ContinuumRevivedCoreChecks/main.swift — add the --zone-hydration-plan-check do block (9 plan assertions + 4 config assertions) and extend expectedKeys in the Settings schema engine block with ZoneHydrationBudgetConfig.maxLiveZonesKey. No app target, no run-matrix.sh line, no ContinuumApp.swift dispatch.
