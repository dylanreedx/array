Spec: docs/2026-06-15-overnight-sprint-workspaces/T11-adaptive-zone-bounds.md. Builder: cheap model (claude-sonnet-4-6). Branch: overnight/workspaces-zones. Files to touch: Sources/ContinuumRevivedCore/CanvasEngine.swift (add `zoneBounds`), Sources/ContinuumRevivedCore/ZoneBoundsConfig.swift (new file), Sources/ContinuumRevivedCore/SettingsSchema.swift (append three fields), Sources/ContinuumRevived/Canvas/CanvasNSView.swift (rewrite `layoutZoneChromeViews`, add `zoneMemberWorldFrames`, add `qaZoneDrawnWorldBounds`, call `layoutZoneChromeViews` from `updateTile`, add `runZoneAdaptiveBoundsSelfCheck`), Sources/ContinuumRevived/App/ContinuumApp.swift (add `--zone-adaptive-bounds-check` dispatch), scripts/run-matrix.sh (register new check), Sources/ContinuumRevivedCoreChecks/main.swift (add Part A table). The existing `--multi-zone-render-check` and `--single-zone-compat-check` will need their chrome-frame assertions updated because `layoutZoneChromeViews` will now use the adaptive rect instead of `zoneWorldFrame`.
