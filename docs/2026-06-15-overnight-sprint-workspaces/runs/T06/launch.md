Spec: docs/2026-06-15-overnight-sprint-workspaces/T06-workspace-runtime-shell.md. Builder: cheap model (claude-sonnet-4-6). Branch: overnight/workspaces-zones. Files touched: Sources/ContinuumRevived/App/WorkspaceRuntime.swift (NEW), Sources/ContinuumRevived/App/ContinuumApp.swift (rename zoneRuntimeController → workspaceRuntime field, repoint all call sites, add --workspace-runtime-install-check dispatch), scripts/run-matrix.sh (add workspace-runtime-install-check line). Scope: WorkspaceRuntime shell — owns document + registry + orchestrator + canvas for the current workspace; install/flushAll/closeAll lifecycle; AppDelegate proxies zone ops through it; all 10 check assertions. No switchWorkspace, no budget move, no addZone-spins-controller, no tier transitions.
