Builder: cheap model (claude-sonnet-4-6). Spec: docs/2026-06-15-overnight-sprint-workspaces/T16-sidebar-outline-view.md. Branch: overnight/workspaces-zones. Files in scope: Sources/ContinuumRevivedCore/SidebarChromeConfig.swift (NEW), Sources/ContinuumRevived/App/WorkspaceSidebar.swift (NEW), Sources/ContinuumRevivedCore/SettingsSchema.swift (append two toggle fields), Sources/ContinuumRevived/App/ContinuumApp.swift (add workspaceSidebar field + NSSplitView wrap + five callback wires + buildSidebarTree + jumpToZone + zone mutate/save helper + runSidebarSelfCheck static func + --sidebar-check dispatch), scripts/run-matrix.sh (register --sidebar-check after --add-zone-check). TDD: write check first (RED stub), then implement to GREEN.
