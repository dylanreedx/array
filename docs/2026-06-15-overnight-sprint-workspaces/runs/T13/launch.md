Spec: docs/2026-06-15-overnight-sprint-workspaces/T13-live-session-resume.md. Builder: cheap model (claude-sonnet-4-6). Branch: overnight/workspaces-zones. Files in scope: Sources/ContinuumRevivedCore/TerminalSessionDescriptor.swift (add scrollback field + schemaVersion 2), Sources/ContinuumRevivedCore/BrowserState.swift (add interactionState field + schemaVersion 2 on BrowserState), Sources/ContinuumRevivedCore/SessionResumeConfig.swift (NEW — UserDefaults config), Sources/ContinuumRevivedCore/SettingsSchema.swift (two new fields), Sources/ContinuumRevived/BrowserEngine/WKWebViewBrowserRuntime.swift (capturedInteractionState + restoreInteractionState), Sources/ContinuumRevived/TerminalEngine/GhosttyTerminalRuntime.swift (capturedScrollback + replayScrollback), Sources/ContinuumRevived/App/TileSpawner.swift (restartTerminalTile cwd-preference + scrollback replay + writeBrowserTileSnapshotOrThrow interactionState + upsertBrowserTile + restartBrowserTile apply interactionState + runSessionResumeSelfCheck), Sources/ContinuumRevived/App/ContinuumApp.swift (--session-resume-check dispatch), scripts/run-matrix.sh (register check). Note: assertion 6 (scrollback on-screen replay) is flagged NEEDS-HUMAN per spec; will land option (c): persist to disk, no on-screen replay, assertion 6 deferred.
