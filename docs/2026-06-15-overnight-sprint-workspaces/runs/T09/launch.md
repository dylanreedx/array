Re-dispatch attempt 1 of T09 (switchWorkspace in-process). Builder: cheap model (claude-sonnet-4-6). Spec: docs/2026-06-15-overnight-sprint-workspaces/T09-switch-workspace-in-process.md. Branch: overnight/workspaces-zones. Files in scope: Sources/ContinuumRevived/App/WorkspaceRuntime.swift (fix departing-viewport persistence in switchWorkspace; strengthen _relaunchSpy seam) and Sources/ContinuumRevived/App/ContinuumApp.swift (fix inv7 from vacuous to real-reachability assertion; add inv2b hit-test assertion; add viewport-mutation step to inv8 so the round-trip exercises the persistence path). The three issues being corrected are: (1) inv7 no-relaunch was vacuous because _relaunchSpy was never invoked by switchWorkspace — fixed by replacing with a real in-process reachability assertion and documenting the structural proof; (2) shape-B gap: active zone tiles in ZoneLayer but not in canvasState.tiles had no hit-test assertion — fixed by adding inv2b that calls canvas.tileId(at:) for B's active tile and flags the canvasState.tiles gap as needs-human; (3) departing workspace viewport was discarded on switch-away without being flushed to disk — fixed by reading canvasView?.viewport into document.viewport and saving before loading the target document, with inv8 mutating the in-memory viewport before switch to exercise the path.
