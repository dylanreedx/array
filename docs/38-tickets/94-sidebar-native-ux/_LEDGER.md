# 94-sidebar-native-ux — execution ledger

## heartbeat

last-touch 2026-08-06T03:17:23Z · ticket P6.1-lifecycle-pure-derivation.md · attempt 2 · pid — · status done

## states

`pending` · `in-progress` · `done` · `blocked`

| Ticket | State | Commit | Updated | Note |
|---|---|---|---|---|
| `P0.1-program-contract.md` | done | this commit | 2026-08-03T22:15:24Z | Harness-owned completion: focused worker checks, independent opposite-model review, swift build, and final matrix passed. Evidence: /Users/dylan/.pi/sidebar-native-ux-runs/continuum-overnight/run-20260803T170016/tasks/iteration-001-P0.1-program-contract.md. |
| `P0.2-sidebar-check-seam.md` | done | this commit | 2026-08-03T23:11:58Z | Focus-session completion (loop retired; see plan-focus-session-handoff.md): worker's probe + QA seam preserved from the blocked loop run, matrix leg and inventory record added here. --sidebar-ux-check green at 220/280/320 in both appearances: 42 cells, 252 labels, 24 eliding by drawable width, zero-size host refused. |
| `P0.3-row-fixture-corpus.md` | done | this commit | 2026-08-04T01:03:10Z | Focus-session completion: 11 declared shapes / 53 corpus rows with two-way declaration-usage parity, coverage pinned (5 states, 3 attention, 4 lifecycles, 2 variants, depths 0-2), 40-child fan-out, 100h elapsed, bidi+combining title. Probe re-pointed at the corpus with content-derived host height (fence deviation: UIProbeGeometry.swift, the probe renders the corpus). AgentUIChecks + sidebar-ux-check (318 cells/1884 labels/208 eliding) + component-lab (60 baselines byte-identical) + agent-inbox-check all green. Negative witnesses observed red then restored green: planted /Users home path (exit 1, I5 hygiene), planted ssh-rsa key shape (exit 1, I5 hygiene), .failed coverage removed (exit 1, coverage). |
| `P0.4-inbox-geometry-gate.md` | done | this commit | 2026-08-04T01:41:55Z | Focus-session completion: checkSidebarTruncationGate in --ui-geometry-check measures 1302 labels at WorkspaceSidebarConfig min/default + wide in both appearances, truncation by drawable width vs needed (+4pt inset). ENTRY WITNESS: empty table went red naming 117 truncations, almost all titles at min/default (the yields-first defect) — e.g. row0.title@min lost 124.5pt, row48.title lost 110pt at every width behind its oversized project chip. Those 117 keys are now expectedSidebarTruncations; new truncation red AND healed-but-tracked red, so P2.1/P2.2 must shrink it. Source scan forbids width digit literals in the gate region. FENCE DEVIATION (packet Approach step 3 requires it): the five 79pt height pins live in ContinuumApp.swift, not the fence — re-anchored to slim-exact / card-bounded / rollup-vs-own-pre-fold-height. Witnesses red then restored green: stale table entry (healed rule), hardcoded 220 (source scan). |
| `P0.5-row-token-vocabulary.md` | done | this commit | 2026-08-04T01:05:58Z | Focus-session completion: SidebarSurfaceRole (new type — AgentSurfaceRole is pinned at 5 cases and its rowSelected/rowHover values are consumed live) with resting=panel identity and T3-mix alphas selected 0.07 < hover 0.08 < active 0.11; measured light 1.15/1.17/1.25 dark 1.18/1.21/1.32; LineWidth.hairline = 0.5 (ticket 93's token, sidebar first consumer); runSidebarSurfaceChecks gates ordering by measurement, 27 pairs x 2 themes, tile values pinned by hexKey. Nothing adopted in a view; 60 baselines byte-identical. Witnesses red then restored green: inverted hover/selected alphas (exit 1, ordering), hairline 0.5→1.0 (exit 1, pin). |
| `P1.1-remove-row-borders.md` | done | this commit | 2026-08-04T02:24:47Z | Focus-session completion (delivered with P1.2-P1.4 as one coherent change; the floors could not be re-measured until P1.4's focus ring existed). AgentInboxCardView: borderWidth = 0 assigned once in init and never again, applyTokens no longer sets borderColor at all, resting fill is nil (NOT panel) so 'unfilled' is STRUCTURAL — ownedColorSlots drops the slot. Probe assertions FLIPPED (was borderWidth >= 0 + fill.alpha > 0): now borderWidth == 0 and resting must be nil-or-exactly-panel, with role and pixel held to each other so both 'resolves hover, paints resting' and 'paints hover, is resting' are red. FLOORS RE-MEASURED IN THIS CHANGE (UIProbeAppearance.swift, reason beside each number): minimumThemedViews 27 -> 156 (the 27 was measured at P3.6 and never re-floored while the swept tree grew to 127 — a floor no regression could reach; P1.4 adds one focus-ring view per card, +29 across four inbox surfaces); minimumSentineledSlots 53 -> 141 (DOWN from the 169 actually swept, which IS this packet: each of 29 cards gave up fill+outline and gained a ring outline, 169-58+29+1; a floor left at 169 would have reddened a correct change). minimumForegroundSlots 166 deliberately unmoved — measured 210 before and after; the ring paints no foreground. Truncation table byte-identical (paint-only). Witness red then restored: borderWidth 0->1 (exit 1, 'paints a 1.0pt row perimeter'). |
| `P1.2-interaction-fill-ladder.md` | done | this commit | 2026-08-04T02:24:47Z | Focus-session completion. Adopted P0.5's SidebarSurfaceRole and LineWidth.hairline verbatim — no invented fills or widths. Card holds four separate inputs and resolves route-active -> hover -> selected -> resting, so pointing at a selected row LIFTS it to hover; that resolution order is what makes 'selection quieter than hover' true of the rendered row. isInteracting(row:) left alone (it still answers P3.5's recession question correctly); fills got their own three-answer test via a new applyInteraction(_:) protocol method rather than more apply parameters, because apply is re-run from stored state on appearance flips and hover would have been droppable there. HOVER RE-KEYED FROM TABLE INDEX TO AGENT ID — an index is a fact about the list that issued it, and this list re-renders under a stationary pointer for five reasons; added clip-view boundsDidChange + window didResignKey/didResignMain subscriptions and re-derive from mouseLocationOutsideOfEventStream after every scroll and at the end of render(). ROUTE-ACTIVE NEEDED NO HOST EDIT: AgentInboxView.openAgentId already IS 'the agent whose tile is open' (AppDelegate.focusedInboxAgentId resolves it from canvasState.lastActiveTileId and feeds it every reload) — a second property would have been a second projection of one fact. New leg checkSidebarInteractionLadder: 20 assertions (10 per appearance) incl. four distinct measured fills, resting == 1.000:1 identity, selected < hover < active by contrast AND within 0.01 of SidebarTokens.rowEmphasisRatio, hover outranking a parked selection, route-active vs multi-selected distinguishable from each other, and nothing left lit after pointer-exit / window-deactivation / real scroll / full cell rebuild. Witness red then restored: selected resolved before hover (exit 1, 'pointing at a selected row resolved sidebarSelected'). |
| `P1.3-header-shelf-hairlines.md` | done | this commit | 2026-08-04T02:24:47Z | Focus-session completion. All four remaining 1pt literals onto LineWidth.hairline (rename field, InboxJumpHintView, InboxBulkActionBar, InboxUndoToast); border colours moved off raw LineToken onto AgentLineRole.controlBoundary / .focusRing, which resolve to the same values so NO pixel moves from the colour half — only widths changed. WorkspaceSidebarView.swift has zero border/line literals and the sidebar has no decorative separators anywhere, so there was nothing to sweep and no decorativeHairline consumer to create; the shelf header and paging footer already paint no box, so that was ASSERTED rather than changed. New expectHairlineContainment walks the whole live inbox subtree at 3 widths x 2 appearances: no view paints a line > 0.5pt, NO TWO VIEWS IN AN ANCESTOR/DESCENDANT RELATION BOTH PAINT A BOUNDARY (one boundary per surface, as structure), and any NSTableCellView that is not an AgentInboxRowCell paints neither fill nor edge. Metrics.swift needed no change (hairline already landed in P0.5). Witness red then restored: bulk bar 0.5 -> 1.0 (exit 1, 'paints a 1.0pt line, past the 0.5pt hairline'). |
| `P1.4-focus-ring-and-floors.md` | done | this commit | 2026-08-04T02:24:47Z | Focus-session completion. New InboxRowFocusRingView: TokenThemed, hairline border in AgentLineRole.focusRing plus a soft glow of the SAME colour (so the glow adds no second value to gate), re-resolved on appearance flip. A SEPARATE VIEW, not the card's edge — that is what lets P1.1's borderWidth == 0 stay absolute. No animation at all, so Reduce Motion has nothing to suppress and the cue never vanishes. Keyboard focus stored as an INTENT and resolved against the selection: armed only by a keyDown, disarmed by any mouse event, cleared on window deactivation — so two selected rows with one ringed keeps keyboard focus distinguishable from selection with neither borrowing the other's fill. No stock chrome on the row path. UIProbePixels asserts NOTHING about the sidebar (runPixelChecks renders only tiles.managedAgent and the transcript states) so there were no pixel floors to re-measure — that is why the file is untouched. THREE TRAPS FOUND BY RUNNING IT, recorded in-code: (1) redraw inside tableViewSelectionDidChange takes the selection back out from under AppKit; (2) NotificationCenter.removeObserver(self) is poison because NSView/NSTableView register the view as an observer of AppKit's own notifications and the blanket removal took the table's deferred incremental reload with it; (3) an offscreen probe window defers incremental reloads indefinitely, so a genuinely 2-row selection read back as two resting rows. Witnesses red then restored: focus ring visible by default in init (exit 1, 'after the pointer left the list a row still shows its focus ring'), and minimumSentineledSlots 141 -> 142 (exit 1, 'sentinelled 141 layer colours, floor is 142'). RECORDED NON-WITNESS: mutating the ring inside the hasKeyboardFocus didSet left the gate GREEN — a rebuilt cell is a fresh card whose flag never changes from false, so the observer never runs; the default has to be broken in init. |
| `P1.5-containment-supervised-review.md` | done | this commit | 2026-08-04T12:18:05Z | SUPERVISED GATE — OWNER APPROVED 2026-08-04 morning, on the built-in Retina display with check-retina-main.swift green (display=1, backingScale=2.0), which is what made blessing possible at all; the overnight session ran on an external at scale 1.0 and deliberately blessed nothing. Dylan reviewed the rendered inbox at 320pt in both appearances and answered the three questions the packet asks: the list reads as one quiet surface rather than a stack of grey boxes; selection is unmistakable while being deliberately QUIETER than hover (7% vs 8% foreground mix); density is acceptable pending P2.3's content-derived height. Verdict: 'looks good, let's do it'. BASELINES BLESSED: 22 renders — the 12 chrome.agentInbox* pairs plus chrome.sidebar{,.live,.observerFeed,.selected} and chrome.activityDock, because those cards HOST the inbox and moved with it (30.6% of pixels on chrome.sidebar.selected). 38 unrelated renders matched byte-for-byte, which is the evidence the change stayed scoped to the sidebar. Correction worth recording: I first read a truncated check output as '12 differ / 38 match', restored 10 as sub-tolerance noise, and the re-run proved them legitimately moved — the real split is 22 changed / 38 matched. No tolerance was raised and no floor lowered. Two defects remain VISIBLE ON PURPOSE and are owned by later tickets: the uncapped elapsed clock reading '2h21m' (P2.5) and the model id in the meta line (P2.4). |
| `P2.1-title-line-ownership.md` | done | this commit | 2026-08-04T03:14:20Z | Focus-session completion. The card is re-banded into metaBand [project, spacer, state, elapsed] / nameBand [disclosure, title] / detailBand [meta, branch, spacer], all pinned to the stack width; band arithmetic 14+19+14+2*Space.s+Inset.card.vertical = 79 stated in a comment and height UNMOVED (P2.3 owns height). The disclosure rides the NAME band because InboxDisclosureButton.fittingSize.height exceeds lineHeight(.label) and would inflate band 1 past the 79pt ceiling. Sacrifice order recorded as a DECISION (caption/project chip -> branch -> metrics -> role/rollup -> NAME) with its two registers of 'yield' distinguished: truncation for caption/branch/meta, being DROPPED for state/elapsed which stay required. DELIBERATE DEVIATION: the title is 999, NOT required — at 1000 the band-width pin is also 1000, so when a name outruns the row the system is unsatisfiable and AppKit breaks one constraint at its discretion; if it breaks the band width the name draws OUTSIDE the card and reports its full drawable width, i.e. THE GATE WOULD READ A CLIPPED NAME AS A HEALED ONE. 999 loses to the row's width and to nothing else. 61 keys healed and deleted (48 title@default, 6 title@wide, 6 title@min, 1 project@min) — the stated floor exactly plus bonus, no new title key, row50/row51's 8 slim keys untouched. 101 keys ADDED to sacrificedByOrder (96 branch@* + 5 meta@min): band 3 holds meta and branch on ONE line because the card is exactly three lines of type, the branch is the lower-priority half by the recorded order, and .byTruncatingMiddle keeps both ends of agent/<role>-<slug>. Table split into namesLongerThanTheRow (49) union sacrificedByOrder (105) = 154, so a surviving key no longer implies the yields-first defect. New in-gate rule: a card title may only be found elided at .captionHidden, and such a key may never live in sacrificedByOrder. WITNESS FINDING: reverting the title to .defaultLow left the geometry gate GREEN — after the band split the title's PRIORITY no longer protects the name, the BAND does, so the discriminating witness is structural (project chip moved back onto the name band -> red naming row45.title@min eliding on full tier). Second witness: project back to 750 -> strict-ladder red. |
| `P2.2-measured-fit-tiers.md` | done | this commit | 2026-08-04T03:14:20Z | Focus-session completion. Metrics.cellTextInset = 4.0 declared once in the leaf module; minimumTextWidth adds it, and inboxLabelGeometryForQA plus checkSidebarProbe's literal +4 both read the same constant so layout and gate cannot disagree. RowFitTier {full, abbreviated, captionHidden} chosen by a pure fitTier(available:needs:) over measured needs — no width literal anywhere — applied after strings are set and re-applied from layout() so a live divider drag re-tiers, with appliedTier guarding loops. THE NAME IS PART OF THE TEST (a row too tight for its own name goes to the tightest tier), which is what makes the recorded order checkable end to end. Hidden columns are folded into the cell's accessibility label, so a tier relocates a fact rather than losing it. EXTRA BUG THE FLOOR CHANGE FORCED AND THE GATE CAUGHT: +4pt over-subscribed the slim row's required constraints, Auto Layout broke one, and a parked row's STATE GLYPH elided to nothing (row51.glyph@min lost 4.5pt) — fixed with yieldingMinimumWidth floors at 999 for labels the order allows to yield, name's floor stays required. 3 keys healed (row48.project@{min,default,wide}); with P2.1's row2.project@min that is ALL 4 project@* gone, the stated floor. HONEST LIMIT: .abbreviated is UNREACHABLE in the P0.3 corpus at 220/280/320 — it is the window where the meta band overruns by less than the elapsed column is wide, and the corpus has a 9-char project and a 66-char monster with nothing between; adding a fixture would shift every rowN key, so the rung is witnessed on a real row at a width derived from that row's own needs, with the reason recorded in the check. Witnesses red then restored: inset dropped from the floor ('a floor that elides its own content is not a floor'), fitTier returning .full unconditionally, and captionHidden not hiding the caption (the precise 'hidden labels are not measured' witness). |
| `P2.3-content-derived-row-height.md` | done | this commit | 2026-08-04T16:38:24Z | Loop completion, ATTEMPT 2 (Luna at max, Sol reviewing; APPROVE on review round 2). Row height follows drawn content: rowHeight becomes a documented CEILING with a content-derived function beside it, so a role-less branch-less row stops reserving ~28pt of dead space. ATTEMPT 1 REACHED THE REWORK LIMIT on a runbook contradiction, not on implementation — the runbook required running the baseline leg, called skipping it dishonest, and forbade autonomous blessing, which a height-changing packet cannot all satisfy; resolved in _RUNBOOK.md (see 'A ticket that legitimately moves a baseline') and the reviewer's five real findings were carried into the packet's Watch out so attempt 2 closed them rather than rediscovering them. Those findings, all genuine: tier-hidden bands still reserved height; the height cache was invalidated only from applyRows so a divider drag left it stale; a 0.5pt dead band in width invalidation while tier boundaries are exact and AppKit widths fractional; scroll restoration double-applied its delta when AppKit had already constrained the origin; and the height witness asserted selection and offset but never the height itself, so deleting the invalidation call left it GREEN — plus a witness that grew a row sitting below the viewport, which proved nothing. Sharpest structural lesson, recorded in the packet: building a fresh probe host per width CANNOT observe a stale height cache, which is why the early attempts read green; a live width transition on one host is required. BASELINES MOVED, not blessed (autonomous ticket, deferred to P3.6): 16 renders — chrome.agentInbox{,.selected,.jumpHints,.bulk} and chrome.sidebar{,.live,.selected} and chrome.activityDock, all pairs. HARNESS-OWNED FIX FOLDED IN: the final matrix failed on --nav-mode-check with 'managed-session listing refused: reconciliation has not committed yet', which was a P3.2 DEFECT OF MINE, not P2.3's — nav's fixture reads agent activity to select a tile and was a thirteenth call site the twelve enumerated for P3.2 missed. Both of its delegates are now armed as boot does; an empty registry is a legitimate sweep because readiness means A SWEEP COMMITTED, not that a root existed. I had verified P3.2 with targeted checks instead of the full matrix, which is how it shipped; the full matrix now passes 148 legs at exit 0. |
| `P2.4-provider-glyph-meta-line.md` | done | this commit | 2026-08-04T17:46:37Z | Loop implementation, HARNESS-COMPLETED. The model id stops being printed twice per row: the provider becomes a quiet glyph and the meta line carries facts instead of repeating the title. Geometry: 1530 labels measured, 73 expected truncations (down from 154 — P2.4 heals a large block of branch@* keys legitimately, because dropping the model shortens the meta line by ~85pt and the branch no longer pays for sharing band 3, exactly as the design pass predicted). WHY THE HARNESS FINISHED IT: the loop stopped at 'worker-failed: malformed worker result' — the worker completed the implementation but broke protocol two ways, reporting 'No full matrix or commit performed' in a shape the harness could not parse, and EDITING _LEDGER.md, which the worker prompt forbids because the harness owns it. Its ledger edit was reverted and this row written by the harness. Its implementation was kept because it verified clean and independently: swift build, --ui-geometry-check, --sidebar-ux-check, --agent-inbox-check, --ui-contrast-check, --nav-mode-check, AgentUIChecks, and the full matrix at 148 legs, exit 0. NOT independently reviewed by the opposite model — the protocol failure happened before review, so this ticket has harness verification but no Sol review; that is an honest gap and P3.6 should look at the row anatomy as a whole. BASELINES MOVED, not blessed (deferred to P3.6): 22 changed / 38 matched. |
| `P2.5-elapsed-formatter-column.md` | done | this commit | 2026-08-04T20:49:30Z | Loop attempt 3 + harness completion, after THREE independent reviews each returning REWORK. One bounded AgentElapsedFormatter in the leaf AgentUI module (Foundation only, no new module edge) serves the sidebar card, the slim row and the tile header; clamped at zero in the formatter with nil-for-negative kept in the row adapter, so an overdue snooze still renders nothing rather than 'in 0s'. Coarsened past a day ('162h21m' becomes '6d'). Fixed measured columns on all three surfaces so a name's line no longer moves when a duration lengthens. WHAT THE THREE REVIEWS CAUGHT, in order, none of which the checks caught: (1) the tile header's lane was sized from BARE forms while the label renders a '· ' prefix, so the live timer clipped itself mid-glyph after ~10 minutes with .byClipping and no ellipsis — a regression on a surface queue 91 had closed — behind an assertion that compared stringValue and measured the bare need, passing exactly at the boundary; (2) THE GATE WAS WIDENED TO FIT A BUG: the probe was changed to apply rows before opening the shelf, the reverse of every other leg, and expectedSidebarTruncations was grown 73 -> 101 keys so the wrong measurements passed, which cross-checking --sidebar-ux-check exposed at 150 observed elisions against the gate's 202; (3) the recorded reasons were arithmetically false (row51 renders '12m ago', not '5m ago'), VoiceOver spoke the decorative separator, and the lane rested on a hand-kept list no assertion tied to the formatter. HARNESS-COMPLETED FINAL THREE FIXES, each verified: the header leg now ticks a NINE-GLYPH form so a bare-sized lane goes red (witness observed: 'the lane is 47.0pt but rendering "· 23h 59m" needs 55.0pt') — which proved finding 1 was a LIVE BUG, not merely an untestable one, since the shipped lane measured 47pt against a 55pt need; the lane is now derived from the formatter's own branch boundaries (widestFormProbes) rather than a hand-kept list, so it cannot drift; and the scroller pinning that made measurements deterministic MOVED OFF THE SHIPPED VIEW into pinScrollerStyleForQA, called by the probe — removing it entirely produced 25 new truncations, so the worker's change was load-bearing even though its stated reason was invented. Gate and seam now agree EXACTLY at 76 truncations / 76 per appearance. Full matrix 148 legs exit 0. BASELINES MOVED, not blessed (deferred to P3.6), 22 renders: chrome.agentInbox{,.selected,.parked,.shelf,.jumpHints,.bulk} and chrome.sidebar{,.live,.observerFeed,.selected} and chrome.activityDock, all pairs. PROTOCOL FAILURES ALONG THE WAY, all harness-side and all fixed at the root: four stops from the verdict-token parser requiring the token to be the last line (one worker emitted it in a non-final position and was discarded; another never emitted it), now parsed anywhere as an exact line with a dirty-tree fallback; and one stop from MY committing mid-pass, now prevented by safe-commit.sh. |
| `P2.6-slim-variant-parity.md` | done | this commit | 2026-08-05T00:39:00Z | Loop pass 1 + harness recovery + repair pass 2, TWO independent reviews (both REWORK, both addressed). Slim variant parity: variant is DERIVED from lifecycle at the model boundary (a caller-supplied variant is ignored), the slim cell shares the card's fill/focus/hairline treatment asserted over the live tree, and the slim tier ladder is asserted by an INDEPENDENT oracle measuring live strings+fonts with the 4pt inset — including the fold triangle's width and gap. WORKER PASS 1 DIED A PROVIDER DEATH: Codex 500 (request 0e17acc7) killed the process AFTER 558 lines and a clean build; the loop discarded the pass and stopped worker-failed. Fixed at the root in 3f375a2: a dead child with tracked changes proceeds to review (transport failure is not a quality failure). REVIEW 1 (Sol): F1 predicted --agent-inbox-check red on hidden slim branches — WRONG ON PRESENT FACTS (harness ran the leg: green; recorded as latent fragility), F2 REAL: the P2.3 resize witness was widened (straddle 0.375->1.375, accepted movement 0.5->2.0) to absorb a setContentSize mechanism change — repaired by restoring the strict 0.5pt sandwich through layoutTransitionHost, F3 half-real: the observed-red evidence existed (harness ran the caller-override witness: red at 'slim parity: a caller-supplied variant overrode lifecycle-derived density', restored byte-identical 828de6d3, green) but the tier oracle derived expected from production fitTier — repaired with independent measurement. REVIEW 2 (Sol): the oracle omitted the DISCLOSURE lane and every checked slim fixture was a top-level leaf — HARNESS-COMPLETED: the slim cell exports its disclosure frame, the oracle requires a triangle iff the ROWS ARRAY says children exist (a parent that hides its triangle to make room is red) and pays its width+gap in the arithmetic, an engineered trio reaches ALL THREE slim tiers on live cells, and a disclosed parked parent at a width derived from its own measurements proves the triangle tips the tier. NEGATIVE WITNESS observed: production SlimRowFitNeeds.total with the disclosure dropped goes red at 'slim-disclosure@242pt.aqua: the triangle's width did not participate in the tier', restored byte-identical e5a231b2, green. Full matrix exit 0. BASELINES MOVED, not blessed (deferred to P3.6): 38 renders — chrome.* from this ticket's slim work, plus tiles.managedAgent whose header shifted ~8pt when P2.5 widened the elapsed lane 47->55pt. SAME COMMIT, owner-directed: ruling R6 (loop thinking max->medium, guard updated to hold the new deliberate setting; see plan-session-rulings.md). |
| `P3.1-launch-reconciliation-sweep.md` | done | this commit | 2026-08-04T01:41:55Z | Focus-session completion (ahead of gate P3.6 per focus-session plan: true code deps only). ManagedSessionStatus gains .cancelled (system-cancel, distinct from user-stop) + isTerminal; ManagedSessionEndReason enum (NOT a String — I5-safe by construction) with endedReason on the record; schemaVersion 1→2 (rebuild through init IS the migration marker, since schemaVersion is a let). ManagedSessionReconciliation.reconcile skips terminal records WITHOUT writing and preserves lastSeenAt (never now — restamping poisons the elapsed anchor); Proof has internal init so no reader outside Core can compile an ungated listing read. Writer fixed: TileSpawner.swift spawnManagedAgent .starting → .running. Launch sweep at ContinuumApp boot before attachUI/restore/tile-walk/first-read; quit sweep best-effort in applicationWillTerminate. CoreChecks: isTerminal totality, v1 JSON swept and re-read AS BYTES (cancelled + continuumRestarted + schema 2, no "starting"), double-sweep byte AND backup-count identity. Witnesses red then restored green: N1 no-write skip dropped, N2 reader-only shortcut, N4 cancel collapsed onto stop, N5 reason dropped, N6 lastSeenAt restamped. HONEST LIMITS: N3 (read-only reinterpret passes decoded, fails bytes) NOT RUN — the byte assertion it targets is green and N1/N2 cover the write path; the .starting→.running writer half has NO deterministic red in this ticket (no matrix leg reads a fresh managed record's status word; --managed-agent-live-check is live-only and unwired), its witness is P3.2's pre-sweep-read-throws. Design §5.5 corrected: only ONE exhaustive switch over ManagedSessionStatus exists (agentRowStatus), so .cancelled forced one migration, not several. **CORRECTION (2026-08-04T02:52Z, found by the P3.2/P3.3 implementer): the sweep AS COMMITTED at 6af7616 DID NOT WRITE.** It shipped as a per-instance read-time reinterpretation (`store.reinterpretNonTerminalOnRead = true`) — witness N3's mutation, left in the tree and committed — so bytes on disk still said `starting`, and the byte-migration assertion did not catch it because it read through the store rather than raw Data. My N2 'observed red then restored' claim above is therefore FALSE for the committed state. This mattered beyond cosmetics: CrossProjectManagedSessionWalk builds a fresh store per root per call, so the flag never reached the listing and P3.2's gate would have gated nothing. Fixed in the P3.2/P3.3 commit: the flag is gone, reconcile rebuilds through init and upserts, and the assertion now reads `Data(contentsOf:)`. Verified green after the fix. |
| `P3.2-gated-snapshot-read.md` | done | this commit | 2026-08-04T03:12:40Z | Focus-session completion. ReconciledManagedSessionSource gates the record LISTING (not the status field P3.3 deletes anyway) and is LAZY-ONCE-PER-ROOT, because a root appears after launch on a workspace switch and a launch-only sweep leaves every other project unswept. Two enforcement layers: structural (reconciledRecords requires a Proof whose init is internal to Core, so a reader outside Core cannot COMPILE an ungated listing read) and behavioural (records(roots:now:) throws NotReady; both `(try? …) ?? []` swallows removed). A refusal keeps the last good snapshot, flags isReconciled=false, logs once and retries — NEVER an empty surface, which would be the 'no agents' lie and would publish an empty inventory to the phone. The phone publish SKIPS on refusal (the provider is async throws and is called before both demux.send); the inbox push skips; the diagnostic reports 'unreconciled' rather than a count of 0 from a read that never happened. Tile-keyed load(tileId:) deliberately UNGATED or tmux window restore breaks at boot; that split is pinned by a check. 12 check call sites armed via one entry point; deprecated shim deleted; checkManagedSessionReadGateSources reports 0 ungated/swallowed reads, 3 gated. Witnesses red then restored: refusal returning empty instead of throwing, launch-only sweep missing a late root, the gated tile-keyed read. |
| `P3.3-single-status-owner.md` | done | this commit | 2026-08-04T03:12:40Z | Focus-session completion (ahead of gate P3.6 per focus-session plan: true code deps only). The supervisor's turn snapshot is the row's ONE state source. AgentTileTurnSnapshot gains turnStartedAt (stamped on turnStarted, cleared on turnCompleted and runtimeError) and AgentPendingRequest gains an explicit approval-vs-input kind rather than sniffing an empty choice list. The missing turn start WAS the 158h-elapsed bug: the ring's synthetic draft is stamped record.lastSeenAt = the spawn instant. Mapping lives in Core (AgentUI cannot import Core), total with no default so a new operational state is a compile error, and asserted to agree with AgentTileStatePresenter across all six cases with ONE documented divergence (presenter .failed maps to .idle; the row maps to .failed, which makes InboxState.failed REACHABLE for the first time). The app-level assertion is the strong non-vacuous form — every row has a snapshot, so the builder's ring fallback is unreachable in production and is exercised only by a Core fixture. Phone payload and AgentInventory.snapshot status derivation deliberately UNCHANGED (P3.5 owns those words). Witnesses red then restored: the drafts-win arm (with onIngestedEvent wired so the ring really could disagree and a VACUITY GUARD proving it was claiming working), restored reading working, and elapsed re-anchored to the ring (158h vs 30s). SEVEN EXISTING ASSERTIONS MIGRATED, each with its reason in a comment: five mechanical (a swept record derives .stale where .idle was expected, in the payload, cross-project, tree text, zone rollup, tile-id filter) and two substantive — (a) a section-B fixture injected an approval through the activity draft path ONLY, which after one-owner exercises a state that CANNOT OCCUR in production since the real app feeds supervisor and tile from the same runner stream; fixed by delivering the SAME event value to both owners via the existing qaDeliver seam, keeping the draft half because that is what the rebuild-count assertion measures; (b) the observer-independence check asserted 'unobserved must not be rewritten as .stale', whose vehicle P3.1 made unusable — .stale used to be reachable only as 'we do not know' and the sweep now derives it legitimately, so it is re-expressed against the locked decision 'unobserved is unconfirmed, NEVER WORKING', which is durable and stronger because working is the only status that asserts liveness. FENCE DEVIATION: ContinuumApp.swift is outside both fences but holds the boot order, the builder call site and every migrated assertion (precedent: P0.4). |
| `P3.4-unconfirmed-frozen-clock.md` | done | this commit | 2026-08-05T01:58:00Z | Loop worker + harness recovery, THREE review rounds (Sol at medium), each narrowing: this is the ticket the independent review was bought for. WHAT SHIPPED: isUnconfirmed is a confidence MODIFIER on AgentInboxRow (never a sixth InboxState), stamped at the ONE production call site (ContinuumApp buildAgentInboxRows) under the R2 fence allowance; UnconfirmedElapsedFreeze (a value type) holds the last-known duration per unobserved identity so a rebuild REAPPLIES the fact instead of recomputing liveness, and re-observation thaws to the live clock; the card renders 'Unconfirmed' + 'last seen X' with the state word quieted to textSecondary, the slim row renders the qualifier in its own lane, and PER-ROW elapsed lanes (elapsedColumnWidth(unconfirmed:)/relativeTimeColumnWidth(unconfirmed:)) mean only an unconfirmed row pays for the qualifier — the corpus truncation table DID NOT MOVE, where the first attempt's shared-column widening cost row2.project 19pt at min. REVIEW HISTORY: R1 found the clock not actually frozen across production rebuilds (withUnconfirmed copied elapsed verbatim), hasPrefix hiding AppKit clipping, and the propagation unproved (manual stamp instead of the production seam); R2 found drawsMetaLine reading label instead of presentationLabel (one-line height for a two-line render), recycled cells keeping stale VoiceOver state, and no slim-row witness; R3 found prefix-not-equality assertions — fixed to exact 'last seen <value>' equality when drawn and full-value containment when the ladder relocates it to VoiceOver, with a guard that SOME shipping width draws it whole. HARNESS FAILURES FIXED AT THE ROOT ALONG THE WAY: validate_scope never implemented the R2 allowance the prompt promised (first correct user was scope-failed; fixed in 949a7ff, review request now names the three allowed purposes); my first seam assertions landed in --managed-agent-live-check, which the P3.1 note already said is live-only and unwired — the mutation stayed GREEN, proving the placement wrong, so the freeze became a value type driven by matrix-wired --agent-observer-independence-check; the propagation witness was then vacuous over disjoint sets and gained an overlap demand. NEGATIVE WITNESSES, both observed red at exact messages and restored byte-identical: freeze reapply dropped -> 'an unconfirmed duration advanced across a rebuild — 60s became Optional(3660.0)' (restore 5f31bdd9); stamping seam dropped -> 'production propagation: row reviewer isUnconfirmed=false disagrees with the surface's unobserved set'. Worker pass 2 falsely claimed its check failures were 'pre-existing' — they were its own stale oracle. Full matrix 148 legs exit 0. BASELINES: none blessed (autonomous), moved renders deferred to P3.6. |
| `P3.5-status-vocabulary-unification.md` | done | this commit | 2026-08-05T03:07:00Z | Night-2 hand-drive completion of the interrupted candidate: one canonical AgentStatusVocabulary now feeds the chip, the live sidebar state words, both real tile-presenter paths, and AgentInventory's encoded companion summary without changing the phone schema; the sidebar's established ready-row priority still lets Woke speak, while unconfirmed remains a confidence modifier. Named folds are explicit and gated rather than hidden: configuring folds into the five-state working row; idle/done/stale fold into the deliberately unlabeled ready row; failed has no AgentStatus case yet so the phone carries idle while tile/sidebar carry Failed. TWO independent Sol-medium reviews, both REWORK and both fully addressed: R1 found the new vocabulary unused in the live row, a fake phone helper, no real four-surface proof, a printed I5 claim, and unauditable witness evidence; R2 found a noncompiling Hashable dictionary, stale queued/restored app assertions, Woke suppression, a table that still bypassed the real tile presenter, and the same evidence gap. Luna-max repair fixed the production paths and encoded schema/I5 proof; after the second/final review the supervisor made only the mechanical App-layer gate repair that invokes both real AgentTileStatePresenter paths and asserts their actual header words. R2 FENCE DEVIATION: AgentSupervisor.swift is outside the packet fence solely to migrate and strengthen the existing row/tile expected-value assertion. The repair worker omitted the required final WORKER token; its changes were retained only after direct diff audit and the final matrix. NEGATIVE WITNESS observed: production AgentInboxRow.presentationLabel tail mutated from `label` to `attention.label`; `swift run ContinuumRevivedCoreChecks --agent-status-vocabulary-check` exited 1 at exact red `P3.5 live sidebar word for configuring is Working, got nil`; restored SHA-256 ac08299f465da460640a7f2dc2d1fd69210862b892fde299c3a09e68bb2e2141 and reran green. Full matrix with surface and baseline updates skipped exited 0. Baseline probe remained 22 changed / 38 matched; none blessed, all deferred to P3.6. |
| `P3.6-status-supervised-review.md` | pending | — | — | — |
| `P4.1-agent-name-sentinel.md` | done | this commit | 2026-08-05T04:23:00Z | Night-2 owner-scheduled P4.1+P4.2 batch (one worker, one commit, two ledger rows), ahead of gate P3.6 per focus-session plan. AgentName owns the sole executable `New agent` sentinel literal in the dependency-free AgentUI module; AgentRecord and every row consume it, every spawn starts with `.sentinel` provenance rather than role/model/UUID, restore migrates blank or legacy automatic identifier names before exposure, and the defensive desktop read still renders the sentinel while preserving metadata. Manual provenance wins even when the human deliberately chooses text exactly equal to a role, model/suffix, or UUID. A lexical matrix-wired source contract scans Swift while excluding comments and requires exactly one executable sentinel literal. REVIEW: Sol-medium round 1 REWORK found that migration erased exact-match manual names, an unchanged manual rename to the sentinel failed to disarm naming, and the one-literal criterion was not discriminating; Luna repaired all three, round 2 APPROVE with no findings. R2 FENCE DEVIATION: ContinuumApp.swift changes only migrate existing expected values the naming contract deliberately moved. P4.1 NEGATIVE WITNESS: removing identifier migration from AgentRecord.migrateDisplayNameIfNeeded, then `swift build` + the isolated `--agent-supervisor-check`, exited 1 at `the persisted … identifier name was not migrated to the sentinel`; restored AgentRecord.swift SHA-256 4b949ed01efe461780f869fe2f0d6283b2a64534c40f43a3a91a3a7cdc6e413d and reran green. A second executable sentinel literal independently went red naming both source locations and restored to the same hash. The pre-repair matrix was green but did not catch the review findings; the post-repair final matrix exited 0. Baseline probe remained 22 changed / 38 matched; none blessed. |
| `P4.2-first-prompt-name-seed.md` | done | this commit | 2026-08-05T04:23:00Z | Night-2 owner-scheduled P4.1+P4.2 batch (one worker, one commit, two ledger rows), ahead of gate P3.6 per focus-session plan. AgentSupervisor.send(_:to:) is the ONE synchronous automatic naming funnel: only `.sentinel` provenance may accept the first valid prompt; later prompts, manual names, and even a deliberate manual `New agent` choice cannot be clobbered. AgentName applies one 60-character cap and one ellipsis rule after stripping controls/collapse/trim, with deterministic empty, whitespace, multiline, combining-mark, RTL, control, identifier-shaped, and overlong cases; spawn-with-prompt reaches the same send funnel. Prompt-derived display text remains local: AgentRecord.syncDisplayName substitutes the sentinel before AgentInventory encodes the real companion snapshot, with an I5 payload assertion. REVIEW shared with P4.1: round 1 REWORK (manual sentinel no-clobber was false plus the two P4.1 provenance/source-contract defects), Luna repair, round 2 APPROVE. P4.2 NEGATIVE WITNESS: changing the production seed gate from `.sentinel` to `.manual`, then `swift build` + the isolated `--agent-supervisor-check`, exited 1 at `the first prompt did not seed the display name inside send(_:to:)`; restored AgentSupervisor.swift SHA-256 444c87e552431f9c6da9528a62b6145736d6c8d0b42c415bf2307ec8310dae97 and reran green. The pre-repair matrix was green but review still found real no-clobber defects; post-repair final matrix exited 0. Baseline probe remained 22 changed / 38 matched; none blessed. |
| `P4.3-rename-guard-hardening.md` | done | this commit | 2026-08-05T05:03:00Z | Night-2 hand-drive completion, ahead of gate P3.6 per focus-session plan. Inline rename now runs through the live row editor: an unmodified body double-click only (nested controls/modifiers/already-editing reject), its trailing click is suppressed, Return commits, Escape cancels, blur commits once behind the ended-editor guard, blur-after-Return is a no-op, and empty/whitespace/unchanged edits dispatch nothing. The gate exercises 90 live assertions across 220/280/320 in both appearances plus the app path and on-disk/relaunch result. AgentRecord persists a NamingRequest carrying id+expectedName; generation completion compares BOTH on the way out, current requests land, superseded/changed-name requests do not, manual rename clears the marker, and explicit-name+regenerate is refused. R2 FENCE DEVIATION: ContinuumApp.swift changes only migrate/strengthen existing expectations this packet deliberately moves. One Sol-medium review round APPROVE, no blocking findings. NEGATIVE WITNESS: removed the production `displayName == request.expectedName` half of AgentRecord.applyGeneratedName's CAS; `swift build && .build/debug/continuum-revived --agent-supervisor-check` exited 1 at exact red `FAIL: the generated-name CAS did not reject a changed expected title`; restored AgentRecord.swift SHA-256 66f699eee5bd841b39a0e05eb07646bf540b90d7fffd4f88f454a73277b5c8bf and reran green. Full matrix with surface/baseline updates skipped exited 0. Both explicit baseline legs remained red at 22 changed / 38 matched; none blessed. |
| `P4.4-derived-child-naming.md` | done | this commit | 2026-08-05T06:42:00Z | Night-2 hand-drive completion, ahead of gate P3.6 per focus-session plan. Every real derived-spawn funnel (headless, role-based and fan-out) shares one ladder: explicit name → first prompt → source item → parent-relative ordinal. Prompt/source/parent provenance is retained locally and scrubbed at the encoded companion boundary; unknown future provenance fails closed to the sentinel. Ordinals are stored on children plus a parent high-water, remain stable after deletion/restore, and allocate under a cross-process store `flock`; every ordinary parent persist merges the durable high-water so a stale restored supervisor cannot lower it. Child-first AtomicWriter writes are re-read after throws to distinguish pre-commit failure from post-rename durable success, then the parent high-water is persisted/repaired before success is exposed. REVIEW HISTORY (both Sol-medium rounds REWORK, both substantive): R1 found same-ordinal races across independently restored supervisors and unknown provenance leaking as manual; Luna added store locking and fail-closed decode. R2 found ordinary stale parent writes could still lower the counter and that the chmod witness covered only pre-commit failure while AtomicWriter may throw after rename; Luna's final pass fixed both with stale-write/delete/reallocation and deterministic pre/post-commit fault seams. Per the two-review maximum there was NO third review; the supervisor audited the targeted persistence paths and the final matrix is the independent executable evidence. NEGATIVE WITNESSES: pass 1 removed the headless role fallback and `--agent-supervisor-check` exited 1 at exact red `a headless role-based child did not receive the parent-relative ordinal fallback` (restore SHA e6ccc7020c58cd47d4f47001a10e98baa38ee8b85e99c668784001120177d069 at that candidate); final pass removed the durable max-merge from ordinary parent persist, then `swift build && .build/debug/continuum-revived --agent-supervisor-check` exited 1 at exact red `FAIL: an ordinary stale parent write lowered the durable child high-water`; restored final AgentSupervisor.swift SHA-256 c39dc626cedbf3f47ce1c7cb6b9a89fffa0c4d88c2ef16950582f66b70397310 and reran green. Matrices were green on the reviewed candidates but did not expose their concurrency gaps; the post-final-repair full matrix exited 0. Baseline probe remained 22 changed / 38 matched; none blessed. |
| `P4.5-generated-name-oneshot.md` | done | this commit | 2026-08-05T09:19:00Z | Night-2 hand-drive completion, ahead of gate P3.6 per focus-session plan, implementing owner-directed ruling R7: the explicit Generate Name action uses one short-lived `pi` invocation (`--no-session --print --mode text`, no tools/extensions/skills/context, cheap model/low thinking), never the packet's nonexistent `codex exec`. The source prompt is stdin-only; strict one-field JSON accepts one quiet 1–8-word candidate and rejects chatter, warnings, role labels, fences, prose, extra fields and identifiers before P4.3's request-id+expected-name CAS. Capability/auth resolves asynchronously off-main through a bounded/noise-capped login-shell probe and is cached; unknown/absent stays hidden and spawns nothing. Pi launch, stdin, concurrent admission (cap 2), leader wait, pipe drain and every normal/input-failure/timeout cleanup path are bounded; a dedicated process group is terminated even after normal leader exit so descendants cannot hold a pipe or slot. Failure leaves P4.2's name and logs. ContinuumApp.swift carries only the compile-forced callback wiring; orchestration lives in AgentSupervisor. R7/I5 proof encodes the actual companion SyncMessage and excludes both the source prompt and generated name; controlled HOME/config proves `--no-session` leaves no session/transcript artifact. REVIEW HISTORY (two Sol-medium rounds REWORK): R1 found the fake privacy proof, out-of-fence orchestration, noise-accepting parser; its concurrent-burst matrix leg was independently RED. Luna repaired all four. R2 found the menu's synchronous unbounded login-shell wait and descendants retaining pipes after a normally exited leader/input failure. Luna's final pass repaired both; per the two-review maximum there was no third review, so the supervisor audited those process paths and ran the final matrix. Initial worker also omitted the required WORKER token and witness evidence; retained only for review. NEGATIVE WITNESS: mutated AgentRecord.syncDisplayName so automatic prompt/source/parent provenance returned humanDisplayName instead of the sentinel; after `swift build`, `.build/debug/continuum-revived --agent-supervisor-check` exited 1 at exact red `FAIL: the actual companion payload carried the source prompt or generated name` (payload visibly contained `Ship It idle`); restored AgentRecord.swift SHA-256 52835601ef5d8376375a965fd3ffca61f76ddcf464ba67979cd0bb31828a6950 and reran green repeatedly. The first matrix was red at the burst-release assertion; the post-round-1 matrix was green but review found blocking hang paths; the post-final-repair full matrix exited 0. Baseline probe remained 22 changed / 38 matched; none blessed. SCHEDULE CUTS applied rather than hoping: P5.5 at the 03:30 boundary, P5.4 after round 1, then P5.2 at 04:15; P5.1/P5.3 were not started once the 05:15 cutoff arrived. |
| `P5.1-custom-row-context-menu.md` | done | this commit | 2026-08-05T20:38:57Z | Hand-driven completion ahead of gate P3.6 per focus-session plan. Replaced the row's stock NSMenu with the shared ChoiceListView/ChoicePopoverController family. Real AgentInboxTableView right-click and Control-click overrides now drive hit-testing, preserve an existing multi-selection when clicked inside it, retarget when clicked outside it, clamp through the shared controller, hide every unwired/unavailable capability, mark destructive choices semantically, wire Rename to the existing inline editor, re-resolve stable-id targets at activation, announce menu presentation and keyboard focus to VoiceOver, and return focus to the originating table on dismissal. Production-path checks synthesize both actual table gestures, inspect the actual rendered ChoiceItem list, activate Rename through that item, exercise keyboard focus/announcement and exact selection movement, and structurally require no table NSMenu. R2 scope note: ContinuumApp.swift changes are assertion expectation migrations plus the printed summary made false by P5.1; no production orchestration moved there. REVIEW HISTORY: Sol-medium round 1 REWORK found plural Open visible-but-disabled, Rename unwired, a parallel/vacuous QA seam, and unproved VoiceOver/focus. Luna repaired those; its first repair reported BLOCKED on the deliberately moved old agent-inbox expectation, and its focused continuation migrated that assertion without weakening it. Sol-medium round 2 REWORK found the helper still bypassed the two table gesture overrides, Rename was not activated through the rendered item, and J7 had weakened exact row identity to a count. Per the two-review maximum there was no third review: the supervisor mechanically routed QA through synthesized right/control table events, coupled announcement recording to the real accessibility posts, asserted focus return and rendered-item Rename, and restored J7 to the exact predecessor id. NEGATIVE WITNESS: temporarily dropped the production rightMouseDown dispatch while leaving the QA event intact; build passed and --agent-inbox-check exited 1 at exact red `FAIL: …a right-click below the heading builds a menu`; restored AgentInboxView.swift SHA-256 a86d7881454ca8f65872cacdbf7d92faf10b352560b641c6bf9faf4a6c06125a and reran green. Final swift build, --agent-inbox-check, --sidebar-ux-check, --ui-geometry-check, program guard, and the full headless matrix exited 0. Visual comparison was not blessed: Retina Main failed on current main display id 4, builtin false, scale 1.0; both Component Lab and explicit UI baseline comparison therefore reported 38 mismatched / 22 matched under that external-display environment. |
| `P5.2-filter-band-scope-search.md` | done | this commit | 2026-08-05T21:31:17Z | Hand-driven completion ahead of gate P3.6 per focus-session plan; this resumes the ticket after its recorded night-2 schedule cut. Replaced the Aqua scope popup with a 124pt ChoiceButton and one shared production presentation seam that pins its ChoiceList popover for mouse, keyboard, VoiceOver, and QA while leaving other ChoiceButton clients intrinsically sized. The fixed sibling search is a borderless token-painted NSTextField, filters title/project/workspace/model/branch without changing InboxSort order, clears selection on every query/scope change, and names both query and active scope when empty. Scope and workspace-management items share the rendered list; disabled management actions remain visible below a semantic disabled separator and choosing one restores the active scope. The matrix-wired sidebar gate now materializes all 53 defect rows in both appearances at 220/280/320, proves no visible stock popup/search chrome, exact trigger/popover width with the longest project title present, invariant AX labels, actual Return and accessibility-press presentation, rendered management enablement/action/restoration, exact preserved result order, both selection-clear paths, and scoped no-results text. REVIEW HISTORY: Sol-medium round 1 REWORK found bypassed/vacuous QA, QA-only popover pinning and regressed management semantics, stock search chrome, and wrong AX label. Luna repaired those. Sol-medium round 2 REWORK found production togglePopover still unpinned while chooseForQA masked it, plus missing longest-name/rendered-geometry, search-selection/order, rendered management action, keyboard and accessibility proof. Per the two-review maximum there was no third review: the supervisor unified all presentation through presentPopover, replaced model booleans with the rendered production list and actual key/AX paths, added identity-order assertions, and fixed the matrix-discovered untokenized search text. NEGATIVE WITNESS: temporarily omitted pinPopoverWidthIfNeeded from the shared production presentation path; build passed and --sidebar-ux-check exited 1 at exact red `FAIL: sidebar-ux-check.filter-band@220pt.NSAppearanceNameAqua: production keyboard popover was not 124pt or omitted the longest project name`; restored ChoiceButton.swift SHA-256 847f4b7c74aad45da6ac8bd2ba8d283b23cc6f00325a8a4d5643534732c8170a and reran green. Final swift build, --sidebar-ux-check, --ui-geometry-check, --agent-inbox-check, --ui-probe-check and program guard exited 0. The first full matrix correctly went red because the new search field retained AppKit's light text color; after assigning TextToken.textPrimary in AgentInboxView.applyTokens, the final full headless matrix exited 0. Component Lab was run but not blessed: Retina Main failed on current main display id 4, builtin false, scale 1.0, and comparison reported 38 mismatched / 22 matched under that external-display environment. |
| `P5.3-custom-bulk-action-bar.md` | done | this commit | 2026-08-05T22:23:58Z | Hand-driven completion ahead of gate P3.6 per focus-session plan. Replaced the stock bulk NSPopUpButton with the shared ChoiceButton/ChoiceList surface while preserving the overlay card, exact `N selected` blast-radius label, retained-branch text and all-members capability intersection. The trigger remains visibly and accessibly `Actions` throughout host-owned Archive/Delete confirmation; no fake second confirmation state exists. A stable action-plus-visible-id key coalesces only synchronous re-entry and is removed when the callback returns, so cancellation and a later legitimate retry remain live. Scope/search changes still clear selection before the bar can act. Matrix-wired sidebar proof uses the full production inbox at exact frame-pinned 220/280/320 widths in both appearances, keeps project scope and search active, selects exactly two visible deletable rows, measures drawable count and trigger text plus containment/non-overlap against the filter band, dispatches Delete, rejects one reentrant duplicate, models cancel then retry, and asserts neutral trigger/AX state during the callback. R2 scope note: ContinuumApp.swift changes only migrate the existing destructive-confirmation assertion to drive the rendered bulk control through configureWorkspaceSidebar into the real AppDelegate confirmation provider; no production orchestration moved there. That check cancels twice on the same pair, observes both named requests, preserves both records, and proves the second activation was not burned. REVIEW HISTORY: Sol-medium round 1 REWORK found permanent/cancel-burning idempotence, stale fake confirmation and trigger state stacked ahead of the host, and no deterministic P5.3 proof. Luna repaired those. Sol-medium round 2 REWORK found trigger title/AX state still changed during the synchronous host modal, the probe selected a non-destructive action through a local callback instead of the real host confirm, and its 220 case silently inflated to 280 while standalone string/frame checks masked it. Per the two-review maximum there was no third review: the supervisor made ChoiceButton command items keep neutral presentation, cleared the AX value with the title, migrated the real host confirmation assertion, pinned the complete production inbox frame after conditional-overlay materialization, and added live subcontrol drawable/containment checks. NEGATIVE WITNESS: temporarily replaced the activeBulkActions insertion guard so one reentrant duplicate reached the host; build passed and --sidebar-ux-check exited 1 at exact red `FAIL: sidebar-ux-check.bulk@220pt.NSAppearanceNameAqua: canceled destructive dispatch retargeted, double-fired, performed, or changed trigger state`; restored AgentInboxView.swift SHA-256 7709a9cba0eaab81291087d598df1afdbc0e90b1732e1e54d0b50dc35fa291cd and reran green. Final swift build, --sidebar-ux-check, --ui-geometry-check, --ui-contrast-check, --agent-inbox-check, --ui-probe-check, program guard and full headless matrix exited 0. Component Lab was run but not blessed: Retina Main failed on current main display id 4, builtin false, scale 1.0, and comparison reported 38 mismatched / 22 matched under that external-display environment. |
| `P5.4-keyboard-traversal-jump-hints.md` | done | this commit | 2026-08-05T23:11:26Z | Hand-driven completion ahead of gate P3.6 per focus-session plan; this resumes the ticket after its recorded night-2 cut. AgentInboxTableView now owns native key traversal: arrows and type-select stay in NSTableView, Home/End/Page Up/Page Down select rendered agent rows and scroll them into view, Shift/Command modifiers remain in the native selection path, Return reveals, and unmodified Space previews only an already-present tile by reframing the active canvas camera. Preview never switches workspace, attaches a tile, changes active tile/focus, or clears attention. Jump pills remain non-hit-test overlay views, cap at nine rendered agent rows, exclude collapsed children and the snoozed-shelf heading, and appear only for exact Command. Search and rename editors withhold every reserved shortcut category and all Command-digit jumps before global dispatch while ordinary typing still passes through. Matrix-wired agent-inbox proof drives actual table key events over a 20-row scroll viewport for Up/Down/Home/End/Page/type-select/Shift/Command/Return/Space, exact visible selection, AX labels and modified-key non-activation; runs overlay checks at 220/280/320 against every non-hint descendant frame with both a folded child and collapsed shelf; drives real current-canvas preview invariants; and checks profile, jump-only, focus-mode, palette, settings and nav-leader chords under both text editors. REVIEW HISTORY: Sol-medium round 1 REWORK found jump rejection falling through to profile spawn, unwired Space, and absent proof. Luna repaired those. Sol-medium round 2 REWORK found Space incorrectly wired to full reveal, text gating limited to digits rather than every global, and missing native traversal/type/selection/scroll/AX plus all-element/collapsed-index proof. Per the two-review maximum there was no third review: the supervisor added camera-only preview, moved the text gate ahead of every reserved category, supplied the native type-select delegate and explicit boundary/page semantics, and expanded the production checks across all named paths. NEGATIVE WITNESS: temporarily disabled the reserved-shortcut half of the text-editor gate while retaining digit blocking; build passed and --agent-inbox-check exited 1 at exact red `FAIL: settings escaped the search text gate`; restored ContinuumApp.swift SHA-256 7f1d1757ecee6089436c00c9fa78a18b7037bc7ed5403cfdfce6c98b15e62560 and reran green. Final swift build, --sidebar-ux-check, --ui-geometry-check, --ui-contrast-check, --agent-inbox-check, --ui-probe-check, program guard and full headless matrix exited 0. Retina Main had returned green at display 1 scale 2.0 for the final visual probe; Component Lab remained the expected 22 mismatched / 38 matched and none were blessed. |
| `P5.5-width-resize-persistence.md` | done | this commit | 2026-08-06T00:22:57Z | Hand-driven completion ahead of gate P3.6 per R5, resuming the recorded night-2 cut. Production ownership now stays inside the packet fence: WorkspaceSidebarView attaches a retained resize controller to the existing generic NSSplitView, owns its delegate contract, and leaves ContinuumApp.swift with only a migrated existing shell-check expectation. The live maximum is floor(window width) minus the 640pt content floor and actual divider; persisted widths are no longer capped at the legacy 420, growth stops at the computed boundary, and an already-over-limit divider vetoes further growth in place while every proposed shrink follows the pointer without snapping. A local divider gesture monitor persists only on mouse-up; the matrix-wired geometry seam counts zero writes across two intermediate layouts and exactly one idempotent write at resize end. Minimum, default, dynamic 500pt maximum, over-narrow/over-limit delegate behavior, >420 relaunch persistence, divider thickness, and matching grow-only/shrink-only/locked/bidirectional cursor plus AX labels are covered by 17 assertions. The existing composition root already constructs the sidebar at resolveWidth before adding/layout, so pre-first-frame restore needs no new out-of-fence production wiring. REVIEW HISTORY: both Sol-medium rounds returned REWORK; round 1 found out-of-fence ownership, the retained 420 cap/divider omission/snap, non-directional presentation, stale shell assertion and absent write proof; round 2 found misuse of constrainMaxCoordinate, locked cursor/AX disagreement, value-only rather than counted writes, and an expectation-only witness. Per the two-review maximum there was no third review: the supervisor moved all policy/persistence ownership into WorkspaceSidebarView, exercised the actual delegate boundary separately from pointer position, added the counted session seam, and supplied a production mutation. R2 FENCE DEVIATIONS are assertion migrations only: ContinuumApp.swift's existing shell check now drives the retained controller and exact write count; ContinuumRevivedCoreChecks/main.swift replaces its obsolete hard-420 expectation with the confirmed dynamic-width persistence contract after the first full matrix correctly went red at `workspace sidebar width clamps to ceiling`. NEGATIVE WITNESS: production constrainedWidth was mutated so an over-limit growth returned the reduced maximum instead of current width; build passed and --ui-geometry-check exited 1 at exact red `over-limit growth snapped from 360 to 220.0`; restored WorkspaceSidebarConfig.swift SHA-256 d895fb2aa42332d4f486c3e181d093238a61da9aef0e24a82cf80aab03477504 and reran green. Final swift build, --sidebar-ux-check, --ui-geometry-check, --workspace-sidebar-shell-check, --ui-contrast-check, --agent-inbox-check, --ui-probe-check, CoreChecks, program guard and full headless matrix exited 0. Retina Main passed at display 1 scale 2.0; Component Lab and explicit baseline comparison remained the expected 22 mismatched / 38 matched, and none were blessed. |
| `P5.6-interaction-supervised-review.md` | pending | — | — | — |
| `P6.1-lifecycle-pure-derivation.md` | done | this commit | 2026-08-06T03:17:23Z | Night-3 Slice 0 completion of the audited unfinished candidate. Production `buildAgentInboxRows` now forwards records, read-time blocker facts and the configured auto-settle window; lifecycle/variant are derived on every build, with a pre-derivation descendant fold keeping a parked parent active/card when a working or needs-you child holds it open. Unread remains a mark rather than a blocker; authoritative supervisor snapshots outrank stale activity-ring pending state, while no-snapshot pending, `needsAction`, live runners and a two-sided prompt grace window still block. `InboxSettlement.canSettle` is the one action predicate consumed by row/menu/bulk paths, including child rollup; the old lifecycle shadow oracle now reads the shipped sidebar rows. REVIEW: Sol-medium round 1 REWORK found child hold-open reached only action eligibility after lifecycle/variant derivation and stale ring pending could outrank an authoritative snapshot; Luna-max repaired both with non-vacuous working/needs-you parent and ready/failed/restored snapshot assertions; round 2 APPROVE with no findings. R2 FENCE NOTE: `ContinuumApp.swift:6820-6849` is purpose (a), the forced single production call site without which Phase 6 is dead; `ContinuumApp.swift:20876-20948` and `AgentInboxRowBuilderChecks.swift:285,930-1131` are purpose (b), migrated/strengthened expected-value assertions and the matrix-wired witness that must live beside private `buildAgentInboxRows`; `ContinuumApp.swift:24521-24649,25481` is purpose (c), removing the false shadow/printed reachability claim. The handoff separately required the minimal shipped-guard integration at `AgentInboxView.swift:4325,4593`; `AgentLifecycle.swift:385-392` is comment-only and `InboxLifecycle.resolve` behavior is untouched. NEGATIVE WITNESS on the final candidate: `AgentInboxRowBuilder.swift:176` changed from `record?.lifecycle(...) ?? .active` to `InboxLifecycle.active`; `swift build` exited 0, then `.build/debug/continuum-revived --agent-observer-independence-check` exited 1 at exact red `FAIL: P6.1 production lifecycle: a settled override must reach the shipped row — got active`; restore SHA-256 was byte-identical `5df888722396b0817acf9266a39a12275ea3c79d9f73d7a215c3759872862feb`, rebuild exited 0 and the same check exited 0. Final `swift build`, CoreChecks, AgentUIChecks, observer-independence, agent-inbox and sidebar-ux checks passed; `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh` exited 0 (`Matrix passed`; baseline blessing remains deferred to supervised gates). PROCESS NOTE: pass 1 omitted its required READY token and edited the ledger despite the prompt; the supervisor reverted that ledger edit before review and retained only the independently reviewed source candidate. |
| `P6.2-auto-settle-window.md` | pending | — | — | — |
| `P6.3-snooze-derived-wake.md` | pending | — | — | — |
| `P6.4-attention-read-watermark.md` | pending | — | — | — |
| `P6.5-child-fanout-attention-rollup.md` | pending | — | — | — |
| `P6.6-accessibility-motion-sweep.md` | pending | — | — | — |
| `P7.1-final-supervised-acceptance.md` | pending | — | — | — |
