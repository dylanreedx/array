# P3.14 — Keep workspace management reachable
Phase: 3 · Depends on: P3.8 · Tag: autonomous · Execution-mode: low

## Goal
The sidebar reframe must not delete functionality. Today its header is the only place to create,
rename and delete a workspace (`WorkspaceSidebarView` ~:79-89, delete disabled unless >1 workspace).

## Files
- `Sources/ContinuumRevived/App/AgentInboxView.swift`
- `Sources/ContinuumRevived/App/WorkspaceSidebarView.swift`

## Approach
Move New / Rename / Delete into the scope dropdown's menu (P3.8) as a separated section acting on the
currently scoped workspace, preserving the existing guards (delete disabled at one workspace, the
confirmation provider, and the `managementMessageLabel` warning path).

## Done when
All three actions work from the scope menu with their guards intact.

## Verify
`--workspace-sidebar-actions-check` **must keep passing** (it covers these actions today) — update it
to drive the new location rather than deleting coverage. P0.11's inventory guard will catch the leg
disappearing.

## Watch out
- Zone navigation also lived in the old tree. If anything still needs "jump to zone," keep a path to it
  (the ⌘K palette already has `.jumpToZone` rows) and note what was removed.
