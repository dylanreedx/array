# Colour hygiene allowlist — P1.7
#
# Every colour violation that existed when the gate landed, one source line at a
# time. `scripts/check-color-hygiene.sh` is red on anything here that no longer
# exists, and red on any violation that is not here — so this file only ever
# shrinks. There is no regenerate/bless mode: an allowlist a worker can rewrite
# is the theatre the ticket warns about.
#
# Format, one entry per line, directly under a `# <reason> · owner: <ticket>`
# comment:
#     <path>:<occurrences>:<the offending source line, trimmed>
# Occurrences count identical lines in the same file, so adding one more copy of
# an already-excused line is still red. Entries are line-scoped by construction:
# the key IS the line's text, and the gate rejects an entry whose text is not
# itself one of the two banned patterns — a file or a glob cannot be excused.
#
# Owners:
#   P1.10 — Sources/ContinuumRevived/Canvas (tile surfaces adopt DesignTokens)
#   P1.11 — Sources/ContinuumRevived/App    (chrome adopts DesignTokens)
#   P1.12 — ios/Continuum/Sources           (iOS consumes the same tokens)

# hardcoded dark-only fill literal · owner: P1.11
Sources/ContinuumRevived/App/ContinuumApp.swift:1:backing.layer?.backgroundColor = NSColor(srgbRed: 0.08, green: 0.10, blue: 0.13, alpha: 1).cgColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:addNetworkRow("No navigation, download, or child-window events captured from the linked browser yet.", color: .secondaryLabelColor)

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:addNetworkRow(Self.networkLiteScopeText(), color: .secondaryLabelColor)

# hardcoded dark-only fill literal · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:body.layer?.backgroundColor = NSColor(red: 0.08, green: 0.11, blue: 0.13, alpha: 1).cgColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:case .debug: return .secondaryLabelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:case .log: return .labelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:default: return .labelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:detailLabel.textColor = .secondaryLabelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:label.textColor = .labelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:let color: NSColor = node.id == selectedDOMNodeID ? .controlAccentColor : .labelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:placeholderLabel.textColor = .secondaryLabelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:statusLabel.textColor = .secondaryLabelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserInspectorTileNSView.swift:1:titleLabel.textColor = .labelColor

# hardcoded dark-only fill literal · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserRestartTileNSView.swift:1:body.layer?.backgroundColor = NSColor(white: 0.93, alpha: 1.0).cgColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserRestartTileNSView.swift:1:status.textColor = .secondaryLabelColor

# hardcoded dark-only fill literal · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserSnapshotTileNSView.swift:1:body.layer?.backgroundColor = NSColor(white: 0.90, alpha: 1.0).cgColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserSnapshotTileNSView.swift:1:caption.textColor = .secondaryLabelColor

# hardcoded dark-only fill literal · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserTileNSView.swift:1:body.layer?.backgroundColor = NSColor(white: 0.10, alpha: 1.0).cgColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserTileNSView.swift:1:faviconLabel.textColor = .secondaryLabelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/BrowserTileNSView.swift:1:findResultLabel.textColor = .secondaryLabelColor

# hardcoded dark-only fill literal · owner: P1.10
Sources/ContinuumRevived/Canvas/ConductorQueueTileNSView.swift:1:body.layer?.backgroundColor = NSColor(red: 0.10, green: 0.14, blue: 0.18, alpha: 1).cgColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/ConductorQueueTileNSView.swift:1:empty.textColor = .secondaryLabelColor

# hardcoded colour literal · owner: P1.10
Sources/ContinuumRevived/Canvas/StatusChipNSView.swift:1:NSColor(srgbRed: color.r, green: color.g, blue: color.b, alpha: 1)

# hardcoded dark-only fill literal · owner: P1.10
Sources/ContinuumRevived/Canvas/TerminalRestartTileNSView.swift:1:body.layer?.backgroundColor = NSColor(white: 0.06, alpha: 1.0).cgColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/TerminalRestartTileNSView.swift:1:status.textColor = .secondaryLabelColor

# hardcoded dark-only fill literal · owner: P1.10
Sources/ContinuumRevived/Canvas/TicketQueueTileNSView.swift:1:body.layer?.backgroundColor = NSColor(red: 0.11, green: 0.15, blue: 0.22, alpha: 1).cgColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.10
Sources/ContinuumRevived/Canvas/TicketQueueTileNSView.swift:1:empty.textColor = .secondaryLabelColor

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.12
ios/Continuum/Sources/ContinuumApp.swift:6:.foregroundStyle(.primary)

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.12
ios/Continuum/Sources/ContinuumApp.swift:24:.foregroundStyle(.secondary)

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.12
ios/Continuum/Sources/ContinuumApp.swift:1:.foregroundStyle(isError ? .red : .secondary)

# P1.8 deleted `AppColors.color(for token:)` — the iOS decoder for
# `AgentStatusPresentation.colorToken` — so the two entries that excused its
# `Color(.tertiaryLabel)` and `default: .secondary` arms are gone with it. The
# list shrank by 2, which is the only direction it moves.

# Apple/SwiftUI semantic colour in a view that paints its own hardcoded fill (the black-on-dark trap) · owner: P1.12
ios/Continuum/Sources/ContinuumApp.swift:2:return .secondary

# hardcoded colour literal · owner: P1.12
ios/Continuum/Sources/ContinuumApp.swift:1:static let background = Color(red: 0.05, green: 0.06, blue: 0.08)

# hardcoded colour literal · owner: P1.12
ios/Continuum/Sources/ContinuumApp.swift:1:static let panel = Color(red: 0.10, green: 0.11, blue: 0.14)
