# 90-agent-ux — Ledger

Durable state. The conversation is NOT the source of truth: this file + `git log` are.

## heartbeat
last-touch 2026-07-27T23:55:00Z · ticket P4.9-reading-is-free · attempt 1 · pid loop · status committed 7dd5062

## tickets

| ticket | state | commit | at | note |
|---|---|---|---|---|
| P0.1-ios-target-in-matrix | done | f61aff0 | 2026-07-25T04:43:14Z | iOS xcodebuild leg after `swift build`; negative test (`_ = Process()` in Core) turns the matrix red at exit 65 while `swift build` stays green; no-Xcode path prints SKIPPED and returns 0 |
| P0.2-uiprobe-harness | done | 20a311c | 2026-07-25T05:00:00Z | `UIProbe.render` + `--ui-probe-check`; 3 negative tests red-before-green; codex found `appResolvedCGColor` resolves via `NSApp.effectiveAppearance`, so render() also sets/restores NSApp.appearance and a luminance witness on auth.pairingToken guards it |
| P0.8-shared-selector-and-wait | done | 1096ba8 | 2026-07-25T05:26:00Z | `UITestSupport.swift`: shared `descendant(withIdentifier:)`/`descendants(withPrefix:)` + async `waitUntil` that suspends on a main-queue timer (Swift rejects the RunLoop spin in async, so the wrong pattern can't come back); managed-agent live check migrated. Its own `--managed-agent-live-check` verification is NOT runnable unattended — sampled blocking in `requestProjectFolderAccessIfNeeded` → `NSAlert runModal`; needs a supervised GUI + Pi auth, so it is SKIPPED here (it is not in run-matrix.sh; nothing passes silently). Added deterministic `--ui-test-support-check` in its place, 4 negative tests red-before-green |
| P0.10-explicit-model-id | done | 7c3d75d | 2026-07-25T05:58:00Z | `AgentModelConfig` (FocusBorderConfig shape) + two `.choice` fields under Agents; default is now the exact id `openai-codex/gpt-5.6-sol` — the old `gpt-5.6` was a non-id prefix of three catalogue entries, so Pi's fuzzy matcher chose silently. Beyond the packet: `processArguments` also emits `--thinking <level>` (precedent: HarnessRoleRunBuilder), otherwise the reasoning-effort picker the packet asked for would be inert. Catalogue pinned as a literal in the check (matrix stays offline) — refresh when the provider catalogue changes. 5 negative tests red-before-green. Codex: no correctness bugs; its 2 medium verification gaps + 1 low fixed before commit |
| P0.11-matrix-check-count-guard | done | abfdb93 | 2026-07-25T06:20:00Z | `scripts/check-matrix-inventory.sh` as the first matrix leg vs a committed 248-record inventory: 112 `check` flags, 125 `leg` invocations (env prefixes stripped), 4 `bundle-check` (see below), 7 `count <target> <n>` floors of run*Checks() call sites per `Sources/*Checks/main.swift`. Disappearance or a decreased count = red naming it; growth prints "inventory grew" and passes; blessing = `CONTINUUM_UPDATE_MATRIX_INVENTORY=1 ./scripts/run-matrix.sh` in the same commit. The guard is a normal leg, so deleting it drops its own record. Beyond the packet: `bundle-check` covers check-app-bundle.sh's `self_checks` array, because `--menu-contract-check` and `--delete-confirm-policy-defaults-check` run only there and a run-matrix.sh-only inventory could not see them. 7 negative tests red-before-green (delete a leg; comment out that leg; comment out a run*Checks() call; delete the guard's own leg; delete the inventory; drop 2 bundle self_checks; leave those flags only in a trailing comment) + growth case exits 0. Codex found 2 real holes, both fixed pre-commit: raw-token greps counted commented-out calls and `func run…Checks` declarations — comments are now stripped from all three sources and `func` lines excluded, hence true counts 21 (Core) / 1 (SyncIntegration). Owner: renames read as delete+add by design; a `count … 0` floor only catches the target vanishing, not a shrinking inline suite |
| P0.3-geometry-gates | done | 4bda832 | 2026-07-25T06:30:00Z | Geometry gates + `--ui-geometry-check`; verified green by the supervisor. ATTRIBUTION ERROR: the supervisor's `git commit` swept this ticket's staged files into docs commit 4bda832 mid-flight (bare `git commit` commits the whole index, not just newly `git add`-ed paths). Code inspected against the packet and matrix re-run green; the worker for this ticket was terminated because its index was taken out from under it. |
| P0.4-appearance-contrast-gate | done | 8d9d1b3 | 2026-07-25T07:05:00Z | Gate BUILT and committed (`UIProbeContrast.swift` + `--ui-contrast-check`), but **NOT wired into run-matrix.sh** — it is red on 177 of 446 real pairs, and both routes to green are closed by the packet: allowlisting is forbidden outright ("if a pair fails the colour is wrong") and recolouring the app is `P1.2`/`P1.3`/`P1.10`/`P1.11` work whose files this packet's `## Files` does not name. Needs 4 owner decisions (house muted-text colour, light-appearance accent variants, appearance-aware card fills vs light text, whether decorative borders are held to 3:1) — all in `P0.4-FINDINGS.md` with measured ratios. Gate proven discriminating (269 pass) and its compositing proven correct against AppKit's documented semantic alphas (`secondaryLabelColor` black@0.50 → `#808080` → 3.95:1, etc). Root causes: Apple's own `secondary`/`tertiaryLabelColor` do not clear 4.5:1 (74 pairs); black-on-dark-fill in Aqua — the real shipped bug (~25); accents as text on white (~44); hairline borders at 1.56–1.68 (28). Codex found 4 real defects (ancestor alpha not applied to fills, `layer.opacity` ignored, NSTextView/NSScrollView/NSTableView own fills unread, glyph tints ungated) — all fixed; coverage 432→446 pairs and the failing set did not move, so none of the 177 was an artifact. Matrix green (leg absent by design; nothing passes silently). **UNBLOCKED 2026-07-25 by P1.6 (`c4d3516`), which wired the leg after adoption made it green legitimately: 177 -> 78 -> 11 -> 0 failing pairs. `UIProbeContrast.swift` itself was never touched, so the apparatus could not have been tuned to pass; the last 11 closed by deleting `FocusBorderConfig.attentionColor = "Orange"` (systemOrange, measured 2.07:1 on a light tile) and routing the semantic through `StatusChipPresenter`. Zero exemptions. All four owner questions were answered by `P1.3` as ruled, so none needed a human. Supervisor-verified independently: 452 pairs gated in both appearances, 0 exempt, worst text 5.88:1, worst non-text 3.70:1.** |
| P0.5-pixel-probes | done | dbbdc5a | 2026-07-25T07:52:00Z | `UIProbePixels.swift` + `--ui-pixel-check` leg: text-rect luminance spread and border-vs-fill delta over the probed managed-agent tile, both appearances — 36 text rects + 10 borders (6 transcript cards), 0 skipped, floors asserted PER APPEARANCE (3 card borders, 8 text rects). Thresholds measured, not chosen: spread >= 0.05 vs worst real 0.141, delta >= 0.03 vs worst real 0.093; both invisible cases measure exactly 0.000. Deliberately flatness, not legibility — ratios are `--ui-contrast-check`'s job (a ratio here would go red on P0.4's 177 known-bad pairs and cannot separate glyph coverage from glyph colour). Two AppKit facts cost the most and are recorded in the code: `visibleRect` is NOT bounded by `bounds` (a 140x18 label reported (-40,-38,240,140), which silently excluded every label) and pixels read back in the display colour space (sRGB magenta reads 0.521, not 0.285), so the coordinate-landing fixture asserts uniform-and-distinct + mirrored-rect-is-not-the-patch instead of an absolute colour. 4 negative tests observed red: fixture flat text, fixture flat border (both asserted in-process every run, alongside the positive case) and the two production edits from the packet (`bodyLabel.textColor` / `borderColor` = the card's own fill). Codex: 5 items — 3 fixed (per-appearance floors, NSButton titles now probed, threshold provenance no longer leans on the approval dock), 2 reasoned away in the commit body (own flag per the P0.3 precedent = own matrix leg + P0.11 inventory record; production witnesses stay comments per existing convention). Matrix green; inventory blessed in the same commit (+1 check, +1 leg, 252 records). |
| P0.6-png-baselines | done | 5187603 | 2026-07-25T08:25:00Z | `UIProbeBaseline.swift` + `--ui-baseline-check` leg: 46 committed PNGs (all 23 static Lab cards x both appearances, 1.6MB) under `docs/38-tickets/90-agent-ux/baselines/`. Renders normalised to 1px-per-point sRGB RGBA8 so a retina and a 1x host produce comparable bytes; live render and decoded PNG go through the identical CGContext path. Failure writes `actual` + a magenta `diff` under `qa-runs/` and names both; all mismatches reported in one run. Blessing only via `CONTINUUM_UPDATE_BASELINES=1 ./scripts/run-matrix.sh` — missing AND stale baselines are both red, so coverage cannot vanish quietly. 4 witnesses observed red with the final code (corner radius 8->2; deleted baseline; stale baseline; bless writes exactly the 4 moved PNGs), quoted verbatim in the file. Two clean runs both worst 0.0000%. TWO DELIBERATE DEVIATIONS: (1) tolerance 0.03%, not the packet's ~0.1% — measured drift is 0.0000% while the packet's own named regression moves only 0.1091%, so at 0.1% a radius 8->6 would pass silently; raise it once with a measured number if another macOS version's font rendering bites. (2) `auth.pairingToken` generated a random credential per render (~4% of pixels differed run to run) — fixed at the source (`LabFixtures.pairingCredential`, canned like every other fixture) rather than excluded, so the packet's "every static entry" holds; the generator's real properties stay gated over 5k draws in `AuthChecks.runPairingAlphabetBiasCheck`. Owner: baselines are a real 1.6MB in git, and any intentional design change from Phase 1 onward will need one bless commit. Codex: 5 items — the 2 substantive (pairingToken exclusion, stale quoted witness output) fixed pre-commit, tolerance answered above, 2 were its read-only sandbox. Matrix green; inventory blessed in the same commit (+1 check, +1 leg). |
| P0.9-ui-tour-check | done | af4ad3d | 2026-07-25T08:47:00Z | `UITourCheck.swift` + `--ui-tour-check`: 38 labelled renders + `index.md` under `qa-runs/<ts>/tour/` — managed-agent tile x 4 states (ready/working/with-approval/long-transcript) x 3 widths (TileGeometry minimum, 640, 900), transcript cards (every `ManagedTranscriptCardKind`) at the same 3 widths, status chips, 2 sidebar fixtures, real Settings panel; both appearances throughout. Filenames carry surface-state-size-appearance; `index.md` repeats them plus an `aqua != darkAqua` digest column (38/38 differ). NOT A GATE, twice over: `run-matrix.sh` captures this leg's exit status and reports it instead of propagating it, and the check holds no visual assertion, so the only thing it can report is mechanical. Both proven: with `bodyLabel.textColor` = the card's own fill (every card body invisible) the tour exits 0 and its PNG *shows* the bug while `--ui-pixel-check` exits 1; with a surface pointed at a nonexistent Lab card the flag exits 1 and the full matrix still reports "Matrix passed." at exit 0. Reuse: `UIProbe.render`, `LabCatalog.managedAgentFixtureEvents`, `UIProbeGeometry.scrollWitnessPrompts`, Lab catalogue by id. Settings is the one bespoke path (it owns its NSPanel; `UIProbe` re-parents, which would leave the panel pointing at a view it no longer contains) — it asserts `effectiveAppearance` itself and uses an isolated defaults suite. `ready` ingests `.sessionStateChanged(.ready)` not `.running`, because the derivation reads a running session as `.working` and the shot would have contradicted its own label. Codex: 5 items — blocking-leg (high) + entrypoint follow-on (medium) fixed by the capture-and-warn wiring, width-sweep gap (medium) fixed by sweeping the transcript cards too (34→38 images, under the ~40 ceiling), prompt duplication (low) fixed by reuse; the last (low, "advisory exit is documented not executable") reasoned away in the commit body. Matrix green; inventory blessed in the same commit (+1 check, +1 leg, 256 records). Owner: ~2MB of PNGs into gitignored `qa-runs/` per matrix run, nothing committed |
| P0.7-retire-isblank-gate | done | e083f97 | 2026-07-25T20:55:00Z | Attempt 2. `ComponentLabPanel.runStaticCardGates` holds all 23 static cards x both appearances to the phase-0 gates on ONE render each — blankness as a FLOOR, geometry (no zero-size / no clipping / no broken required size), contrast, pixel-flatness, committed baseline: **46 renders, 452 contrast pairs, 480 text rects, 30 borders, 46 baselines matched, 6.4s** (packet's ceiling is "a couple of minutes"). Gates are the SAME code the four dedicated legs run — `UIProbeContrast.evaluate`, `UIProbePixels.sweep`, `UIProbeBaseline.mismatch` extracted out of them, not a second implementation that could drift — and the probe id stays `entry.id` so a measurement key means the same thing in this leg and in `--ui-contrast-check`. The old hand-rolled render (its own borderless window over an opaque dark backdrop, one appearance only) is gone in favour of `UIProbe.render`. THE FOUR HAND-PICKED COLOUR-COUNT ASSERTIONS ARE REMOVED, and the subsumption is DEMONSTRATED rather than argued: dropping the `select(...)` call from the `chrome.sidebar.selected` fixture — the exact regression the sidebar-delta assertion existed to catch — is red on 10.2085% of that card's pixels in aqua and 10.2162% in darkAqua with a diff image, where the retired assertion compared two integers. 7 negative tests observed red at exit 1 with the final code, one per gate, quoted verbatim at `runStaticCardGates`: the packet's own witness (width pin removed → 2 baselines differ, 44 matched), the subsumption witness, that pin at 900 → `broken required constraint — measured 560.0, needs == 900.0`, `LabCatalog.mutedLabelColor` off its token → `7 unreadable pair(s) of 452 measured ... 1.61:1, needs >= 4.5:1`, both sweep coverage floors per appearance → `only 0 text rect(s) in NSAppearanceNameAqua, needs >= 192` / `only 0 border(s) ... >= 12`, a hidden fixture → `render is blank/uniform (1 colors)`, one card excluded → `only 44 static card/appearance render(s) gated, needs >= 46`. Floors MEASURED, and per appearance not in aggregate (P0.5's recorded trap: an aggregate floor is satisfied by one appearance carrying the run); the render count is floored AT 46 = 23 x 2, not under it, because the packet's verification is "the check count did not shrink". Codex: 6 findings — 3 FIXED (render-count slack 40 → 46, probe-id divergence from the dedicated leg, a stale Tier-1 doc comment left above the new one), 2 reasoned away IN THE CODE (`expectNoAmbiguousLayout` stays in `--ui-geometry-check` because it is documented at its own definition as NOT a generic invariant — `hasAmbiguousLayout` is true for every flexible `NSStackView` child AppKit positions with its priority-260 align constraint, so a 23-card subtree walk would force weakening it; and the baseline comparison standing down under `CONTINUUM_UPDATE_BASELINES=1` is not an environment-dependent hole, since under that flag NOTHING in the matrix compares — `--ui-baseline-check` writes too — and the next normal run re-establishes it, while writing here would give the same 46 files two writers), 1 was its read-only sandbox. Matrix green at exit 0 including the iOS leg; inventory unchanged at 261 (no new flag, no new leg — the gates live inside the existing `--component-lab-check`); no baseline needed blessing (no pixel moved). Owner: this leg is now red on any intentional design change to a Lab card until the baselines are blessed — the same bless commit `--ui-baseline-check` already required. Attempt 1's note, for the record: attempt 1 STOPPED ON THE ENVIRONMENT, not on the ticket — see `_ENV-BLOCKER-1x-display.md`. The host's window backing scale flipped from 2 to 1 mid-session (the 1920x1080 external is now `Main Display`; measured: a 520x260pt card wrote 1040x520px at 13:09:25Z and a 280x96pt card wrote 280x96px at 13:12:33Z), so at HEAD with a clean tree `--ui-baseline-check` is red on **46 of 46** committed baselines (8–9% of pixels, worst channel delta 140 — antialiasing at 1x vs the 2x host they were blessed on) and `--ui-pixel-check` is red on its own correct fixture (`expectVisibleBorder` skips 1 px then reads `round(1pt * scale)` px, so at scale 1 the "border band" IS the fill → delta exactly 0.000). Blessing would be the forbidden move: it would overwrite 46 2x baselines with 1x renders. Owner's call — make the Retina display primary and re-run (no code change), or fix the substrate to render at a fixed declared scale (a ticket of its own; costs one bless commit). Implementation of P0.7 is COMPLETE and builds, parked in `git stash@{0}` "P0.7-wip" (sources only, tree left clean): `ComponentLabPanel.runStaticCardGates` runs blankness-as-floor + geometry (no zero-size / no clipping / no broken required size) + contrast + pixel + baseline per static card in both appearances on ONE render each, via new reusable `UIProbeContrast.evaluate` / `UIProbePixels.sweep` / `UIProbeBaseline.mismatch` extracted out of the three existing legs (one implementation, not a second that could drift), and the four hand-picked `distinctSampledColors` assertions the baselines subsume are deleted. It ran clean at 2x. Still owed before commit, on a 2x host: the two aggregate coverage floors (text rects / borders) measured, the packet's "removing a width pin fails the Lab check" witness, and codex cross-review |
| P1.1-agentui-module | done | f8bd18f | 2026-07-25T10:05:00Z | `ContinuumRevivedAgentUI` (Foundation only) + `ContinuumRevivedAgentUIChecks` + its own matrix leg; StatusChip.swift and StatusChipChecks.swift `git mv`-d in unchanged, output text byte-identical. iOS gets the product (project.yml + `xcodegen generate`); P0.1's leg reports "iOS build succeeded". BEYOND THE PACKET, and the load-bearing decision: `StatusChipPresenter.display(for:)` is keyed on `AgentStatus`, which lived in Core — leaving it there forces AgentUI → Core, the one direction the packet forbids. So the enum moved into AgentUI (raw values untouched, so `AgentDescriptor`'s persisted/synced encoding is identical) and ~40 files gained a one-line `import ContinuumRevivedAgentUI`; nothing else in them changed. Consequence: the committed Core-dependencies guard in CoreChecks pinned Core's list to GRDB alone and went red, so its expected string now names AgentUI too — NOT a loosening (still exact string equality over the whole list; a third dependency still goes red, observed by adding GhosttyKit → exit 1), and the packet pre-authorises it by blessing Core → AgentUI. 4 negative tests observed red: broken chip contrast → new leg exits 1 (1.35:1); a file in AgentUI importing Core → `swift build` exits 1 "circular dependency between modules" (so the direction rule is compiler-enforced, not a convention, and AgentUIChecks links AgentUI alone); the P0.11 inventory shrink (`count ContinuumRevivedCoreChecks 21 → 20`) named before blessing; the dependencies guard. Inventory blessed in the same commit: +1 leg, +`count ContinuumRevivedAgentUIChecks 1`, 258 records — the move is recorded, not lost. Codex: 1 high finding fixed pre-commit (Sync/CoreChecks/SyncChecks imported AgentUI transitively without declaring it; all six importing targets now name it), its other item was its read-only sandbox not being able to build. Matrix green. Owner: if you would rather AgentUI depend on Core and keep the enum in place, the revert is Package.swift plus the ~40 import lines |
| P1.2-tokencolor-light-dark | done | 4a5ff92 | 2026-07-25T10:34:00Z | `TokenColor.swift` (light/dark leaves + `resolved(for:)`) and `TokenColorChecks.swift`, exactly the packet's shape — Foundation only, `ChipColor` as the leaf so `WCAGContrast` applies to a resolved pair unchanged, no `system`/`auto` case, no NSAppearance mapping. Nothing beyond the packet; nothing adopts it yet (that is P1.3 onward). 3 negative tests observed red at exit 1: `.dark` returning the light leaf; `init(_:)` giving dark a different colour; `allCases` overridden to `[.dark]` — the last is the load-bearing one, since without it a later per-appearance gate could check one theme twice and report green. Ordering of `allCases` is pinned to `[light, dark]`, not just membership. Inventory blessed in the same commit: `count ContinuumRevivedAgentUIChecks` 1 → 2, no other record moved. Matrix green including the iOS leg (the new file needs no project.yml change — P1.1's product globs the module directory). Codex: no correctness bugs, no guessing, no missed reuse, no verification gaps |
| P1.4-type-scale | done | 47b090a | 2026-07-25T09:25:00Z | 7 roles, ladder 18/15/13/11/9 with a 2pt min step, 2 mono roles, minimumLegibleSize(atZoom:) composed with ReadabilityPolicy; the guard check DERIVES the band boundary from Core rather than hardcoding it. SUPERVISOR-COMPLETED: its worker wedged (0% CPU, 0-byte iter log, no subprocess, 45min) and was killed; the work was complete except the derived boundary was asserted against 0.60 (another tile kind's value) instead of 0.70 — its own failure message already said 0.70. Fixed that one constant; matrix verified green by the supervisor. |
| P1.5-spacing-radius-scale | done | 2c79168 | 2026-07-25T11:48:00Z | `Metrics.swift` + `MetricsChecks.swift`, exactly the packet's shape — 2pt `Space` ladder 2/4/8/12/16/24, `Radius` card 6 / container 10 / pill 999, `Inset.card`/`Inset.row` composed OUT OF the ladder (so a fresh number is red, not just an off-grid one), `Metrics.rowHeight(for:lines:insets:)`. Nothing adopts; no view touched. `lineHeightMultiple` = 1.25 is measured, not chosen: `NSLayoutManager.defaultLineHeight(for:)` on macOS 15 over the P1.4 ladder reads 1.2222/1.1818/1.2308/1.2000/1.1667, so 1.25 is the smallest quarter-point step at or above all of them and a derived height can never under-report AppKit (an under-report IS the clip). Those 5 measurements are pinned as literals AND the table must cover exactly the shipped role sizes, so narrowing the type ladder cannot leave a role unverified. 5 negative tests observed red at exit 1: today's inverted nesting (tile 6 containing cards 8); `Space.s` 4→5; multiple 1.25→1.1 (titleL 20 vs AppKit 21); `rowHeight` dropping its `lines` factor; row inset 10 instead of a ladder value. Attempt 2 — attempt 1 left a stale in-progress heartbeat, no commit, clean tree. Inventory blessed in the same commit: AgentUIChecks 3→4, and it also picks up CoreChecks 20→21, pre-existing drift from P1.4 (added a call to CoreChecks/main.swift, blessed only the AgentUI record; growth passes the guard so the stale floor went unnoticed) — a floor increase, not a loosening. Codex cross-review COULD NOT RUN: provider 503 `biscuit_baker_service_me_circuit_open` through all retries on both endpoints; this diff rests on the matrix plus the 5 negative tests. Matrix green at exit 0 including the iOS leg. Owner: 9 values move when P1.10 adopts (tile radius 6→10, card 8→6, header spacing 10→12, card vertical 10→12, compose vertical 6→8, header 52→54, compose 44→41, answer field 24→33) — mapping table is at the foot of Metrics.swift and the P0.6 baselines will need one bless commit then. Also seen once, NOT from this diff: pre-existing flake `network-lite log did not capture a committed or finished event after start` (TileSpawner.swift:5985, WKWebView navigation timing), green on the clean run |
| P1.3-surface-text-border-tokens | done | a12c909 | 2026-07-25T12:38:00Z | `DesignTokens.swift` + `DesignTokenChecks.swift`, the packet's two files. 22 tokens: 11 surfaces (5 chrome + 6 card fills), 3 text, 3 lines, 5 accents; semantic names only; nothing adopts (P1.10/P1.11/P1.12) and the P0.4 leg stays off (P1.6). All four P0.4-unblocking rulings implemented: house `textSecondary` (0x54585F/0xAFB6C2, worst 5.99 light / 6.53 dark — Apple's semantic labels used nowhere), accents with a darkened light variant + hue held across themes (drift 0.9°–4.5°, gated ≤10°; contrast is symmetric so one value per theme serves accent-as-text AND accent-as-fill, hence no `accentXText`), appearance-aware card fills asserted structurally (light lum > 0.5, dark < 0.2 for all 11 — the black-on-dark-under-Aqua bug cannot return), borders at 3:1 with `separator` the ONE reasoned exemption, provably absent from the gated set rather than floored at 1. Pairing is explicit, so `documentedPairs` DERIVES 104 pairs from the tokens' own declarations — P1.6 gates a finite documented list, not a cross-product. Tightest pair `border` on `cardUserMessage` dark at 3.44:1 vs 3.00. BEYOND THE PACKET (from codex's one real finding): margins are pinned per foreground token to ±0.01 PLUS the identity of its worst background, and the table must cover exactly the gated foregrounds — so a tweak that still clears the floor goes red until the provenance table at the head of DesignTokens.swift is updated, and a new token can't skip it; accents must also land in a 4.50…8.00 band, because a near-black "blue" clears every floor and still fails an accent's job. Witnesses from P0.4-FINDINGS on backgrounds they are really painted on: white@0.25 on white@0.10 = 1.68:1 fails the line floor (witness value itself pinned 1.60–1.75), tertiary grey on `cardMessage` = 2.07:1 and `#808080` on white = 3.98:1 both fail AA, and `textSecondary` beats both on every surface in both themes. 10 negative tests observed red at exit 1 (dark card fill under light appearance; `textSecondary` = `#808080`; border back to white@0.25; accent with no darker light variant; 71° hue drift; narrowed legal set 104→98; exemption laundered into a gate 104→115; stale pinned margin; over-dark accent WITH its margin updated — the band still caught it; new gated token missing from the table). Codex: no correctness bugs; unpinned margins fixed; 2 items reasoned in the commit body (values are chosen-then-measured rather than search-generated, which is what "justified by a computed ratio" asks; `AccentToken` vs `StatusChipPresenter` duplication is P1.8's job and outside this packet's Files — flagged for P1.8). Matrix green at exit 0 including the iOS leg; inventory blessed in the same commit (AgentUIChecks 4→5). Owner: adopting these values in P1.10/P1.11 moves real pixels, so the P0.6 baselines will need one bless commit then |
| P1.6-token-contrast-gate | done | c4d3516 | 2026-07-25T19:55:00Z | `TokenContrastChecks.swift` (the packet's one file) + P0.4's `--ui-contrast-check` WIRED as a matrix leg: **452 pairs gated in both appearances, 0 exempt** — no allowlist, which is what the packet forbids. The leg cost 11 real defects to close (177 -> 78 -> 11 -> **0**), all outside the packet's Files and all authorised by its own instruction ("if pairs still fail, the tokens are wrong — fix the tokens, never the gate"); the tokens were right, the call sites bypassed them. (a) Six lab-card labels on Apple's semantic greys — `secondaryLabelColor` #808080 = 3.95:1 on white and `tertiaryLabelColor` #BDBDBD = **1.88:1**, the worst pair in the tree — collapse onto one `LabCatalog.mutedLabelColor` on `textSecondary`. The lab cards ARE the surfaces the gate renders headlessly, so leaving them on the AppKit greys meant a green gate only WITH an exemption. (b) The pairing credential was `controlAccentColor` (#007AFF, 4.02:1) — an undarkened accent used as TEXT, root cause 3 of the 177 — now `accentWorking`. (c) The needs-attention marching-ants ring was `systemOrange` at **2.07:1**, now `StatusChipPresenter.display(for: .needsAttention).accent`, so ring + dock outline + tile glyph are one colour by construction. **P1.11's OWNER-RULING NOTE WAS WRONG ON THE FACTS and no ruling was needed:** it called the ring "the user's own Settings choice", but `FocusBorderConfig.attentionColor` was explicitly documented as NOT user-configurable ("orange means human action is required") — nothing anyone picked is overridden, the hue is still amber (36 deg/35 deg across themes), and it now has the darkened light variant the old spelling lacked. That constant is DELETED (sole call site moved), which supersedes doc 72's criterion naming it. **THE 0.8 ALPHA IS GONE TOO**, the load-bearing half: a stroke at some alpha over an unknown backdrop is not a documented pair, so nothing can hold it to 3:1 — same reasoning P1.11 used to drop the sidebar's translucency. Solid, the ring IS `accentApproval` on `canvas`/`tileBody`, two of P1.3's 104 pairs, asserted BY NAME as the attention ring so it cannot be tuned out from under the call site. The new suite is deliberately not a second copy of P1.3's: P1.3 proves STRUCTURE, this one PRINTS THE NUMBERS (104 pairs x 2 themes, every ratio and floor on stdout — a green run you cannot read is one you cannot audit), and it runs BEFORE P1.3's because when a colour is wrong the ratio is the useful message, not the provenance table the same edit also invalidates. Beyond ratios it asserts PAIR IDENTITY against an INDEPENDENT derivation from the tokens' own `legalBackgrounds`/`contrastFloor` (deriving it from `documentedPairs` would compare it to itself) plus a no-duplicates rule, and the metric anchor comes FIRST. HONEST LIMIT recorded at the call site: the contrast gate does NOT see the real ring — the overlay strokes in `draw(_:)`, so it is neither a layer border nor a `textColor`, the only two things `UIProbeContrast` reads; what it measured at 2.07:1 was the lab card's hand-painted stand-in, which had the same colour by copy. Ratio gated at the palette level, pixels by the `observer.rollup` baselines; neither alone suffices and both are written down. 7 negative tests observed red with the final code: a lab label back to `.secondaryLabelColor` (flag exit 1, named); packet witness (1) `textSecondary.dark` = #565656 (exit 1 — but caught by `runStatusChipChecks` FIRST, since P1.8 sources the idle accent from that token, so this gate never sees it, which is why the synthetic sweep below exists); packet witness (2) `border.dark` white@0.25 on white@0.10 (`11 pair(s) below floor: border on canvas in dark is 1.88:1`, each failing row marked FAIL inline in the table); `legalBackgrounds` -> `[]`; removing the leg (P0.11: `matrix lost a committed entry: leg ... --ui-contrast-check`); the ring back to `systemOrange` (`--ui-baseline-check` red on `observer.rollup` both appearances, 847/752 px, worst channel delta 105 — this is what proves the canvas fix is gated at all); and a pair swapped for a duplicate with count AND foreground set unchanged (`104 pairs, 103 distinct`) — P1.3's `count == 104` passes that one, the identity check does not. 22 baselines blessed in two passes, every diff inspected first (14 for the label/fixture colours — each diff exactly one label or one border, 32 others matched untouched — then 4 for the alpha drop). Codex: 6 findings, 4 FIXED here (pair identity was foreground-only; the `accentApproval` replacement witness was held to the 3.0 line floor when the failure it replaces is accent-as-TEXT, now 4.5 on both surfaces in both themes; witness (1) was not demonstrated through this gate's own sweep, now run on a synthetic token through the identical `TokenPair` evaluator at 11/22 failing while the shipped token clears all 22; and the "baselines gate the ring" claim was overstated, answered by the alpha drop plus the honest limit). Its "centralize a shared pair manifest" is DECLINED with reason — a shared helper is exactly the circularity the independent derivation avoids. Matrix green at exit 0 including the iOS leg, colour hygiene (33 allowlisted, 0 new) and the inventory guard (261 records; +1 check, +1 leg, AgentUIChecks 5->6, blessed here). PROCESS NOTE for the next worker: I used `git checkout <path>` to undo a negative test before staging, which reverted the whole file's unstaged work — stage first, then `git checkout` restores from the index. Owner: the attention ring is now solid dark amber instead of translucent orange — visible, deliberate, and the only colour that clears 3:1 on a light tile. |
| P1.7-raw-color-lint | done | 9281b9f | 2026-07-25T13:34:00Z | `scripts/check-color-hygiene.sh` + `color-hygiene-allowlist.txt`, registered as a matrix leg BEFORE `swift build` (P0.11 rationale: a new raw colour is named in seconds, not after a build). Two grep rules over Canvas/App/ios: raw colour construction, and an Apple/SwiftUI semantic colour in a file that also paints its own hardcoded fill (tonight's black-on-dark pattern). 96 existing violation lines allowlisted, each under its own `# <reason> · owner:` comment — P1.10 Canvas / P1.11 App / P1.12 iOS. ANTI-THEATRE IS MECHANICAL, which is the load-bearing part: the key IS the line's own text + an occurrence count (so one entry excuses one line, and a second copy of an excused line is red); an entry whose text is not itself a banned pattern is rejected (a file- or glob-scoped pass cannot be written); an entry with no reason/owner comment is rejected; a line that leaves the tree makes its entry stale and red. DELIBERATELY NO bless/regenerate env var, unlike baselines and the inventory — those legitimately change, this list only ever shrinks, and one a worker can regenerate is exactly the theatre the packet forbids; `--print-violations` prints records so a reason and owner still get typed by hand. Fixtures/witnesses (ComponentLab, UIProbe*, UITourCheck, UITestSupport, *Checks) excluded by path with a comment per the packet, not by hundreds of entries. BEYOND THE PACKET (codex's 2 high findings): the pattern set is broader than the six literal spellings the ticket names, because a lint that knows one spelling is one you walk around — `NSColor.init(red:`, `SwiftUI.Color(red:`, `displayP3Red:`/`calibratedWhite:`, the multi-line `NSColor(` form, and iOS `UIColor.secondaryLabel`/`Color(.label)` all caught; measured to match ZERO extra lines today, so it closes routes without reclassifying code or padding the list. Also found in my own re-read: `printf \| grep -q` under `pipefail` reports the pipeline as failed when grep exits early on first match and the writer takes SIGPIPE — it was nondeterministically DROPPING rule-2 records; herestrings throughout, 3 consecutive runs now agree at 96. 14 negative tests observed red at exit 1 with the final code (the packet's `NSColor(red:)`; `.init`/SwiftUI-qualified/displayP3Red/multi-line spellings; `UIColor.secondaryLabel` and `.tertiaryLabelColor` on a hardcoded-fill file; duplicated excused line; removed allowlisted line → stale; entry with no owner; wholesale entry; deleted allowlist; narrowed scan dir; removing the leg → P0.11 guard). Codex: 2 high fixed as above, tab-in-source field-shift (medium) fixed by normalising tabs on both sides of the comparison (no scanned file has a tab today), and the last medium reasoned away in the commit body — rule 2's "file paints a hardcoded fill" is file-level because that is what the packet specifies ("in the same file as a raw fill"); dataflow tracing is out of scope. Matrix green at exit 0 including the iOS leg; inventory blessed in the same commit (+1 leg, 259 records). Owner: the 96 entries are real debt and should reach 0 as P1.10/P1.11/P1.12 adopt DesignTokens — each adoption must delete its own entries, since a stale entry is red |
| P1.8-one-status-presenter | done | 8410905 | 2026-07-25T14:55:00Z | Six status→appearance maps collapsed onto `StatusChipPresenter`; `AgentStatusPresentation` and its `colorToken` string channel DELETED outright (not retyped) — the row already carries `status`, so a second channel that can disagree with the first no longer exists, and `AgentsBoardRow`/`CanvasMirrorTileStatus` each lost the field. `StatusChipDisplay` gained `accent: TokenColor`, sourced from P1.3's gated tokens and never re-picked here: accentInput/Working/Approval/Done for configuring/working/needsAttention/done, and `textSecondary` for idle+stale — the two states that ask nothing of you, which is what the sidebar already painted for both (`SidebarAgentStatusKind` folds configuring/idle into `.unknown`); this closes P1.3's flagged AccentToken-vs-presenter duplication. Two shapes, per the packet's "watch out": the filled pastel pill for chip callers, `accent` for the bare glyph/dot (title bar, managed header, sidebar, iOS). REAL VISUAL CHANGES, all deliberate: configuring's glyph is `◐` everywhere (was `◌` in the tile — where it collided with the sidebar's *stale* — and `◍` on the board), labels are the presenter's Title Case ("Needs attention", not "needs you"/"needsAttention"), idle drops teal for muted, and every hue is now a gated token instead of `.systemOrange`/`.systemPurple`. 22 baselines blessed in this commit after inspecting the diffs — changed pixels are exactly the status glyphs, the phase label and the title-bar pill (which widened with its longer text); no layout moved. Appearance resolution is per call site and reasoned: `StatusChipNSView.dynamicNSColor` (new, one bridge) for surfaces that follow the system appearance, explicit `.dark` for the three that paint their own dark-only literals (allowlisted, owner P1.10) and for iOS (dark-only literals, TODO(P1.12)). BEYOND THE PACKET: `check-color-hygiene.sh` gained rule 3 — only `StatusChip.swift` may map an `AgentStatus` to a glyph or a colour, over all of `Sources`+`ios` (the board's map lived in Core, outside the P1.7 scan dirs), with NO allowlist because the done-criterion is one mapping. It reads a 3-line window, so the multi-line switch form cannot walk past it. Allowlist SHRANK 96→94 (iOS `AppColors.color(for token:)` deleted). 4 new presenter assertions: glyph distinctness (the `◌` collision), accent PROVENANCE (an accent must BE a declared token — a hand-picked one is red), accent ≥4.5:1 on all 11 surfaces × 2 themes, and pill/accent hue coherence with idle/stale's muted collapse pinned as a fact rather than left to drift. The Lab now asserts the RENDERED colour: it reads each row's `textColor` back per appearance for all six statuses (via `makeAgentsBoardView(rows:)`, so no fixture/baseline moved) and compares to the presenter. 9 negative tests observed red: the real pre-migration sidebar maps (4 glyph + 4 colour lines named) and the tile's `◌`; the multi-line and inline switch forms; a hand-picked accent (#0A4FB0 "is not a declared AccentToken value"); a glyph collision; a saturated idle accent (sat 0.765 > 0.15); and `.systemOrange` at a call site → "rendered #FF8D28 in light, presenter says #845000" (#FF8D28 is P0.4's own 2.31:1 finding). Codex: 5 items, 4 fixed pre-commit (iOS pill rendered `status.rawValue` not `display.label`; rule 3's multi-line bypass — and while fixing it I found the rule was INERT for one revision because POSIX awk reads `\b` as backspace, now spelled as a character class with the glyph set as alternation; Lab covered only 3 of 6 statuses; the mirror test restated the presenter instead of deriving from the joined overlay), 1 was wrong on the facts (`TileNSView.label(for:)` was still used by `ChromeSnapshot`) but I deleted it anyway so no wrapper residue is left. Matrix green at exit 0 including the iOS leg; inventory unchanged at 259 (no new leg or check — rule 3 lives inside the existing hygiene leg). Owner: iOS/desktop status labels are now Title Case in the tile pill and the phone's board pill; if you want the tile bar lowercase that is a presenter change, not a call-site one |
| P1.9-live-appearance-switching | done | b79e54b | 2026-07-25T15:55:00Z | `TokenThemed.applyTokens()` in AppearanceSupport.swift, called from `init` AND `viewDidChangeEffectiveAppearance`, on TileNSView (+ its private TitleBarView/CornerOverlayView), ManagedAgentTileNSView, TranscriptCardView, WorkspaceSidebarView, WorkspaceTopBarView. Colour VALUES unchanged (the dark literals stay — adoption is P1.10/P1.11, who own the P1.7 entries), so no baseline needed blessing; the `.darkAqua` pin stays per the packet. Also here: the one NSAppearance→TokenTheme mapping (`tokenTheme`, which P1.8's `StatusChipNSView.dynamicNSColor` now reuses instead of its own copy) and `TokenColor.cgColor(for:/in:)` for P1.10/P1.11. TWO REAL BUGS FOUND: (1) `windowBackgroundColor.withAlphaComponent(0.92).appResolvedCGColor` does NOT resolve in the app's appearance — `withAlphaComponent` resolves a dynamic catalog colour on the spot, against whatever appearance is current, leaving nothing dynamic to re-resolve (measured 0.12 grey where white is correct); fixed with a new `appResolvedCGColor(alpha:)` at both chrome call sites. (2) TerminalTileNSView paints the tile body from the Ghostty theme, so it now overrides `applyTokens()` to re-assert it — outside the packet's Files, but a regression this change would otherwise have introduced. GATE: `UIProbeAppearance` inside `--ui-probe-check` (same leg — it asserts the mechanism, not colour values, so no new flag and the inventory is unchanged at 259): (1) sentinel sweep — every layer colour a TokenThemed view owns is overwritten magenta, the appearance flipped for real (NSApp + window), any survivor named and red; 8 views / 19 painted colours, floors printed each run; (1b) the swept set is checked against the `TokenThemed` conformances declared in `Sources/ContinuumRevived`, so a new conformer this gate does not render is red rather than an unobserved hole; (2) the two chrome fills are system colours, so their resolved value must MOVE between appearances; (2b) `applyTokens()` is called inside a HOSTILE drawing appearance and must still paint the view's own — this is what caught bug (1), and it exists because the naive negative test PASSED: AppKit makes the new appearance current while it delivers `viewDidChangeEffectiveAppearance`, so a snapshotting view re-resolves correctly at exactly that moment and a flip alone cannot tell the two spellings apart; (3) a token fixture holds the exact light leaf under Aqua and the exact dark leaf after the flip, and an identical fixture that snapshots in `init` is asserted to FAIL that same assertion every run (the packet's regression witness, executed rather than described). 5 negative tests observed red with the final code (drop the hook from TranscriptCardView → 6 survivors named; assign the tile border outside applyTokens → `ManagedAgentTileNSView.border`; drop the conformance → "declared in the source but never swept"; both wrong colour spellings in the sidebar → the hostile assertion; an unswept new conformer). Recorded AppKit fact: `CALayer.borderColor` defaults to opaque BLACK, not nil, so `borderWidth > 0` is what makes a border slot real — the first run failed on 7 invisible borders. Allowlist: 2 entries had their line text updated (same debt, same P1.10 owner) because `root`/`row` became stored `contentBackdrop`/`composeBackdrop`; 94 → 94, no growth. Codex: 4 items — its medium became 1b; its "high" (`protocol TokenThemed: NSView` invalid Swift) is wrong on the facts, a protocol may inherit a class constraint and the build is green; its other medium (no build evidence) is answered by the matrix; its low (StatusChipNSView not folded in) left for P1.10 — the chip's colours are single-value ChipColors re-assigned from the presenter on every `apply`, appearance-invariant today. Matrix green at exit 0 including the iOS leg and baselines (one pre-existing network-lite WKWebView flake on the first run, green on re-run). Owner: when P1.10/P1.11 put these views on tokens, `applyTokens()` is the only place they need to touch, and gate (2)/(2b) will then apply to the tile too |
| P1.10-adopt-tokens-tile | done | 9799b62 | 2026-07-25T16:50:00Z | The packet's four files consume `DesignTokens`/`Typography`/`Metrics`: three unrelated dark literals → `tileBody`+`tileChrome`, the six card fills → the six `SurfaceToken.cards` cases (which carry a LIGHT leaf — that is what retires the black-on-dark-under-Aqua bug), the invisible white@0.14 hairline → `LineToken.border`, and every height a function of its type (header 52→54, compose 44→41, glyph column 28→23 = one rendered line of `.titleL`). Dock + user-input card are `TokenThemed` now, on `SurfaceToken.overlay` / `cardMessage`, and their attention hue is READ OFF `StatusChipPresenter.display(for: .needsAttention).accent` rather than picked — the status the tile is in whenever either is up, so their outline matches the tile glyph by construction. Carried on outline+header+buttons, not as a tinted fill: a fill at some alpha over a surface is not a documented pair and P1.6 could not gate it, while accentApproval-on-overlay is one of the 104 already asserted. THE 92pt DOCK IS DERIVED: `Inset.card.vertical + lineHeight(.label) + lineHeight(.captionMono) + buttonRow + 2*Space.m` = 86, the button row MEASURED off a real NSButton (bezel chrome is AppKit's, per Metrics' own instruction). Verified numerically because a short derived height does NOT clip — the dock's stack is pinned on all four edges so AppKit compresses labels instead of spilling them; `--ui-geometry-check` asserts `preferredHeight >= fittingSize` at every probed width, measured TWICE since the fixture's `requestOpened` carries `detail: nil` and `NSStackView` drops that row plus its gap (fixture alone only measures the SHORT state). Tightest slack 1.0pt at 320/640/900 in both appearances; the three buttons fit side by side at 320pt (tour render inspected). NEW GATE (check 4 in `UIProbeAppearance`, inside the existing `--ui-probe-check` leg — inventory unchanged at 259): over the REAL tile in both appearances, every layer colour an adopted view paints must be a value the palette can produce for that theme — 26 colours, 6 owners. Scoped by slot KIND (a fill must be a SURFACE, an outline a line/accent), which both says more and is what makes a wrong-theme pin detectable: UNSCOPED THE PALETTES ARE NOT DISJOINT — `#14171C` is `tileBody` dark AND `textPrimary` light, found by the first version of this gate — so a fill pinned to `.dark` would have passed under Aqua. Disjointness-within-a-kind is now asserted every run. Non-adopted painters are an explicit list with their owning ticket (`TileNSView` + its 2 chrome views, P1.11), so the hole is visible and can only shrink. Also closed a blind spot the adoption makes load-bearing: the tile's 3 backdrops are plain `NSView`s, so the sentinel sweep's owned-layer rule (never a subview's layer) could not see them — handed over via `qaTokenPaintedLayers`, and the sweep now opens a user-input request so the new conformer is swept rather than reported declared-but-never-rendered; floors 8/19 → 10/26. 6 negative tests observed red: a literal back in a card fill (named independently by `check-color-hygiene.sh` — two gates, one defect); a real token pinned to `.dark`; a real token of the wrong family (`textSecondary` as a fill); the backdrop hand-off shrinking (25 vs floor 26); the appearance hook dropped from `UserInputCardView` (2 sentinel survivors); an adopted owner leaving the probed tree. P0.4 AUDIT (advisory, leg still off by design — P1.6): **177 of 446 → 78 of 452** unreadable pairs, and every remaining managed-agent failure is `TileNSView`'s own 1.68:1 outline (P1.11's file); these four files contribute ZERO. Worst pixel text spread 0.141 → 0.405, worst border delta 0.093 → 0.106; thresholds left where they are (a floor further under the worst case is not a loosening), their stale provenance comments updated to the measured values. Allowlist 94 → 78 (all 16 entries for these two files); the other `owner: P1.10` tags belong to files this packet does not name (`TileNSView`, `NoteTileNSView`, `FileTreeTileNSView`, …) — mis-tagged by P1.7, left alone rather than re-scoped mid-ticket, and really P1.11/P1.12 work. Baselines: exactly 6 blessed — the 3 changed surfaces × both appearances — and 40 others matched untouched, which IS the "nothing else moved" evidence; all 6 inspected before blessing. BEYOND THE PACKET: `NSFont.token(_:)`/`NSFont.Weight(_:)`/`NSEdgeInsets(_:)` in AppearanceSupport.swift, because Typography and Metrics both say that mapping is "the thin view layer's job (P1.10/P1.11 adopt)" and that file is already the one TokenColor bridge — `monospaced` → `monospacedSystemFont` (a true mono face is tabular by construction, so it serves `captionMono`'s no-reflow AND `bodyMono`'s alignment; keying the two mono roles to different faces would smuggle an axis into the scale `TextStyle` does not carry); and the stale P1.10 note in `runHostileCurrentAppearanceCheck` answered (the tile stays out of it deliberately — `cgColor(for:)` takes the theme as an argument and never reads `NSAppearance.current`, so it could only pass trivially; check 4 covers token consumers instead). CODEX CROSS-REVIEW COULD NOT COMPLETE: provider 503 `biscuit_baker_service_me_circuit_open` through all retries on both attempts (same failure P1.5 hit). It surfaced one finding before dying — `cornerRadius = 0` in the dock is still a literal — and was checking for an explicit zero radius; there is none (`Radius` is card/container/pill) and 0 is a structural fact here (the dock is a full-bleed strip between transcript and compose, so `Radius.card` would round against a square tile edge), documented inline rather than adding a one-caller `Radius.none` to a file this packet does not own. HONEST LIMITS recorded in the gate: check 4 gates layer colours, so a TEXT colour reverting to `.labelColor` is hygiene rule 2's catch and `--ui-contrast-check`'s; sizes/paddings are covered by the committed baselines, which is how this ticket's own 6 moves surfaced. Matrix green at exit 0 including the iOS leg. Owner: the tile's outer frame and title bar are still dark literals under Aqua (P1.11), so the light render has a dark chrome band around a light body — expected mid-adoption, not a bug; and the title-bar status pill overlaps the tile title at 320pt, which is P1.8's widened pill in `TileNSView`'s private `TitleBarView`, pre-existing and outside this packet's files |
| P1.11-adopt-tokens-chrome | done | 8b0b777 | 2026-07-25T18:35:00Z | The packet's ten files consume `DesignTokens`/`Typography`/`Metrics`. THE GOAL IS A NUMBER: the tile edge was `white@0.25` on `white@0.10` = **1.68:1**; it is now `borderStrong` on `canvas`, measured off the REAL rendered views at **6.91:1 light / 8.90:1 dark**, and the 1.68:1 pair is executed every run as the witness that the assertion discriminates. Tile radius 6 → `Radius.container` per Metrics' own mapping table (the other half of P1.10's inverted-nesting fix); title bar → `tileChrome`, title → `textPrimary` (was `.lightGray`, near-invisible on a light bar), dots → `textSecondary`, hairline → `separator`, close tint → `textSecondary`, corner brackets → `borderStrong` (fill `nil`, not `.clear` — stroke-only, so 4 fill slots leave the sweep, recorded in the floor). THE SEVEN OFFSETS ARE GONE: the `-58` was an undocumented dependency on the close button PLUS the drag-dot cluster, both of which scale with zoom, so it is now that live instance-level sum; pill height = `lineHeight(.caption) + Space.s` (16pt, unchanged) and `+6`/`+15`/`+18` collapse into one `Space.s` rung. Five content tiles' independently re-declared `white:0.10`/`white:0.90` collapse into one `applyDocumentTokens(to:)`; the diff renderer's six `NSColor.system*` and the six `calibratedRed:` git badges become `AccentToken`s on the mapping `StatusChipPresenter` already uses (calibrated mattered on its own — 0.32/0.78/0.45 renders as sRGB `#5FCE86`, off the tested luminance). Sidebar + top bar both → `SurfaceToken.panel` (not 92% and 96% of the same system colour; the alpha went with the literal, because a translucent panel makes whatever is behind it part of every text pair and P1.6 cannot gate that), four Apple semantic labels → two text tokens with the hierarchy preserved. Canvas → `SurfaceToken.canvas`. TWO REAL DEFECTS FIXED, both surfaced by evidence not by reading: (1) the status pill printed straight OVER the tile title on the ~180pt tiles inside a zone card — found by inspecting `observer.rollup`'s baseline before blessing it, which is exactly what the packet's "review the diff images" is for; the title now truncates against the pill; (2) at low zoom the pill clamped its x and drew on top of the drag handle — the chrome's screen-px floors give a zoomed-out tile a 63pt close button and a 72pt dot cluster, so a 180pt tile at zoom 0.35 has nothing left; the pill is now SUPPRESSED rather than clamped, which costs nothing readable (`minimumLegibleZoom(for: .caption)` is 1.0). DELIBERATE DEVIATION — the eleven descriptor fills: the packet asked for "semantically distinct but derived from the token set" and both cannot hold, because only `tileBody`/`tileChrome` are legal as a tile BODY and painting a tile in `canvas` or a card tint gives it a fill whose documented pairs it does not honour. Fill is `tileBody` for every kind; the distinction moved onto TYPE, where the title bar already carried it. NOT argued in prose — `runDescriptorTileFillCheck` pins that the WIDEST of the 55 pairwise ratios among the retired literals is **1.13:1** (46 under 1.10:1) and all eleven had luminance < 0.2, so the fill channel never distinguished anything and under Aqua was the black-on-dark bug eleven times over; it also asserts all 11 kinds' token fills in both appearances and the distinctness of the `displayName`s the distinction moved ONTO. NOT ADOPTED, each reasoned in `CanvasNSView.applyTokens()`: the zone-colour and focus-border registries (USER configuration — seven named hues from Settings; mapping the user's "mint" onto `accentDone` would change what they picked), `controlAccentColor` on selection/marquee/jump badges (the system accent they set), and the nav-mode HUDs (white on a black scrim over a dimmed canvas — correct in both appearances; doing it properly needs `scrim` + on-scrim tokens P1.3 never declared). GATES all inside the existing `--ui-probe-check` leg, inventory unchanged at 259: check 4 widened from one tile to ELEVEN adopted surfaces and `literalOwnersPendingAdoption` is now **EMPTY**; and P1.10's recorded honest limit is CLOSED — `foregroundSlots(in:)` reads the AppKit colour properties that are not layer colours (`NSTextView`/`NSTextField.textColor`, `NSTableView.backgroundColor`, `NSButton.contentTintColor`, and every distinct `.foregroundColor` in a text storage): **132 layer + 172 non-layer** colours per run, plus 130 pill placements clearing both handle and title (14 suppressed; BOTH branches required to occur). Check 2b would have decayed into a tautology once the sidebar/top bar left system colours, so it gained a `SystemFillFixtureView` subject that still paints `appResolvedCGColor`. FOUR MEASUREMENT TRAPS, all recorded in the code: `.cgColor` on a DYNAMIC `NSColor` resolves against `NSAppearance.current` (the system appearance outside a draw cycle), so a correct dark-leaf label read back as `#FFB347` while hosted in `.aqua` — read inside the view's drawing appearance; a bezelled control's fill comes WITH its bezel in AppKit's palette (its `textColor` is ours and IS checked); a hidden view paints nothing; a transparent fill is legal only when the nearest ancestor that paints one paints a token surface. 13 negative tests observed red with the final code, quoted verbatim in `UIProbeAppearance` — and THREE found real coverage holes rather than confirming the gate: the file-tree fixture had no ROWS so row/badge colours were entirely ungated (fixed with a canned snapshot covering all six git statuses), the diff's six accents live in ATTRIBUTED RUNS not `textColor` (fixed with a storage walk), and the first pill check set the bar's frame by hand where the old literal `58` still cleared (fixed by driving a real `CanvasNSView` at zooms 1.0/0.6/0.35). Also recorded: what these gates do NOT catch — swapping one legal token for another of the same family passes, which is what the baselines and a human's eye are for. Allowlist 78 → 33 (all 45 entries for these ten files; zero `calibrated*` uses remain, which rule 1 already banned, so no new rule was needed). 18 baselines blessed, every one inspected first. P0.4 advisory audit (leg still off by design — P1.6): **78 → 11 of 452**. Matrix green at exit 0 including the iOS leg; one pre-existing network-lite WKWebView flake on the bless run, green on the clean run. Codex: 4 findings, all on the descriptor deviation; the concrete one was right and is fixed (the code referenced a `runDescriptorTileFillCheck` that did not exist, and only one `TileKind` was probed) — its objection to the deviation itself stands as reasoned above, now with executable evidence. **OWNER — P1.6 CANNOT GO GREEN WITHOUT A RULING:** 1 of the 11 remaining unreadable pairs is in a file this ticket touched — the needs-attention marching-ants border at **2.07:1** (`#FF983D` on `#FAFBFC`). It is `focusBorderColor(named: FocusBorderConfig.attentionColor)`, the user's own Settings choice, at 0.8 alpha. Dropping the alpha does not fix it (solid `systemOrange` on white is 2.31:1): orange-on-white cannot clear 3:1, so this needs a decision about user-selectable colours, not a code change. The other 10 are the two `terminal.*` Lab cards, untouched here. Second owner note: the tile title now TRUNCATES on narrow tiles and the status pill disappears entirely below ~zoom 0.5 on a small tile — both deliberate, both preferable to overprinting, but both are visible behaviour changes |
| P1.12-ios-consumes-tokens | done | dd48a8c | 2026-07-25T23:05:00Z | **ATTEMPT 2 — DONE.** Attempt 1's implementation restored from `git stash@{0}` (untracked parent carried the new bridge file) onto a HEAD that includes the environment fix `3979b84`, then verified end to end. `StatusChipView` — compiled and referenced nowhere — is now what the phone ships (2 call sites); `StatusPill` and `AppColors` are DELETED, so iOS light mode is real for the first time (`statusAccent`'s `resolved(for: .dark)` pin, P1.8's own TODO, is gone). New `DesignTokens+SwiftUI.swift` is the ONE theme-resolution point: `TokenTheme(_ colorScheme:)`, explicit-sRGB `Color(token:theme:)`/`Color(chip:)`, a `TokenPalette` overloaded per token FAMILY (a fill can only be a `SurfaceToken`), and `.tokenBackground`/`.tokenForeground` modifiers reading `@Environment(\.colorScheme)` themselves — chosen over threading a theme through ~15 views because a view that forgets is silently pinned, the P1.9 failure class. `Font(role:)` maps `TextRole` to iOS SEMANTIC styles so the phone keeps Dynamic Type, weight still from `Typography`. DELIBERATE DEVIATION on `zoneTint`: the packet says route it to tokens, P1.11 ruled a zone's colour is USER configuration — mapping someone's "mint" onto `accentDone` would override their choice AND make the phone paint a hue the Mac does not, the exact failure this ticket closes; what WAS wrong is the vocabulary (iOS knew "amber"/"teal"/"pink"/"green", absent from `ZoneChromeNSView.color(named:)`, so the Mac painted its teal fallback where the phone painted four different hues, and iOS fell back to grey where the desktop falls back to teal) — now name-for-name the desktop registry. BEYOND THE PACKET, two mechanical rules in `check-color-hygiene.sh` (existing leg, inventory unchanged at 261): rule 4 — only the bridge may name a `TokenTheme`, call `resolved(for:)`, or construct an explicit-colour-space `Color`, no allowlist (a call site pinning a theme reads as reasonable code and no raw-literal rule sees it; and since rule 1 must ALLOW `Color(.sRGB, …)` for the bridge, that is the one raw spelling left to bypass it with); rule 5 — the two zone-tint registries extracted from their switches and compared name-for-name and hue-for-hue every run, with a vacuity guard. Both vacuity guards earned their place: rule 3 was silently inert for one revision, and **rule 4's guard caught a bug in this commit's own first pattern** — it keyed on `Color(`, matching neither the bridge's `self.init(.sRGB, …)` inside an `extension Color` nor a call site's `Color.init(.sRGB, …)`; now anchored on the colour-space argument. Allowlist 33 → 27, six iOS entries removed and none added; the four SEMANTIC ones are excused BY THE FILE'S STATE not by the list — with no hardcoded fill left under `ios/`, rule 2's trap cannot be sprung there, and re-adding one raw literal re-arms it across the whole file (observed, 30 `.primary`/`.secondary` lines). 8 negative tests observed red at exit 1 with the FINAL code: theme pinned at a call site; bridge deleted; `Color(.sRGB, …)` and `Color.init(.displayP3, …)` at a call site; the bridge dropping its own explicit-sRGB spelling; iOS "orange" renamed back to "amber"; the iOS registry signature renamed so extraction stops matching; a raw literal re-arming rule 2. VERIFIED: `swift build` green; **two consecutive full matrix runs green at exit 0** including the iOS `xcodebuild` leg, 46/46 baselines matched at 0.0000%, 452 contrast pairs both appearances 0 exempt, inventory 261. No baseline needed blessing — the diff is iOS-only and the desktop pixels did not move. CODEX (gpt-5.5) raised 6, 3 FIXED here: the explicit-sRGB bypass (now rule 4's second arm); the mirror tile's resting outline `Color.white.opacity(0.15)` → `LineToken.border` (fine while the phone only painted a dark panel, INVISIBLE now that iOS has a light appearance — the same white-hairline-on-light defect P1.11 measured at 1.68:1); and `StatusChipView`'s weight silently moving semibold → medium, kept but no longer silent (adopting a role means adopting its weight, and it is the collapse the desktop already made per Typography's own mapping table). Its `zoneTint` item asked only that the deviation be recorded — done here and in the commit body. **TWO HONEST LIMITS, both open:** (1) no executable check proves the bridge resolves light and dark to different leaves — the iOS target has no test host in this repo (P0.1's leg is build-only), the token VALUES are gated by P1.6, and rule 4 plus `TokenPalette`'s per-family typing make a pin unspellable, so the one unexecuted line is the `ColorScheme` → `TokenTheme` mapping itself; (2) the packet's simulator confirmation is OWED — the phone needs a paired Mac to show agent rows, so same-glyph-same-hue parity is a supervised step, not a headless one. Owner: the mirror tile's highlight/drag stroke is still `Color.orange` where the desktop paints selection in the user's `controlAccentColor` — a real cross-platform disagreement left for Phase 8, since making them agree is a decision about which is right, not a token lookup; and the phone's status pill is now medium rather than semibold. Attempt 1's note, for the record: it STOPPED ON THE ENVIRONMENT, not on the ticket, NOT ON THE TICKET — see `_ENV-BLOCKER-2-text-antialiasing.md`. At HEAD with a clean tree, `--ui-baseline-check` is red on **24 of 46** committed baselines (identical pixel counts with and without this ticket's diff, identical across two consecutive runs), and the diff images are magenta ON LETTERFORMS ONLY: `5080adc` pinned the probe's SCALE but glyph rasterisation still comes from the window's display, and this host went back to 2x (built-in Retina, only display) after the 1x period that blessed them. Blessing 24 renders to get green is the move the runbook forbids; pinning font smoothing is `UIProbe.render`'s job and a substrate ticket with its own 46-baseline bless commit. Every remaining ticket hits the same leg, hence `LOOP: STOP`, not a skip. IMPLEMENTATION IS COMPLETE, parked in `git stash@{0}` "P1.12-wip" (commit `2c7cc86`; tree left clean apart from this ledger + the blocker doc): new `ios/Continuum/Sources/DesignTokens+SwiftUI.swift` is the ONE place iOS resolves a theme — `TokenTheme(_ colorScheme:)`, `Color(token:theme:)`/`Color(chip:)` on explicit sRGB (ticket 87 watch-out #1), a `TokenPalette` overloaded per token FAMILY so a fill can only be a `SurfaceToken`, `.tokenBackground(_:)`/`.tokenForeground(_:)` modifiers that read `@Environment(\.colorScheme)` themselves (chosen over threading a theme through ~15 views: a view that forgets is silently pinned to one appearance, which is the P1.9 failure class), and `Font(role:)` mapping `TextRole` to iOS SEMANTIC styles so the phone keeps Dynamic Type (titleL→.title3, title→.headline, body→.body, label→.caption = the 12pt the pill already shipped, caption→.caption2; weight from `Typography`, both mono roles `.monospaced()` per P1.10's reasoning). `AppColors` is DELETED — its two dark-only literals become `SurfaceToken.canvas`/`.panel` across 15 call sites, and `statusAccent(for:)`'s `resolved(for: .dark)` pin (P1.8's own TODO) is gone, so iOS light mode is real for the first time. `StatusPill` is DELETED and its two call sites now render `StatusChipView` — the shared component that was compiled and referenced nowhere, which is the ticket's Goal; `StatusGlyphView` paints `StatusChipDisplay.accent` through the bridge, and the canvas status dot's grey fallback becomes `textSecondary` at 0.4. DELIBERATE DEVIATION on `zoneTint`: the packet says route it to tokens, but P1.11 ruled a zone's colour is USER configuration and mapping someone's "mint" onto `accentDone` would both override their choice and make the phone paint a hue the Mac does not — the opposite of "render twice". What WAS wrong is the vocabulary: iOS knew "amber"/"teal"/"pink"/"green", which `ZoneChromeNSView.color(named:)` does not have (so the Mac painted its teal fallback where the phone painted orange/teal/pink/green), and it fell back to grey where the desktop falls back to teal. iOS is now name-for-name the desktop registry. BEYOND THE PACKET, two mechanical rules in `check-color-hygiene.sh` (inside the existing leg, so the P0.11 inventory is unchanged): rule 4 — only the bridge may name a `TokenTheme` or call `resolved(for:)` under `ios/`, no allowlist, because a call site pinning a theme reads as reasonable code and no raw-literal rule can see it; rule 5 — the two zone-tint registries are extracted from their switches and compared name-for-name and hue-for-hue every run, with a vacuity guard (rule 3 was silently inert for one revision, and a source-shape comparison that matches nothing is that failure with a green exit). Allowlist 33 → 27: all six iOS entries removed, none added, and the four SEMANTIC ones are excused by the file's state rather than by the list — re-adding one raw literal re-arms rule 2 and turns all 30 `.primary`/`.secondary` lines back into named violations, observed. 7 negative tests observed red at exit 1: theme pinned at a call site; the bridge deleted; iOS "orange" renamed "amber"; iOS "mint" repainted `.green`; the DESKTOP registry gaining "pink"; a raw literal back in iOS; the iOS registry renamed so extraction stops matching. `swift build` green; iOS `xcodebuild` leg green (`** BUILD SUCCEEDED **`) after `xcodegen generate`; hygiene green. NOT verified and owed on a host with a working baseline substrate: the full matrix, codex cross-review, and the packet's simulator confirmation (the phone needs a paired Mac to show agent rows, so the meaningful light/dark comparison is a supervised step; an unpaired-screen luminance witness against the exact `canvas` leaves is the honest automatable subset if you want one) **UNBLOCKED 2026-07-25 ~11:25Z by the supervisor: the environment blocker is FIXED in `3979b84`** — `UIProbe.bitmap(of:)` now owns its drawing context and pins font smoothing / subpixel positioning / subpixel quantization off, so glyph rasterisation no longer follows the host display. All 46 baselines re-blessed under the pinned path; four consecutive baseline runs and two consecutive full matrix runs green. The block was environmental and MINE (deferred from `5080adc`), never a fault of this ticket, so the never-retry-a-block rule does not apply. Its implementation is parked in `git stash@{0}` ("P1.12-wip: iOS token adoption") and was reported complete but unverifiable: swift build green, iOS xcodebuild leg green, colour hygiene green with 7 negative tests observed red. Still owed: full matrix, cross-review, and confirmation that the phone actually renders a status chip rather than raw `turn.{status}`. |
| P2A.1-agent-record | done | 344af7f | 2026-07-26T00:35:00Z | `AgentID` + `AgentRecord` in a new `Sources/ContinuumRevivedCore/Agents/` plus `AgentRecordChecks` — the packet's two files, nothing adopts them yet. THE LOAD-BEARING FIELD is `tileId: UUID?`, demoted from identity to a nullable VIEW BINDING (nil == headless); the three failures that demotion fixes are named at the head of the file (`ZoneHydrationBudgetConfig`'s 4-live-zone CANVAS LAYOUT budget freezing agents past it, `ZoneRuntimeBudgetConfig.closeOnZero` tearing down observers for non-current workspaces, relaunch rebuilding an empty tile). Codable is hand-written for one reason: `JSONCodec.makeEncoder` is `.iso8601` with no fractional-second component, so both Dates encode as `timeIntervalSinceReferenceDate` Doubles per `AgentActivityEvent`'s recorded precedent, NOT `timeIntervalSince1970` (lossy on round-trip). VERIFIED BY A 200-VALUE SWEEP over real `Date()` values, not a canned interval, and that distinction is the point: the canned fixture PASSES the lossy spelling (exactly representable) while the sweep goes red at 99-100 of 200. Decode-forward via `decodeIfPresent` on every optional; `schemaVersion` starts at 1 and is READ OFF the payload, so a newer build's record carries its version through instead of being clamped; `AgentID` encodes as a bare UUID string (asserted on the wire text), not `{"rawValue": …}`. I5 WITNESS: encode + Core's `SyncPayloadTaintScanner`, asserting a `.hostLocalPath` violation AT `cwd` specifically (a record flagged for another reason would otherwise pass while saying nothing about the path) for ALL FOUR host prefixes the scanner knows, floored at 4 — a `/var/folders/` worktree root is exactly the P2C case a lone `/Users/` fixture misses. HONEST LIMIT written into the code, not implied: the scanner matches host paths by PREFIX, so a `/tmp/...` cwd is NOT flagged and the discriminating case asserts that out loud. This is a BACKSTOP for the common case, not a proof of the invariant; the invariant is that nothing publishes the type, and a publisher-side guard is P2A.2's. Packet watch-outs honoured: `ManagedAgentSessionRecord` untouched (coexistence; P2A.7 restores), no lifecycle field (Phase 4's `settledOverride`), `displayName` kept distinct from `role`. 8 NEGATIVE TESTS OBSERVED RED, each quoted at its check: lossy Date spelling (99/200 differed); `role` via `decode` (keyNotFound); `schemaVersion` clamped to `currentSchemaVersion` (got 1, needs 2); `encode` rather than `encodeIfPresent` for `tileId` (explicit null on the wire); `AgentID`'s single-value Codable deleted (wrapper object); the witness on a non-host `/tmp` path (found []); the `/var/folders/` root dropped from the sweep (got 3); and — recorded for what it does NOT witness — dropping `cwd` from `encode(to:)`, which is red at the ROUND-TRIP check, not at the I5 witness. CODEX (gpt-5.5) raised 5, 4 FIXED pre-commit: `createdAt` was `let` where the packet's shape says `var` (the packet is the contract); the fixture re-typed the model/thinking literals instead of reusing P0.10's `AgentModelConfig.defaultModel`/`defaultThinking`; the future-key payload still carried `schemaVersion: 1` so it could not catch a clamping decoder; and the header comment overstated the I5 property, now narrowed. Its 5th (publisher-side guard / teach the scanner AgentRecord's shape) DEFERRED with reason — the packet's Files name two files and its Verify asks exactly for the taint witness, so the guard is P2A.2's, and the limit is written where the next worker reads it. Matrix green at exit 0 on TWO runs including the iOS xcodebuild leg, 46/46 baselines matched, 452 contrast pairs both appearances, inventory 261. A third run hit the pre-existing `network-lite log did not capture a committed or finished event` flake (browser-inspector WKWebView navigation timing, already recorded under P1.5 and P1.9) — unrelated to a Core type with no call sites. Inventory blessed here: `count ContinuumRevivedCoreChecks` 21 -> 22 for the new `runAgentRecordChecks()` call site, a floor increase and the only record that moved; no baseline needed blessing. Owner: nothing consumes `AgentRecord` yet — P2A.2 stands up the store. |
| P2A.2-agent-store | done | 8c3716b | 2026-07-26T02:05:00Z | `AgentStore` + `AgentStoreChecks` — `ManagedAgentSessionStore`'s shape (`AtomicWriter`, one JSON file per record, upsert/load/delete/loadAll) rooted at `~/Library/Application Support/continuum-revived/agents/<agentId>.json`. THE ROOT IS THE TICKET, so it is asserted as a path, not argued: the project store writes under `ProjectStoreLayout.managedSessionsDirectory` and only `activeController.managedSessionStore` is reachable at runtime, so a cross-project inbox cannot be built on it. Nothing adopts the store yet (P2A.3); `ManagedAgentSessionStore` untouched, P2A.7 owns migration. Two additions beyond a straight copy: `loadAll` sorts by createdAt then id (enumeration order is not stable, and a sidebar that reorders between refreshes is a bug the frozen-list decision would then have to fight), and an unreadable file is skipped and NAMED on stderr rather than thrown. The skip is proven to be a real skip and not `AtomicWriter`'s backup recovery covering for it — a clobbered record WITH a backup is asserted RECOVERED, a corrupt one without a backup asserted DROPPED. Ordering carries two vacuity guards (expected order must differ from both insertion and filename order), so a degenerate fixture says so instead of passing an unsorted `loadAll`. 7 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, each quoted at its check: agents dir rooted in a project's `.continuum-revived/managed-sessions`; the resolver's `smokeTest` branch deleted (`got nil` — nil means the real store, exactly the pollution the watch-out names); the nil-root init bypassing the resolver (`got /Users/…/Library/Application Support/continuum-revived/agents`); `.sorted(by:)` dropped (`["alpha-first", "beta-late", "beta-tied", "alpha-tied"]` — which is ALSO the evidence that raw enumeration order is neither sorted nor insertion order); sort by id alone; the do/catch removed (`noValidBackup(path: …/corrupt/agents/truncated.json)` — the whole listing lost to one file); one constant filename for every record (`concurrent upserts lose no agent — got 1 of 32`, observed with checks 1–4 unrun since check 1 catches that edit first, and recorded as such). CODEX (gpt-5.5) raised 5, 3 FIXED pre-commit: a default-constructed `AgentStore()` went straight to the canonical path and ignored `CONTINUUM_APP_SUPPORT` — the packet's watch-out with the sign flipped, since every matrix leg runs under that override — so the nil branch now goes through the resolver and there is an `init(smokeTest:environment:)` for the launch-time spelling; that property is now asserted for the DEFAULT-constructed store (the one a future ticket writes by accident), the single assertion that reaches through the real process environment, which it saves, restores and asserts it restored; and `AppDelegate.resolveAppSupportDir(smokeTest:)` now FORWARDS to the Core resolver rather than keeping a second copy of the convention (the packet asks for the pure logic in Core "so it is testable"), which also closes its separate point that nothing proved the two stay in step — there is only one now. Its other 2 answered: read-only sandbox could not build, and the diff cannot self-prove the observed-red lines (one build + run per mutation, this program's standing convention). Matrix green at exit 0 on TWO runs including the iOS xcodebuild leg, 46/46 baselines at 0.0000%, 452 contrast pairs both appearances; a third run hit the pre-existing `network-lite log did not capture a committed or finished event` flake (WKWebView navigation timing, recorded under P1.5/P1.9/P2A.1). Inventory blessed here: `count ContinuumRevivedCoreChecks` 22 -> 23, a floor increase and the only record that moved; 261 records, no new flag or leg. HONEST LIMIT at the head of the file: this store is why `AgentRecord` stays host-bound in practice (local path, no publish path), but nothing here PREVENTS another file handing a record to Sync — P2A.1's taint witness is the backstop; the real guard is a source-level rule that Sync may not name the type, and it belongs in the hygiene script, which this packet does not name. Owner: `AgentStore()` now honours `CONTINUUM_APP_SUPPORT`, which the app-side resolver already did — behaviour is unchanged for the app, but a Core-side default store is no longer able to write to the real agents directory from a smoke run. |
| P2A.3-agent-supervisor | done | 919b977 | 2026-07-26T03:20:00Z | `AgentSupervisor.swift` (new) + `wireManagedAgentTile` rewritten — the packet's two files, plus the leg in `run-matrix.sh`. `managedAgentRunners` is DELETED (nothing else referenced it); the tile spawns/re-finds its agent through the supervisor, routes `onSubmitPrompt` to `send`, and SUBSCRIBES. THE MULTICAST IS THE LOAD-BEARING HALF, not a convenience: a single-consumer stream makes the consumer the owner by construction (there is nowhere for events to go once it is gone), so `events(for:)` copies `ActivityStore.subscribe()`'s snapshot-then-tail contract — history yielded BEFORE the subscriber registers, which is what lets a re-attached view see the transcript rather than a mid-turn tail. Two identities moved with ownership: the Pi `--session-id` is `continuum-agent-<agentId>` not `continuum-<tileId>` (the conversation belongs to the agent), and every delivered event is restamped with the AGENT's thread so all consumers see one stream regardless of how many runner processes produced it (the tile rebinds to its own thread on the way in, exactly as before at that boundary). Events hop back on `DispatchQueue.main.async`, which is FIFO — a `Task { @MainActor }` per event is not, and order is asserted. `PiAgentRunner` untouched (Phase 5 replaces it); the seam is a two-method `AgentRunning` protocol it already satisfies, so the matrix drives the supervisor with a `ScriptedAgentRunner` — no Pi, no network, no wall-clock. Persistence is on LIFECYCLE events only (`sessionStateChanged`/`turnStarted`/`turnCompleted`/`runtimeError`): every write is an `AtomicWriter` write (temp + fsync + read-back) and `contentDelta` arrives per token, so persisting all of them would put an fsync per token on the main thread. NEW LEG `--agent-supervisor-check`: 8-event scripted turn fanned to two subscribers attached before the prompt + one after, all three receiving the identical restamped sequence; record persists on SPAWN as well as send (headless spawn, no tile, no prompt); `stop` makes a blocked `run()` actually RETURN and moves the stored `lastActivityAt`; a `send` on a busy agent refused, not replacing the in-flight runner; and a source scan of `Sources/ContinuumRevived` (67 files) asserting `AgentSupervisor.swift` is the ONLY file that constructs a `PiAgentRunner`, so no view can become a second owner (the done-criterion is otherwise only assertable by reading the diff). 11 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code: fan-out to the first subscriber only (`A got 0, B got 8`); no history replay (`got 0 of 8`); `stop` not reaching the runner (`stopCount 0`); `sessionStateChanged` dropped from the persist set (identical stored intervals, which is why the message prints reference intervals and not `Date` descriptions); no restamping (`at index 1 got turnStarted:t1@provider-thread, expected …@agent-ED800B2E…`); a `PiAgentRunner` in `ManagedAgentTileNSView` (both files named); `spawn` not persisting; the leg removed (P0.11 named both records); `stop()` not signalling the blocked runner (`completedRuns 0`); the busy-agent refusal deleted (`runCount 2`, both prompts). TWO OF THOSE FOUND HOLES IN MY OWN ASSERTIONS rather than confirming them, both recorded at the assertion: "the record persists" was satisfied by `send`'s write, so a headless spawn that is never prompted would have vanished on relaunch unnoticed; and reading `runCount` straight after a refused `send` is green even when a second runner started, because `run` is invoked on a background queue — it is a windowed assertion now. Codex (gpt-5.5) raised 6, 4 FIXED: **HIGH and real** — `send` assigned over `runners[id]`, leaving the first Pi process running, unreachable by `stop`, on the same session id; now one runner per agent, REFUSED rather than replaced (safe for the UI, which latches `promptInFlight` and disables compose for the turn, and the honest answer for P2D's orchestrator, since queueing/steering a live turn is `P5.7`'s and inventing it here would be a second answer to supersede); the stop assertion was VACUOUS (`stop` clears `runners[id]` synchronously, so `isRunning == false` proved only that a dictionary entry went away — now asserted on a post-return counter inside the runner); deleting a managed-agent tile left its subscription task alive until the next event, so `deleteTile` cancels it (deliberately NOT stopping the agent or clearing `tileId` — closing a tile must not kill an agent, and detach is P2A.5's); and my "re-wiring replaces the subscriber" comment overclaimed — the replacement replays history, right for a fresh view and duplicating in one that already ingested, so the comment now names the three call sites (each a just-constructed tile) instead. 2 reasoned away in the commit body: the 500-event replay cap (a re-attach after a very long run shows the tail; an uncapped history is an uncapped per-token buffer for the life of the app, and the durable transcript is `ManagedAgentTranscriptModel`'s and the activity log's job) and the source scan's brittleness (a backstop of the same class as `UIProbeAppearance.declaredConformers()`; the protocol makes the factory the only injection point). Matrix green at exit 0 on TWO consecutive runs including the iOS xcodebuild leg, 46/46 baselines at 0.0000%, 452 contrast pairs both appearances, 0 exempt; one run in between hit the pre-existing `network-lite log did not capture a committed or finished event` flake (WKWebView navigation timing, recorded under P1.5/P1.9/P2A.1/P2A.2). Inventory blessed here: 261 → 263 (+1 check, +1 leg); no `count` record moved (the check is not in a `*Checks` target) and no baseline needed blessing (no pixel moved). **HONEST LIMIT — the live path is NOT covered headlessly:** what is gated is the supervisor's ownership and fan-out, not a real prompt reaching a real Pi through the new wiring. `--managed-agent-live-check` needs Pi auth and a supervised GUI (it blocks in the folder-access `NSAlert` unattended, recorded under P0.8) and NO matrix leg boots the app delegate, so `wireManagedAgentTile` itself is compiled and reviewed but unexecuted. Worth one supervised run before Phase 5 builds on it. Owner: nothing is restored at init, so records from a previous launch accumulate on disk until P2A.7 adopts them, and `agent(forTile:)` therefore dedupes within a session only; and a second prompt during a turn is now dropped with a warning on stderr rather than silently starting a second process. |
| P2A.4-tile-as-subscriber | done | 36b48e4 | 2026-07-26T04:10:00Z | The subscription moved from `wireManagedAgentTile` into the view: `attach(agentID:supervisor:)` replays the agent history then tails it, `detach()` cancels the subscription and nothing else, and `managedAgentTileSubscriptions` is deleted (tile close now calls `detach()`). The stream is created in `attach`, not inside the task, so P2A.3's snapshot-then-tail boundary stays exact; events are rebound to the tile's thread at ingest as before. Double-count has a LOCAL half the packet's watch-out names: the projection already holds part of what the replay repeats, so `attach` is a no-op for the agent already attached and resets the projection before any other replay. The app keeps only its own interest in the stream — the 88.4c phone/activity mirror, now via `onIngestedEvent`. Verified by a new tile section in `--agent-supervisor-check` driving the REAL `ManagedAgentTileNSView` with scripted runners: a turn runs unattached, the tile replays those 3 and renders them, tails 2, then detaches mid-turn — agent still running, blocked `run()` neither returned nor stopped, remaining subscriber still gets `.stopped`, detached tile ingests nothing. 7 negative tests observed red at exit 1 with the final code, quoted verbatim at `checkTileIsASubscriber`. Two consecutive full matrix runs green including the iOS leg; inventory unchanged at 263 (no new flag/leg). Codex: 7 findings, 3 fixed pre-commit (detach-then-re-attach to the SAME agent double-ingested — 13 events vs a 7-event history, now its own assertion + witness; `resetProjection` left stale arranged subviews — now asserted against the card stack and cleaned per `BrowserInspectorTileNSView`; witness wording said "same agent" for a live-rewire test). Two reasoned away in the commit body: the phone mirror stops when the tile closes, which is UNCHANGED from P2A.3, and making it independent needs a view-free status derivation = `P2A.6`; the third is that point restated. **Owner: closing a managed-agent tile now detaches — the agent keeps running with no view and nothing re-attaches it until P2A.5. No matrix leg boots the app delegate, so `wireManagedAgentTile` is still compiled and reviewed but unexecuted; the gate is on the tile + supervisor it wires together.** |
| P2A.5-attach-detach-lifecycle | done | ec2d5c1 | 2026-07-26T04:55:00Z | `AgentSupervisor.attach(agentID:to:)` / `detachView(agentID:)`: the binding is `AgentRecord.tileId` and nothing else moves — no runner started, stopped or replaced — and `attach` unbinds any other agent still claiming that tile (`agent(forTile:)` is a `first(where:)`, so two claimants answer nondeterministically once Phase 3 retargets a tile). `deleteTile`'s `.managedAgent` branch clears the binding next to the tile's own `detach()` and still never calls `stop`; `wireManagedAgentTile` routes through `attach` so one site records the binding whichever branch ran, which is also the site P3.9 reaches. Verified in `--agent-supervisor-check` §8: close on an IN-FLIGHT prompt → persisted record with `tileId == nil`, `agent(forTile:)` nil, runner still in flight (`stopCount 0`, `completedRuns 0`), an event the still-blocked runner emits AFTER the close reaches the remaining subscriber restamped, closed tile ingests nothing more; then re-attach to a new tile replays pre- AND post-close work, a second agent displaces it, and only `stop()` makes the blocked `run()` return — record kept, since stopped is a state not a removal. 6 negative tests red at exit 1, 4 of them production edits (`detachView` not clearing `tileId`; clearing memory only, caught because every binding assertion reads the STORE; `agentSupervisor.stop` added to the close branch; the `detachView` call deleted; `attach` not displacing; `attach` not persisting) + a vacuity witness for the reaper sweep. REAPER WATCH-OUT: no exclusion needed and asserted, not argued — `SessionPruner`'s only mutating call is a tmux `detachSession` and a supervisor agent is a Pi process, so a real sweep over a maximally stale binding built with `ZoneRuntimeController.startReaper`'s own `managedSessionStore.load(tileId:)?.lastSeenAt` expression over a real `ManagedAgentSessionRecord` logs `detachSession`, logs no kill, and leaves the agent running. `ScriptedAgentRunner` gained `emit(_:)` (a live turn can produce one more event) because `send` is correctly refused mid-turn, so post-close delivery is otherwise untestable. Codex: no correctness bugs, 4 findings — both reaper ones fixed (production binding expression; claim narrowed to what is measured, `startReaper`'s wiring named as uncovered), the two asking for `deleteTile` to be EXECUTED reasoned away in the commit body (AppDelegate over a live canvas/workspace runtime/focus broker/canvas save, and `DeleteConfirmPolicy.always` runs an `NSAlert`; the source scan holds the one invariant the packet is about). Owner: `ManagedAgentSessionRecord` is still deleted on close (its `tileId` is a `let` — view state that cannot outlive its tile), so until P2A.8 migrates sync keys a detached agent is listed by `AgentStore` but not by the tile-keyed status surfaces. Matrix green at exit 0 incl. iOS; inventory unchanged (no new flag/leg). Unrelated pre-existing flake hit twice: `--browser-inspector-network-lite-check` (real WKWebView load) fails ~1 run in 10, measured 9/10 standalone; two full matrix runs green. |
| P2A.6-headless-agents | done | 225bd00 | 2026-07-26T05:55:00Z | `agent.newHeadless` in `CommandRegistry` (⌘K row "New Agent Without a Tile…"); `agent.newManaged` stays spawn + attach a tile. A headless agent is exactly a record with `tileId == nil` — the branch never touches `tileSpawner` or the canvas. THE FIRST PROMPT IS COLLECTED AT SPAWN, in the same `NSAlert` `spawnHarnessRoleFromPalette` uses (generalised to `promptForAgentTask`, harness strings unchanged): a headless agent has no compose row, and `spawn` only starts a runner for a non-empty prompt, so a prompt-less branch would be an inert record nothing can reach — the done-criterion is "a RUNNING agent with no tile". `AgentSupervisor.spawn`'s existing `prompt` parameter carries it; the app's cwd/model/project resolution is now one `spawnSupervisedAgent` helper shared with `wireManagedAgentTile` rather than two copies. WATCH-OUT, stoppability: `AgentSupervisor.stopAll()` from `applicationWillTerminate`, so a tile-less agent's Pi process cannot outlive its session; in-session stop needs a surface = Phase 3. NO AGENT CAP INVENTED (`TerminalSpawnAdmission` caps terminals and a headless agent is not one) — whether agents want their own cap is the owner's call. Verified in `--agent-supervisor-check` §9 with the count DERIVED from `ZoneHydrationBudgetConfig.defaultMaxLiveZones` + 2 = 6, not picked: six agents spawned WITH a prompt run at once with no tile, each persisted `tileId == nil` and delivering restamped events; a real `CanvasState` is carried alongside and "no record claims a tile the canvas does not have" is asserted while all six are headless and again after one is attached (that is what "no tile exists in CanvasState" is asserted AS — the vacuous reading would be a `tiles: []` literal); the attached one replays the work it did while headless into a real `ManagedAgentTileNSView`; `stopAll` makes all six blocked `run()`s return, asserted on the runners' post-return counter (per P2A.3's recorded trap: `isRunning == false` is true the instant `stop` is called), and every record survives. 9 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, quoted at `checkHeadlessAgents`: `tileId: UUID()` in the branch; the branch reaching the tile spawner; `agent.newHeadless` removed from the registry; the dispatch case removed (a COMPILE error — the switch is exhaustive — so reachability is asserted on the registry and the palette rows, which are what ⌘K reads); `applicationWillTerminate` not calling `stopAll`; `stopAll` reaching no runner (`0 of 6`); a record claiming a tile the canvas does not hold (vacuity witness for the invariant); plus the two below. TWO FOUND HOLES IN MY OWN ASSERTIONS: a headless branch that simply CALLS `spawnManagedAgentFromPalette` PASSED the first draft of the tile-creating pattern (it makes a tile without naming the spawner), so `spawnManagedAgent` is now in that pattern; and the first draft spawned with `prompt: nil`, so the branch and the helper are both scanned for the prompt and the executable half spawns WITH a prompt instead of spawn-then-send. Codex (gpt-5.5): 4 findings — the prompt-less spawn (HIGH, real) and the matching verification gap FIXED, its third (reuse `spawn`'s `prompt` parameter) is how they were fixed, its fourth reasoned away in the commit body (source-scan brittleness is the standing trade of `piRunnerConstructionSites`/`managedAgentCloseBranchSource`: the method needs a live `CanvasNSView` and its own modal is an `NSAlert`, so no leg can execute it; a rename makes the scan THROW rather than pass). Palette row expectations grew by one row — the exact-order lists and the two index-based `isSelectable` assertions in `ContinuumRevivedPaletteChecks` now include the new row (still exact equality over the whole set), plus a new assertion that a "headless" query matches only it. "background" was dropped from its filter tokens so a "back" query does not surface a spawn action. Matrix green at exit 0 including the iOS xcodebuild leg; inventory unchanged at 263 (§9 lives inside the existing leg). **HONEST LIMIT: no matrix leg boots the app delegate, so the ⌘K branch itself is compiled, scanned and reviewed but UNEXECUTED — the modal and the spawn it triggers want one supervised run.** Owner: a headless agent is INVISIBLE until Phase 3's inbox (by design, not a bug) — the only surfaces are the id on stderr, `supervisor.records` and `AgentStore` on disk; and quitting now stops every running agent, tile-bound ones included. |

| P2A.7-restore-on-relaunch | done | 658280b | 2026-07-26T07:20:00Z | `AgentSupervisor.restore()` adopts the store's records at boot, called BEFORE the canvas tile walk — which is the load-bearing half: `installInitialManagedAgentTile` -> `wireManagedAgentTile` re-finds an agent via `agent(forTile:)`, so with restore after the walk (or absent) every relaunch spawned a brand-new agent over a surviving record and lost its identity/model/role. NO PROVIDER PROCESS IS STARTED — a relaunched agent is idle until prompted, and nothing is lost because Pi's `--session-id` is derived from the agent id. Transcript history is the packet's OPTION (b): a "previous session — send a prompt to continue" card, since the desktop transcript lives only in the view; re-deriving it from Pi's session JSONL is its own ticket. BEYOND THE PACKET'S TWO FILES, with reason: a card cannot be rendered from `ContinuumApp.swift` alone, so `ManagedAgentTranscriptModel.appendNotice(id:title:text:)` (Core, idempotent on id) and `ManagedAgentTileNSView.showPreviousSessionNotice()` were added — the in-files alternatives were faking the notice as a user prompt or as a synthetic `.sessionStateChanged`, both of which put text no one wrote onto a real channel. Watch-outs: a record whose `cwd` is gone is MARKED (`staleIDs`) and skipped, never adopted and never deleted (a worktree root can come back, and a later restore then adopts it); `ManagedAgentSessionStore` is not read, so a tmux agent cannot be restored from both stores. NEW LEG `--agent-restore-check`: three records written straight to a temp `AgentStore` (nothing in-process ever held them), 2 adopted + 1 marked, identity fields preserved, `agent(forTile:)` resolving, ZERO runners constructed (asserted on the scripted factory's handout count, not a nil dictionary entry), a real `ManagedAgentTileNSView` attached showing `idle` + the notice, a prompt then starting it, and four restores neither duplicating, clobbering the live copy, nor pulling from the other store. 11 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code: restore adopting nothing; the stale-root guard removed; restore auto-starting each agent (`restore constructed 2 runner(s)`); the live-copy-wins branch removed; the boot `restore()` moved after the tile walk (ordering scan names the line numbers); the notice call removed from `wireManagedAgentTile`; `appendNotice`'s idempotency guard removed; the notice never reaching the view hierarchy (`0 view(s) for 1 card(s)`); the restored tile keeping `configuring`; `staleIDs` never cleared; the notice condition widened back to `wasRestored`. CODEX (gpt-5.5) raised 7 — 3 REAL DEFECTS FIXED pre-commit: a restored tile rendered `configuring` (now sets `idle` directly, the way `resetProjection` does, rather than ingesting an event no provider produced); `staleIDs` was never cleared when a root came back, so the Phase 3 inbox would see one agent as both stale and live; and the notice was placed on `wasRestored` alone, which prints the placeholder UNDER a real transcript when a tile is re-wired to a restored agent that has since been prompted — hence the narrower `needsPreviousSessionNotice` (restored AND no history). 2 of its 3 verification gaps closed (tile status asserted with a `configuring` vacuity guard; stale-to-live recovery exercised); the third is NOT — no matrix leg boots the app delegate, so boot integration stays a source scan, the same limit recorded for P2A.5/P2A.6, and the ORDERING is what that scan asserts because it is the half that breaks silently. Its low "session-id derivation is not proof of Pi resume" is NARROWED, not closed: the check asserts the production `PiAgentRunner.processArguments` composes `--session-id <the restored id>` and not `--no-session`; that Pi HONOURS it needs the supervised `--managed-agent-live-check`. Matrix green at exit 0 including the iOS leg, 46/46 baselines matched, 452 contrast pairs both appearances; one run in between hit the pre-existing `network-lite` WKWebView flake (recorded under P1.5/P1.9/P2A.1-P2A.3). Inventory blessed here: 263 -> 265 (+1 check, +1 leg); no baseline needed blessing. PROCESS NOTE: a `git checkout <file>` used to revert a negative-test mutation destroyed this ticket's unstaged implementation of `AgentSupervisor.swift` and it had to be re-applied — mutate against a `/tmp` snapshot, never against the index. Owner: agents now accumulate across launches and are all adopted at boot, so the app-support `agents/` directory is load-bearing from here on, and a stale-rooted agent is silently absent from `records` until the Phase 3 inbox surfaces `staleIDs`. |
| P2A.8-sync-key-migration | done | e72af76 | 2026-07-26T09:05:00Z | Aggregate key `AgentActivityEvent.tileId` -> `agentId` (`AgentRecord.id.rawValue`), `tileId` demoted to an OPTIONAL VIEW HINT (nil = headless) whose only consumer is the phone's "Show on canvas"; `byTile` -> `byAgent`, `TileActivity` -> `AgentActivity` (hint read off the canonically-last event, so the fold stays order-independent), `ActivityLogFile.currentSchemaVersion` 1 -> 2. FORWARD-COMPATIBLE DECODE IN THREE PLACES: an event with only `tileId` decodes as `agentId = tileId` (one value historically, so nothing is invented), a `byTile` snapshot re-keys AND keeps its legacy key as the hint (else a migrated row would render with Show-on-canvas hidden for an agent that demonstrably had a tile), a version-1 log file still loads; an event with NEITHER key throws instead of inventing an id. `AgentsBoardRow.id == agentId`; `ApprovalResponseTarget` addressed by agent, because a tile-addressed response could not answer a headless agent's approval at all. iOS rows/navigation/detail keyed by agent, and "Show on canvas" is ABSENT (not broken) with no tile. BEYOND THE PACKET'S `## Files`, with reason: `ApprovalResponseRequest.tileId` -> `agentId` (SyncTransport/ApprovalResponder) and the whole push surface (APNSPushService AgentContext/PushIdentity/dedup key/deep link `?agentId=`/userInfo key/PushApprovalResponseIntent) — both read their id off the now-agent-keyed row, so leaving them named `tileId` would be exactly the half-migrated key the packet forbids; both accept the legacy key on decode, and the deep-link string has no consumer outside this repo. Also `keyedByTileHint` in Core as THE one tile-hint join (contested tile -> lowest agent id wins) for the snapshot, the receiver's tile-keyed status map and the canvas overlay — it must not be `uniqueKeysWithValues`, which would TRAP, since two entries can name the same tile (a detached agent's last event still carries the hint it had). I5 re-verified over the NEW payload: the taint sweep's fixtures carry `agentId` AND a `tileId` hint with deliberately different values (24 tone x status + runId + overflow), and the managed-bridge secret/path witnesses are untouched and still fail as designed. Committed old-shape fixture `Sources/ContinuumRevivedCoreChecks/Fixtures/activity-log-pre-p2a8.json` (event + byTile snapshot + version-1 file), READ OFF DISK so it cannot be reshaped along with the code that reads it, plus an assertion that it really names no `agentId`. New real-path check with `agentId != tileId` AND a headless agent through the production sender/receiver (every other fixture in that file binds agent == tile deliberately, so this is the one that could catch a path still keying by tile): snapshot keyed by agent, hint survives the wire, both agents project rows, `agentStatusesByTileId == {boundTile: working}` only, headless approval respondable by agent id. 6 NEGATIVE TESTS OBSERVED RED: event legacy decode removed (keyNotFound agentId on the committed fixture); `rows(from:)` dropping tileless agents; the hint taken from arrival order instead of canonical order ("detaching clears the hint" red); `ApprovalResponseRequest`'s legacy decode removed; the byTile hint injection removed ("keeps its legacy key as the tile hint (measured nil)"); `keyedByTileHint` keyed by agent instead of tile. CODEX (gpt-5.5): 5 findings, 4 FIXED pre-commit (legacy snapshot losing the hint; inline fixture -> committed file; agent == tile fixtures masking a half-migration -> the distinct-id real path; the tie-break rule duplicated 3x -> `keyedByTileHint`). The 5th is REAL and is the NEXT ticket, reasoned away in the commit body: `currentManagedAgentActivities()` still enumerates managed-session records filtered by LIVE CANVAS TILES, so a headless agent is representable but not yet published — that enumeration change is `P2B.1-agent-inventory`, which depends on this ticket and could not have been built on a tile key. Matrix green at exit 0 including the iOS xcodebuild leg; inventory unchanged at 265 (no new flag, no new leg); no baseline needed blessing. Two unrelated flakes seen while iterating and NOT in the final run: the `ssh-wrapped LaunchProfile` tmux check (`script: tcgetattr/ioctl`) failed twice on direct CoreChecks invocations. Owner: headless agents show in NO inbox until P2B.1 lands — this ticket is what unblocks it; and a phone on an OLDER build reading a new desktop's payload is not covered (the packet's mechanism is decode-side only; dual-emitting both keys would be the alternative). |

| P2B.1-agent-inventory | done | 8e23b94 | 2026-07-26T10:05:00Z | `AgentInventory.snapshot(terminalDescriptors:liveStatuses:agents:activityByAgent:replicaId:now:)` in Core is now THE union of terminal sessions and agents; `DegradedDesktopActivitySnapshotSource` is a thin caller that projects its `DesktopManagedAgentActivity` transport shape onto the `AgentRecord` shape the fold reads (kind label -> `displayName`, so the synthesised summary is the same "Managed agent idle" string it always published). Folds with the EXISTING `apply(_:_:)` — no second definition of canonical order. Headless agents need no special case: the tile is only the optional hint, never the key. `liveStatuses` is documented as keyed by AGGREGATE identity (a terminal's tile id IS its agent id), so the one keyspace has no ambiguity. BEYOND THE PACKET, from codex's best catch: recorded drafts are REBOUND to the record's current `tileId` instead of inheriting the one they were recorded with — otherwise a detached agent keeps advertising a tile and the phone's "Show on canvas" points at nothing, the exact identity-is-the-view bug P2A.6/P2A.8 removed. 6 NEGATIVE TESTS OBSERVED RED with the final code, quoted at each check: skip tileless records (`fold into 4 inventory entries — got 3`); inherit the stale hint (`got Optional(...A003)`); drop both sorts (`independent of input array order`); agent counter +10 (`contiguous 1…5 — got [1, 2, 13, 14, 15]`); `runId: record.cwd` (taint scanner, hostLocalPath) and `runId: record.worktreeBranch` (substring sweep only — a branch is not a path prefix, which is why both assertions exist). Positional sequence numbers land in the scanner's pid window, excluded NARROWLY by key path AND pattern per ruling C-20260701-008 (counts/indices are not taint). The determinism check canonicalises the whole payload re-keyed by UUID string, because Swift encodes a non-String-keyed dictionary as an unkeyed array in iteration order that `.sortedKeys` cannot reach — comparing raw snapshot bytes would have been a flake, not a proof. `DesktopCompanionSyncPublisherTests` deliberately untouched and green = the regression witness for the promotion (field-level: keys, statuses, summaries, hints, timeline order, I5 sweep; NOT a byte-diff against an implementation that no longer exists — the code says so rather than overclaiming). Matrix green at exit 0 incl. the iOS leg; inventory blessed in the same commit (`count ContinuumRevivedCoreChecks` 23 -> 24, no new flag/leg, 265 records); no baseline needed blessing. CODEX (gpt-5.5): 8 findings — 3 fixed (stale hint; overstated byte-identical claim; the byte comparison now covering the whole published payload), 1 accepted in the commit body (the adapter fabricating an `AgentRecord` with empty model/thinking/cwd is a documented projection of a transport shape with no record behind it), and 3 are ONE real gap that is the NEXT ticket: `currentManagedAgentActivities()` still enumerates managed sessions filtered by LIVE CANVAS TILES, so the APP does not yet feed headless agents to this fold — the packet's done-criterion is that the function accepts them, and the wiring is `P2B.2`/`P2B.4` by the queue's own ordering. Owner: headless agents still reach no consumer until that wiring lands. Two unrelated flakes while iterating, not in the final run: the ssh-wrapped tmux check fails when the checks binary inherits a consumed stdin (`< /dev/null` fixes it — worth knowing for future negative-test loops), and `--browser-inspector-network-lite-check` failed once on a real fetch. |
| P2B.2-cross-project-walk | done | 53fcc5d | 2026-07-26T11:10:00Z | `CrossProjectManagedSessionWalk` in `AgentInventory.swift` walks `registry.projects` roots and reads each project's managed-session directory through the SAME `ManagedAgentSessionStore` the active project uses (no second decode path; no `ZoneRuntimeController` opened per project — observer-independence, P2B.8). A root that no longer exists is SKIPPED, not thrown. Ordered by project identity then tile id, NOT registry order (the fold downstream numbers sequences positionally, so a registry-ordered walk would renumber unrelated agents' events between two publishes of identical state); deduped by tile id, so two registry entries pointing at ONE path list an agent once. Cached 2s (the packet's "cache per wake"), invalidated by a changed root set AND by a backwards clock jump so a cache entry stamped in the future cannot outlive a sleep/wake or NTP step. `currentManagedAgentActivities()` now unions the active project's live store (first — it is the writer, so never staler than disk) with the walk; the canvas-tile filter is GONE, not relaxed, because it was exactly what hid other projects' agents. NEW REAL-PATH LEG `--cross-project-agents-check`, written as a same-file `AppDelegate` extension so it can reach the private function and `registryStore` without widening either: three registry projects, two holding a record, one whose root does not exist, and NO canvas and NO workspace runtime — that absence is what makes it discriminating, since the previous implementation opened with `guard let canvasView else { return [] }`. 5 NEGATIVE TESTS OBSERVED RED with the final code: walk removed from the app path (`got 0`); root sort dropped (`the walk's order is independent of registry order`); dedup dropped (`got 2`); cache write dropped (`a read inside the TTL is served from cache — got 3`); backwards-clock guard dropped (`stamped in the future — got 3`). The first cache/clock assertion I wrote was NOT discriminating (the cached root set differed, so it re-read anyway and passed without the guard) — it was reordered and given a fresh on-disk record until dropping the guard actually went red. CODEX (gpt-5.5): 4 findings. 1 FIXED — the comment claimed the old tile filter carried no other weight because closing a tile deletes its record, true for the legacy record but false as stated for the supervised `AgentRecord`, which deliberately outlives its tile. Its HIGH finding is REAL and deliberately out of scope, reasoned away in the commit body: `AgentStore`-backed agents (headless, or detached after a tile close) still reach no consumer, because this function has only ever published legacy records — feeding them is P2B.4's one-snapshot wiring by the queue's own ordering, and a half-version here would be the fifth independent derivation P2B.4 exists to delete. Its other two are that same gap from the check side. Matrix green at exit 0 on two runs incl. the iOS leg; inventory blessed in the same commit (+1 flag, +1 leg, 265 -> 267), a floor increase. One run hit the pre-existing `network-lite log did not capture a committed or finished event` flake (WKWebView navigation timing, already recorded under P1.5/P1.9/P2A.1/P2B.1), green on the re-run. Owner: cross-project discovery covers LEGACY managed-session records only — a headless agent created since P2A.6 still shows in no inbox until P2B.4. |
| P2B.3-row-context-join | done | ea69e59 | 2026-07-26T12:30:00Z | `AgentContextIndex` (Core/Agents) maps agent identity -> (workspaceName?, zoneName?, projectName?, tileTitle?, agentKind, model?, role?); `AgentsBoardRow.context` optional + `rows(from:context:)` joins it. Placement is NOT re-derived — the index CALLS `SidebarTreeBuilder.build` and walks the tree, so the sidebar and the inbox cannot disagree about where an agent lives; the agreement check is therefore a REGRESSION NET, and the independent assertion is that dropping `projectCanvases` loses the tile that is only reachable by centre-in-zone membership. Modelling decisions the packet left open: `model` is OPTIONAL (a terminal session has none — `AgentDescriptor` records a kind and a status, never a model), an `AgentRecord`-backed agent is `.managed` by construction, the RECORD's project beats the project zone its tile sits in (a headless agent has only the former), and the builder's "" fallback for an unresolvable project zone becomes nil ("" renders as a stray separator in "project · title · model"). 9 negative tests observed red at exit 1 with the final code: cwd-as-project-fallback (twice — orphan assertion, and the I5 taint sweep naming `hostLocalPath` when run alone), no ""->nil normalisation, `projectCanvases` dropped, record placement never resolved, terminal kind ignoring its descriptor, title from the descriptor not the tile row, join keyed on the tile (the headless row loses context), zone project preferred over the record's. IMPORTANT: the cwd-fallback negative did NOT fire on my first fixture — every agent named a live project, so the fallback was unreachable — fixed by adding `contextOrphanAgentId`, an agent whose project has left the registry. Matrix green at exit 0 including the iOS leg; inventory blessed in the same commit (`count ContinuumRevivedCoreChecks` 24 -> 25). One flake seen and NOT worked around: `--browser-inspector-network-lite-check` failed once ("network-lite log did not capture a committed or finished event after start") then passed twice standalone and on two later full runs — a WKWebView timing leg, untouched by this diff. Codex: 4 items — project precedence (medium) is now pinned as property 8; 3 reasoned away in the commit body. OWNER: production rows still carry nil context by design — this packet's `## Files` names no app code and `P2B.4-feed-all-consumers` is the wiring ticket. Codex also found a real CALLER gap for P2B.4: `ContinuumApp.currentManagedAgentActivities()` builds `AgentRecord`s from legacy cross-project managed sessions with `projectId` nil and `model` "", so those agents will resolve no project and no model until P2B.4 threads `CrossProjectManagedSessionWalk.Discovered.projectId` through. |
| P2B.4-feed-all-consumers | done | f36b768 | 2026-07-26T13:40:00Z | One `ActivityLogSnapshot` on the delegate (`agentActivity`), rebuilt by `rebuildAgentActivitySnapshot()` from `AgentInventory` — sidebar tree, canvas badge + zone rollup, dock attention count and companion payload all read it. DELETED, not layered over: `workspaceSidebarAgentStatuses` (the sidebar's own per-project walk + observer overlay), `currentObservedAgentStatusesByTileId` (the badge path's own read), the companion closure's own fold, and `currentAgentTileIds`'s status derivation (it keeps only the z-order nav-cycling needs). `refreshAgentAttentionSurface` is gone too — every caller goes through `refreshAgentSurfaces`, which rebuilds ONCE and pushes all four, so the dock has no push path of its own. FIRST TIME FED: supervised `AgentRecord` agents (headless P2A.6 / detached P2A.5) reached NO surface before this — `currentManagedAgentActivities()` only ever listed legacy per-project files; appended never overriding, and projected onto `DesktopManagedAgentActivity` rather than passed as an `AgentRecord` (host-bound, one step from the boundary — I5). Also: another project's raised hand now moves the dock badge (it counted active-canvas tiles), and a managed agent's runner events refresh the DESKTOP, debounced 0.5s like the publish beside them because that path runs per streamed token. TWO MEASURED FACTS the design turns on, both recorded in the code: (1) `ProjectStore.listSessions()` returns every descriptor through `restoredForBoot()`, which FORCES `AgentDescriptor.status` to `.stale` — a persisted terminal status is never the live one, so the observer map is an input and the check flips status through `applyObserverStatuses`, the production entry point (this cost the first fixture, which asserted `.working` off disk and got `.stale`); (2) a terminal session with no `AgentDescriptor` is a plain shell — it stays in the snapshot (the phone has always listed one as "Shell idle") but is NOT a desktop agent, or every shell tile rolls up as "1 unknown" in the sidebar; carried as `agentIdentities` inside the same value so the two cannot drift. NEW LEG `--agent-inventory-wiring-check`: 5 agents / 2 projects / 1 managed / 1 headless / 1 shell, no workspace runtime and no `ZoneRuntimeController` (observer independence, P2B.8). 8 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, quoted at the check: sidebar filtered to installed tile views (`agent …E2 is working in the companion payload but nil in the sidebar` — the packet's own witness), dock back on `currentAgentTileIds` (`got nil`), supervised append dropped (`got [4 ids]`), `agentIdentities` taking every descriptor, canvas filter dropped (the deleted tile's leftover descriptor `…E9` appears), `observedAgentStatuses` dropped as an input (`got …stale`), canvas back on the raw observer map (`zone rollup … working: 0`), one `listSessions()` back in the companion closure. Matrix green at exit 0 on two runs incl. the iOS leg; inventory blessed in the same commit (+1 check, +1 leg, 267 -> 269); no baseline needed blessing. CODEX (gpt-5.5): 5 findings — 3 FIXED (dock-only push path removed; headless status limit stated at the code; the companion consumer gated by a source scan = negative test 8), 2 reasoned away IN THE CODE: the companion closure recomputes before returning rather than returning the stored value as the packet words it (returning it alone publishes `.empty` on the startup and pairing publishes, which run before any surface refresh — an empty phone inbox until something else moved), and the nav cycle likewise recomputes; both are READERS of the one derivation, and the reader/pusher invariant is stated once at `AgentActivitySurface`. OWNER: a hand raised by a HEADLESS agent is still not counted until a tile ingests it — `recordManagedActivity` is wired from the tile's ingest and `isRunning` cannot separate "working" from "waiting on an approval"; closing it needs an app-owned subscription to `agentSupervisor.events(for:)`, which P4.1 and P5.10 both land on. |

| P2B.5-drop-terminal-filters | done | f3bb9e2 | 2026-07-26T14:55:00Z | The `kind == .terminal` filter in `currentAgentTileIds` is DELETED — "an agent tile" is now a tile the one inventory (P2B.4) has an agent for, so ⌥-agent-cycle and ⌥-needs-attention reach managed-agent tiles instead of stepping over them. Terminal agents unaffected and a plain shell is still not cyclable: `agentStatusesByTileId` is keyed by `agentIdentities`, which is where "is this an agent" is decided. THE PACKET'S OTHER FILTER NO LONGER EXISTS: `workspaceSidebarAgentStatuses` was deleted by P2B.4 (f36b768) when it collapsed the four derivations into one snapshot, and `SidebarTreeBuilder` has been kind-agnostic since — so the sidebar half was a VERIFICATION debt, not an implementation one, and is paid as such (it had NO coverage, which is exactly how the described defect could return). `--workspace-sidebar-live-status-check` gains a managed-agent tile (`kind: .managedAgent`, no `TerminalSessionDescriptor`, status in the per-project `ManagedAgentSessionStore`): its row must read "working"; "no agent" is swept for across every agent-bearing tile (the packet's own regression witness) with a vacuity guard that a tile with NO agent still reads "no agent"; its zone rollup moved 1 -> 2 working, so the managed agent reaches the rollup too. 3 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, quoted verbatim at the assertions: the pre-P2B.4 sidebar filter restored in `SidebarTreeBuilder` (-> `a managed-agent tile must show its record's status in the sidebar — got 'no agent'`, and with that assertion stood down the sweep catches it too); the kind filter restored in `currentAgentTileIds` (-> `--agent-inventory-wiring-check` red, `⌥-cycling must reach a managed-agent tile … got [2B4…E1]`); the fixture's managed record deleted, which proves the row's status comes from the record and not from something incidental to the tile. No new leg, no new flag — P0.11 inventory unchanged at 269. Matrix green at exit 0 on two runs including the iOS leg. Codex (gpt-5.5): 3 items, no correctness bugs. 1 FIXED — the filtered cycle was proven only for terminal tiles (the managed agent is `working`, the assertion was on `.needsAttention`), so `currentAgentTileIds(status: .working) == [managedAgentTile]` now proves the filter selects a managed agent by its status. 2 reasoned away in the commit body: the live `ManagedAgentTileNSView` precedence path is P2B.4's and untouched here (the fixture omits the view for P2B.4's own recorded reason — a fresh view reports `.configuring` and would make the fixture describe the view instead of the store), and witness comments not being reviewer-verifiable from the diff is this program's convention throughout. OWNER: the packet's noted collapse stands — `SidebarAgentStatusKind` folds `configuring`/`idle` into `.unknown`, so a managed agent that is starting up renders unknown-grey; that is P1.8/P3.2, not here. |
| P2B.6-stop-badge-clobbering | done | 602ab88 | 2026-07-26T15:45:00Z | `applyAgentStatusesToCanvas` no longer writes `nil` over a tile the inventory has no entry for — one `guard let status = statuses[tile.id] else { continue }`. Absence of an entry is not an entry saying "no agent"; a managed tile that computed its own status from its own ingest keeps it. The packet's trap is held in the same guard: an entry that EXISTS and maps to no badge (`idle`/`configuring`) still clears, so a terminal that genuinely went idle can go dark. NOTE — the line the packet names (~:4545, `applyObserverStatuses` looping all tiles) no longer exists: P2B.4 (f36b768) moved that loop into `applyAgentStatusesToCanvas` and made it read the one snapshot, so the observer map is no longer the source. The unconditional `nil` assignment survived that move intact, which is the bug this ticket actually fixes. New leg `--observer-sweep-badge-check`: a managed tile with a live `ManagedAgentTileNSView` and NOTHING on disk raises its hand via `ingest(.requestOpened)`, then two sweeps with no entry for it leave the badge at `needsAttention` (vacuity guard: the inventory really has no entry); the terminal agent the sweeps DO have an entry for is still driven, and still clears when that entry says idle. 2 NEGATIVE TESTS OBSERVED RED at exit 1: guard reverted to the unconditional assignment -> `an observer sweep with no entry for a managed tile must not erase its badge — got nil`; guard widened to also skip an entry whose badge is nil -> `a terminal agent that went idle must still be able to clear its badge — got Optional(...working)`. ONE EXISTING ASSERTION REVERSED IN PLACE, not deleted: `--agent-status-check` read "observer path should clear a plain tile absent from the status snapshot" — exactly the behaviour this ticket removes. It now asserts the shell tile's `.stale` badge SURVIVES, plus a NEW vacuity guard that the tile is absent from the snapshot, so that block's assertion count goes UP. P0.11 inventory regenerated: pure growth 269 -> 271, no removals. Matrix green at exit 0 on three runs including the iOS leg. Codex (gpt-5.5): no correctness bugs. 1 FIXED — it proved `wireManagedAgentTile` always resolves or spawns a supervised agent before the tile subscribes, so my "between spawn and record write" framing was wrong; the doc comment now states the honest limit (the check proves the RULE; the production shape that reaches it is the one `--agent-status-check` witnesses). 1 reasoned away (negative-test runs aren't reviewer-verifiable from a read-only diff — this program's convention throughout). OWNER: accepted trade, ruled by the packet — a tile that WAS an agent and stops being one (session descriptor deleted) now keeps its last badge instead of being cleared by the next sweep. |
| P2B.7-incremental-refresh | done | e9b0517 | 2026-07-26T17:40:00Z | An agent event folds into the held snapshot via new `AgentsBoardProjection.appendLocal` (stamped `snapshotSequence + 1`, so the fold's canonically-last derivation actually takes it) instead of scheduling a full disk rebuild; `refreshAgentSurfaces` splits into rebuild + `pushAgentSurfaces`, and the full recomputation is now reserved for structural change — every existing caller already was one (workspace switch, project open, tile add/delete, observer sweep, activation), so none moved. `AgentsBoardChangeSet` (added/updated/removed by agent id) is published on each push, diffed against `agentActivityChangeBase` = the snapshot as the surfaces last SAW it, so a push that coalesced several events reports all of them. It compares ROW fields, not `AgentActivity` equality: `AgentInventory` numbers events positionally, so creating one agent shifts every later-sorting agent's sequences and an equality diff would report the whole board — the reload being removed. `WorkspaceSidebarView` untouched per the packet (diffing consumer is P3.6). New `--agent-incremental-refresh-check` MEASURES "no disk read" rather than inspecting for it: the entire fixture is deleted from disk before the event is fed and the bystander terminal agent must survive byte-for-byte, with the vacuity guard at the end — the structural path is then called and that same agent vanishes. 8 negative tests observed red (ingest dropped → `got Optional(idle)`; pre-ticket full rebuild in its place → `the terminal agent is nil`; `appendLocal` at `sequence: 1` → app + core red; push diffing against the current snapshot → `every agent as ADDED — got 0`; `agentIdentities` insert dropped; change base advanced on rebuild instead of push; `changeSet` on `AgentActivity` equality → `renumbering ... got 4 touched`; ring witness weakened → `evicts the oldest at the cap ... got 0`). Codex: 4 items, all addressed, 2 changed code — (a) the 200-event ring cap, where an event evicts the oldest so a count-only witness goes blind: witness is now count + newest kind + OLDEST timestamp, all renumbering-invariant, with a core check sitting at exactly 200; (b) the reader-rebuild-between-pushes case, which surfaced a real fact — `AgentDescriptor.restoredForBoot` stamps `statusUpdatedAt = now`, so a persisted TERMINAL agent's `updatedAt` moves on EVERY `listSessions()`, hence a structural rebuild always reports those rows updated. That measured behaviour is now pinned, along with a supervised agent staying stable across a plain re-read. Its other 2 items (a comment overclaiming what a later rebuild reproduces; `changeSet` not reusing a typed row surface) are answered in the commit body. Matrix green at exit 0; inventory blessed in the same commit (+1 check, +1 leg, 273 records, no drops). Owner: (1) Phase 3's diffing list will re-render terminal rows on every structural rebuild until that boot-restore stamp is fixed at its source — not this packet's file; (2) what is proven disk-free is the SNAPSHOT update, not the push, which still reloads the sidebar and builds its tree from disk by design. Noted, unrelated: `ContinuumRevivedCoreChecks`'s ssh-wrapped tmux check flaked twice mid-run (`tcgetattr/ioctl: Operation not supported on socket`) and was green in every full matrix run |
| P2B.8-observer-independence | done | d23a48d | 2026-07-26T19:20:00Z | `AgentObservation.unobservedAgentIds(in:observedAgentIds:)` in Core is the one definition of "nothing live is reporting this agent" (pure — no disk, no controller, no store); `AgentActivitySurface.unobservedAgentIds` is written by `rebuildAgentActivitySnapshot` and carried beside `agentIdentities`, NOT pushed into `AgentActivity` (that crosses to the phone; this is a fact about THIS host). `currentManagedAgentActivities()` now returns a `ManagedAgentActivityScan` = activities + which of them something live reported, under one rule: the status came from a live source rather than a file. An agent's own event clears the mark in `ingestAgentActivityIncrementally` with no rebuild. THE LOAD-BEARING DETAIL: a terminal agent's observation is `observedAgentStatuses` ALONE, not `liveStatuses` — `canvasView?.agentStatus(for:)` reads back the badge the app itself wrote, and since P2B.6 made badges sticky, counting it would have the app confirm its own echo (an agent observed forever after one sweep). A managed tile's `currentAgentStatus` is derived from the agent's own event stream, so it does count. DELIBERATE DEVIATION from the packet's `## Approach`: NO time threshold on the flag. `restoredForBoot()` re-stamps `statusUpdatedAt = now` on every `listSessions()`, so a terminal agent's information always measures seconds old — a threshold would exempt every terminal agent forever and fail the packet's own `## Verify`. Measured in the gate: -0.0s (terminal) vs 3600s (managed) on the same hour-old fixture. The age is not lost, it is on `AgentActivity.updatedAt`, so a renderer can hold "unobserved AND older than X" itself; the policy belongs where it is rendered (Phase 3/9), which is also why no Settings entry was added here. New leg `--agent-observer-independence-check`: no workspace runtime, no controller, no canvas → 4 agents (terminal, managed active-project, managed OTHER project, headless supervised) listed from disk with persisted statuses and all 4 marked; then a live tile view, a runtime observer status and an agent's own event each override the file and clear the mark; the released project's agent stays listed and marked throughout. 5 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, quoted at the check: flag dropped from the surface (`got 0 marked of 4 agents`); the packet's literal 900s aging (`got 3 marked of 4`, terminal exempted); managed records observed unconditionally (`got 2 marked of 4` + `--cross-project-agents-check` red at `got 2 observed`); incremental clear dropped (`got 3 marked`); canvas-badge echo counted as observation (`an agent whose observer went away must go BACK to unobserved — got 2 marked`). Matrix green at exit 0 on four runs including the iOS leg; P0.11 inventory blessed in the same commit, pure growth 273 → 275, no removals. Codex (gpt-5.5) TWO PASSES: pass 1 found the threshold/`Verify` contradiction and the observer-teardown case, both of which CHANGED CODE (threshold removed; section 4 witnesses teardown, which is how the canvas-echo bug was found); pass 2 judged the deviation justified and raised 3 more — the terminal `.stale`-after-`restoredForBoot` fact is now PINNED as an assertion instead of prose, `SessionObserver`'s map being SPARSE is documented at the rebuild (a watched tile with no derived status yet reads unobserved — honest under this definition; separating "an observer exists" from "an observer has reported" needs a set published out of `ZoneRuntimeController`, not this packet's file), and the teardown source-scan's limit is stated where it is made (it proves `close()` publishes `[:]`, not that the GUI wiring delivers it). OWNER: (1) every agent outside the workspace you are looking at is now marked — true, but that is most agents, so Phase 3 should apply its own grace period off `updatedAt`; (2) `pushAgentSurfaces`'s change-set is computed from the snapshot only, so a rebuild where ONLY observation state moved reports nothing — P3.6's diffing list must read `unobservedAgentIds` directly. |

| P2C.1-worktree-manager | done | 3a096af | 2026-07-26T20:15:00Z | `WorktreeManager` in Core is the first thing in the program that actually runs `git worktree`: `slug(role:prompt:id:)` (pure, `[a-z0-9-]`, runs collapsed, body <=32, id-suffixed) + `add` -> `<repo>/.worktrees/<slug>` on a NEW branch `agent/<slug>` + `list` (porcelain) + `remove(force:)`. `GitDiffEngine`'s process pattern reused verbatim in shape, and every `Process` behind `#if os(macOS)` because Core builds for iOS (P0.1's leg goes red at exit 65 otherwise). `.worktrees/` gitignored, as the packet asks. TWO DEVIATIONS, both forced by measurement, both argued in the commit body: (1) the id suffix folds all 16 uuid bytes via FNV-1a, not `uuidString.prefix(8)` — the prefix version was OBSERVED RED because every fixture id here is `A0000000-0000-4000-8000-...`, so two agents collided on `implementer-fix-auth-a0000000`, the exact collision the suffix exists to prevent; FNV because Swift's `Hasher` is per-process seeded and a slug must be stable across launches; (2) `Worktree` carries `branch: String?` + `isMain` rather than the sketched `(path, branch)` pair, so a detached HEAD and the main checkout are reported honestly instead of faked or dropped. An existing branch is refused (`.branchExists`) before anything touches the disk — git also refuses, but only after a half-prepared worktree and a raw 255 indistinguishable from any other git failure. `remove` keeps the branch on purpose (it holds the agent's work; deletion is P2C.3's call). 8 properties against a fresh temp `git init -b main` repo per check (the real repo is never touched); 3 negative tests observed red at exit 1 with the final code and quoted verbatim at each check: `sanitize` passthrough -> `slug "fix auth/login (urgent)!-99b77365" contains a character outside [a-z0-9-]`; `add` without `-b` -> `branch agent/implementer-fix-auth-e4bc66a4 does not exist after add`; the branch guard disabled -> git's raw 255 instead of `.branchExists`. Matrix green at exit 0 incl. the iOS leg; inventory blessed in the same commit (`count ContinuumRevivedCoreChecks 25 -> 26`, 275 records). Codex: 0 correctness bugs, 0 missed reuse; its 1 `guessing` item is deviation (1) above, kept and argued; its 1 verification gap was its own read-only sandbox refusing to build. Owner: nothing CALLS this yet — wiring it into spawn is P2C.2. |
| P2C.2-isolated-spawn | done | a60e53f | 2026-07-26T21:05:00Z | `AgentSupervisor.spawn(..., isolated:)` is the first caller of P2C.1's `WorktreeManager`: `add` against the project root, record `cwd` = `<repo>/.worktrees/<slug>`, `worktreeBranch` = `agent/<slug>`, and `piRunner(for:)` (which reads `record.cwd`) therefore starts Pi in the worktree. SHAPE DECISION: a separate THROWING OVERLOAD, not a defaulted `isolated:` parameter — only isolation can fail, and folding it in would make all twelve existing callers write `try` for an error their call can never raise; both overloads funnel into one private `makeAgent`, so there is no second spawn path, and the AgentID is minted BEFORE the worktree because the slug is derived from it. NO FALLBACK is the load-bearing rule: `add` failing fails the spawn, and nothing is created before it succeeds, so a failed isolated spawn leaves zero records in memory AND on disk (both asserted). Verified in `--agent-supervisor-check` MARK 10 against a TEMP `git init` repo per run (the real repository is never a `worktree add` target — P2C.1's trap, inherited). Four properties: cwd under `.worktrees/` with a matching `agent/<slug>` branch and `git worktree list` AGREEING; the runner's directory checked with `git rev-parse --abbrev-ref HEAD` INSIDE it, so "works on its own branch" is a git fact rather than a comparison against the same constant the code built; a non-isolated spawn unchanged (project root, nil branch, no new worktree); and TWO REAL `add` failures — a cwd that is not a repository (asserted as `.invalidRepository` specifically) and `.worktrees` occupied by a FILE (which fails PAST the repository check, where a fallback is most tempting) — each throwing out of `spawn`. No fake manager anywhere: the failures are real, so the manager's own error path is covered too. Note the second failure surfaces as Foundation's `NSFileWriteFileExistsError`, not a `WorktreeError`; the check accepts any error rather than re-typing it in P2C.1's manager, which this ticket does not own. 4 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, each quoted at its assertion: `isolated` ignored (`cwd ... is not inside .../.worktrees/`); `worktreeBranch` left nil; the `add` failure swallowed with `try?` (`the agent landed in the main checkout ...`); and `piRunner(for:)` built with a fixed cwd. CODEX (gpt-5.5): 0 correctness bugs, 0 guessing, 0 missed reuse, 1 verification gap — the recorder witnesses what the SUPERVISOR hands a runner, not what the PRODUCTION `PiAgentRunner.Config.cwd` would be — FIXED by making `PiAgentRunner.config` `public let` (was private) and asserting the production factory's cwd, with its own negative test. Matrix green at exit 0, 141 legs, on three full runs (two before the fix, one after). No new leg, no inventory change, no baseline blessed. Owner: nothing CALLS the isolated overload yet — cleanup is P2C.3, and exposing `isolated` on a spawn surface is deliberately NOT here (the packet's watch-out: it is a UX decision). |
| P2C.3-worktree-cleanup | done | be1e783 | 2026-07-26T22:40:00Z | `AgentSupervisor.archive(_:)` is the cleanup the archive/delete action performs: stop the runner, delete the record, remove the checkout, delete the branch ONLY if the repository already has everything on it. `WorktreeManager` gains `isMerged` / `deleteBranch` / `prune` / `orphans(repo:knownAgents:)` / `repair`. NEVER `git branch -D`, and TWO INDEPENDENT GUARDS say so — `archive` asks `isMerged`, and `deleteBranch` is `git branch -d`, which git refuses on unmerged work anyway; recorded honestly in the code: dropping the `isMerged` guard is NOT red (the branch survives carrying git's message instead of this code's reason), the destructive mutation is `-d` -> `-D` and that IS red in the Core check. DIRTY-WORKTREE DECISION: no `--force`, ever — the packet permits it only once a diff is captured and that is P2C.5, so a dirty tree is RETAINED and reported; same for `repair`, which removes a clean orphan, retains a dirty one with a reason, prunes the admin record of one whose directory is already gone, and NEVER deletes a branch (the record that said who owned it is gone, so the branch is all that is left of that agent). Every archive returns an `ArchiveReport` naming what went and what was kept. TWO THINGS MEASURED, NOT ASSUMED: (1) `resolvingSymlinksInPath` leaves a path that no longer EXISTS alone, and git echoes the unresolved path it recorded at `add` time for a worktree whose directory is gone — so the prunable worktree was invisible to `orphans` (observed `repair found 2 orphans, expected 3`); `WorktreeManager.resolved` now resolves the existing PARENT and re-appends the leaf. (2) `cleanUpWorktree` checks the `.worktrees/` container before deriving the repo two components up, and the check's fixture is a REAL worktree a human created at `repo/manual/tree` — the case that would actually be destroyed; a first attempt pointed the record at the project root and was NOT discriminating, because the derived "repo" is then the temp directory, not a repository at all. Verified in `--agent-supervisor-check` MARK 11 (8 properties) + `ContinuumRevivedCoreChecks` 9-11, all against a temp `git init` repo per run (P2C.1's inherited trap: the real repository is never a `worktree add` target); the orphan half uses SUCCESSIVE supervisors over one store, so "the record is gone" is observed by something that never held it. 10 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, each quoted at its assertion: no worktree cleanup; `stop` removed from archive; container guard bypassed; empty known set (2 orphans); the store not consulted (a STALE agent read as an orphan — the P2A.7 case, where repair would prune the one thing that could bring it back); `-d` -> `-D`; raw path comparison in `orphans`; `force: true` in `repair`; warn-instead-of-throw on an unreadable store; cleanup before the durable delete. CODEX (gpt-5.5): 5 findings, 3 FIXED pre-commit — `knownAgentDirectories` warned and continued on an unreadable store (a destructive answer, not a degraded one; it now THROWS `CleanupRefusal`, and the sharper case it closes is that `AgentStore.loadAll` deliberately SKIPS an undecodable record, so the `.json` file count is asserted against the loaded count); `archive` cleaned up BEFORE confirming the durable delete (reversed — a record that comes back on relaunch pointing at a deleted checkout is the worst combination available); and an overreaching P4.1 doc comment. Its verification gap is closed by MARK 7 (a corrupt record file makes both calls refuse) and MARK 8 (a store that cannot delete cleans up nothing, witnessed with a read-only records directory — a first attempt replaced the record file with a directory, which `removeItem` deletes recursively, so the case was vacuous). Its remaining HIGH — nothing in production calls `archive` — is reasoned away in the commit body: the packet's Files name two files, neither a UI file, and no agent archive/delete action exists yet (Phase 3's row context menu / Phase 4's lifecycle own it); same precedent as P2C.2's uncalled isolated overload. Matrix green at exit 0 incl. the iOS leg; the run before it hit the pre-existing `network-lite` WKWebView flake (recorded under P1.5/P1.9/P2A.x). No new flag, no new leg, no inventory change, no baseline blessed. Owner: `archive` and `repairWorktrees` have no UI, and nothing sweeps orphans on its own — `repairWorktrees(repo:)` is a call somebody has to make. |

| P2C.5-per-agent-diff | done | 0155fa2 | 2026-07-27T03:45:00Z | `AgentDiffSource` in Core: `reviewSource` (pure) + `counts` + `diff`, all nil for a NON-isolated agent — `worktreeBranch != nil` is the isolation test, the same one `AgentRowContext.isIsolated` uses. REUSE, not re-derivation: `DiffReviewSource.branchVsBase` already emits git's THREE-DOT `base...branch`, so the merge-base rule the packet requires is inherited; `GitDiffEngine` gains `counts` + `parseNumstat` on its EXISTING `runGit`/`Source`, so there is no third `Process` implementation here. `--numstat` rather than counting the parsed model because `diff` reads every hunk line under a 1MB cap — a large agent diff would answer `outputTooLarge` where the counts survive. I5: `Counts` is three integers and NO PATHS (the packet's watch-out); the payload names branches only. Verified in `ContinuumRevivedCoreChecks` against a TEMP `git init` repo per check (P2C.1's inherited trap), reusing `WorktreeManagerChecks`' `makeWorktreeTempRepo`/`runTempGit` (both lost `private`) instead of copying them. FIVE PROPERTIES: accurate counts over real commits in a real worktree (2 files, +4/-1); merge base WITH A DISCRIMINATOR — a commit landing on the base branch after the fork does not move the numbers AND the two-dot range over the same pair IS different (3 files/4 deletions), so the property cannot pass by the base having stayed still; non-isolated nil from both entry points against a cwd that does not exist, so a stray git call would throw rather than return nil; numstat parsing incl. a binary row (`-`/`-`) counted as a changed file; and a fork from `release/foo`. 5 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, each quoted verbatim at its check: two-dot range (`a commit on the base branch after the fork was attributed to the agent — filesChanged 3, expected 2`); binary rows dropped (`got 2 file(s), expected 3`); non-isolated falling back to `.workingTreeVsHEAD`; `counts` ignoring the branch range (`filesChanged — got 0, expected 2`); `baseBranch` hardcoded to "main" (`an agent forked from release/foo must report ITS OWN work against release/foo`). MEASURED: `git add -A` in the MAIN checkout picked up `<repo>/.worktrees/<slug>` as a GITLINK (discriminator read 4 files/5 deletions instead of 3/4) — the real repository gitignores that directory, a bare `git init` does not; the fixture commits named paths only. CODEX (gpt-5.5): 2 findings, one gap from two angles — NOTHING RECORDS THE BRANCH AN AGENT FORKED FROM, so `baseBranch` is caller-supplied. Verification half FIXED (property 5). API half deliberate and NOT closed here: refusing to guess belongs in this file, but recording a fork point means a field on `AgentRecord` (P2A.1's schema) written by the spawn path (P2C.2) and this packet's `## Files` name neither. Matrix green at exit 0 with the final code incl. the iOS leg; two intervening runs were red ONLY on the pre-existing `network-lite` WKWebView flake (P1.5/P1.9/P2A.x/P2C.3), and that leg then passed 3/3 standalone with this code. Inventory blessed in the same commit (`count ContinuumRevivedCoreChecks 26 -> 27`, 275 records; no new flag, no new leg). No baseline blessed. OWNER: (1) a caller that names the wrong base branch gets a wrong-but-plausible number — recording the fork point at spawn is the follow-up, and it is a P2A.1/P2C.2 change; (2) nothing CALLS this yet — rendering is Phase 3, review-cost ordering is P9.4, per the packet. |

| P2D.1-spawn-agent-extension | done | a8ab0b4 | 2026-07-27T05:05:00Z | `Resources/PiExtensions/continuum-spawn-agent.ts` registers one Pi tool, `spawn_agent(prompt, role?, isolated?)`, and it is INERT by design: it returns `spawned: <role>` immediately, never reaches back into Continuum, never blocks. Continuum already translates every tool call in the stream, so the CALL is the API — creating the child is P2D.2. Written against the REAL `ExtensionAPI`, reusing the shape of the repo's own `.pi/extensions/linear/index.ts` (typebox `Type.Object`, `execute(_toolCallId, params)`) rather than the packet's sketch pseudo-schema. BEYOND THE PACKET, from codex's missed-reuse finding and confirmed in Pi's own docs: `promptSnippet` + `promptGuidelines` — Pi leaves a custom tool OUT of the system prompt's "Available tools" section entirely without a `promptSnippet`, so the packet's own done-criterion (discoverable by an agent that allows it) was only half met; each guideline bullet names `spawn_agent` because Pi appends them flat with no tool-name prefix. VERIFIED LIVE, outside the matrix as the packet requires (it needs Pi): `pi --mode json -ne -e … -nbt -t spawn_agent -p "…"` exits 0 with one `tool_execution_start`, `toolName: "spawn_agent"`, all three args intact. NEGATIVE WITNESS: the same command with `-e` DROPPED also exits 0 and contains ZERO `tool_execution_start` events, so the capture is evidence of this file loading rather than of the model inventing a tool from the prompt. Stream committed verbatim as `Sources/ContinuumRevivedCoreChecks/Fixtures/spawn-agent-tool-call.jsonl` (52 lines, Pi 0.82.0 / `openai-codex/gpt-5.6-sol`), RE-CAPTURED after the promptSnippet change so the fixture matches the shipped file; P2D.2 runs against it and never needs a live model. Recorded in the notes: the args appear TWICE — assembled over `toolcall_delta` fragments (partial JSON by construction) and whole on `tool_execution_start`; parse the latter. `P2D-orchestration-notes.md` carries the contract, manual install (`~/.pi/agent/extensions/` or project `.pi/extensions/` — NOT auto-installed from the app, per the packet), the `--tools spawn_agent` allowlist requirement, and the re-capture command. Matrix green at exit 0 incl. the iOS leg, TWICE (before and after the promptSnippet change). No new flag, no new leg, no inventory change, no baseline blessed; `swift build` gains only a fourth pre-existing "unhandled file" warning for the new fixture, matching the two already in that directory. CODEX (gpt-5.5): 0 correctness bugs; 1 missed-reuse = the promptSnippet change above; 1 "guessing" (the fixture does not encode the Pi version) answered in the notes rather than the artifact — the capture is verbatim, so a version field cannot be added without falsifying it, and nothing downstream depends on the version, only on the `tool_execution_start` shape; 2 verification gaps reasoned away in the commit body (an automated check over the fixture is P2D.2's stated job, and a repeatable load smoke test would be a matrix leg the packet explicitly places outside the matrix). OWNER: nothing in Continuum reads this stream yet and the extension is installed nowhere — an orchestrator needs both a `cp` and `--tools spawn_agent` until P2D.2/P2D.3 wire it up. |

| P2D.2-detect-spawn-tool-call | done | f488a43 | 2026-07-27T07:20:00Z | `SpawnRequest` in Core (NOT `Codable`, deliberately — the translator drops tool args for I5 and a value that cannot be serialized at all is the strongest statement of that) + `PiEventTranslator.onSpawnRequest`, a LOCAL-ONLY side channel fired from `tool_execution_start` (which carries the WHOLE args object; the `toolcall_delta` fragments are partial JSON by construction, per P2D.1's recorded note) + `PiAgentRunner.observeSpawnRequests` (queue-confined, same confinement as the translator) + `AgentSupervisor.handleSpawnRequest`. The event stream is BYTE-IDENTICAL with and without an observer — the `.itemStarted` still carries only the tool NAME — which is asserted, not argued. The child inherits the parent's model/thinking/project (the request names a task, not a model) and `spawn`/`makeAgent` gained the `parentAgentID` `AgentRecord` already had a field for since P2A.1. `repositoryRoot(of:)` gives a child of an ISOLATED parent a SIBLING worktree instead of `.worktrees/<parent>/.worktrees/<child>` (P2C.3's cleanup keeps its own container guard because it DELETES; this one only chooses where to add). CAPS, per the packet's watch-out: `maxSpawnDepth = 2`, `maxChildrenPerParent = 4`; a refusal is SAID on the requesting agent's stream as a failed tool item whose reason carries counts and caps ONLY — never the prompt, never a path — because that transcript is what the bridge publishes from. VERIFIED in two places against ONE artifact (P2D.1's committed capture, read from disk by both, never copied): new `runSpawnRequestChecks` parses 1 request out of the real 52 lines, proves prompt/role/capture-cwd appear in no `AgentRuntimeEvent` and in no PUBLISHED `AgentActivityEvent` (stamped through `AgentActivityEvent(stamping:)` — a draft is not Codable, so encoding the draft would witness a shape nothing sends) while `tool.spawn_agent` still crosses, ignores a `read` call with spawn-shaped args, refuses 6 malformed arg shapes, and scans 305 source files for a retroactive Codable conformance; and `--agent-supervisor-check` MARK 13 drives the PRODUCTION path end to end (fixture line → real translator → runner seam → supervisor) against a temp `git init` repo: one child, role/prompt as sent, the parent link in the STORE, the child's OWN runner actually running that prompt (so 'a child was created' is not satisfied by an inert record), a real sibling worktree git agrees about, both caps, and the refusal in the transcript. 12 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, each quoted at its assertion: the `observeSpawnRequests` wiring deleted; `parentAgentID: nil`; `prompt: nil` on the child spawn; the depth guard removed; the child-cap guard removed; `refuseSpawn` warning without delivering; `repositoryRoot` always returning `cwd`; the prompt leaked into the item title, and separately into the `itemId` (the second one is what reaches the I5 assertion — a title leak trips the exact-tool-name guard first); the `toolName ==` whitelist dropped; `prompt` defaulted to ""; the translator ALSO reading the `toolcall_end` fragments (2 children); and a retroactive `extension SpawnRequest: Codable`. TWO OF THOSE FOUND HOLES IN MY OWN ASSERTIONS rather than confirming them, both recorded at the assertion: `hasPrefix(container)` accepted the nested `.worktrees/<parent>/.worktrees/<child>` the sibling rule exists to prevent (now exact parent-directory equality), and the Codable scan matched this checks file's own COMMENT about the forbidden extension (now anchored at line start). CODEX (gpt-5.5): 0 correctness bugs, 0 guessing, 0 missed reuse; its single verification gap — the Codable scan read only the declaration line, so a retroactive extension anywhere else would pass — is FIXED by the tree-wide scan, with its own negative test. Matrix green at exit 0 TWICE incl. the iOS xcodebuild leg (48/48 baselines at 0.0000%, 474 contrast pairs); two intervening runs were red ONLY on the pre-existing `--browser-inspector-network-lite-check` WKWebView flake (P1.5/P1.9/P2A.x/P2C.3/P2C.5), which then passed 6/6 standalone, 3 of those with the matrix's own env. Inventory blessed in the same commit (`count ContinuumRevivedCoreChecks 27 -> 28`); no new flag, no new leg, no baseline blessed. PROCESS NOTE FOR THE OWNER: my first negative-test harness reverted mutations with `git checkout -- <file>`, which wiped ~500 lines of UNCOMMITTED implementation in `AgentSupervisor.swift`; it was rewritten from context and re-verified green before any negative test ran again, and the harness now reverts from `cp` backups. Never `git checkout` an uncommitted worker file. OWNER: (1) the caps are named constants, not Settings — the packet asked for caps, not configuration, so user-tunable fan-out is a follow-up; (2) nothing renders the parent/child link yet (P2D.4 nests rows, P2D.3 gives roles a registry); (3) the extension is still installed nowhere — an orchestrator needs the `cp` plus `--tools spawn_agent` from `P2D-orchestration-notes.md`. |

| P3.12-row-context-menu | done | aadde14 | 2026-07-26T22:40:00Z | `InboxRowAction` (10 actions) + `NSMenu` on the TABLE with this view as its delegate — the sidebar's first context menu. THE FIVE SHARED ACTIONS DELEGATE TO P3.11 rather than copy it: `isAvailable` calls `InboxBulkAction.<same>.isAvailable`, and the check asserts the two agree row by row over all 10 candidate rows plus title equality, so "the same capability rules as bulk" holds by construction and the two menus cannot drift into two vocabularies. Its four private predicates went `fileprivate` for that (the only edit to P3.11's code). THE OPPOSITE CALL TO P3.11 ON HIDE-VS-GREY, each following its own packet: the 320pt bar hides what a selection cannot take, a menu line greys it and puts the reason in the tooltip — which is also how the packet's watch-out is discharged (Phase 4 / P3.13 own 7 of the 10, so today they are drawn and DISABLED with 'Not available yet.', never wired to nothing; only `Open in Tile` is live, through P3.9's existing `onRevealRow` so a reveal keeps ONE definition). A capability refusal NAMES the offending agent ('claude · docs sweep is still working.'), which is the 'which of my six?' question a hidden item cannot answer. SETTLE/UN-SETTLE IS THE ONLY EITHER/OR — the packet's own spelling (slash vs '·') and the only pair whose forward action is a no-op. THE MEASURED FINDING THAT CHANGED THE CHECK: `NSMenu.autoenablesItems` defaults TRUE and re-enables every item whose target responds to its selector — all ten — but that pass runs in `NSMenu.update()` at DISPLAY time, so with only a structural assertion every enablement check stayed green against the bug. `openRowMenuForQA` now calls `update()`, which is what makes the enablement it reads the enablement AppKit would show. 10 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, quoted verbatim above `runAgentInboxChecks`: `autoenablesItems` restored to default (Open in Tile live again with its refusal still in the tooltip), `targetRows` ignoring the selection, `targetRows` ignoring whether the click was INSIDE it, `isWired` ignored, both halves of the settle pair, no re-resolution when a stale item fires, Open in Tile routed through `onRowAction`, `hasTurnInFlight` including `.ready`, the capability clause ahead of the not-wired one, and the Snooze/Wake swap. NO VISUAL GATE, and it is a limit not an omission: an `NSMenu` is drawn by the window server in its own window, so it cannot be rendered into a Lab card or held to a PNG — every fact the menu carries is asserted instead (items, order, titles, enablement, tooltips), and how it looks is a supervised manual step. Codex (gpt-5.5): 3 distinct findings — the Snooze/Wake swap FIXED with its own witness; 'Stop Agent should ask `AgentSupervisor.isRunning`' answered (the list is a pure function of its rows by P3.1/P3.6, `state` IS that projection, and a disagreement belongs in the projection where the bar, the chip and iOS are fixed with it); 'the app never wires `onRowAction`' answered as the watch-out honoured. Matrix green at exit 0 including the iOS leg, twice (before and after the review fix); inventory unchanged at 277 — no new flag, no new leg. OWNER: wiring `onRowAction` is the smallest next step and `Stop Agent` is the one item that could land before Phase 4; it was left out because an aggregate row id is a record id OR a tile id (P3.1) and what an explicit stop means for a terminal-session row is not ruled anywhere. Until then, right-clicking a row shows 9 items with 8 greyed and 'Not available yet.' on each. |
| P3.13-inline-rename | done | 7ac1f42 | 2026-07-27T00:20:00Z | Double-click a row's NAME (not the rest of the row) opens an inline `NSTextField`: Enter commits, Esc reverts, blur commits, and empty/whitespace/unchanged keeps the previous name. Persisted via new `AgentSupervisor.rename(agentID:to:)` -> `AgentRecord.displayName` -> `AgentStore`, so the name outlives the tile and comes back after a relaunch, and every surface that joins through the record (row, scope join, `AgentInventory.safeSummary` the phone reads) shows it. REUSED `--zone-rename-inline-check`'s two hard-won facts rather than rediscovering them: the `isOpeningRename` gate (`selectText(_:)` posts a synchronous end-editing during setup, which an ungated delegate reads as a blur and commits inside the opening gesture) and teardown-before-callback. ONE FACT THIS LIST ADDED: clear the state BEFORE removing the field -- removing a field holding the field editor posts `controlTextDidEndEditing` synchronously and the delegate re-entered and committed twice (`Enter commits the typed name once -- got ["Migration reviewer", "Migration reviewer"]`). THE FIELD IS A SUBVIEW OF THE LIST, NOT OF THE CELL: cells are rebuilt on every incremental apply (P2B.7), so a field in one would be torn out from under the typing by the next streamed token -- an incremental push leaves an open rename alone (and repositions it), while a push that changes the LIST is a full reload where every row moves, so the rename finishes rather than floating over another agent. `sanitizedDisplayName` makes user text a label (whitespace collapsed, capped at 60, any ABSOLUTE path reduced to its last component) because the name is published in a synced summary where `/Users/...` is the exact `hostLocalPath` shape `SyncPayloadTaintScanner` forbids -- asserted both ways (sanitised summary clean, unsanitised NOT). 12 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code: title hit-test dropped, Enter not committing, Esc committing, empty-name guard dropped, blur handler dropped, `persist(record)` dropped, path strip dropped, `tableView.doubleAction` unset, `configureInboxRename` unwired, `endRename` added to the incremental apply, the one in `render` removed, `field.delegate = self` removed. Section G of `--agent-inbox-check`; matrix green at exit 0 including the iOS leg; inventory unchanged at 277 (no new flag, no new leg); no baseline moved -- the field exists only while you type, so no Lab card renders it, which also means its two token pairs (`overlay` + `textPrimary`, `borderStrong` outline -- both documented in P1.3) are NOT covered by the P1.6 contrast gate. Codex (gpt-5.5): 8 items, 3 FIXED -- sanitizer widened from the scanner's four prefixes to any absolute path (so `/tmp/...`, `/Volumes/...` are caught, while `fix/parser` stays a name), the duplicated prefix list removed with it, and `isRenameDelegateWiredForQA` added because the Enter/Esc/blur helpers call the delegate methods directly and an unwired field would have passed them (witnessed red). Reasoned away in the commit body: the 60-char cap is a judgement the packet's "keep it short" asks for and nothing else rules on; the check routes the gesture instead of synthesising an `NSEvent` into `NSTableView.mouseDown`, which runs a modal tracking loop with no place headlessly (the limitation `selectRowsForQA` already records), so the table's own wiring is asserted via `isDoubleClickWiredForQA`. OWNER: (1) a TERMINAL-SESSION row cannot be renamed -- its aggregate id is a tile id, not a record id (P3.1), and what renaming one means is unruled, so the app refuses it on stderr rather than half-writing it; (2) the row menu's `Rename` still routes through `onRowAction`, which no host wires (P3.12's shape) -- `AgentInboxView.beginRename(agentId:)` is public for the day one does; (3) the field's colours are the first painted pair in this app with no contrast leg over them. |

States: `pending` · `in-progress` · `done` · `blocked`
| P2C.4-branch-on-rows | done | e899a55 | 2026-07-27T01:20:00Z | `AgentRowContext` gains `worktreeBranch` + `checkedOutBranch` (stored) and `isIsolated` + `isBranchMismatch` (derived); `AgentContextIndex.build` takes `checkedOutBranches` keyed by the SAME agent identity as everything else, so the fold stays PURE — the git read is the caller's. `WorktreeManager.currentBranch(repo:)` + `CheckedOutBranchCache` (TTL 2s, backwards-clock guard, and a `gitReads` counter so "does not shell out per render" is an assertion rather than a claim). `BranchChipNSView` is a thin TokenThemed renderer on the `StatusChipNSView`/`ApprovalDockView` pattern: `SurfaceToken.overlay` fill, `LineToken.border` outline, `TextToken.textSecondary` text, and `AccentToken.accentApproval` + a `⚠` GLYPH for the warning (state not carried by hue alone); `display(for:)` is the one (context -> chip) mapping, so the tile and the Lab card cannot draw two different chips. The tile header renders it, `attach` learns it from the supervisor (`branchContext(for:)`), and `ingest` re-reads it at a TURN BOUNDARY. THE ONE DEVIATION, argued not accepted: the packet says warn when the agent's branch is not "the repo's currently checked-out branch". Read as the MAIN checkout that rule is DEGENERATE — `worktree add -b` cuts a fresh branch and git REFUSES a branch already checked out in another worktree (both now asserted, property 12d), so every isolated agent would warn forever and the flag would be a second rendering of `isIsolated`, and the packet's own "isolated matching" Lab state could not exist at all. So "the repo" is the working copy the agent RUNS IN (`AgentRecord.cwd`), and the warning means "this agent has left the branch it was given" — reachable, rare, actionable. The Goal (which of five is about to touch YOUR checkout) is answered by the shared/isolated distinction instead: a shared agent's chip reads `⎇ main · shared`. 8 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, each quoted at its assertion: `attach` not applying the context (`an isolated agent's tile shows NO branch`); `isBranchMismatch` hardcoded false; the cache bypassed (`ten re-renders cost 10 extra git call(s)`); the warning naming the ASSIGNED branch instead of the one the work lands on; the shared suffix dropped; the index dropping `record.worktreeBranch` (`got nil`); the detached-HEAD guard removed (`got Optional(HEAD)`); and the turn-boundary refresh removed (`still reads ... with no warning`). REAL PATH: `--agent-supervisor-check` MARK 12 — temp `git init` repo, real isolated + shared spawns, real tiles, production `attach`, and the mismatch produced by a REAL `git checkout` observed through an ordinary ingested `.turnCompleted` (not a hand-written re-apply). New Lab card `managed-agent.branch-chip` carries the packet's three states, so the chip is covered by geometry, per-appearance contrast (474 pairs), the pixel sweep and PNG baselines in both appearances; and the `tiles.managedAgent` fixture is now an ISOLATED agent so `--ui-geometry-check` exercises a header with a chip at the 320pt tile minimum, where a header that cannot fit one would clip. I5: the branch NAME crosses, the PATH does not (P2C.4's own ruling), so property 7 now asserts the name is carried VERBATIM — the OPPOSITE of what it asserted under P2B.3 — while `cwd` still never appears; both new fields go through the taint scanner and a host path smuggled into `worktreeBranch` is witnessed as a violation, so the new field is not a blind spot. CODEX (gpt-5.5): 6 findings. 1 FIXED — the chip was applied ONCE at attach and could stay wrong for the whole session; hence the turn-boundary refresh, and the check now drives it through an ingested event. 2 are the deviation above. 2 are one gap, reasoned away: nothing in production builds an `AgentContextIndex`, so the "on rows" half reaches no row yet — the packet's Files name no row-rendering file and Phase 3 (P3.1/P3.6) owns the inbox list; same precedent as P2B.3's own uncalled index and P2C.2/P2C.3's uncalled APIs. 1 is `isIsolated` derived rather than stored, deliberate and documented. Matrix green at exit 0 on two full runs incl. the iOS leg. Floors raised TO MEASURED, never lowered: static cards gated 46 -> 48, themed views 11 -> 12, sentinelled layer colours 23 -> 25; `BranchChipNSView` added to `tokenAdoptedOwners`, so every colour it paints is gated as a DesignTokens value in both themes. 4 baselines blessed (2 changed, 2 new), both appearances opened and reviewed first. No inventory change (275). OWNER: the chip refreshes at a turn boundary, so a `git checkout` YOU run inside an agent's worktree between turns is not reflected until its next turn ends; and no inbox row shows a branch yet — Phase 3 has to pass the joined context it already has a field for. |
| P2D.3-role-registry | done | 3f02fe6 | 2026-07-27T09:25:00Z | `RoleRegistry` (Core) scans `<projectRoot>/.pi/agents/*.md` via the existing `HarnessRoleParser` and answers two questions the AppDelegate's inline scan could not be asked outside a running app: what roles does this project define, and what does role X run with. `resolve(roleId:inheriting:)` — role's own `model`/`reasoning` win, otherwise the inherited pair; an id this project does not define THROWS, and P0.10's rule holds through roles (a bare alias like `gpt-5.5` throws rather than reopening Pi's fuzzy `--model` matching; an unrecognized `reasoning` likewise, since `pi --thinking ludicrous` fails the run). `handleSpawnRequest` resolves AFTER the depth/child caps, so a request that was doomed anyway is refused for the reason that stopped it; an unknown role is a new `SpawnRefusal.roleUnresolved` said on the requesting agent's transcript, naming NO role id — not because ids are unsafe (the packet says they may be published) but because P2D.2's witness holds the requested role out of every event on a parent's stream. `runnerConfig(for:)` split out of `piRunner(for:)` (the runner exposes nothing, so the matrix could not read it) carries the role's `tools` as `--tools`, derived from the record's role id + cwd rather than a new persisted field — role files are tracked, so an isolated agent's worktree carries the same `.pi/agents`, and editing a role takes effect next run instead of being frozen at spawn; `model`/`thinking` stay from the RECORD. New `runRoleRegistryChecks` leg: this repo's real roles listed, a nameless temp fixture excluded, unknown id nil-and-throwing, a role naming no provider settings inheriting verbatim, and AGREEMENT between the two launch paths (the `--model/--thinking/--tools` pairs `HarnessRoleRunBuilder.buildLaunchProfile` builds equal those `PiAgentRunner.processArguments` gets from the resolution, so they cannot drift). `--agent-supervisor-check` MARK 13 gained real committed role files in its fixture repo: the captured `spawn_agent` child now runs its ROLE's model/thinking/tools, role-only children still inherit the parent's, and an undefined role is refused without echoing its id. 4 negative tests observed red: parent settings passed instead of the role's; `extraArgs` dropped from `runnerConfig`; the `name` requirement dropped (`lists ["fixture-role", "nameless"]`); `resolve` falling back for an unknown id (red in BOTH checks). TWO DELIBERATE DEVIATIONS: (1) the packet says five roles, there are NINE on disk — the check floors on the five it names rather than pinning the set; (2) `HarnessRoleParser` does not require `name` while the packet's verification wants a nameless fixture skipped, so the requirement lives in `RoleRegistry`, not in the parser other callers share — OWNER CONSEQUENCE: a `.pi/agents/*.md` with no frontmatter `name` no longer appears in the palette (all nine of this repo's roles declare one). Codex: 4 findings — 2 fixed pre-commit (`runnerConfig` now reads `toolsArguments(roleId:)` not `resolve`, so a provider-metadata typo cannot silently strip a live agent's `--tools`, asserted directly; the invalid-model/reasoning checks now assert WHICH failure threw), 1 reasoned away in the commit body (unifying the flag mapping with `HarnessRoleRunBuilder` would change the harness path's omit-when-absent semantics, which the packet does not ask for — the agreement assertion is its own answer to that drift), 1 was its read-only sandbox. Matrix green (3 full runs); inventory blessed in the same commit, `count ContinuumRevivedCoreChecks` 28 -> 29, growth only. |
| P3.1-inbox-row-model | done | f95cec5 | 2026-07-26T07:05:00Z | `AgentInboxRow` + `InboxState`/`InboxAttention`/`InboxLifecycle`/`RowVariant` in AgentUI (Foundation only), total `AgentStatus -> InboxState` map, and the join in Core. TWO DEVIATIONS, both forced by the module boundary: (1) the join lives in `Core/Agents/AgentInboxRowBuilder.swift`, not in the file the packet named — AgentUI may not import Core (P1.1, compiler-enforced) and `AgentsBoardRow`/`AgentRowContext` are Core types, so "build from AgentsBoardRow + AgentContextIndex" is unwritable there; (2) `id` is a bare `UUID`, not the sketched `AgentID` — `AgentID` is `AgentRecord`'s identity so it cannot name the terminal sessions that are half the list, it lives in Core, and the board's aggregate key is the one `ActivityLogSnapshot.byAgent` and P2B.7's `AgentsBoardChangeSet` already diff on. `AgentRowContext` gains `displayName` (a NAME, I5-admissible, added to the P2B.3 hand-enumerated taint sweep) because the row's title is the name the AGENT owns — a headless agent has no tile title. Nothing re-derived: status from `AgentsBoardProjection`, context from `AgentContextIndex`, branch precedence identical to `BranchChipNSView.display`. `elapsed` is the only computed field — oldest event of the unbroken trailing working run, against the caller's `now`, clamped at 0 (a state-change scan, not `turn.started`, which only `ManagedAgentActivityBridge` emits). 6 negative tests observed red at exit 1 and quoted verbatim in the commit body (needsAttention->ready; elapsed from newest event; branch precedence flipped; clamp removed; displayName not joined; tile title preferred). Codex: 6 findings — both medium verification gaps FIXED (a tiled agent whose record and tile are named differently, and a real terminal session in the fixture), 3 reasoned away in the commit body (tileTitle fallback exists because a terminal session has no record; needsAttention->approval is documented as P3.2's split and pinned by an unreachable-`.input`/`.failed` assertion; BranchChipNSView is in the AppKit app target so Core cannot call it). Matrix green at exit 0 incl. the iOS leg; inventory blessed in the same commit (AgentUIChecks 6->7, CoreChecks 29->30, 275 records). Owner: `.input`/`.failed` unreachable until P3.2, `attention` always `.none` until P3.3, `lifecycle` always `.active` until P4 — by construction, not omission. |
| P3.2-five-states-three-colours | done | c4c1943 | 2026-07-26T07:55:00Z | `InboxState.accent` / `.label`, both nil for `ready` — colour is reserved for act-now / in-motion / broken, and the resting state is not an error. The four coloured states take a P1.3 `AccentToken` each (checked to be from the gated palette and distinct, so five states cannot collapse to four on screen). `state(for:pending:)` adds the fact `AgentStatus` cannot carry: `needsAttention` splits into `.approval` (adapter holds a request id) vs `.input` (a plain question), and `pending` OUTRANKS the status. Core derives it in `AgentsBoardProjection.pendingRequest(in:)` off the ring's canonically-last event — the same element `apply` folds the status from — and `AgentInboxRowBuilder` passes it. AGREEMENT WITH P1.8 pinned rather than assumed: where the two vocabularies speak of the same thing they resolve to the same token (`working`, `needsAttention`), and the two deliberate divergences are asserted as facts — `configuring` is the chip's violet but folds into in-motion blue here, and `idle`/`stale`/`done` are painted by the chip but uncoloured in the inbox. 7 negative tests observed red at exit 1 (ready given an accent; ready given a label; precedence reversed; the split collapsed each way; `configuring` re-pointed at violet; the needs-attention guard dropped). Codex: 6 findings — 1 FIXED (high, missed reuse: `respondableRequest` and `pendingRequest` are two meanings of "pending approval" on one type, so the one-way implication `pendingRequest == .approval` -> `respondableRequest != nil` is now a checked invariant and the asymmetry is MEASURED on the fixture that separates them), 5 reasoned away in the commit body. THE ONE WORTH THE OWNER'S EYE: codex is right that the production join cannot produce `working` + pending approval, so the precedence is unexercised end-to-end. Deliberate. Its suggested fix (scan back past later events, as `respondableRequest` does) is the version written first and rejected — it makes the interrupted run `working -> needs-attention -> working` read as waiting forever, contradicting the elapsed semantics P3.1 ships. The precedence is a property of the MAPPING, which is what the packet's `## Verify` asks for, and is what makes P5.9's live out-of-band approvals correct without editing the function. Matrix green at exit 0 incl. the iOS leg; inventory unchanged at 275 (no new flag, no new leg — the checks live inside the existing suites). Owner: `.failed` is still unreachable (no `AgentStatus` records failure; Phase 4 supplies the fact, and the case is kept coloured and caller-reachable so that lands as a fact and not a sixth state). One P3.1 fixture expectation moved `.approval` -> `.input` because its draft carries no request id — same property, now reading the split. No renderer consumes `accent` yet; P3.6 is the list view. |
| P3.3-attention-axis | done | 0c048f2 | 2026-07-26T08:40:00Z | `InboxAttention.resolve(unread:raisedHand:)` (total, woke > unread when both hold), `label` (a word only for `woke` — unread is a MARK, not "Unread" on every resting row), `isYours`, and `AgentInboxRow.label` = `state.label ?? attention.label`, which is P3.2's priority with the `(woke)` rung under it. Read-state lives in `AgentSupervisor` beside `records` and NOT in `AgentRecord`: `deliver` marks unread on `.turnCompleted` only when the agent is not focused; `focus(agentID:)` / `focusTile(_:)` clear AND arm it; `archive` takes it with the agent. MARK 14 drives 4 real turns through the production `deliver` path off a `ScriptedRunnerQueue` and asserts all six properties, including local-only from both sides (the encoded record carries no read-state key, scan vacuity-guarded on `id`; a second supervisor restoring the same store reports `.none`). 5 negative tests observed red at exit 1: focus not clearing; the `focusedAgentID != id` guard dropped; archive leaving it behind; `resolve` answering unread first; the row label reading attention before state. NOT WITNESSED, honestly: the forbidden-key scan (a witness means adding the persisted field it exists to forbid) — vacuity-guarded instead. Matrix green; inventory unchanged at 261 (no new flag, no new leg). Codex: 6 findings — 1 fixed (a doc comment said everything is UNREAD after relaunch; the empty in-memory set means the opposite), 5 are one point reasoned away in the commit body: nothing in the app calls `focus`/`focusTile` and `AgentInboxRowBuilder` still hard-codes `attention: .none`, both outside this packet's `## Files` and both owned by P3.6 (the list view that reads the axis) / P3.9 (open a row). OWNER: the axis is therefore NOT visible in the running app yet — it is a fact with no consumer until P3.6 feeds rows from the desktop side, and that hand-off is recorded at `unread`'s declaration. |
| P3.4-frozen-sort | done | 68764b4 | 2026-07-26T09:55:00Z | `InboxSort.sortForInbox(rows:)` — three blocks: live (newest-spawned first), the snooze shelf (same frozen order), history (most recently ENDED first), with children placed immediately after their parent depth-first inside each block. NO ACTIVITY TERM IS AN INPUT to any comparator, so a status change / elapsed tick / attention mark cannot move a row; `AgentsBoardProjection.rows` is untouched and stays attention-first for the phone, and the divergence is ASSERTED on a real fixture in CoreChecks (the one place both orders are visible, since AgentUI may not import Core) — board `[41,43,42]` vs frozen `[43,42,41]`, and the frozen order is unmoved when the attention changes hands. THREE DEVIATIONS FROM `## Files`, all forced by the signature the packet itself specifies (`sortForInbox(rows:)` is a pure function of rows, so the rows must carry what it sorts on and nothing did): (1) `AgentInboxRow.createdAt` — REQUIRED, no default, because a defaulted `now` would put every unknown row at the TOP, the loudest way to be wrong — plus `AgentInboxRow.parentId`, since a depth cannot say WHOSE child a row is; (2) `AgentRowContext.createdAt`/`.parentId` joined in `AgentContextIndex.build` from `AgentRecord.createdAt`/`.parentAgentID` and `TerminalSessionDescriptor.createdAt`, both added to the P2B.3 hand-enumerated I5 sweep (a field left off that list is a hole, not an exemption); (3) `InboxLifecycle.settled` -> `settled(at: Date)`, so history's sort key is total by construction, mirroring `snoozed(until:)`. Tiebreak is `AgentStore.isOrderedBefore`'s reversed, so equal spawn times resolve identically on both surfaces. Always a permutation: an orphaned child is a root, a corrupted `parentId` cycle is appended rather than dropped or recursed forever. THE LOAD-BEARING CHECK IS A PROPERTY OVER A SORT, not a straight-line assertion: `frozenSequenceHolds` is asserted TRUE for `sortForInbox` and FALSE for a status-keyed sort IN THE SAME RUN, over 200 seeded (SplitMix64) trials that churn state/attention/elapsed in random orders and shuffle the input — so the packet's regression witness is executed, not described. Fixture is guarded against vacuity three ways (arrives in a different order than it leaves; expected order != id order; spawn times distinct). 8 NEGATIVE TESTS OBSERVED RED at exit 1: a status term added to the comparator; nesting removed; history falling back to spawn order; settled rows left in the live block; the id tiebreak dropped; the cycle guard removed; the builder not joining `createdAt` (`got Optional(0001-01-01 ...) for Headless Reviewer`); the index not joining `parentId` (`a child's row names its parent ... got nil`). Matrix green at exit 0 including the iOS leg, two clean runs; inventory blessed in the same commit, growth only (`count ContinuumRevivedAgentUIChecks 7 -> 8`, 275 records). CODEX (gpt-5.5): 1 high finding, reasoned away in the commit body — nothing in production calls `sortForInbox`, so the app is still attention-first. Deliberate: there is no desktop inbox list yet, `AgentInboxRowBuilder.rows` reaches no renderer (P3.1) and read-state has no consumer (P3.3); P3.6 is the list view that calls all three, and this packet's `## Files` names no rendering file. OWNER: two calls worth your eye — snoozed rows shelve BETWEEN live and history (my reading of the packet's 'settle/snooze DOES move a row'; the packet only rules on settled, and P4.7 can move it in one line), and `InboxLifecycle.settled` now carries a Date, which P4.1 inherits. HARNESS NOTE for the supervisor: `ContinuumRevivedCoreChecks` fails early in a DETACHED shell — `FAIL: ssh-wrapped LaunchProfile should create real tmux session ... script: tcgetattr/ioctl: Operation not supported on socket` — so a witness run backgrounded with no tty aborts before reaching the check under test and looks like a hang. Run it in the foreground. |
| P3.5-in-flight-fade | done | ec0a968 | 2026-07-26T10:45:00Z | `RowEmphasis` (.full/.receded) + `AgentInboxRow.emphasis(for:attention:isInteracting:)`, pure over the two axes only (never elapsed/lifecycle/position, so the fade cannot become a second sort key). RECESSION IS INVERTED ON PURPOSE: `working` recedes and `ready` recedes only once READ; `approval`/`input`/`failed` never do, and neither does anything `woke` in any state — the one place the attention axis outranks in-motion (the opposite of the label priority, where `working` outranks `(woke)`, because there the question is "what is it doing" and here it is "is this mine"). Hover/selection/active clears recession. `Opacity.receded` = 0.88 lives in P1.3's file and is DERIVED, not chosen: solving ratio(a*fg+(1-a)*bg, bg)=4.5 over every documented text pair in both themes puts the break-even at a=0.8724 (textSecondary on canvas, light), so 0.88 is the smallest hundredth that clears AA — worst faded pair 4.58:1. HONEST LIMIT, stated in the token's own doc comment: 0.88 is a SUBTLE dim, and the packet's "fading is not a licence to fail contrast" is what bounds it; a deeper fade needs a dimmer text token that clears 4.5:1 at full strength, which is a P1.3 palette change and was not smuggled in under an alpha. `TokenPair.faded(_:)` + `ChipColor.composited(over:alpha:)` + `DesignTokens.recededTextPairs` derive the faded set from `documentedPairs`, so a new text token is gated faded the moment it is gated at all; accents are excluded because `accentOpacity` is full for BOTH emphases (the one coloured thing on a waiting row stays findable). Gate: the full 15-pair state/attention matrix against a table (x isInteracting), exactly-3-recede (a rule that dimmed nothing would satisfy every other assertion), 54 faded pair/theme contrast measurements, and the faded-set size pinned to the literal 27. 4 negative tests observed red at exit 1 with the final code, quoted verbatim at `runRowEmphasisChecks`: receding an approval (the packet's named witness), receding a finished-unread row, dropping the isInteracting clear, and Opacity.receded=0.87 -> `receded textSecondary@0.87 on canvas clears 4.5 in light — 4.48:1`. Codex: 5 findings — its 2 low ones fixed (literal count pin; the compositing comment no longer claims to be the screen's blend and names `--ui-contrast-check` instead), its 3 view-level ones are CORRECT AND UNADDRESSABLE HERE and are written as an obligation at `RowEmphasis` rather than waved away: NOTHING PAINTS THIS YET — the packet's second file is "the list view (P3.6)", which does not exist, and this module is Foundation-only. P3.6 must apply `textOpacity` to the TEXT LAYER only (a container alpha would fade the accent with it), pass hover/selection/active in, and own the rendered witness. Matrix green; inventory unchanged at 261 (no new flag or leg — the checks run inside the existing AgentUIChecks target). Owner: one full-matrix run failed on `--browser-inspector-network-lite-check` and passed on re-run and on both later full runs; nothing in this diff touches the browser, so it reads as a pre-existing timing flake in that leg. |
| P3.6-inbox-list-view | done | e8f5ae2 | 2026-07-26T11:55:00Z | `AgentInboxView` (NSTableView, ~79pt cards DERIVED from the type — 1 `.title` + 2 `.label` lines + 2 gaps + `Inset.card`, not the packet's magic 78) is now `WorkspaceSidebarView`'s content, fed from the one agent-activity snapshot. Everything Phase 3 built as a value is painted for the first time: P3.4's frozen order, P3.2's five states/three accents, P3.3's attention, P3.5's emphasis. P3.5's THREE OBLIGATIONS discharged on the view that owns each colour — `textOpacity` on the five word labels only, `accentOpacity` (full, always) on the state label, hover/selection into `emphasis(for:attention:isInteracting:)`. Read-state reaches rows via a new `attention:` arg on `AgentInboxRowBuilder.rows` (the seam P3.3 named; Core cannot reach a supervisor). Branch line reuses `BranchChipNSView.branchGlyph`/`sharedSuffix` so tile chip and row cannot disagree. THE TREE REMAINS (packet's watch-out): built, reloaded, answering every `…ForQA` the three `--workspace-sidebar-*-check` legs and the zone/workspace callbacks read — HIDDEN, not deleted; P3.8 takes workspace nav, P3.14 management. DELIBERATELY ABSENT: the packet's `+12 −4` diff column — `AgentInboxRow` carries no counts (P3.1 did not model them; `AgentDiffSource.counts` is git I/O) and adding the field would be P3.1's file plus a guess. TWO SUBSTRATE FIXES this ticket forced, being the first surface of its shape: (1) `UIProbeContrast.visit` multiplied `alphaValue` BY `layer.opacity` — on a layer-backed view those are ONE fact, so a fade was SQUARED: `Opacity.receded` 0.88 measured as 0.774 and 8 rows red at 3.89:1 for a fade the screen never applies. Counted once now; witness 9 proves it is still counted (receded=0.50 → 130 red pairs). (2) the row card is an `AgentInboxCardView`, a plain NSView — a layer-backed `NSTableRowView` carries AppKit's own sublayers the appearance sweep holds a TokenThemed view answerable for (measured: 7 sentinels surviving a flip on layers this code never touched), and a bare CALayer is invisible to `UIProbeContrast`, which reads fills off VIEWS, so every label was measured against the panel two levels up instead of its own card. Gate: `--agent-inbox-check` (new leg) in two halves — the view against a canned model (order, labels, painted alphas, hover, selection, incremental refresh) and the app against disk (registry + terminal agent + HEADLESS `AgentRecord` + plain shell through `refreshAgentSurfaces` into a real sidebar; the shell is filtered out, the headless agent is a row the tile-keyed tree could not draw at all). NINE negative tests observed red at exit 1 with the final code, quoted verbatim above `runAgentInboxChecks`. Phase-0 gates over 2 new Lab entries + the 5 existing sidebar cards: 52 static renders (floor 48→52), 846 contrast pairs both appearances 0 exempt worst text 5.12:1, 27 TokenThemed views / 53 sentinelled slots (floors 12/25→27/53), 14 baselines blessed (10 changed + 4 new; each `.actual` reviewed, diff set identical over 3 runs). Also found and fixed by the pixel gate: a 280pt sidebar squeezed the project chip to a glyph-less sliver (`text rect is flat — luminance spread 0.000 over 176 px`), closed with font-measured width floors and the TITLE truncating first. Codex (gpt-5.5): 2 findings. The HIGH one is FIXED and witnessed (negative test 8) — keying the incremental repaint on `AgentsBoardChangeSet` alone left a renamed project / re-joined zone / moved read-state on screen forever, because that set is about ACTIVITY and half a row is not; staleness is now change-set ∪ row-inequality, with a no-op push asserted to rebuild NOTHING so it stays a diff. The MEDIUM one (one Lab entry per state rather than one card holding five) is declined in the commit body: the gates are per-text-rect and per-pixel so five cards in one surface gate identically, there is only one `RowVariant` to render today (`slim` is P3.7/P4), and `managed-agent.branch-chip` set the precedent. Matrix green at exit 0 including the iOS leg; inventory blessed in the same commit (+1 check, +1 leg). One full run failed on `--browser-inspector-network-lite-check` and passed on re-run — the same pre-existing flake P3.5 recorded, nothing in this diff touches the browser. Owner: the sidebar header now says "Agents" while its three buttons still manage workspaces (tooltips say so) until P3.14; the five existing sidebar Lab cards render the inbox because the real sidebar does. |
| P3.7-slim-rows | done | fa4902e | 2026-07-26T13:10:00Z | `AgentInboxSlimCellView` + `tableView(_:heightOfRow:)`: settled/snoozed render at 35pt (`Metrics.rowHeight(for: .title, insets: .row)` — the packet's "~36pt", derived not chosen) against the card's 79pt; everything else stays a card, and `RowVariant.forLifecycle` (P3.1, already gated) is what keeps `ready`/`failed` uncollapsible rather than a condition this view could forget. One line: `StatusChipPresenter`'s glyph (working ● / needsAttention ◆ for both approval and input / idle ○; `failed` ✕ is the one glyph chosen here, because no `AgentStatus` records failure yet), the agent's name, the branch badge and a relative time. The card view is REUSED for a parked row so it keeps the selection outline, the appearance re-theme and a documented `tileBody` fill for `UIProbeContrast` to measure against. `apply(rows:changed:)` now calls `noteHeightOfRows(withIndexesChanged:)` — `reloadData(forRowIndexes:)` re-asks for a row's view but NOT its height — and row views are keyed `id:variant` so a lifecycle move replaces the view in place (P4.12 animates it). Relative time reuses `AgentInboxCellView.elapsedText`'s units ("12m ago" / "in 25m", empty for an overdue snooze = P4.6's raised hand) off an injectable `clock`, because a wall-clock string in a Lab card is a baseline that matches for one minute an hour. TWO DELIBERATE DECISIONS: (1) the glyph fades at rest and is full on hover, diverging from P3.5's "an accent never recedes" — a parked row is by definition out of the attention flow, and the fade is `Opacity.receded`, the same token the words use, so it stays inside P1.6's envelope (measured worst text pair on the new card 5.06:1 vs the 4.5 floor, both appearances); (2) the slim branch badge drops the card's `· shared` suffix, measured — with it the first render truncated to `⎇ mai…hared` at 320pt. New Lab card `chrome.agentInbox.parked` is the SAME seven agents as `chrome.agentInbox` with three parked, so the baseline pair is one list collapsed rather than two lists; `minimumCardsGated` 52 → 54 and 2 PNGs blessed, the only pixels that moved (52 other renders matched before blessing). Inventory unchanged — no new flag, no new leg. 6 negative tests observed red at exit 1 with the final code: `noteHeightOfRows` dropped (`laid out at 79.0pt, wanted 35.0pt`), slim rows built as the card cell, glyph pinned to full alpha, variant dropped from the view key, `height(for: .slim)` returning the card height, and the fixture losing its parked rows. That fifth witness is what exposed the per-row height assertion comparing the table against the function that set it — it now spells the two heights out. Codex (gpt-5.5): 1 HIGH, declined as factually wrong (`protocol P: NSTableCellView` IS valid Swift — a class-constrained protocol may inherit a class type; it compiles and the matrix runs it). Matrix green at exit 0 including the iOS leg. OWNER: `--agent-supervisor-check` failed 2 of 5 full-matrix runs (`an event produced after the tile closed did not reach the supervisor's remaining subscriber` — the 10s `waitUntil` at AgentSupervisor.swift:2123) and passed 6 standalone runs including under the matrix's own env. Pre-existing async flake, unreachable from this diff, but frequent enough to deserve a ticket. |
| P3.8-scope-dropdown | done | fcb22bf | 2026-07-26T14:05:00Z | `NSPopUpButton` above the list (`ContinuumAgentInboxScope`) + pure `InboxScope` in ContinuumRevivedAgentUI beside `InboxSort` (`filter(rows:scope:openAgentId:)`, `entries(for:catalog:including:)`, tagged storage codec) + `WorkspaceSidebarConfig.resolveScope/setScope`. Order-preserving by construction and asserted as filter∘sort == sort∘filter. `AgentInboxRow` gained `workspaceName`, which NOTHING DRAWS — a pure filter can only read what the row carries, and the packet's workspace scopes are impossible without it (the alternative was the view reaching into Core for a join it was already handed; same call P3.1 made about where the join lives). THE TWO HARD RULES: a scope flip clears the selection (P3.11 must never bulk-act on a row you cannot see) and the agent open in the focused tile is force-included whatever the scope says (`AgentSupervisor.agent(forTile:)`, falling back to the tile id because a terminal-session agent is keyed by its tile). The view deliberately does NOT read `UserDefaults` — it is rendered by 3 committed baselines and resolving a persisted default in `init` would make those PNGs depend on whose defaults ran the check; `configureWorkspaceSidebar` restores and persists, and that wiring is itself gated (witnesses 6/9/10, standard-domain value saved and restored the way `--workspace-sidebar-shell-check` already does). BEYOND THE PACKET, both from codex and both witnessed: (1) the menu is the UNION of the rows and a CATALOG of what is open (registry's non-missing projects + workspaces) — a row-derived menu cannot offer the project you are about to start your first agent in, which is what "one entry per open project" asks for; (2) a second empty-state message, because "No agents yet" in front of seven filtered-out agents is the empty state lying. 10 NEGATIVE TESTS OBSERVED RED at exit 1 with the final code, quoted verbatim at `runAgentInboxChecks`: selection retained across a flip (the packet's named regression), force-include dropped, popup built with `addItem(withTitle:)` (it REMOVES a same-titled item, so a workspace named after its project ate an entry and shifted every tag after it — the exact hazard the hand-built `NSMenu` avoids), one empty message, no-op filter, app not restoring/persisting, selected scope dropped from the menu, catalog ignored, catalog not fed, `missing` projects offered. Witnesses 9/10 are why section B's registry now has 3 projects: with only the one the rows already name, both edits stayed GREEN. MEASURED AND RECORDED: `reloadData()` already empties `selectedRowIndexes`, so deleting the explicit `deselectAll` leaves the check green (probed over 4 flips, one growing the row count) — it stays because P3.11/P4.12 could reasonably start restoring selection across a reload, and witness 1 is the direction with teeth. Matrix green at exit 0 including the iOS leg, 3 runs after blessing (worst baseline delta 0.0000%). 16 baselines blessed, every diff inspected in both appearances: the popup is ~32pt of new chrome, so the 3 inbox cards and the 5 sidebar-family cards all moved. `AgentInboxView.scopeControlHeight` is measured off the control's own font (not a literal), and the 3 inbox Lab cards + the 2 appearance-sweep surfaces are that much taller — which is what put `--ui-probe-check` back on its floor of 27 themed views / 53 slots after the popup cost it one rendered row card. Inventory blessed here (AgentUIChecks 8->9, 277 records). OWNER: (a) Lab contrast pairs 926 -> 902 and borders 132 -> 128 — the 5 FIXED-SIZE sidebar cards each render one fewer row card now; no floor crossed (per-appearance 192/12 vs 412/64), the inbox's own coverage was preserved by growing its cards, and the dropped pairs were duplicate measurements of the same row types. Grow those five cards by `scopeControlHeight` if you want the number back (costs one bless commit). (b) A scope is a NAME, so two open projects sharing a display name are ONE entry matching both — declined deliberately (ids are not on the row; P3.1 flattened them out to share the type with iOS, and a shared name shows a superset, hiding no agent), pinned in the pure checks so it cannot change unnoticed. (c) Renaming the scoped project does NOT reset the scope: the old name still decodes, stays in the menu as the selection, and the list opens empty saying "No agents in this scope" — one click from fixed, and the first version of that doc comment claimed a fallback that does not happen (codex caught it; comment corrected and the behaviour is now asserted). |
| P3.9-reveal-on-click | done | e8373ce | 2026-07-26T11:55:00Z | **SUPERVISOR-COMPLETED.** The worker finished the work and was killed by the 9000s iteration timeout mid-verification, which left the driver with no LOOP token so it halted the run. I verified the tree it left before committing: `swift build` clean, full matrix GREEN, `ContinuumRevivedAgentUI` still imports only Foundation, focus-ring fix intact (2 occurrences), sentinel floors 27/53, allowlist 27. `revealAgentFromInbox` splits the aggregate key by record lookup — a managed agent reveals in the tile it already has, a terminal-session row reveals its own tile and disarms the read-mark, and a headless agent gains a view through P2A.5's `attach` (deliberate, not a silent second spawn; spawning a second tile for an agent that has one is refused because `attach` would unbind the first). READING IS FREE is stated and honoured: a reveal changes only the read-mark and the viewport. Cross-workspace reveals reuse the sidebar's existing primitives, so the frozen order is untouched. Its own report noted Codex raised 3 findings, all addressed, the cross-project attach limitation stated on stderr, documented, and pinned by a new assertion with its own witness. |
| P2D.4-parent-child-nesting | done | d429cbc | 2026-07-26T15:40:00Z | Nesting was half-landed by P3.4 (a child already followed its parent), so this ticket is DEPTH + FOLD. `InboxSort.nest` assigns `depth` (root 0, child parent+1, capped at `AgentInboxRow.maxDepth` = 2 = `AgentSupervisor.maxSpawnDepth`, pinned to it by an assertion since AgentUI may not import App); an ORPHAN is promoted back to 0 whatever depth it arrived with, which is also what a cross-block child (live child of a settled parent) gets. `depth` is now `internal(set)` so the sort is its one writer — depth is a property of a row's place in a LIST, invisible to a per-agent builder, so `AgentInboxRowBuilder` passes 0 and the sort overwrites it. New pure helpers `InboxSort.visibleRows(_:collapsed:)` / `.parentIds(in:)` live in AgentUI so iOS can reuse them. `AgentInboxView` draws the indent (`Space.xl` per level) and a token-coloured `InboxDisclosureButton` on parents only, folds the WHOLE subtree, and keeps the fold across pushes; collapsed state is view-local (no defaults key, nothing synced, packet's rule). P3.6's header claim that "a child row is never hidden behind its parent" is overruled by this packet, and the note says so. 8 negative tests observed red at exit 1 with the final code, quoted verbatim at `runAgentInboxChecks`: cap removed (depths 0,1,2,3), orphan promotion removed, subtree fold reduced to direct children (grandchild survives), triangle derived from the COLLAPSED list (it vanishes the moment you use it, so the group can never be unfolded), indent forced to 0, click wiring cut, plus the two below. Codex (gpt-5.5): 3 findings, ALL FIXED, none argued away — (HIGH) folding a settled parent hid the live child promoted out of its group, so hiding now requires `depth > 0` i.e. what the sort actually nested, and `parentIds` counts the same way; (HIGH) the disclosure is view state so `apply(rows:changed:)`'s value comparison could not see it move — a parent whose last child left while folded kept a dead triangle, now repainted via a `disclosureMoved` term; (MEDIUM) the missing cross-block collapse check, added to the pure suite. Owner: P3.7's "1 cell rebuilt on a settle" is now 2 — that fixture's tail IS the orchestrator's child, so settling it also removes the parent's triangle; not a loosened bound, both rows are asserted by name and the pre-settle triangle is asserted for vacuity. 14 baselines blessed (agentInbox / agentInbox.parked / agentInbox.selected / sidebar / sidebar.live / sidebar.selected / activityDock x 2 appearances): 4 diffs inspected, every changed pixel is in the one parent row whose chip and title shift right for the triangle; matrix green twice after the bless at worst 0.0000%, contrast pairs 902 -> 916 and text rects 824 -> 852 (the new glyph is gated, not invisible). Matrix inventory unchanged (no new flag, no new leg) |
| P3.10-jump-shortcuts | done | e405393 | 2026-07-26T19:35:00Z | ⌘1–⌘9 jump to the first nine rows (select + `onRevealRow`, the same pair a click makes). COLLISION RESOLVED BY SCOPE, not theft: ⌘1–⌘4 stay `spawnProfile` — `ReservedShortcut.classify` and `FocusDispatch.resolve` are untouched — and `InboxJump` is a second reading applied only while the inbox holds first responder (`AppDelegate.handleInboxJump`, ahead of the reserved dispatch in `handleHotkey`). Own `ShortcutLayer.inbox` so Settings shows the nine chords AND CoreChecks' intra-scope uniqueness assertion keeps its teeth (declaring them `.global` is red on the duplicate — the collision surfacing as the packet asks). OWNER CONSEQUENCE, stated not hidden: a reveal hands focus to the tile (P3.9), so a jump gives the keyboard to the canvas and a second jump needs the sidebar focused again; that is the price of ⌘1–⌘4 keeping their meaning, versus the packet's other option of re-homing four documented globals. Hint pills are an OVERLAY on the card (the T3 regression this packet names), asserted as a layout fact — every status label frame identical before and after — and they ride the app's EXISTING global modifier monitor (`handleFlagsChanged`) under the same focus guard as the jump. `KeyChord` learned digits 5–9 (the catalog otherwise said '⌘key23'). `InboxJumpHintView` uses `Radius.card` not `Radius.pill`, the note `BranchChipNSView` already carried: an unclamped 999 radius painted a white shape over every row and took the list's text with it — caught by `--ui-pixel-check`, not by eye. 6 negative tests observed red at exit 1 with the final code, quoted verbatim above `runAgentInboxChecks`: pill in the headline stack (every status word shifts 27.5pt left), the jump unwired from `handleHotkey` (and NOT the 'was consumed' line, which stays green because ⌘2 is profile 2 — hence the separate landing + tile-count assertions), the responder test dropped, `contains(.command)` for `==`, the hints unwired from the monitor, and the hints without their focus half. Visual gate: new `chrome.agentInbox.jumpHints` Lab card + 2 committed baselines (56 card/appearance renders gated, floor raised 54 → 56), both renders reviewed and the check re-run 3x at 0 mismatches; `InboxJumpHintView` added to `UIProbeAppearance.tokenAdoptedOwners` so its overlay fill and border are held to DesignTokens values in both appearances. Codex: 3 findings, one root, all FIXED not reasoned away — the pills were a view-local `flagsChanged` override that cannot see the ⌘ release after a jump (stuck pills), duplicated the app's modifier tracking, and left the check driving a view-local path instead of the shipped one; witnesses 5 and 6 exist because of that review. Matrix green including the iOS leg; inventory unchanged at 277 (no new flag, no new leg). Environment note for the next iteration: `--browser-inspector-network-lite-check` went red once on a run and passed standalone and on the next full run — flaky/network-dependent, unrelated to this ticket. |
| P3.11-multi-select-bulk | done | 3569c2b | 2026-07-26T21:40:00Z | Multi-select (`allowsMultipleSelection`; the three gestures and the range anchor stay AppKit's — reimplementing them over `mouseDown` would mean owning the anchor, the autoscroll and drag-select, and would take `clickedRow` and with it P3.9's asserted reveal out of the path) + `InboxBulkAction` (settle/snooze/mark-unread/archive/delete) + `InboxBulkActionBar`, a floating card over the bottom of the list from 2 selected rows. ENABLEMENT IS THE AND ACROSS MEMBERS: a blocked member (approval/input) removes Settle, an unread one removes Mark Unread, a running one removes Archive and Delete, both leave only Snooze, an archived row leaves only Delete, an empty selection nothing (`allSatisfy` over nothing is TRUE — the guard is load-bearing). An unavailable action is HIDDEN, not greyed, per the packet's own wording ("appear only when every selected row can take the action"); codex argued for greying and it is reasoned away in the commit body — five greyed items in a 320pt sidebar pose "which of my six blocks this?" and the bar has no room to answer. A PULL-DOWN and not five buttons because those five titles do not fit 320pt at `.token(.label)` and truncated buttons render as rects with no glyphs (`UIProbePixels` calls that flat). NOTHING PERFORMS ANYTHING — Phase 4 owns the persisted lifecycle, P3.3 read-state — so the bar hands the host `(action, ids)` in screen order, and with `onBulkAction` unset the menu does not appear at all (the shipped sidebar would otherwise offer five silent no-ops; codex found that). 11 negative tests observed red at exit 1 with the final code, quoted verbatim above `runAgentInboxChecks`. TWO OF THEM CHANGED THE CHECK, not the code: (1) "both selected rows are at full strength" is GREEN against the single-`selectedRow`-index bug, because a selection built one id at a time rebuilds each row while that row is itself the last-selected (probed: `build row 1 selectedRow 1 set [1]`, then `build row 4 selectedRow 4 set [1, 4]`) — so the assertion is now a repaint of the row that is NOT last-selected, under a live selection; (2) `reloadData()` empties the selection WITHOUT posting a selection change while `deselectAll` (scope flip, fold) does, so the whole-list push needed its own bar-teardown assertion. Visual: new Lab card `chrome.agentInbox.bulk` under geometry + contrast + pixel + baseline in both appearances; its first render was red on geometry (`NSPopUpButton spills vertically — frame y 17.0…41.0 outside parent 0…40.0`), fixed by making the control own the bar's vertical. Blessing wrote exactly its own 2 PNGs and moved none of the other 56 — the numeric form of "the bar is an overlay"; determinism confirmed over 3 runs. Matrix green including the iOS leg; inventory unchanged (no new flag, no new leg — the card rides `--component-lab-check`). KNOWN GAP, stated in the code and not papered over: the three gestures themselves are NOT verified headlessly — `NSTableView.mouseDown` runs a modal tracking loop until mouse-up, and `doCommand(by: moveDownAndModifySelection)` does not extend outside a real key event (tried with the list as first responder: still 1 row). What is exercised is `selectRowIndexes(_:byExtendingSelection:)`, the API those gestures end in, plus everything downstream of the selection set; feel is a supervised manual step. Codex also caught the kept-work caption over-promising: `AgentSupervisor.cleanUpWorktree` DELETES a merged branch (`git branch -d`) and retains an unmerged one, so "Keeps ⎇ …" was a claim the row (which carries no merge state) cannot make — the caption now reads `Unmerged work kept: ⎇ …`, which is the guarantee that actually holds. Owner: picking an action is a no-op until a Phase 4 ticket wires `onBulkAction` |
| P3.14-preserve-workspace-management | done | c4a70a9 | 2026-07-27T02:10:00Z | New / Rename / Delete moved out of the sidebar header and into the scope popup's menu (P3.8) as a separated section, and the three header buttons are DELETED. The verbs share the popup's own action via a tag range (`managementTagBase`) rather than carrying item actions — a popup sends both its own action and a selected item's, and one dispatch is the shape `scopeEntries` already uses. `menu.autoenablesItems = false` IS LOAD-BEARING, the same trap P3.12 wrote down for `rowMenu`: measured red — with it removed, `NSMenu.update()` re-derives enablement just before the menu draws and a disabled Delete comes back live. Picking a verb re-selects the scope before the host is told, because `pullsDown: false` titles the popup with whatever was last selected. TARGET = THE SCOPED WORKSPACE first (the packet's ask), then the old chain (outline selection, then current workspace) under any other scope — so `.all`, the default, behaves exactly as the buttons did. CODEX FOUND ONE REAL HIGH: my comment said a duplicate name is not a guess this view may make, and the code then guessed anyway by falling through to the open workspace — a name-based scope is safe for FILTERING (a wrong match only shows extra rows) and that argument does not survive a destructive verb. Fixed: an ambiguous or unknown workspace scope has NO target, so both verbs grey out; its own fixture (two same-named workspaces) is the last block of `--workspace-management-polish-check`. Guards preserved end-to-end: delete off at one workspace, the confirmation provider still in front of it, the `managementMessageLabel` warning path untouched in the header (a closed menu is not where a rejected rename can be read). 6 negative tests observed red: no management block in the menu; Delete always enabled; the scope branch dropped from the target resolution; the scope-selection restore dropped (view-level — see below); `autoenablesItems` left at AppKit's default; the ambiguity fallback put back. COVERAGE SPLIT, honestly: the packet named `--workspace-sidebar-actions-check`, but that leg covers row clicks and the sidebar toggle — the three verbs are covered by `--workspace-management-polish-check` (`sidebar.clickCreateForQA` etc.), which is what was updated to drive the new location; the actions leg is untouched and still green. The scope-restore assertion lives in `--agent-inbox-check` on a bare view, because EVERY app path that runs a verb also pushes rows and the menu rebuild inside that push re-selects the scope anyway — asserted through the app it would have been toothless (measured). TRAP FOUND THE HARD WAY: a `workspaceRenamePromptProvider` that returns nil does NOT decline — it falls through to the real `NSAlert.runModal` and hangs a headless run (sampled the wedged process). The decline probe uses a cancelled delete instead. 20 baselines blessed, every diff inspected: the sidebar cards lose the button row and the list moves up; the inbox-only cards move by 1121px because the popup now sizes to 'Delete Workspace…'. Determinism proven over 3 renders (bless + 2 clean matrix runs). ZONE NAVIGATION: nothing removed by this ticket — the workspace/zone/tile outline has been hidden since P3.6, is still built and still answers `onSelection`, and ⌘K keeps its `.jumpToZone` rows. Matrix green at exit 0 twice in a row; no new flags or legs, so the inventory is unchanged. Seen once on an intervening run and NOT from this diff: the pre-existing `network-lite log did not capture a committed or finished event after start` flake. |
| P4.1-lifecycle-state | done | 17bf823 | 2026-07-27T05:30:00Z | Four STORED lifecycle facts on `AgentRecord` (`settledOverride` tri-state, `settledAt`, `snoozedUntil`, `archivedAt`) + `SettledOverride` in AgentUI so Core and iOS both reach it. Nothing derived (P4.2 owns `InboxLifecycle`'s computation) and nothing synced (no payload gained a key; the taint witness is extended to assert the new fields add nothing to flag). TWO DEVIATIONS FROM THE PACKET'S FILE LIST, both resolved toward what exists: (1) `InboxLifecycle` is NOT re-declared — it already lives in `AgentInboxRow.swift` from P3.1 in a STRICTLY RICHER form (`settled(at:)`/`snoozed(until:)` carry their dates in the case, which is what lets `InboxSort` order history by end time with no fallback), so the packet's sketch is the weaker spelling and declaring it would be a redeclaration AND a regression; (2) the checks are SPLIT because `ContinuumRevivedAgentUIChecks` depends on AgentUI ALONE by design (P1.1) and cannot see an `AgentRecord` — enum half in the packet's named file, record half as `runAgentRecordLifecycleCheck()` in `AgentRecordChecks.swift`. Wire form: `.neutral` is written as ABSENCE (the way a headless record omits `tileId`), so a pre-P4.1 record and a fresh one are byte-identical; dates take `timeIntervalSinceReferenceDate`, measured not assumed — with `timeIntervalSince1970` on both sides, 104/93/107 of 200 real `Date()` values drift. 9 negative tests observed red at exit 1 against the final code, quoted verbatim at their assertions; TWO ARE REPORTED WHERE THEY LANDED RATHER THAN WHERE THEY WERE AIMED (the honest version): `decode` for `decodeIfPresent` alone is red at check 1 ("AgentRecord fixture failed to encode/decode") and so does NOT witness the pre-P4.1 payload — the edit that does is always-encode + require-key together, red at P2A.1's own decode-forward fixture; and collapsing `archivedAt` onto the settled key is red at the 200-case sweep, not at the archived assertion. Also caught by measurement: the first spelling of "a fresh record is .neutral" PASSED against a changed initialiser default, because the shared fixture forwards `settledOverride:` explicitly — the check now builds a bare `AgentRecord` with no lifecycle arguments. CODEX (gpt-5.5): no correctness bugs, no guessing, no missed reuse; one LOW verification gap FIXED not argued away — the synthesised raw-value `Codable` would throw on a word from a newer build while `AgentRecord` (which decodes a `String` by hand) would not, so `SettledOverride` gained a custom `init(from:)` delegating to the tolerant path plus its own witness. Matrix green at exit 0 (full run after the fix; an earlier full run before it was green too). Inventory floor `ContinuumRevivedAgentUIChecks` 9 -> 10, regenerated and committed; no leg or flag changed. ENVIRONMENT NOTE for the next iteration, unrelated to this diff: CoreChecks' real-tmux leg ("ssh-wrapped LaunchProfile should create real tmux session", ssh stderr `script: tcgetattr/ioctl: Operation not supported on socket`) went red on the first run after several rebuilds and green on the immediate rerun, three separate times — flaky here, and green in both full matrix runs. Owner: nothing WRITES these fields yet; P4.2 derives from them and the later Phase 4 tickets set them, so P3.11's bulk-action bar stays a no-op until one of those wires it |
| P4.2-effective-settled | done | cb36d74 | 2026-07-27T06:20:00Z | `InboxLifecycle.resolve(...)` — one pure, total function over P4.1's stored facts, plus `LifecycleBlockers` (an OptionSet: pending approval / pending input / running session / queued turn, because the four facts compose). PRECEDENCE: archived -> blockers -> unexpired snooze -> `.settled` -> the keep-active pin -> inactivity past `autoSettleAfter` -> active. `now` is a parameter and nothing reads a clock. THREE DECISIONS THE PACKET LEFT OPEN, each argued at the code: (1) BLOCKERS ARE THEIR OWN VOCABULARY, not derived from `InboxState` — `.queuedTurn` and a starting session are invisible in the five-state vocabulary, so deriving would lose two of the four (`forPending` reuses P3.1's `PendingRequest` for the two that do overlap); (2) AN UNEXPIRED SNOOZE OUTRANKS `.settled` — a snooze is the only instruction carrying a FUTURE date, so resolving it as settled strands the wake-up in history forever, while deferring the override loses nothing (it is still stored and re-resolves the moment the snooze expires; asserted both ways); (3) `autoSettleAfter` is SECONDS and nil-by-default, so this file needs no calendar and P4.3 owns the `autoSettleAfterDays` setting and its conversion. Settled's date is `settledAt ?? lastActivityAt ?? now` — `.settled(at:)` carries one by construction (P3.4 sorts history on it), and the auto rung dates the settle when the work went QUIET, not when the sweep noticed. VERIFICATION IS TWO HALVES: a 31-case hand-written table naming every rung (both of the packet's named witnesses among them) and an EXHAUSTIVE 1,728-case sweep (3 overrides x all 16 blocker sets x 3 snoozes x 3 activity dates x 2 archive x 2 decision dates) carrying an oracle AND the rungs' invariants; the table is what keeps the oracle non-circular, and the sweep prints its own non-vacuity (active 830, archived 864, settled 16, snoozed 18). 14 NEGATIVE TESTS OBSERVED RED at exit 1 against the final code, quoted at the checks: blockers made to defer to `.settled` (the first named witness), `resolve` narrowed to `contains(.pendingApproval)` (the second), snooze above blockers, the pin folded onto neutral, both boundary comparisons flipped (`>=`/`>` each way), archived below blockers, the settled-date fallback cut to `now`, the blocker step deleted, the archived step deleted, `.snoozed(until: now)`, `forPending(.input)` mapped to approval. TWO ARE REPORTED WHERE THEY LAND, not where they were aimed: subtracting `.sessionRunning` inside `isBlocking` is red one check earlier at the vocabulary check ("FAIL: 4 blocks") and so does NOT witness the named case, hence the `contains` variant; and every table-visible mutation is caught by the table first, so the sweep's teeth were witnessed separately with the table's call removed. CODEX (gpt-5.5), two rounds: round 1 raised two verification gaps, both FIXED not argued away — the sweep covered 6 of 16 blocker compositions (now all 16) and asserted invariants only, leaving combinations the table does not name unpinned (`.neutral` + an EXPIRED snooze + stale inactivity must settle; now every case has an expected lifecycle). Round 2 raised the ARCHIVED RUNG as invented scope, REASONED AWAY: the packet's own watch-out ("do not conflate archived with settled in this function") presupposes the function sees it, `InboxLifecycle.archived` has no other producer, and by the time `archivedAt` is set `AgentSupervisor.archive` (P2C.3) has already stopped the runner and DELETED the record — so no live blocker can coexist with it, and putting blockers above archived would resurrect a row pointing at a record that is gone, which is strictly worse than hiding one. Matrix green at exit 0 on two consecutive full runs incl. the iOS leg (a third run hit the known pre-existing `--browser-inspector-network-lite-check` WKWebView flake). Inventory floor `ContinuumRevivedAgentUIChecks` 10 -> 11, regenerated and committed; no leg or flag changed, no baseline blessed. OWNER: nothing CALLS `resolve` yet — the packet's Files name this file and its checks, the writers are P4.3-P4.6 and the reader is `AgentInboxRowBuilder` once one of them wires a fact, so the desktop still shows every row as `.active`. Same shape as P3.4's `sortForInbox`, which landed a run before its list view. |
| P2D.5-child-rollup | done | 0279b9c | 2026-07-27T08:05:00Z | `ChildRollup` + `InboxSort.rollups(in:)` (what is under each parent) and `LifecycleBlockers.includingDescendants` (a descendant's blockers folded into the parent's before P4.2's `resolve` sees them, so they land on the rung that already outranks "I said done" and a snooze — no new precedence). THE TWO WATCH-OUTS, decided and asserted: TRANSITIVE (a fold hides grandchildren too, so direct-children-only would under-report exactly the rows that vanished) and each descendant counted ONCE — the fixture is three deep so those are two different wrong numbers (1 vs 3 against 2). DERIVED, never stored. Measured on the same pre-fold list as `parentIds(in:)` and by the same `depth > 0` test, so the rollup and the disclosure triangle cannot disagree that a group exists; a child promoted to a root because its parent settled into history is nobody's rollup, reusing P2D.4's cross-block witness. RENDERED on the card's meta line and ONLY while FOLDED (an expanded parent's children speak for themselves), LEADING the line because it truncates by tail and the tally must survive the squeeze, and joining that line rather than taking a fourth because `rowHeight` is derived from exactly three lines of type — card height asserted unmoved at 79pt. CODEX (gpt-5.5), two rounds, both FIXED not argued away: round 1 found the packet's "settling a parent with a blocked child is refused" simply unimplemented — `InboxBulkAction.settle` only ever read the row's own state — so `isAvailable(for:rollups:)` now withholds Settle while a descendant is blocked or running, threaded through the bulk bar, the context menu and the tooltip ('… has work under it waiting on you.' / '… still running.'), defaulted to empty so a caller with no list gets the pre-ticket rule exactly. Round 2 found `holdsParentOpen` counts STATES while `LifecycleBlockers` has two members (`.sessionRunning`, `.queuedTurn`) no state can express: UNCLOSABLE here rather than deferred — a row carries no blocker facts (P3.1) and nothing constructs a `LifecycleBlockers` in production yet (P4.2 landed `resolve` with no caller) — so the rule is pinned exhaustively over all five states instead. THAT SWEEP CAUGHT A CLAIM OF MINE: I first wrote it as an equivalence with the row's own settle rule and it went red on `working` — a working row is settleable on its own account and a working CHILD is not ("blocked or running" is the packet's wording), so the asymmetry is deliberate and the sweep states the rule per state. `failed` deliberately does NOT hold a parent open, matching what a failed row answers for itself. 12 NEGATIVE TESTS OBSERVED RED at exit 1 against the final code, quoted at the checks: direct-children-only; the parent inside its own subtree; `parentId` without the `depth > 0` test; rollups measured on the COLLAPSED list; the cell drawing it whatever the disclosure says; `metaText` appending instead of leading; the `holdsParentOpen` term dropped from settle; `holdsParentOpen` as `needsYou` only, as `needsAnyone`, and as `children > failed`; the menu not passing rollups to the bulk rule; and `includingDescendants` as `self` / as `reduce(.unblocked)` / with `intersection` / first-descendant-only. TWO ARE REPORTED WHERE THEY LAND, not where they were aimed — the set-algebra block catches three of the four `includingDescendants` mutations before the named witness evaluates, and the named `failed` case catches two `holdsParentOpen` mutations before the state sweep — so each was re-witnessed with the earlier check removed ('a blocked child outranks the parent\'s own "I said done" — got settled(at: …)'; 'a `ready` descendant must not hold its parent open — held true'). A thirteenth, `resolve`'s blocker rung made to defer to `.settled`, is red one check earlier at P4.2's own table, which is where that dependency belongs. Matrix green at exit 0 on two full runs incl. the iOS leg; no leg, flag, assertion or baseline changed and the inventory is unmoved (the new functions are called from inside existing `run*Checks()`, not from a `main.swift`). The intermittent `--browser-inspector-network-lite-check` WKWebView failure hit twice mid-ticket — pre-existing and unrelated: 3/3 green in isolation with this diff, 4/4 at HEAD without it, and P4.2's entry already names it. OWNER: nothing produces a PARKED row yet — `AgentInboxRowBuilder` still hands every row `.active` — so the derived half is a proof about a function and the slim variant's silence about rollups is not yet reachable; P4.3–P4.6 write the facts. Also, only `.settle` consults the rollup: archive/delete refuse a running ROW and extending that down the tree is P2D.6's question about what a fan-out means, not this packet's. |
| P2D.6-fan-out | done | 80e8873 | 2026-07-26T18:16:00Z | `AgentSupervisor.fanOut` + `AgentRecord.sourceItemId` + selection/check-off in `TicketQueueTileNSView` + `agent.fanOut` palette command. N items -> N agents, one prompt and one worktree each, siblings unless an orchestrator asked. `FanOutReport` accounts for every item as launched/deferred/refused with the cap it was held to (4, the twin of `maxChildrenPerParent`); a parented batch is also held to the parent's remaining child slots; an item that already has a live agent is refused, not double-spawned. Completion fires only on `.turnCompleted(.completed)` and hands the item id to the SOURCE SURFACE. THREE PACKET DEVIATIONS, all forced: the tile had no row selection to reuse (added, gated behind the new handler exactly as `dispatchHandler` gates the row buttons, so the no-handler render is unchanged); it has no 'existing update path' to mark an item done (Linear rows are read-only here and conductor is read from dotfiles, so the check-off is tile render state + `completedFanOutItems`); and the new status label takes `.lightGray`, not `TextToken.textSecondary`, because this pre-token tile paints its own dark fill and the token resolves dark in Aqua. New leg `--agent-fanout-check` against a real temp `git init` repo: 3 rows selected through the rendered checkboxes -> 3 agents on 3 git-confirmed worktrees with their own prompts; completing one checks off exactly that row; 6 items -> 4 started + 2 deferred and RENDERED; a repeat refused; a second supervisor over the same store still resolves item -> agent; a parent with 3 of 4 slots used falls to cap 1; plus P2A.6-style reachability (registry + palette row + install-branch scan). NINE negative tests observed red. Matrix green on three full runs incl. iOS; inventory blessed (+1 check, +1 leg, 279 records). The intermittent `--browser-inspector-network-lite-check` failure hit twice, 3/3 green in isolation (already named in P4.2/P2D.5). Codex: 4 items - 2 fixed (completions were never replayed onto a rebuilt tile; the check bypassed the app path), 2 answered in the commit body. OWNER x2: (1) nothing in this app has EVER fetched Linear rows into the ticket-queue tile, so the fan-out controls - like the pre-existing per-row dispatch - have nothing to select in the running app until that fetch is wired (needs an API key; outside this packet's Files); (2) the check-off is in-memory, so a relaunched tile draws every row unchecked even though `sourceItemId` survived. |
| P4.3-auto-settle-inactivity | done | a98aa19 | 2026-07-26T18:52:00Z | `AgentAutoSettleConfig` (Core, the `FocusBorderConfig`/`AgentModelConfig` shape: key + default + clamp + `resolvedFromDefaults`) bound by ONE `SettingsField.choice` under Settings ▸ Agents. Default 3 days, stored values clamped to 1-90, explicit `"Off"` = nil = disabled (the packet's nullable-off spelling; never an empty string). `Resolution.window` does days x 86,400 because P4.2's `resolve` takes seconds and no calendar — rung 5, `.neutral` only, so the keep-active pin suppresses it and any blocker outranks it. `AgentLifecycle.swift` gained ONLY the rung-5 doc pointer: the rung already existed and its `>=` already gives the packet's threshold semantics, so no behaviour change was needed there. INACTIVITY = the AGENT's (`lastActivityAt`), never the human's reading. Two decisions: the picker offers 1/2/3/5/7/14/30/90 rather than ninety rows (the 1-90 range still holds for stored values — "45" resolves, "0"/"1000" clamp), and an unparseable value falls back to the DEFAULT not to Off (a silently disabled sweep is invisible until the list is a graveyard). 3 negative tests observed red: unset resolving to nil, the field bound to a near-miss key (`continuum.agents.autoSettleDays`), and `>=` -> `<` in `resolve` (red both in the new checks and in P4.2's own AgentUIChecks witness). Behaviour checks are driven by `resolvedFromDefaults(...).window`, not a hand-written `3 * 86_400`, so a wrong conversion cannot pass. Matrix green twice; inventory blessed in the same commit (count ContinuumRevivedCoreChecks 30 -> 31, 279 records). Codex: 4 items — the option-subset (low, reasoned away in the commit body) and one HIGH left open for the owner: **`AgentInboxRowBuilder` still hardcodes `InboxLifecycle.active` ("P4 populates these two"), so nothing in production consumes this window yet.** It is outside this packet's named `## Files` and NO Phase-4 packet authored so far claims it; wiring needs `AgentRowContext` to carry the five stored lifecycle facts (it carries only `createdAt`). Flagged, not improvised — someone should author that ticket before P4 is called done. Note: `ContinuumRevivedCoreChecks` run bare has a flaky ssh/tmux leg (`script: tcgetattr/ioctl` on a socket) that failed ~2 of 5 standalone runs and never under `run-matrix.sh` — unrelated to this ticket. |
| P4.4-auto-unsettle | done | b97e402 | 2026-07-26T19:10:00Z | `SettledOverrideClearReason` (activity vs user) + `SettledOverride.afterActivity()` in AgentUI, and the writers in `AgentSupervisor`: `send` clears a settle (a user message is the plainest activity), `deliver` clears on `isRealActivity` — a total switch over all 12 event cases, `.starting`/`.running`/`.turnStarted`/`.requestOpened`/`.userInputRequested` in, and **`.ready` deliberately OUT** (the packet's watch-out: settling into ready IS the normal end of work). THE CLEAR FORCES A PERSIST: `.requestOpened`/`.userInputRequested` are not `isPersistWorthy`, so without `|| unsettled` the agent would read `.neutral` in memory and come back `.settled` next launch (negative test 4 proves it). DELIBERATE NARROWING, recorded rather than improvised: only `.settled` clears — resetting the keep-active `.active` PIN would discard a human decision and let the next inactivity sweep bury the row, i.e. the same stale-override failure inverted (codex flagged it as guessing; kept with the reason in the commit body). Attribution is a dict AND a log line through the file's one `warn` seam, because the question is usually asked after the process that held the dict is gone. `clearSettle(agentID:)` is the human's separate path (reason `.user`); nothing calls it yet — the settle button is a later Phase-4 ticket. Verification: new `checkAutoUnsettle` as section 15 of `--agent-supervisor-check`, every settled agent made settled BY THE STORE and adopted with `restore()` (so the clear is observed against state this process never wrote, which is what a relaunched settled agent is). 7 negative tests observed red at exit 1: deliver not clearing; `.ready` classified as activity; `send` not clearing; the forced persist dropped; `afterActivity()` clearing the pin too; the activity clear not logged; `.userInputRequested` demoted. Also asserted no-ops: 12 observer-shaped events, `stop()`, `focus`/`focusTile` (reading is free, P4.9), `branchContext` (the header refresh), and `restore()` itself. A `qaDeliver` seam was added and documented: every event today arrives inside a `send`, which clears the settle itself, so there is no other way to observe an ARRIVING event hitting a still-settled agent (the production caller is Phase 5's persistent session). Codex: 2 medium fixed (an end-to-end `.userInputRequested` witness; the attribution is now observable through a real logging path, both asserted), 1 medium reasoned (the pin), and its one HIGH — `deliver` stamping `lastActivityAt` for every event, not just activity — DECLINED with reason: that stamp is pre-existing P2A.3 behaviour outside this packet's ask, none of the three excluded triggers (status poll, observer refresh, viewing) goes through `deliver` at all, and events only arrive during a turn, so idle chatter cannot hold the P4.3 window open. Flagged for the owner rather than changed. Matrix green at exit 0 (second run: first hit the known pre-existing `network-lite` WKWebView flake). Inventory unchanged at 261 — no new leg, flag or checks target. |
| P4.5-snooze-presets | done | 180a441 | 2026-07-26T19:22:00Z | `SnoozePresets.swift` in AgentUI (Foundation-only, so iOS can consume it): `SnoozePreset` = in 1 hour / this evening (18:00, suppressed when under an hour away, and after 18:00 too — a wake-up in the past is not an option) / tomorrow 09:00 / next week = the FOLLOWING Monday 09:00 (the weekday walk starts at TOMORROW, which is why a Monday lands +7 and a Sunday lands +1). Plus `SnoozeOption` carrying copy and date together — a menu that rendered the title and resolved the date a moment later would resolve "this evening" against a different `now`, which is exactly the preset where offered-ness and date must come from one instant. All calendar arithmetic: `date(byAdding: .day)` + `date(bySettingHour:matchingPolicy: .nextTime)`, never seconds. `.inOneHour` adds an `.hour` component on purpose — it promises ELAPSED time, so on the spring-forward morning 01:30 + 1h really is 03:30, and across fall-back the wall clock reads 01:30 twice. SCOPE HELD: nothing writes `snoozedUntil`; the shelf and wake logic stay with P4.6/P4.7, and `InboxLifecycle.resolve` already handles the date once a caller stores one. Verification: `runSnoozePresetChecks` in the existing AgentUIChecks leg (fixed Gregorian/`America/New_York`/`en_US_POSIX` calendar, `now` injected from components — no clock, no locale read). THE DST WITNESS IS A COMPARISON, per the packet: every transition case pins our answer AND that `+86_400` in the same calendar gives the wrong hour (03-08 10:00 spring, 11-01 08:00 fall) or the wrong day. 5 negative tests observed red: tomorrow on today's day; `isOffered` always true; tomorrow as `now + 86_400`; the weekday walk starting today. THE FIFTH IS THE ONE WORTH KNOWING — `dayAfter` written as `startOfDay(date + 86_400)` PASSED the entire suite at first, because setting 09:00 afterwards repairs a naive day-step mid-day; it only breaks near midnight, so the DST leg gained a 23:30 spring-forward case (naive skips to 03-09) and a 00:30 fall-back case (naive stays on 11-01), and it is red on both now. Codex (gpt-5.5): no correctness bugs; its medium finding (`.inOneHour` unchecked across the repeated hour) fixed with a fall-back 01:30 case asserting 3,600s elapsed; its low finding (red-first notes are prose, not mechanical evidence) left as is — that is the convention in every checks file here and each observation was made in this run. Matrix green (second run: first hit the same pre-existing `network-lite` WKWebView flake P4.4 recorded). Inventory unchanged — no new leg, flag or checks target. |
| P6.0-prose-is-not-a-card | done | 7c5e0b6 | 2026-07-26T19:52:00Z | Pulled forward out of phase order by the owner. `TranscriptProseView` beside `TranscriptCardView` in the packet's one file, with `makeTranscriptEntryView(for:)` as the single kind->presentation decision (both the tile's stack and the tour's per-kind gallery go through it, so the two cannot drift); `TranscriptEntryView` protocol (`NSView, TokenThemed` + `apply(_:)`) is what lets `cardViewsById` hold either, and both classes still spell `TokenThemed` explicitly so `declaredConformers()`'s source scan keeps seeing them. `.message` = no border, no fill, no title, no status word, prose on `SurfaceToken.tileBody`; `.userMessage` = the same prose over a `SurfaceToken.cardUserMessage` fill at `Radius.card`, no border and no caption; the four record kinds are UNCHANGED. `TextToken.textPrimary` on `tileBody` is NOT a new pair — `textPrimary.legalBackgrounds` is `SurfaceToken.allCases`, so the 452-pair sweep already gated it (15.5:1 light / 14.0:1 dark); contrast still has 0 exemptions and now prints 1140 pairs. FIXTURE GREW, FLOOR DID NOT MOVE: `minimumCardBordersPerAppearance` stays 3 and two kinds left `TranscriptCardView`, so `managedAgentFixtureEvents` gained a COMPLETED `.plan` item (completed, so the Lab's `activeToolCount == 1` still holds) and `makeManagedAgentFixtureView` appends a real user prompt — post-change count is **3 structured borders per appearance, 6 across both**, and the user turn is also what puts the new owner under the appearance sweep (an assistant turn paints nothing, so without it `tokenAdoptedOwners` would have gone red on an owner that painted nothing). Appearance sweep 57 views / 84 slots (floors 27 / 53). NEW ASSERTION in `--ui-pixel-check` (no new leg, no new check record — inventory unchanged): per prose view, per appearance — identifier prefix, `borderWidth == 0` (WIDTH not colour: `CALayer.borderColor` defaults to opaque black, so a nil-colour rule is red on correct code — found by running it), fill absent for `.message` / exactly `cardUserMessage` for `.userMessage`, radius follows the fill, insets measured off the label's ALIGNMENT rect (an `NSTextField`'s frame sits 2pt outside it, so comparing the frame reported 10.0 vs 12.0 on correct layout — also found by running it), plus a RENDERED delta over the leading padding strip, because "no chrome" is otherwise satisfied by drawing nothing. Threshold 0.02 is measured: 0.054 light (the binding case) / 0.095 dark, 0.000 when the fill is dropped. 7 negative tests observed RED at exit 1 against the FINAL code: prose given a border ("prose paints a 1.0pt border in #767C86FF"); `.userMessage` on the `.message` path ("a user turn paints nil in light, expected cardUserMessage = #E5EEFBFF"); `cardUserMessage` set to `tileBody`'s value, which the property arm passes and the pixel arm does not ("delta 0.000 needs >= 0.020" — the pixel arm is not dead); `.toolCall` routed to prose ("only 2 transcript-card border(s) probed, needs >= 3" — the floor is not vacuous); the owner removed from `tokenAdoptedOwners`; the appearance override dropped ("kept the sentinel across an appearance flip"); the identifier prefix changed. WITNESS 6 IS RED ONLY THROUGH THE NEW ASSERTION — `--ui-probe-check`, `--ui-geometry-check`, `--ui-baseline-check` and `--ui-contrast-check` all stayed at exit 0 with the prefix changed, because they match on view class and tree position and merely PRINT the identifier; the packet asked me to say so, and this is the honest answer: nothing else would have caught a silently un-probed transcript. 4 baselines re-blessed (both tile fixtures x both appearances), all four diffs opened in both appearances first, worst delta 26.5% of `tiles.managedAgent` (the transcript redrawn); `--ui-baseline-check` re-run 3x after blessing at 0.0000%, deterministic. Blessed TWICE, deliberately: once for the split, again after codex's packet-literal fixes moved the assistant's vertical padding. Codex: 6 findings — radius on `.message`, vertical inset on `.message` (both packet-literal: "Inset.card on prose is horizontal padding, not a card"), the fill oracle read from the code under test (now restated in the gate, so a wrong token cannot self-validate), and the missing radius/inset assertions all FIXED here; the "guessed threshold" answered by recording both measured deltas at the constant; the exact-identifier gap reasoned away at the assertion (the gate has no access to the model's card ids, so prefix + non-empty id is the assertable form). Matrix green at exit 0 including the iOS leg; no floor lowered, no check loosened. OWNER: two consecutive ASSISTANT turns are separated by `Space.m` (8pt) over a 16.0pt line pitch — 1.5x the intra-paragraph baseline distance. Reported as a measurement, not fixed: a divider or a wider inter-turn gap is a design decision you have not made. Whether it reads as a conversation is yours to judge (Lab > tiles > managedAgent, or the blessed PNGs) — the gates say only that prose has no chrome, a user turn is distinguishable, and the records are still boxed |
| P6.1-per-agent-model-effort | done | 4abde21 | 2026-07-26T20:38:00Z | Pulled forward out of phase order by the owner. Two `NSPopUpButton`s in the tile's COMPOSE area (not the header - they are about the prompt you are about to send), options read from `AgentModelConfig` and never re-typed, hand-built `NSMenu` + explicit tag range + `autoenablesItems = false` per P3.8/P3.12/P3.14. `AgentSupervisor.setProviderSettings(agentID:model:thinking:)` (rename's shape: validate, write, `persist`) + `providerSettings(for:)`; `runnerConfig(for:)` UNTOUCHED - it already read the record, which is why this needed no schema change, no new persistence and no RPC. `branchSource` renamed `agentSource` (the tile now reads/writes the record through it). THREE TRAPS FOUND BY RUNNING IT, all recorded in the code: (1) an `NSMenuItem` SEPARATOR's default tag is 0, which is the first option's tag, so `selectItem(withTag: 0)` selected the separator and the picker rendered with an EMPTY title - selection is by item now; (2) the compose row's pinned single-body-line height could not hold a popup, so it is now that height PLUS a `providerControlHeight` MEASURED off a popup exactly as P3.8's `scopeControlHeight` is (= 41.0pt today); (3) the approval-dock Lab preview's hardcoded 210pt tiles stopped fitting and laid the transcript out to 520x0 - now derived from the tile's own chrome, and the card's `preferredSize` with it (560x720 -> 560x936, so those two baselines are RENAMED not deleted). THE `next turn` CLAIM IS A DISABLED FIRST MENU ITEM, not a caption: at the 320pt tile minimum there is no room beside two controls, and the packet's rule there is to prefer no claim to a wrong one - which also makes `autoenablesItems = false` load-bearing and assertable (witness 4 drives `NSMenu.update()` itself). Verification: new section 16 of `--agent-supervisor-check` over a `ScriptedAgentRunner` - no Pi, no network, no clock - asserting `runnerConfig(for:)` over a record RE-READ FROM DISK and the vector `PiAgentRunner.processArguments` would build. The fixture's model/thinking differ from the defaults with an explicit vacuity guard: the defaults and a fresh record are equal by construction, so a default-valued agent would leave witness 2 green against the bug. NINE negative tests observed RED at exit 1 against the final code, quoted verbatim in the file: persist dropped; `runnerConfig` fed the global default; the popups left out of `applyComposeAvailability()`; `autoenablesItems` at AppKit's default; the options re-typed as a 5-entry literal; `attach` re-seeding from the global default; the compose row at its old height (this one is red in `--ui-geometry-check`, not here: `measured 24.0, needs == 41.0`); plus the two for codex's findings. Visual: Lab card `managed-agent.provider-controls` on NON-default values (gpt-5.4-mini / xhigh). Its fixture ends mid-turn, so it renders the pickers UNAVAILABLE while `tiles.managedAgent` renders them live - the two PNGs are the visual pair for the in-flight rule, which is otherwise only a Bool. Contrast 1140 -> 1184 pairs, 0 exempt. 4 baselines re-blessed + 2 renamed by size, all six diffs opened in both appearances first; `--ui-baseline-check` re-run 3x after blessing, worst 0.0000%, deterministic. Matrix green twice (iOS leg included); inventory unchanged - no new leg, flag or checks target. Codex (gpt-5.5): 3 findings. Two FIXED with their own red witnesses - only the field that MOVED is submitted (passing both froze the thinking level on a record holding an off-catalogue model), and a refused write puts the record's value back instead of leaving an unwritten choice on screen. Its low finding (the global-default regression uses an isolated `UserDefaults` suite, not `.standard`) reasoned away in the commit body: mutating `.standard` in a matrix run writes the tester's real preferences, the suite exercises the identical `resolvedFromDefaults(defaults:)` path P0.9/P4.3 use, and witnesses 2 and 6 prove the structural claim anyway. OWNER, three things: (1) A ROLE'S DECLARATION VS A USER'S PICK is undecided - both land on the same record field and nothing re-resolves a role after spawn today, so there is no clobber, but a later ticket that adds one needs your ruling (the packet says report, do not pick a winner). (2) The tile's minimum height is still 240pt while the compose area now takes 82 and the header 54, so a minimum-height tile with an approval open has ~18pt of transcript - measured, not changed; raising `TileGeometry.minimumSize` is a product decision. (3) OWED, NOT FAKED: no end-to-end confirmation that a picked model actually answers. That needs `--managed-agent-live-check`, which needs Pi auth and a supervised GUI and blocks in the folder-access NSAlert unattended (P0.8). |
| P3.15-wire-destructive-row-actions | done | bcb6bc4 | 2026-07-27T08:05:00Z | Pulled forward as the queue's highest-priority ticket: the owner could not delete an agent by any route. The real fix is the PER-ACTION gate — `wiredRowActions`/`wiredBulkActions` capability sets beside the callbacks — because `isWired` asked one question for all nine items, so wiring Delete meant also offering Snooze/Wake (no writer until P4.6/P4.7) and the shipped app therefore assigned neither callback for eleven tickets. Live today: Stop Agent / Archive / Delete on the row menu, Archive / Delete on the bar; settle/un-settle/mark-unread/rename left for P3.16 (one set element each now). Both verbs route to the existing `AgentSupervisor.archive` (untouched) behind P3.14's confirmation seam (request struct + injectable provider, so no check can reach `runModal`), and `ArchiveReport` is surfaced — a retained branch reads `Unmerged work kept: ⎇ …`. RESURRECTION CLOSED, and this was the packet's hard part: the record is derived from the tile, so archive alone would have re-minted the agent at next launch. The tile is NOT deleted; `AgentStore` gains a tile-id-only `deleted-agent-tiles.json` (outside `agents/`, so `loadAll` never reads it) and `wireManagedAgentTile` reads it before spawning — a prompt submitted in that tile clears the mark and revives deliberately. 8 negative tests observed red at exit 1 against final code: production `configureInboxActions` call deleted; `.snooze` declared wired; durable store delete skipped (caught by FILE existence, not the dictionary); `stop` reordered after the delete ('the record is on disk again'); retained branch dropped from the message; confirmation bypassed; respawn guard removed; ignored-rows clause dropped. Matrix green at exit 0, 143 legs, iOS leg included. Codex cross-review: 7 findings, 4 fixed (mixed selections now report the non-managed rows instead of dropping them; the message names only agents actually removed; tombstone encodes `[UUID]`; the resurrection check now runs the boot walk's own `installInitialManagedAgentTile` plus a source assertion that the walk calls it). 2 stand as STATED LIMITS for the owner: (a) the relaunch is modelled inside one process, not a second `applicationDidFinishLaunching`; (b) the tombstone is an app-support sidecar rather than a tile-schema change — the packet's 'mark the tile' option, and it keeps a local-only fact off the sync boundary. Two unrelated legs flaked once each en route (`--browser-inspector-network-lite-check`, the ssh/tmux CoreChecks leg wanting a tty); both pass standalone and in the green run. |
| P3.16-inbox-lists-agents-only | done | f29d9ff | 2026-07-27T10:05:00Z | Pulled forward at the owner's instruction: the list was full of things that are not agents. Plain shells were ALREADY excluded — what they saw is a terminal tile RUNNING an agent (a descriptor with a non-nil `AgentDescriptor`, which `agentIdentities` admits on purpose for the canvas badge, the outline, the rollups and the phone). The change is one carried value, not a filter at the row build: `AgentActivitySurface.managedAgentIdentities` = the agents an `AgentRecord` backs (the supervisor's own records — headless, tiled, orchestrator children), derived in the same fold as `agentIdentities` so the two cannot drift, read by `buildAgentInboxRows`. `agentIdentities` is UNTOUCHED; narrowing it is the wrong layer and P2B.5's two witnesses catch it (negative test 4 ran that fork: `--agent-inventory-wiring-check` and `--workspace-sidebar-live-status-check` both red). FORCE-INCLUDE ANSWERED, not discovered: the scope decides what is VISIBLE, the row source what EXISTS — the check's focused tile IS the terminal-hosted agent and it is still not a row. The loss stays visible: a THIRD empty message ('No managed agents · terminal agents aren't listed'); no new scope entry, deliberately, because a scope filters rows that exist. 8 negative tests red at exit 1 against final code: row source back to `agentIdentities`; keyed on tile ids; children dropped; `agentIdentities` narrowed (P2B.5 x2); third message collapsed; count wiring dropped; `maximumNumberOfLines` = 1; managed-SCAN row source. Codex cross-review found one real defect in four shapes and it was right — the first version's row source (the managed scan) admitted a legacy `ManagedAgentSessionRecord` no `AgentRecord` claims, which is not `AgentRecord`-backed; fixed to the supervisor's records, fixture grew such a session, and it is asserted out (it would have drawn a nameless 'Agent' row no menu verb can act on). OWNER: (a) STATED LIMIT — a legacy managed session is in neither set, so an inbox holding nothing else says 'No agents yet' in front of one; pre-P2A artifact, every managed tile today registers an `AgentRecord`. (b) The truncation gate was first written as a view in a 220pt window, which AppKit widened to 296pt where the message is one line — green against a 1-line label, proving nothing; it now measures the string at `minWidth - 2*Space.l`. Matrix green at exit 0, 143 legs, 0 FAILs; one earlier run failed only `--browser-inspector-network-lite-check`, which passes standalone and passed in both later full runs (same flake recorded under P3.15). |
| P4.6-snooze-raised-hand | done | 27b4cd6 | 2026-07-27T12:45:00Z | `InboxLifecycle.raisedHandWhileSnoozed(record:now:)` over a new `SnoozedAgentFacts` VALUE type — AgentUI may not import Core, so the packet's `record:` is the facts subset the predicate reads and nothing else. Plus `snoozeHonoured(record:now:)`, which is what makes "wake" visible: it WITHHOLDS the shelf date so `resolve` falls through step 2 exactly as an expired snooze does. No new rung and no fifth `LifecycleBlockers` bit — P4.2's 31-case table and 1,728-case sweep still cover the precedence unchanged — and the STORED snooze is never cleared, so a woken row can be re-snoozed without losing that it was deferred (the packet's watch-out). SCHEMA ADDITION: `AgentRecord.snoozedAt`, optional and reference-interval encoded like the other three lifecycle dates; `currentSchemaVersion` deliberately unmoved (codex finding, argued not changed: an optional field both builds decode as absent is what `decodeIfPresent` is for, and it is the convention P4.1's three dates and P2D.6's `sourceItemId` set). THREE DECISIONS THE PACKET LEFT OPEN, each argued at the code: (1) a pending approval/question wakes with NO newness test — it is unanswered by definition and `resolve`'s step 1 already un-shelves a blocked row however old the block is, so a newness test here would let the two disagree about one row; (2) the newness test is STRICT (`>`) — a failure at the same instant as the snooze reads as one the human had in front of them, because the alternative is a row that wakes itself the moment it is parked; (3) A LIVE SNOOZE WITH NO `snoozedAt` SUPPRESSES BOTH DATE SIGNALS — only a pre-P4.6 record can be one, and waking on any failure it cannot date would wake permanently and delete the snooze for that record, while a pending request still wakes it. Codex named (3) as invented semantics; it is recorded here as the explicit migration decision it asked for, and the alternative it proposed (stamp a baseline `snoozedAt` on meeting a legacy snooze) needs a writer, which does not exist yet. VERIFICATION: a 15-case predicate table (the packet's five among them, `snoozedAt - 1s` as the witness), the boundary swept -5…+5s for BOTH date signals (22 resolutions, 10 wake / 12 stay — the table's two hand-picked instants stay green under a `>=` spelling, which is why the sweep exists), and a surfacing check composing `snoozeHonoured` with `resolve` and `InboxAttention.resolve` → `.woke`. 6 NEGATIVE TESTS OBSERVED RED at exit 1 against the final code, quoted at the checks: `>=` for `>`; nil `snoozedAt` as `.distantPast`; the pending signal made to require newness; `snoozeHonoured` reduced to the stored date; the not-snoozed guard deleted; the completed-run signal dropped. TWO MORE on the record half in CoreChecks, reported where they land: the two snooze dates collapsed onto one key is red at the lifecycle sweep (200 of 200 differed), and `snoozedAt` dropped from `encode(to:)` with that sweep blinded is red at the named assertion ("when a snooze was set and when it ends are two facts — snoozedAt nil"). Matrix green at exit 0 including the iOS leg; inventory `ContinuumRevivedAgentUIChecks` 11 -> 13, regenerated and committed; no leg, flag, assertion or baseline changed. OWNER: NOTHING WRITES A SNOOZE YET — `ContinuumApp.wiredInboxRowActions` still omits `.snooze`/`.wake` and `AgentInboxRowBuilder` still hands every row `.active`, so this is a proof about a function, the same shape P3.4 and P4.2 landed in. Codex's two HIGH findings are exactly that gap; held as scope rather than fixed, because wiring a reader while no writer exists produces a path that is permanently false. P4.7 (the shelf) owns the menu write and the row-level check, and is where that debt is owed. Codex could not run the checks itself (read-only sandbox denied its build cache) — the green above is mine. Attempt 1 killed by the SUPERVISOR ~11 min in for an owner build; taken fresh from the packet, the stash was not restored. |
| P4.14-row-status-is-turn-state | done | 9759e99 | 2026-07-27T17:30:00Z | `AppDelegate.agentRowStatus(liveStatus:hasLiveRunner:persisted:)` replaces `agentStatus(for:)` as the ONE row-status derivation both branches of the managed scan call — the owner's stuck row was PROCESS LIVENESS rendered as TURN ACTIVITY (`ManagedSessionStatus` has no alive-and-idle case, nothing writes the field after creation, `.running → .working`). Precedence: live tile view → a runner in flight (`AgentSupervisor` holds `runners[id]` for exactly a turn) → the persisted field, which now contributes only what it can witness. `.running` contributes NOTHING and reads idle; `.stopped` reads stale so a dead session stays distinguishable from a live one. No lazy `.stopped` write, no freshness window. New section 17 of `--agent-supervisor-check` samples one scripted offline turn at three points through BOTH the real call site (`currentManagedAgentActivities()` over the owner's exact on-disk record) and the production row path (`refreshAgentSurfaces` → P2B.4 snapshot → `AgentInboxRowBuilder`, reading ready/working/ready). All 6 required witnesses observed red at exit 1 incl. the owner's symptom verbatim: "the sidebar row still reads working after the turn ended, while the tile reads done". Matrix GREEN at exit 0 (188 passed, inventory 279, 60 baselines matched); ContinuumApp.swift assertions 1356 → 1378, nothing loosened. BEFORE/AFTER for the owner: zone rollups counted every spawned agent as working — the wiring fixture's zone goes working 1 → 0, the sidebar live-status zone 2 working → 1 — because those counts were repeating a field written at spawn. A managed agent's row in the sidebar TREE now reads "unknown" where it read "working" (that tree folds configuring/idle into one muted kind, long-standing); it is still NOT "no agent", asserted separately so P2B.5's defect cannot come back. The INBOX row reads "ready". Four other fixtures moved with the derivation, each with the reason at the assertion (cross-project walk, inventory wiring incl. the ⌥-cycle now filtering on `.idle` with `.working` asserted EMPTY, observer independence, sidebar live status). Also repaired a LATENT RACE my timing shift exposed in `checkDetachOutlivesItsTile`: `probeAtClose` was captured before the blocking turn's `.turnStarted` reached the probe, so `== +1` watched 3 → 4 → 5 and never matched — measured 4 fails/23 runs with this change vs 0/30 without, 12/12 green after (a condition added to the wait, not an assertion relaxed). Codex gpt-5.5 cross-review: 2 findings real and fixed (the two stale fixtures), 2 were the row-builder verification gap and are closed, 1 declined with reasoning in the commit body (liveStatus outranking hasLiveRunner is what keeps tile and row identical in the ms between `send()` and the first event, and a live view sees blockers a runner boolean cannot). OWED: a supervised prompt-to-done pass in the real app — `--managed-agent-live-check` needs Pi auth + GUI and blocks on an NSAlert unattended. |
| P4.7-snoozed-shelf | done | 4b604fe | 2026-07-27T20:15:00Z | `InboxSort.partition(rows:now:)` + `InboxPartition` (pure, order-preserving, exhaustive and disjoint by construction) and a SECOND KIND OF TABLE ROW in the view (`InboxListItem.shelfHeader`), not an `AgentInboxRow` in disguise — P3.16 had just finished making this list agents only, and a heading in `rows` would join the selection, the bulk actions, the menu and the jump chords. The view CONCATENATES the sections rather than partitioning in place, which is load-bearing: `sortForInbox` blocks a `.snoozed` row by its case alone, so a snooze that ran out since the row was built would still sit in the middle block. The packet's woken-agent rule holds twice with NO new precedence — an expired snooze is answered by `section(for:now:)` on `resolve`'s own `>` boundary, and P4.6's raised hand never arrives as `.snoozed` (`snoozeHonoured` withholds the date, step 1 answers `.active`). The watch-out is structural: the partition reads `lifecycle` and nothing else, so it cannot re-decide P4.2's blocker precedence, and the check drives that through the real `resolve`. COST: two index spaces, paid in one place — `setItems` builds both maps in one pass, every AppKit entry point (`clickedRow`, hover, menu, selection) converts, `viewFor` fails closed instead of defaulting to row 0, `shouldSelectRow` keeps the heading out of a selection, and the empty state hides on `items` so a list holding only a folded shelf cannot say 'No agents in this scope' above a `Snoozed (2)` line. 5 negative tests observed red with the final code: shelf open by default (`got 7 rows of 7`); expiry dropped from `section(for:)` (`got ["A1", "C3"]`); the heading made selectable (`refusing rows []`); the header drawn but nothing folded (shelf baseline red at 3.38%/3.30%); `rowIndex(forTableRow:)` reverted to the identity (`a click below the heading reveals its own agent — got []`). 4 baselines written after inspecting the renders and diff images — `chrome.agentInbox.parked` now OPENS the shelf so the slim variant and its accented glyph stay under the contrast/pixel/baseline sweeps, and the new `chrome.agentInbox.shelf` card holds the collapsed default; the parked diff is exactly the three parked rows moved down one heading. Lab check run 3x identical; `minimumCardsGated` 58 -> 60. `--ui-probe-check` correctly refused a `TokenThemed` type nothing renders, so `appearance.agentInboxShelf` was added to the sweep. Matrix inventory unchanged (no new flag or leg; the new check is called from `runInboxSortChecks`, not main.swift). Codex gpt-5.5: 0 correctness bugs, all 3 findings fixed — the `?? 0` fallback, the QA gestures now round-tripping through the table row the way `clickedRow` does, and a new block exercising reveal/selection/hover/menu/rename on a settled row BELOW the heading where the two index spaces differ. Owner: the shelf is view-local and collapsed on every launch, per the packet; remembering it is a defaults key beside the scope's. |
| P4.8-settled-tail-paging | done | 0bded65 | 2026-07-27T22:55:00Z | `InboxSort.pageSettled` + `SettledPage` page the settled tail: 10 on screen, the rest behind a `Show 25 more (N settled hidden)` footer that adds 25 a press and goes away when history runs out, and THE PACKET'S WITNESS — the agent open in the focused tile force-included wherever it falls, IN PLACE and without costing a recent row its slot (30 settled, open at 27: 11 shown, 19 hidden). The page is a PREFIX of the order it is handed, so history stays `mostRecentlyEndedFirst` and P3.4's frozen order never reaches it; the active block and the shelf are not pageable at all. `settledLimit` is view-local like `shelfExpanded`. Fixed on the way: `shelfHeaderTableRow` matched "first row that is not an agent", which the footer would have answered — now matched on the case. NEGATIVE TEST OBSERVED RED: dropping `|| row.id == openAgentId` fails the witness at exit 1 ('got [S0…S9]'). TWO FILES OUTSIDE THE PACKET, both forced by real gates, neither a weakening: `AgentInboxSettledMoreView` is a new `TokenThemed` conformer and `--ui-probe-check` failed it as declared-but-never-swept, so `LabFixtures.inboxPagedRows()` (12 settled) + an `appearance.agentInboxSettledTail` sweep surface were added — NOT more rows on `inboxParkedRows()`, whose two committed baselines exist to hold the card-vs-slim comparison, and NO baseline was blessed (sweep surfaces carry no PNG; themed views 111→112). CODEX raised one finding and it was taken: the first draft's footer clamped to `min(25, hidden)` ('Show 20 more' over 20 remaining), which reads better but is a product change the packet did not ask for — the packet spells the string out, so the literal wording shipped and the clamp is left to the owner as a one-line change. Resumed from the supervisor-parked `stash@{0}`, which held a complete attempt that had never cleared the ui-probe sweep. |
| P4.9-reading-is-free | done | 7dd5062 | 2026-07-27T23:55:00Z | NO PRODUCTION CHANGE, and that is the finding: P3.9's `revealAgentFromInbox` was already written as plain navigation (attach a view if needed, focus it, clear the read-mark, touch no lifecycle) and `attach`/`wireManagedAgentTile` deliver no event, so nothing on the open path un-settles. This ticket is the GATE — section I of `--agent-inbox-check`, on a genuinely SETTLED agent adopted from the store, because section C's 'no lifecycle moved' line runs over `.neutral` agents where a reveal has nothing to move. Through the app's own click: the tile is attached and focused with no second agent minted, the unread mark is cleared (the half that IS allowed to move — the packet's watch-out, and the mark is EARNED by a `.turnCompleted` delivered with focus elsewhere, which doubles as a witness that a turn ending does not un-settle), and the override, the clear attribution, `settledAt` and the record ON DISK are all untouched; a second open takes the focus-only path with the same result; then the tile's own production prompt closure un-settles it to `.neutral` attributed `.activity`, in memory and on disk. 2 negative tests observed red with the final code: `clearSettle` added beside the reveal's `focus` — the packet's own witness — ('opening a settled agent must leave it settled — got Optional("neutral")'), and `InboxLifecycle.resolve`'s settled rung returning `.active`, REPORTED WHERE IT LANDS (the pre-state guard, 'tail []'), which is what proves the tail assertions read the production resolution and not a hand-placed row. **HONEST LIMIT, and codex's one HIGH:** `AgentInboxRowBuilder` still hardcodes `InboxLifecycle.active` ('P4 populates these two'), so a shipped row cannot say which section it draws in — 'stays in the settled tail' is asserted over `InboxLifecycle.resolve` + `InboxSort.partition` reading the record the real reveal left behind, and the check's summary line says so rather than claiming the rendered list. **OWNER: this is the SECOND ticket to hit the unauthored builder-wiring gap** (P4.3's review flagged it first). Nothing in production consumes P4.1's stored facts; wiring needs `AgentRowContext` to carry them, and it is a list-wide presentation change (rows going slim and into a tail) that wants its own packet, baselines and an owner decision — improvising it here was refused. Codex's other two items are the same gap from the other side (`inboxSections()` duplicates the resolution because there is no production fold to reuse) and are recorded at the code. Matrix green at exit 0, twice; inventory unchanged (no new leg, flag or checks target). |
