# Overnight execution progress

Durable ledger for the Ralph loop. One row per attempted ticket. Source of truth for "done"
alongside the git log on `overnight/agent-orchestration`.

| ticket | status | commit | matrix | note |
| --- | --- | --- | --- | --- |
| (infra, not a ticket) | done | f42102a | matrix: green | Pre-existing bug found while running ticket 01: `zoneBoundsConfig group8` check in `ContinuumRevivedCoreChecks/main.swift` looked for `paddingKey`/`emptyMinWidthKey`/`emptyMinHeightKey` in the SettingsSchema `"general"` section, but an earlier unrelated commit (1fc7afb, general-settings split) moved those fields to a `"zones"` section — the check called `Foundation.exit(1)` on the mismatch, which silently skipped the entire back half of the check suite on every run, for every ticket, since before this queue started. Fixed as an isolated one-line commit (verified green on clean HEAD before re-applying ticket work). Flagging for morning review since it blocked 100% of the queue and predates this program. |
| (infra, not a ticket) | done | eb7d4fe | n/a | First iteration hit a harness bug: `scripts/overnight-iteration-wf.js` hardcoded the implementer's repo path to the old `continuum-revived` checkout instead of this `continuum-overnight` worktree — the implementer agent correctly refused to fabricate progress in the wrong repo. Fixed as its own commit so future iterations don't hit it. |
| 01-store-protocol-seam.md | done | c71d601 | matrix: green | Implemented cleanly after the harness fix above. Both Opus and Codex gpt-5.5 raised the same scope note (wf.js touched) which was resolved by excluding that file from the ticket commit. Codex also flagged `ContinuumApp.swift:7134` as an unmigrated `ProjectStore` parameter — verified it's a distinct, private, never-called duplicate of `loadOrCreateProject` predating this ticket (dead code, not reachable from any migrated call site), so left untouched per ticket policy. Both reviewers clear after resolution. |
| 02-op-enum-logged-op-envelope.md | done | a4cba75 | matrix: green | Resolved by hand after the loop livelocked retrying it 3×. The livelock exposed a harness bug (a skipped head-of-queue ticket was re-picked forever); fixed separately. The implementer's final dirty working tree had in fact addressed every review concern before the iteration was cut: `Op.CodingKeys` + payload-key enums use explicit frozen string rawValues; `init(from:)` uses an `AnyKey` raw-key count to reject a payload with a known discriminator plus any extra/unknown key; the `FracIndex` exact-value (`.first==0.25`/`.last==0.75`) and prepend/append (`between(.first,x)`/`between(x,.last)`) tests are present; and the dependencies-guard check now anchors on `.target(name:"ContinuumRevivedCore"`, not the earlier `.library` product substring (false-green fixed). Verified directly: `swift build` clean, `ContinuumRevivedCoreChecks` green (I5 scan 24 cases/0 forbidden tokens/2097 bytes, SpatialOpTests passed, wall-clock guard 0 banned tokens, dependencies guard zero deps), and the full matrix passed. All three prior rounds of Opus+Codex concerns are addressed; committed as the op-log envelope layer. |
| 03-membership-tile-register.md | skipped | - | matrix: green | Build + matrix green, Opus cleared, but Codex(gpt-5.5) did not: the new `Tile.zoneId` LWW register is encoded/round-tripped but not wired into production — `WorkspaceRuntime.swift:194,486` still gates group-zone rendering on `projectId != nil` so persisted `ambientTiles` won't render on relaunch, and `CanvasNSView.swift` still derives live membership from a private geometry-based map instead of consuming/emitting `setTileZone`. Codex also flagged `WorkspaceDocument.setTiles(_:forZone:)` (`WorkspaceDocument.swift:38`) reassigning `zoneId` on existing tile IDs only, silently dropping frame/title/kind/zIndex/metadata/runtimeRef vs the old full-array-replacement behavior — masked by checks that compare tile IDs only (`main.swift:1650,1776`) and an LWW test using a local fold helper instead of production merge/apply logic (`main.swift:1877-1891`). Working tree left dirty (4 files) exactly as the workflow left it, per guardrail: commit only if both reviewers clear. |
| 04-zorder-fractional-index.md | skipped | - | matrix: green | Harness bug hit first: the Workflow tool's `args` did not thread into `scripts/overnight-iteration-wf.js` (ticket path rendered as literal `undefined` in every prompt); the implementer agent recovered by reading the queue/`_PROGRESS.md` itself and correctly built ticket 04 anyway (build+matrix green), but the resulting Opus review was reviewing against the wrong ticket (misidentified as 03) and Codex errored — both false signals, not a real ticket verdict. Re-ran dual review directly against the correct ticket path on the same uncommitted diff (did not re-run Implement, to avoid a second blind pass corrupting an already-correct tree). Real verdict: both reviewers reject. Opus: `FracIndex` is missing required `Hashable` conformance (`SpatialOp.swift:29`); a legacy `Tile.init(zIndex: Int)` shim (`CanvasState.swift:642`) defeats the ticket's intended compile-enforced migration, leaving ~185 call sites including named ones (`ComponentLab.swift:61`, `ZoneRuntimeController.swift:332,447`, `WorkspaceRuntime.swift:792`, `NoteTileNSView.swift:158`) still passing `zIndex:`; missing the ticket-required migration-correctness test, backend real-file `WorkspaceStore`/legacy-`canvas.json` checks, and the visual gate. Codex independently found the same shim/Hashable/test gaps plus a real correctness bug: `.last` (0.75) is a concrete reusable value, not a true sentinel, so `CanvasEngine.bringToFront`/`TileSpawner`/`ComponentLab`/`WorkspaceDocument.nextZonePosition` can hand out a duplicate `zPosition` when the current max is already `.last`; legacy migration maps each rank independently via `forLegacyRank` instead of collection-wide `distribute`, which can overshoot `.last` and feed the same bug. Working tree left dirty (12 files) exactly as the implementer left it, per guardrail. Separately: the `args`-threading bug in the Workflow tool invocation itself needs a fix or workaround before the next iteration trusts its review stage — flagging for morning review since it silently produces a wrong-ticket review rather than an error. |
