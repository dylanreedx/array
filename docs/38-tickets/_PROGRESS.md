# Overnight execution progress

Durable ledger for the Ralph loop. One row per attempted ticket. Source of truth for "done"
alongside the git log on `overnight/agent-orchestration`.

| ticket | status | commit | matrix | note |
| --- | --- | --- | --- | --- |
| (infra, not a ticket) | done | f42102a | matrix: green | Pre-existing bug: `zoneBoundsConfig group8` check read moved SettingsSchema keys from the wrong section and `exit(1)`'d, silently skipping half the suite. Fixed. |
| (infra, not a ticket) | done | eb7d4fe | n/a | Harness bug: implementer repo path hardcoded to the wrong checkout. Fixed. |
| 01-store-protocol-seam.md | done | c71d601 | matrix: green | Clean; both reviewers cleared. |
| 02-op-enum-logged-op-envelope.md | done | a4cba75 | matrix: green | Hand-resolved; frozen wire format, full matrix passed. |
| 03-membership-tile-register.md | skipped | - | matrix: green (headless) | Not cleared after 3 review rounds (Opus+Codex both rejected). Top concerns: out-of-scope ProjectStore.swift saveCanvas re-stamp violates the ticket's "Stop if" file fence (and forces one-way v1→v2 canvas migration on first save); setTiles(_:forZone:) replaces the whole Tile record instead of writing only zoneId (can clobber runtimeRef/metadata/frame from a stale caller, and the new T02 test enshrines it); WorkspaceProfileStore.captureProfile/saveProfile persists the nested WorkspaceDocument verbatim, bypassing the v3 re-stamp so old code can silently drop membership; I5 taint check builds its own allow-list projection (tautological, not the production sync projection). Tree left dirty as the workflow left it. |
| 04-zorder-fractional-index.md | skipped | - | matrix: green (headless) | Fast-forwarded past (2026-07-01, Dylan's call): real correctness bugs + the same class of scope tension as 03 — needs human hands, not more loop time. Rejected attempts preserved via `git stash list`. |
| 05-delete-tombstone.md | skipped | - | matrix: n/a | Fast-forwarded past: another migration re-model, grouped with 03/04 for human review. Prior attempt stashed. |
| 08-sync-observation-type-split.md | pending-retry | - | matrix: green (headless) | Not cleared after 3 rounds (Opus+Codex both rejected). Top concern: verification doesn't gate — ActivityStoreTests is XCTest but `run-matrix.sh` never runs `swift test` (only `swift build` + the `*Checks` executables), so the ticket's "matrix proves it" contract is false; fix = add `swift test` to the matrix or port checks to a `*Checks` executable. Also: `append()` re-folds the whole log every call (O(n log n)/append, quadratic ingest) instead of the prescribed O(1) `apply(snapshot, event)` tail-fold; snapshot-then-tail subscriber contract diverges when a store is seeded with foreign higher-sequence events; `replay(fromSequenceExclusive:)` returns insertion order, not sequence order. Tree left dirty as the workflow left it. |
| 10-session-topology-snapshot.md | pending-retry | - | matrix: green (headless) | Not cleared after 3 rounds (Opus clear, Codex rejected). Top concerns: space/tab-only input classified `malformedLine` but ticket requires `ParseError.emptyInput` for whitespace-only (check only covers newline-only); ticket lines 313-316 demand an empty-sessions snapshot for zero-session output while `parse("")` throws — likely a ticket self-contradiction needing a human ruling. (Codex also couldn't run swift build in its read-only sandbox — advisory only.) Prior 08 dirty attempt stashed as stash@{0}; ticket 10 tree left dirty as the workflow left it. |

| 12-injectable-substrates.md | pending-retry | - | matrix: green (headless) | Not cleared after 3 rounds (Opus+Codex both rejected). Top concern needs a HUMAN RULING: ticket's "Done when" mandates `LoggedOp { opId; payload: Data }` in Core, but ticket 02 already owns the public name `LoggedOp` (op: Op variant) — implementer shipped `TransportLoggedOp` instead (substantively correct, literally unmet; Core now carries two op-envelope types and the "re-home into ContinuumRevivedSync without rename" promise is murkier). Also: production retrofit not performed (TileSpawner/ContinuumApp still call TmuxSession/Process directly — seam unexercised by any real flow); real-path check prints pane_id/isAlive/elapsed to stdout but writes no manifest artifact (ticket line 300 requires one); lossy-transport test only covers dropRate 1.0 (always-drop impl would pass); InMemoryTmuxControl.killWindow leaves an empty session where real tmux destroys it. Tree left dirty as the workflow left it. |

## Reset for the Fable-orchestrated retry (2026-07-01)

Config now: **Fable 5 orchestrates** each iteration, **Sonnet 5 implements**, **Opus + GPT-5.5
review**; the loop's matrix skips the four terminal-**surface**-rendering checks (they can't run
headless — a supervised GUI matrix pass covers them); new files are `git add -N`'d so reviewers see
them. Tickets 03,04,05,08,10,12,14 are reset to un-attempted so the new config re-drives them. The
implementer is instructed to read these prior findings first. Rejected dirty attempts for 04/05/08/10/12
were **stashed** (recoverable via `git stash list`) for human review.

**Prior findings to address on re-attempt:**
- **03 (membership register):** bump-then-save schema-version bug — a v1 canvas loaded via `ProjectStore`
  keeps `schemaVersion==1`; saving with `zoneId` set writes the new field under a v1 declaration, so an
  old build silently drops membership instead of hitting the v2 guard. Also: register must be wired into
  production rendering (`WorkspaceRuntime` gated on `projectId != nil`; `CanvasNSView` used a geometry map,
  not `setTileZone`); `setTiles(_:forZone:)` dropped non-`zoneId` fields; an LWW test used a local fold
  helper, not the production merge path.
- **04 (z-order fractional index):** `FracIndex` needs `Hashable`; remove the `Tile.init(zIndex:)` shim and
  migrate ALL ~185 call sites (compile-enforced); `bringToFront` must not lower an already-frontmost item;
  `WorkspaceDocument` embedded `groupZoneTiles` legacy ranks must be migrated (not collapsed to 0.5); zone
  hit-testing must sort by `zPosition`, not array order; add migration/backend tests.
- **05 (delete tombstone):** the new `ContinuumRevivedSync/Tombstone.swift` was left untracked while
  `Package.swift`/tests depend on it — the `git add -N` rule now fixes this; ensure the new target's files
  are all tracked so a plain commit doesn't break the package.
- **08 (sync/observation type split):** implementation was CLEAN — its only blocker was the headless
  surface-check matrix failure, now fixed. Should pass now.
- **10 (session topology snapshot):** clean; blocked by the surface-check + an untracked new file — both
  now fixed. Should pass now.
- **12 (injectable substrates):** clean (`TransportLoggedOp` naming collision with ticket 02 resolved);
  blocked only by the surface check — now fixed. Should pass now.
- **14 (project session naming):** must actually construct a `ZoneRuntimeController` via the lock-free
  init and exercise `projectSessionName()` / `killProjectSessionCommand()` (the surrogate check only hit
  the pure `TmuxSession.*` statics — zero coverage on the new controller methods). Add the assertion to the
  ComponentLab self-check. Also drop the inline comment claiming `ProjectStore` has no protocol — it does
  (`StoreProtocols.swift:5 ProjectStoring`).

New attempts append their rows to the table above.

| ticket | status | commit | matrix | note |
| --- | --- | --- | --- | --- |
| 08-sync-observation-type-split.md | done | 368cf7e | matrix: green (headless) | Continued the session-limit-interrupted attempt per C-20260701-004 ruling (classified continue-after-reset; pre-workflow safety stash kept). Cleared round 2, both reviewers (Fable + Codex GPT-5.5). Checks live in ContinuumRevivedCoreChecks (no XCTest). Still owes the supervised GUI-matrix pass before merge. |
| 10-session-topology-snapshot.md | done | c8e6a56 | matrix: green (headless) | Retry under the C-20260701-005 ruling (empty/whitespace = zero-session snapshot, no ParseError.emptyInput). Cleared round 2, both reviewers (Fable + Codex GPT-5.5). Checks wired into ContinuumRevivedCoreChecks. Still owes the supervised GUI-matrix pass before merge. |
| 11-activity-tree-snapshot.md | done | 988095e | matrix: green (headless) | Cleared round 3, both reviewers (Fable + Codex GPT-5.5). Pre-committed ruling 642af29: forward-declare minimal AgentSnapshot (ticket 35 unlanded). Name collision found mid-flight: ticket 08 had already shipped a public `ActivityTreeSnapshot` (byTile fold read-model) — renamed it to `ActivityLogSnapshot` and reserved `ActivityTreeSnapshot` for this ticket's SidebarTree envelope (C-20260701-009, resolved in-commit; downstream tickets 21/57/58/61/74 still say ActivityTreeSnapshot where they mean the byTile type — read via the C-009 ruling). Still owes the supervised GUI-matrix pass before merge. |
| (infra, not a ticket) | done | da7810b | matrix: green (headless) | `--session-resume-check` (real ghostty surface, ghostty_app_tick until-condition) started timing out overnight — proven pre-existing at base edf2486 via stash+rebuild+rerun with zero ticket code. Same class as the four gated surface checks (display asleep/locked ≈ headless), so it was added to the CONTINUUM_SKIP_SURFACE_CHECKS gate; it joins the supervised GUI-matrix pass owed before merge. Without this, every remaining ticket would cascade-skip on an unrelated red. |
| 12-injectable-substrates.md | done | 58a6a0a | matrix: green (headless) | Iter-4 attempt was session-limit-killed mid-flight; classified continue-after-reset per C-20260701-004 precedent (safety stash kept as stash@{0}). First workflow run burned 3 rounds on the pre-existing `--session-resume-check` red (see infra row above), never reached review; re-driven after the gate fix and cleared round 2, both reviewers (Fable + Codex GPT-5.5). TransportLoggedOp ruling honored (OpId reused verbatim); round-2 fix retrofitted the one 1:1 production call site (AppDelegate.killTmuxSessionForDeletedTerminalTile) behind TmuxControl and made InMemoryTmuxControl record every protocol call. Reviewer-accepted deviation, documented in ProcessTmuxControl.swift:10-25: argv built directly, not via TmuxSession.wrap (wrap is tileId-keyed `-A` attach; protocol needs name-keyed headless `-d -P -F '#{pane_id}'`, and TmuxSession.swift is fenced). Still owes the supervised GUI-matrix pass before merge. |
| 13-invariant-spine-harness.md | done | d7be1b3 | matrix: green (headless) | Cleared round 3, both reviewers (Fable + Codex GPT-5.5). Round-1 rejections: I8 stub used wall-clock via `restoredForBoot()`'s `Date()` default; missing fail/pass `failureReason` conditional-encode assertion. Round-2 Codex rejection: synthetic outcome-fail manifest persisted on disk during a green run (poisons the load-bearing outcome signal) — fixed. Reviewer-accepted deviation: `TerminalSessionDescriptor.restoredForBoot()` gained a `now: Date = Date()` param (file outside ticket fence; behavior-preserving, needed for the wall-clock ban). Non-blocking Fable note: `writeAndVerify` leaves per-run manifest temp dirs in NSTemporaryDirectory (intentional — manifests are the readable artifact). Still owes the supervised GUI-matrix pass before merge. |
| 31-agentkind-closed-enum.md | done | 6ce1110 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 21-idle-reaper-detach.md | done | 9b8a241 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 32-derive-agent-status-fn.md | done | eb88e0f | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 15-new-tile-new-window.md | done | d57e4c4 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 33-status-derivation-golden.md | done | ed44632 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 34-kind-classifier-tmux.md | done | 0ceb220 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 16-capture-tmux-window-target.md | done | b9757fd | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 19-close-tile-kill-window.md | done | c0a7902 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 20-project-release-detach.md | done | 58e84f7 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 22-per-workspace-ambient-session.md | done | cc4e319 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 23-private-managed-session-record.md | done | 9fdabf8 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 35-agent-state-reader-protocol.md | done | 73e8b25 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 36-pi-reader.md | done | 8d5c8e0 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 37-claude-reader.md | done | 64ec368 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 48-host-remotereach-model.md | done | 8749b17 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 24-lazy-resume-on-focus.md | done | d166d2e | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 59-scope-optionset-model.md | done | 71ecc95 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 67-agent-adapter-protocol.md | done | 2979905 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 28-view-session-cleanup.md | done | d9570a4 | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 54-bootstrap-auth-every-path.md | done | e9ef18a | matrix: green (headless) | gpt-5.5 fallback; pending Fable audit |
| 30-shared-view-exemption.md | skipped | - | matrix: n/a | Blocked: depends on grouped-view-session spawn (ticket 27), which never landed and is NOT in the autonomous queue. Skip until 27 is built by hand (C-20260702-013). |
| 03-membership-tile-register.md | done | 0468b4b | matrix: green (headless) | Human-authorized pass (C-20260701-001 resolved). Tile.zoneId LWW register + WorkspaceDocument.ambientTiles (canvas v1→2, workspace v2→3); encode-level re-stamp doctrine kills the version-masquerade bug across ALL save paths incl. WorkspaceProfile; field-scoped setTiles/setTileZone; production wiring: CanvasNSView setTileZone sink + register-first seeding + install() clobber fix, WorkspaceRuntime ambient-zone rendering from the register. LWW convergence proven through the production write path in OpId order. Still owes the supervised GUI-matrix pass before merge. |
| 04-zorder-fractional-index.md | done | 993c684 + ab4811f + f2b0e44 | matrix: green (headless) | Human-authorized pass (C-20260701-002 resolved), split 04A/04B/04C. Tile.zPosition + ZonePlacement.zPosition FracIndex registers (canvas v2→3, workspace v3→4); no compat shim (~150 call sites compile-migrated); legacy int ranks via monotonic fromLegacyRank, zoneZOrder via listed-then-unlisted distribute (no 0.5 collapse); bringToFront/bringZoneToFront never lower the frontmost; renormalizeZOrder deleted; all sorts (zPosition, id). Wire amendment on Op.createTile/setTileZIndex payloads (pre-transport, zero persisted op data) — flagged for human sign-off. Still owes the supervised GUI-matrix pass before merge. |
| 06-oplog-apply-compaction.md | done | 880cec0 | matrix: green (headless) | Cleared round 3, both reviewers (Claude review model + Codex GPT-5.5). C-20260701-006 dep satisfied (03/04/05 landed). New ContinuumRevivedSync materialize/compact engine (OpId total-order fold, delete-wins tombstone-in-resolve, FracIndex zone sort, LWW field regs, canonical sorted-keys byte-identity, applySnapshot boundary) + ContinuumRevivedSyncChecks with baseline manifest (opsProcessed/canonicalBytes/maxLatencyMicros) + canvas.json/baseline_ops fixtures + real ProjectStore round-trip; ContinuumRevivedCore untouched. Still owes the supervised GUI-matrix pass before merge. |
| 05-delete-tombstone.md | done | 99eabb8 | matrix: green (headless) | Human-authorized pass (C-20260701-003 resolved). ContinuumRevivedSync target stood up (ticket 06 layout) with TombstoneSet/TombstoneRecord/CompactionLedger + delete-wins policy doc; new ContinuumRevivedSyncChecks executable wired into run-matrix.sh; ComponentLab close-middle-tile visual gate. File hygiene proven: fresh checkout of the commit builds and passes SyncChecks. No materialize/compactor (ticket 06 scope untouched). Still owes the supervised GUI-matrix pass before merge. |
| 07-convergence-fuzz-red-green.md | skipped | - | matrix: green (headless) | Not cleared after 3 rounds (Claude + Codex both rejected). Needs HUMAN hands. Top concerns: (1) Done-when #1/#5 unverified — seed-1 canonical byte count hard-pinned `expectedSeed1CanonicalBytes=1348` but observed ONLY on Apple Silicon; no x86_64 runner in env, so the Intel matrix arm the ticket mandates goes RED-blind if JSONEncoder Double formatting differs — pin must be made arch-robust (explicit float rounding) or the x86_64 arm actually run; (2) no per-seed ORACLE — final converged bytes never asserted `== canonicalEncode(materialize(all generated ops))`, so a bug that drops the same op on every replica still converges green (Codex); (3) message-DROP adversary missing — deliverSome retains unselected msgs and drainAll delivers everything, so only delay/reorder/dup are fuzzed, not loss (Codex, main.swift:7548-7611); (4) compaction adversariality narrowed — DisseminationLedger.safeMark only compacts through fully-acked points, so the watermark-race/merge-equivalence dimension is never fuzzed (defensible per ticket's "provably acked" rule, but needs human sign-off since this gate authorizes all transport work). Tree left dirty as the workflow left it. Rejected attempt stashed for human review (stash@{0}) so the tree could be cleaned for ticket 09. |
| 09-taint-scan-i5.md | skipped | - | matrix: green (headless) | Not cleared after 3 rounds (Claude review CLEAR, Codex rejected). Needs HUMAN hands — the C-20260701-008 geometry-vs-taint ruling is the crux. Top concerns (Codex): (1) the geometry-verification approach opened a bypass — `SyncPayloadTaintScanner.swift:121-124` propagates `inVerifiedGeometryDict` through arrays and lines 160-162 suppress EVERY NSNumber while that flag is set, so a payload like `{"frame":{"x":[12345],"y":0,"width":0,"height":0}}` hides a pid-shaped integer even though `x` is not a legitimate scalar geometry number; the new probes don't cover this bypass. (2) the mandatory UInt64>Int.max overflow guard (Done-when #3) is never exercised — fixtures use `5_000_000` (above pid ceiling but far below Int.max) and the poison probe uses `Int`, so a regression that calls `intValue` before guarding would pass the matrix. Fix needs a geometry-allow model that only exempts scalar numeric geometry leaves (not whole subtrees/arrays) plus a fixture with a genuine `UInt64 > Int.max` value. Tree left dirty as the workflow left it. |
| 39-reader-golden-fixtures.md | skipped | - | matrix: n/a | Blocked: depends on all three readers incl. the Codex state reader (`CodexStateReader`), which does not exist on disk and whose ticket 38-codex-reader is UNCLASSIFIED/excluded from the autonomous queue (README) with no ledger row. The I6 check block calls `CodexStateReader().locate(...)`/`.read(...)` and cannot compile without it. Cannot build on a missing foundation — skip until 38 is built by hand (C-20260703-014). (Aside: the shipped readers are `ClaudeAgentStateReader`/`PiAgentStateReader`, not the ticket's `ClaudeStateReader`/`PiStateReader`, and lack `taintCheck`/`defaultStaleWindowSeconds` — a supervised adaptation is owed even once 38 lands.) |
| 41-fsevents-push-watch.md | skipped | - | matrix: n/a | Blocked: depends *entirely* on the `SessionObserver` (with budgets), built by ticket 40-session-observer, which is **Supervised** — excluded from the autonomous queue, never built (no `SessionObserver` type on disk beyond a comment mention in AgentActivityEvent.swift:20, no commit, no ledger row). Ticket 41 adds `AgentStoreWatcher` as a stored property on the observer and routes `onChange` into the observer's `readAndUpdate`/`armWatchers` path — none of which exist. Cannot build on a missing foundation. Skip until 40 is built by hand (C-20260703-015). |
| 55-synctransport-seam.md | done | 568532a | matrix: green (headless) | Cleared round 3, both reviewers (Claude review model + Codex GPT-5.5). New SyncTransport protocol + SyncMessage/ConnectionState/SyncTransportError + adversarial FakeSyncTransport actor (seeded RNG threaded via init, per-pair DeliveryPolicy: partition/reorder/delay/drop/dup, goOffline/reconnect flood, I5 taint precondition on transit) + 8 seeded logic checks + real-disk backend round-trip (FakeSyncTransport→materialize→ProjectStore.saveCanvas/loadCanvas, runtimeRef==nil asserted). Manifest records measured values. Zero Core-file changes; Package deps unchanged. Still owes the supervised GUI-matrix pass before merge. |
| 56-transport-fuzz-soak.md | skipped | - | matrix: n/a | Blocked: depends on the convergence fuzz (ticket 07), which is **skipped** (needs human hands, correctness bugs — see 07 row). 56 is explicitly "an additional layer on top of" that fuzz: it rewires the *existing* convergence-fuzz block to run through `FakeSyncTransport`, and needs `canonicalEncode`, the shared `randomSpatialOp` helper, and the `LCG`/fuzz scaffolding — none of which exist in `ContinuumRevivedCoreChecks/main.swift` (grep-confirmed absent, since 07 never landed). "Both must be merged and green before any code here is written." Cannot build on a missing foundation. Skip until 07 lands by hand (C-20260703-016). (Ticket 55 transport seam IS done at 568532a — the missing dep is the fuzz, not the seam.) |
| 14-project-session-naming.md | done | eb8a8b2 | matrix: green (headless) | Ledger gap fix (fix round 3): the ticket landed in commit eb8a8b2 ("project/ambient tmux session naming — TmuxSession naming helpers, D16 detach-not-kill wiring, self-check + lab card (ticket 14)") but was never given a ledger row, so a later re-attempt found nothing to change and produced an empty diff, which reviewers correctly flagged as "no diff to review." Re-verified eb8a8b2 against every Done-when item in the ticket line by line: `TmuxSession.projectSessionName`/`ambientSessionName`/`killProjectSessionCommand` (TmuxSession.swift), `ZoneRuntimeController.projectSessionName()`/`killProjectSessionCommand()` + a D16 detach-not-kill lifecycle doc comment above `close()` (substance present — D16 citation, detach-not-kill, kill-reserved-for-deletion, no-kill type-enforced since the controller holds no TmuxControl/Process — but the header wording differs from the ticket's literal `// LIFECYCLE POLICY (D16 — locked)` string: cosmetic Done-when deviation, Claude-reviewer-noted, non-blocking), the five table-driven Logic checks with measured-value manifests (`ticket14-session-naming` invariant in ContinuumRevivedCoreChecks/main.swift), the controller backend self-check via the lock-free `init(projectRoot:projectStore:project:)` over two real temp-dir ProjectStores with distinct project UUIDs (`runProjectSessionNamingSelfCheck`, wired to `--zone-project-session-naming-check` in both ContinuumApp.swift and scripts/run-matrix.sh per the ticket's C-20260702-010 ruling), and the ComponentLab "Session Naming" visual gate asserting the three exact labels for the fixed UUID. `sessionName(tileId:)` untouched, no change to TileSpawner.swift or any descriptor type. `swift build` clean; full `./scripts/run-matrix.sh` green including the ticket-14 Logic check, `--component-lab-check`, and `--zone-project-session-naming-check`. No code change was needed or made; this row only closes the documentation gap that caused the false "no diff" rejection. |
| 58-activity-projection-transport.md | skipped | - | matrix: red (flaky) | Not cleared after 3 rounds (Claude review + Codex both rejected). Needs HUMAN hands. BLOCKING (Claude): the backend integration check is nondeterministically red (~25% of runs) — the test's LoopbackSyncTransport count-based delay model (ActivityProjectionTests.swift:61-93) only releases a held message on a *subsequent* send or the single post-burst `flush()`; the async gap-fill choreography (drop seq 5 -> receiver replay request -> sender replay + cold-reconnect) lands after that flush, so whichever replay/reconnect message is the trailing pending element with countdown>0 stays stranded forever and the receiver never reaches snapshotSequence 10 within the 2s timeout. Fix = make the harness deliver eventually (drain/flush pending until convergence-or-timeout) and prove determinism over many runs; then verify convergence happens for the RIGHT reason (real replay / cold-reconnect fallback, not a lucky extra send). Codex: shared two-topic demux startup race — receiver.connect() starts receiveLoop in an unstructured Task then immediately sends .activitySubscribe (ActivityProjectionReceiver.swift:73) while SyncMessageDemux only broadcasts to subscribers registered at drain time (SyncMessageDemux.swift:52), so on a shared already-pumping transport the sender's .snapshot can broadcast before the receiver registers -> silent loss of the required snapshot-first item; symmetric issue in ActivityProjectionSender.start(); checks use fresh/private demuxes so the matrix never gates this race. Fix = synchronously register subscriptions before any send, and add a Checks case with a pre-existing spatial subscriber on the same demux. Rejected attempt stashed for human review (stash@{0}) so the tree could be cleaned for the next ticket. |
| 66-connection-supervisor.md | skipped | - | matrix: n/a | Blocked: its "Depends on" section (line 310) names the **transport fuzz and soak (ticket 56)** as a hard dependency, and 56 is `skipped` (dep-blocked on the unbuilt convergence fuzz 07 — see 56/07 rows). The code substrate (55 FakeSyncTransport `goOffline`/`reconnect` at 568532a, 59 Scope at 71ecc95, 12 injectable substrates) IS on disk, so 66 could likely *compile*; but the ticket explicitly ties trustworthiness of its green to that soak ("a bug in the fake would produce false green results here" — line 310). Without 56 having validated the fake under adversarial delivery, any green from 66's logic checks (which reuse the same fake API) is untrustworthy — exactly the false-green the runbook forbids. Cannot build a trustworthy verdict on an unvalidated foundation. Skip until 07 → 56 land by hand (C-20260703-017). |
| 74-agent-message-bus-seam.md | skipped | - | matrix: green (headless) | Not cleared after 3 rounds (Claude review CLEAR, Codex rejected). CONTRACT-CONFLICT needing a human ruling: the ticket's Backend real-path check mandates instantiating a real `ZoneRuntimeController` and calling `controller.agentBus.post(testMessage)`, but `ZoneRuntimeController` lives in the app target `Sources/ContinuumRevived` (not importable by a `*Checks` library target), so — like ticket 14's controller self-check — the only place to run it is a flag-wired self-check method *inside* the app target. Doing so puts an `agentBus.post` call at `ZoneRuntimeController.swift:712`, which directly violates the ticket's own Done-when grep gate ("a grep for `agentBus.post`/`agentBus.subscribe` in `Sources/ContinuumRevived` returns zero hits other than the property declaration"). The two Done-when items are mutually exclusive under the app-target architecture. Pure Core half is clean (AgentMessageBus.swift + AgentStatusEngine.ingestBusMessage + 3 logic checks, Claude-clear). Also: ticket's conceptual dep on the unbuilt supervised observer (ticket 40) is non-structural (disclaimed by the ticket), so it did NOT block the code — the block is the self-contradicting real-path/grep pair. Rejected attempt stashed for human review (stash@{0}) so the tree could be cleaned. Needs HUMAN ruling (C-20260703-019): either exempt the self-check call site from the grep gate (as C-20260702-010 did for ticket 14's flag wiring), or relocate the real-path check to a `*Checks` target if `ZoneRuntimeController` is made library-importable. |
| 70-approvals-needsattention.md | skipped | - | matrix: n/a | Blocked: "Depends on" (line 249) names the **ACP driver (ticket 69)**, which is not done, not in the autonomous queue, and has no ledger row. Worse, the mandatory, explicitly-non-bypassable Backend real-path check ("adapter event → store update → **observer cycle** → store read") requires a `SessionObserver` tick, and `SessionObserver` (ticket 40, **Supervised**) does not exist on disk (grep-confirmed; same block as C-20260703-015). `respondToApproval` forwards through `controller.managedAdapters[…]`, and no `managedAdapters` seam exists (ticket 66/69 territory; 66 skipped). The pure logic checks alone (type semantics + `deriveAgentStatus`) would compile — the `AgentRuntimeEvent.requestOpened/requestResolved` cases from ticket 67 (2979905) are present in AgentStatusEngine.swift — but 4 of the "Done when" items (5, 6, 8) and the entire real-path section require the observer/adapter production path. Shipping only the pure half and stubbing the mandatory real-path check would be fake-green, which the runbook forbids. Cannot build on a missing foundation. Skip until 40 (observer) + 69 (ACP driver) land by hand (C-20260703-018). |

## Reset for the amended retry pass (2026-07-03, overwatch)

Gap-filling rulings written for the four review-rejected tickets (see `_CONFLICT_LOG.md` C-007/C-008/
C-019/C-020 and the retry-ruling banners at the top of each ticket): 07 (arm64-only per Dylan + per-seed
oracle + DROP adversary + compaction sign-off), 09 (scalar-leaf-only geometry exemption + UInt64>Int.max
fixture), 58 (synchronous demux registration + deterministic drain-until-convergence harness), 74
(grep-gate exemption for the flag-wired self-check, precedent C-010). Prior attempts are stashed and may
be salvaged. These rows reset them for the loop to re-drive; if 07 lands, 56 → 66 unblock in turn.

| ticket | status | commit | matrix | note |
| --- | --- | --- | --- | --- |
| 07-convergence-fuzz-red-green.md | pending-retry | - | - | Retry under C-20260701-007 retry rulings (arm64-only pin, per-seed oracle, DROP adversary, compaction signed off). |
| 09-taint-scan-i5.md | pending-retry | - | - | Retry under C-20260701-008 retry rulings (scalar-leaf geometry only, genuine UInt64>Int.max fixture). |
| 58-activity-projection-transport.md | pending-retry | - | - | Retry under C-20260703-020 rulings (sync demux registration before send + shared-demux check; deterministic drain harness). |
| 74-agent-message-bus-seam.md | pending-retry | - | - | Retry under C-20260703-019 ruling (flag-wired self-check exempt from grep gate, precedent C-010); salvage stashed Core half. |
| 07-convergence-fuzz-red-green.md | done | ce3a87f | matrix: green (headless) | Retry under C-20260701-007 rulings (arm64-only byte-pin, per-seed oracle == canonicalEncode(materialize(all ops)), permanent-DROP adversary, compaction fence signed off). Cleared round 2, both reviewers (Claude review model + Codex GPT-5.5). I4 convergence fuzz drives real materialize/compact/applySnapshot in ContinuumRevivedCoreChecks; 50 seeds × 200 steps, oracle + Codable round-trip backend check + canonical-stability + compacted-materialization checks. Still owes the supervised GUI-matrix pass before merge. Unblocks 56 → 66 (now dep-satisfiable by hand). (Row RESTORED: the ticket-09 workflow reverted this legitimate uncommitted record as collateral when Codex round-1 flagged it as "unrelated to ticket 09".) |
| 09-taint-scan-i5.md | skipped | - | matrix: green (headless) | HARNESS FALSE-POSITIVE, not an impl defect (C-20260703-021). Substance dual-cleared: Claude review "merge-ready"; Codex cleared every substantive concern across 3 rounds (array-crossing geometry-exemption bypass `frame:[{x:12345}]` fixed r2; stale I5 manifest stub reconciled r3; genuine UInt64>Int.max overflow fixture + scalar-leaf-only exemption present per C-008). SOLE remaining Codex block = untracked `scripts/codex-primary-loop.sh` — a human-dropped daytime Codex-primary-driver reconfiguration artifact, NOT ticket scope; not mine to move (didn't create it). Staged impl left in tree (SyncPayloadTaintScanner.swift added + main.swift taint block: 19 Op cases + 24 tone×status events) for a human to commit once the stray file is set aside. STOP file present (07:32) — loop halting for reconfiguration; did not re-drive. |
| 09-taint-scan-i5.md | done | da87b29 | matrix: green (headless) | Committed by overwatch: substance dual-cleared in run-20260703T065946 (Claude merge-ready; Codex cleared all substantive concerns over 3 rounds — scalar-leaf-only exemption, array-bypass probes, genuine UInt64>Int.max fixture). Sole rejection was a harness false-positive (untracked codex-primary-loop.sh polluting git status — C-20260703-021), resolved by committing that script (474be0f). Objective gate re-run green before this commit. |
| 58-activity-projection-transport.md | done | 25b7fb5 | matrix: green (headless) | gpt-5.5 primary; pending Fable audit |
