# 91-agent-tile-ux — execution ledger

## heartbeat

last-touch 2026-07-30T23:46:14Z · ticket P4.1-custom-composer-shell.md · attempt 1 · pid — · status done

## states

`pending` · `in-progress` · `done` · `blocked`

| Ticket | State | Commit | Updated | Note |
|---|---|---|---|---|
| `P0.1-program-contract.md` | done | this commit | 2026-07-28T04:48:16Z | Recovered the worker's preserved patch after upgrading Codex CLI 0.135.0 → 0.145.0. The guard now cross-validates the fixed packet header, exact dependency grammar/order/uniqueness, ledger state metadata with real BSD-date UTC validation, 50 packet/ledger rows, three supervised gates, and a fail-fast script-relative matrix invocation locked at executable line 4; 25 isolated negative mutations must all go red while the live program remains untouched. Negative witness from the worker and recovery: deleting forged-pending protection failed; independent Codex review then found and drove fixes for misleading header text, regex/trailing/duplicate dependencies, swallowed/disabled matrix calls, malformed done metadata, and invalid calendar timestamps. Final `./scripts/check-agent-tile-ux-program.sh`, `swift build`, matrix inventory, and full headless `run-matrix.sh` passed; matrix artifact `qa-runs/20260728T044806Z/app-bundle/manifest.json`. Codex 0.145 final review: APPROVE. |
| `P0.2-agent-content-target.md` | done | this commit | 2026-07-28T06:28:52Z | Stood up `ContinuumRevivedAgentContent` (Foundation only, no dependencies, one namespace file) plus the `ContinuumRevivedAgentContentChecks` leg wired into the matrix after the AgentUI leg. The leg holds the platform-neutral boundary with four gates: (1) module identity/linkage — the checks target links AgentContent ALONE, so a reverse dependency stops compiling (witnessed: `import ContinuumRevivedCore` in AgentContent fails the build, exit 1); (2) a source import ALLOWLIST of exactly `Foundation`, scanned over a small Swift lexer (nesting block comments, single-line/multiline/raw literals with escapes, `\(…)` interpolations incl. comments inside them, backtick identifiers); (3) SwiftPM's EVALUATED manifest via `swift package dump-package --scratch-path <temp>` — each of the two target dicts must match exactly, any extra key (path/sources/pluginUsages/linkerSettings/swiftSettings) is red; (4) the COMPILER's own record — the `.d` files in `<dir of this executable>/ContinuumRevivedAgentContent.build/`, so no forbidden module can reach the built module by any route, with a freshness assertion that every current source appears in it. Negative witnesses, each observed red against the final check: 12 source-import evasions (plain, `@_exported`, tab, `;`-joined, leading `/* */`, `import class AppKit.NSView`, `internal import`, `public import SwiftUI`, `@preconcurrency package import`, `import/**/AppKit`, newline-split `import\nAppKit`, `import Network` bracketed by two `try! /"/` regex literals) — all FAIL by assertion except the Core one, which fails at compile; 8 manifest evasions (Core dependency; `.product(name:"GRDB")` that no blocklist named; `dependencies :` with a space; block-commented decoy; raw-string decoy; unused clean decoy + constant-named live target; `package.targets[i].dependencies.append("ContinuumRevivedCore")` after construction; `linkerSettings = [.linkedFramework("AppKit")]` after construction; `Target.Dependency` constant smuggled into the checks array) plus `linkerSettings`/`path`/`swiftSettings` arguments; and gate 4 proven live by widening the import allowlist to permit AppKit — `import AppKit` still failed with "the BUILT … module consumed [AppKit, QuartzCore]". False-positive guards verified GREEN on compiling source: interpolation with a nested literal, interpolation with a quote inside a comment, `value! / 2`, `values.count / count`, a multiline doc string containing the words `import AppKit`, and a planted stale `release/…/stale.d` naming AppKit. Verification: `swift build` clean; `swift run ContinuumRevivedAgentContentChecks` passed (~1.1s, all four legs); `CONTINUUM_SKIP_SURFACE_CHECKS=1 ./scripts/run-matrix.sh` exit 0 with the new leg green and `Matrix inventory checks passed (282 committed records)`; matrix artifact `qa-runs/20260728T062705Z/app-bundle/manifest.json`. Inventory regenerated: exactly two records ADDED (`leg run swift run ContinuumRevivedAgentContentChecks`, `count ContinuumRevivedAgentContentChecks 4`), none removed or lowered; no matrix leg, floor, contrast requirement, baseline or I5 boundary touched. Independent review: Codex 0.145.0, read-only, eight rounds; rounds 1–7 returned REQUEST-CHANGES with 19 blocking findings, all accepted and fixed except one, and round 8 returned APPROVE with no findings. The line-oriented scanner was twice replaced rather than patched — first by the lexer, then by deleting the static manifest parse in favour of `dump-package` — because each patch met a new evasion. Honest limits: (a) `dump-package` is a SECOND evaluation of the manifest, so a manifest deliberately written to answer the probe differently from the build that launched it could still lie to gate 3; not chased, documented in the code at that gate — these gates exist to catch accidental coupling by a future ticket, an author gaslighting a probe they could equally delete is outside what an in-repo check can hold, and the consequence that matters is covered by gate 4 without trusting the manifest at all; (b) gate 2 refuses a bare regex literal in an ambiguous position rather than lexing it, so a future AgentContent ticket wanting one must extend the lexer — it fails closed and says so; (c) gate 4's forbidden list is a blocklist of UI frameworks and Continuum modules, not an allowlist, because Foundation itself pulls in a toolchain-dependent set (Combine, Dispatch, XPC, Observation, Security) that would rot with every SDK; (d) `AgentContentModule` is a bare namespace — the semantic types are P1.1's work — and the allowlists deliberately do NOT pre-authorise swift-markdown, so P2.1 must extend them under review; (e) no AppKit/UI work in this ticket, so no geometry/contrast/Component Lab/baseline flag applies beyond the full matrix run. |
| `P0.3-semantic-tile-tokens.md` | done | this commit | 2026-07-28T08:05:27Z | Added the v2 tile's visual vocabulary as ADDITIVE types in `DesignTokens.swift`/`Metrics.swift` plus a new `runAgentTileTokenChecks()` suite, with nothing adopted in a live view (so no baseline could move). Five surfaces (`composer`, `artifact`, `codeSubdued`, `rowSelected`, `rowHover`), four line ROLES (`decorativeHairline` exempt; `controlBoundary`/`focusRing` at the 3.0 line floor; `attention` at the 4.5 TEXT floor, because the same accent labels the state in words — P1.6's call for the approval dock), and three radii (`AgentTileRadius` tile 12 > composer 10 > artifact 8 > shipped card 6), each inside `_DESIGN.md` §11's band and on the 2pt grid. NEW TYPES RATHER THAN NEW ENUM CASES on purpose: `DesignTokenChecks.swift`/`TokenContrastChecks.swift` are OUTSIDE this packet's file fence and pin a 22-token count, a 104-pair set and a per-token worst-ratio table, so growing `SurfaceToken`/`LineToken` would have forced this ticket to renumber gates it may not touch; the tile ladder therefore carries its own 30-pair set (`AgentTileTokens`) and every shipped floor is bit-identical. The new suite lives in `AgentUIChecks/main.swift` for the same fence reason — a new check FILE is not one of the four fenced paths. THE PACKET'S NAMED REQUIREMENT (semantic lines cannot be aliased to the decorative hairline) is held two independent ways: by VALUE (no gated role may resolve to `separator`'s colour in either theme) and by MEASUREMENT (the hairline peaks at 1.37:1 over the five tile surfaces — under the 3.0 component floor — while every gated role clears its own floor on all of them), plus a role→token identity table proving each role reuses a P1.3 token instead of inventing a value, which is what carries its gating on the eleven shipped surfaces. Verification: `swift build` clean; `swift run ContinuumRevivedAgentUIChecks` green (60 new measurements, 16-surface distinctness, 12 pinned worst ratios ±0.01, 4 pinned row-emphasis ratios); `swift run ContinuumRevivedAgentContentChecks` green; `CONTINUUM_SKIP_SURFACE_CHECKS=1 ./scripts/run-matrix.sh` exit 0 (`Matrix passed`, artifact `qa-runs/20260728T075855Z/app-bundle/manifest.json`), including `--component-lab-check`, `--ui-geometry-check`, `--ui-contrast-check`, `--ui-baseline-check` and `--agent-supervisor-check`. Inventory regenerated: EXACTLY one record changed, `count ContinuumRevivedAgentUIChecks 14 → 15`; nothing removed or lowered, 282 records, no leg, floor, contrast requirement, baseline or I5 boundary touched. NEGATIVE WITNESSES, each edit made against the final code and observed red at exit 1 (all quoted verbatim in main.swift): (a) `focusRing` aliased to `separator` → `focusRing resolves to the same value as decorativeHairline in light (DDE0E6)`; (b) the same edit with the by-value guard DELETED and the identity assertions short-circuited → `10 pair(s) below floor: focusRing on artifact in light is 1.16:1` (so the measurement leg stands alone); (c) hairline given a floor → `decorativeHairline must be the decorative role, but it carries a floor`; (d) `rowHover` = `tileBody` → `the ladder lost a step`; (d2) `rowHover` distinct but imperceptible (0xF9FAFB/0x15181D) → `rowHover is 1.01:1 against tileBody`; (e) composer radius 14 → `outside its 10.0…12.0 design band`; (f) `surfaceTextTokens` narrowed → `expected 30 documented tile pairs, got 25`; (g) `Radius.container` 10→6 → P1.5's own nesting rule fires first, so also (g2) 10→8 → `the shipped container radius must stay 10.0`; (h) `rowSelected` pulled inside the shipped ladder → `canvas is darker than rowSelected in light`. Independent review: Codex 0.145.0, read-only, two rounds. Round 1 REQUEST-CHANGES: BLOCKING — the inventory still pinned the AgentUI count at 14, so deleting the new suite would have stayed green (fixed by regenerating); NON-BLOCKING — the provenance note falsely claimed all five surfaces sit inside the shipped ladder (fixed, and the load-bearing half is now GATED by leg 1b: `rowSelected` must be the darkest light and lightest dark of all sixteen surfaces, which is why it is every foreground's worst background — witness (h) above). Round 2 APPROVE with two documentation-only findings, both verified independently and fixed: `tileBody` is 0.963466/0.008454 (not 0.968/0.0086) and the shipped dark extreme is `cardUserMessage` at 0.028849, not `overlay` (0.022002); and the older `canvas` "darkest/lightest extreme" comment is now explicitly scoped to the eleven shipped surfaces rather than edited. Honest limits: (a) the palette is temporarily SPLIT — text legality for the new surfaces is declared in `AgentTileTokens` while `TextToken.legalBackgrounds` still names only the shipped eleven, so two pair sets exist until the live-tile migration merges them; deliberate, since merging them today means renumbering out-of-fence gates. (b) `--ui-contrast-check` reads the real view tree, so these surfaces are unmeasured THERE until a renderer paints them (P3.x) — this ticket gates them only at token level, which is all a token-level ticket can honestly claim. (c) `attention` exposes one colour (approval) and gates two accents (approval, error); a warning hue must be added to `attentionAccents` to be gated, which fails closed. (d) Row-emphasis floors are deliberately low (selection ≥1.20, hover ≥1.05) because these are fills, not state indicators — a selected row also draws `focusRing`; a supervised visual review still owns whether they READ correctly. (e) Radius bands were checked against `_DESIGN.md` §11 prose, not against rendered corners. (f) Environment note, no gate involved: `ContinuumRevivedCoreChecks`' loopback ssh/tmux leg needs `script(1)` to open a tty, which fails when the runner's stdin is a socket (`script: tcgetattr/ioctl: Operation not supported on socket`); re-running the matrix with stdin from /dev/null makes that leg pass, and both final matrix runs were green that way. No leg was skipped. |
| `P0.4-transcript-fixture-corpus.md` | done | this commit | 2026-07-28T09:42:37Z | Recovered the earlier worker's preserved patch (`/tmp/p04-preserved/P0.4-implementation.patch`) after the supervisor made the program guard's ledger self-tests state-independent (ce68297) — the prior block was the guard, not the packet, and it is gone: `./scripts/check-agent-tile-ux-program.sh` is green (`self-test: ok (25 negative cases red, live program untouched)`) with this row set to `in-progress` and again at `done`. WHAT LANDED: 19 fixtures in `Sources/ContinuumRevivedAgentContentChecks/Fixtures/*.md` covering all 16 `_DESIGN.md` §6 block families and all 7 inline runs, split by `TranscriptFixtureForm` into 10 markup fixtures and 9 structured payloads so a typed record is never encoded as Markdown (locked decision 2); `Fixtures.swift` declaring the corpus (id = filename stem, text in files) and gating directory↔declaration parity both ways, family/inline coverage, markup-vs-structured separation, falsifiable marker probes, hygiene/I5 (no `/Users/`, `/home/`, ssh key, `-----BEGIN`, bounded `sk-`/`ghp_`/`xoxb-`/`AKIA`/`AIza`/`eyJ` shapes, no address, and no live machine home/user/host name), sentinel declaration in BOTH directions, and a 5,000-delta split that reassembles exactly; `ContinuumRevivedCoreChecks/AgentTranscriptFixtures.swift` giving Core the same corpus with no second copy of it, the 14 ids Core-side work names, the sentinel confined to `secrets-sentinel`, and the 5,000 replayable deltas P0.5 needs. REVIEW DROVE ONE REAL REWRITE. Codex 0.145.0, read-only, four rounds against the staged diff. Round 1 REQUEST-CHANGES (blocking): the one-home gate scanned only `Sources/ContinuumRevivedCoreChecks`, so a copy under any other target stayed green. Round 2 REQUEST-CHANGES on the widened scan (two blocking): it failed OPEN on enumerator/metadata/read errors, and basename matching globally reserved generic names like `approval-request.md` across all of `Sources/`. Round 3 REQUEST-CHANGES (two blocking): corpus-named files hit `continue` and so skipped byte comparison AND the read-failure path, and the `files.count > 1`/parent-named-`Fixtures` heuristic still reserved name COMBINATIONS. Round 4 APPROVE, no findings. FINAL SHAPE OF THAT GATE: a copy is identified by CONTENT, never by name alone. A candidate is any file under `Sources/` (canonical directory excluded by resolved-path boundary) whose name matches a fixture or whose size matches a fixture exactly; it is read, and an unreadable candidate is recorded and FAILS the gate. Red if byte-identical to any fixture at any path under any name, or if it carries a fixture's name and repeats one of that fixture's ≥24-character lines verbatim (the drifted copy, which bytes cannot see). A companion assertion requires every fixture to have such a line, so the drift rule can never be vacuous. Round 3's ordering nit was also taken: the unreadable assertion now precedes `scanned > 0`. VERIFICATION: `swift build` exit 0; `swift run ContinuumRevivedAgentContentChecks` exit 0 (`19 fixtures cover 16 block famil(ies) and 7 inline run(s), 19 inventory probe(s), 1 sentinel-bearing fixture(s), 1 streaming fixture(s)`); `swift run ContinuumRevivedCoreChecks` exit 0 (`19 shared fixture(s), 14 required id(s), sentinels confined to secrets-sentinel, 5000 replayable delta(s)`); `CONTINUUM_SKIP_SURFACE_CHECKS=1 ./scripts/run-matrix.sh` exit 0 `Matrix passed` three times across this iteration, last at artifact `qa-runs/20260728T094007Z/app-bundle/manifest.json`, including `--component-lab-check`, `--ui-geometry-check`, `--ui-contrast-check`, `--ui-baseline-check` and `--agent-supervisor-check`. Inventory: exactly two records GROWN (`count ContinuumRevivedAgentContentChecks 4 → 5`, `count ContinuumRevivedCoreChecks 31 → 32`), 282 records, none removed or lowered; no leg, floor, contrast requirement, baseline or I5 boundary touched. As in P0.3, the matrix was run with stdin from /dev/null so CoreChecks' loopback ssh/tmux leg can open a tty; no leg was skipped. NEGATIVE WITNESSES — every one re-observed red against THIS code in this iteration, restoring from a /tmp copy each time (never `git checkout`, since the fixtures are untracked): (1) `/Users/someone/…` appended to `prose-plain.md` → `fixture prose-plain contains /Users/`; (2) undeclared `stray-fixture.md` → `present-but-undeclared: ["stray-fixture.md"]`; (3) `@@` stripped from `diff-unified.md` → `declares diff but its text contains no diff marker (/^@@ /)`; (4) a sentinel appended to `tool-call-arguments.md` → `carries a sentinel secret without declaring it`; (5) `prose-plain` renamed to `prose-basic` in the corpus only → AgentContent stays green (ids derive from filenames) while CoreChecks goes red, `the corpus has no fixture prose-plain … fixture ids are test API`; (6) the corpus copied to `ContinuumRevivedCoreChecks/prose-plain.md` → `look like copies of the transcript corpus`. On the rewritten one-home gate: (A) renamed byte-identical copy `ContinuumRevivedPaletteChecks/local-copy.txt` → `(byte-identical to a fixture)`; (B) byte-identical SAME-NAME copy in a non-`Fixtures` directory `…/Help/approval-request.md` → same, which is round 3's finding 1 made falsifiable; (C) drifted same-name copy → `named after a fixture and repeats its text: "The ledger writer in the Atlas indexer keeps one open segment per shard and rolls it"`; (D) unreadable same-name candidate (chmod 000) → `could be a copy of the corpus but cannot be read`. FALSE-POSITIVE GUARDS observed GREEN: one unrelated corpus-named file with its own content; TWO unrelated corpus-named files in one directory (round 3's finding 2); and an unrelated corpus-named file inside a directory literally called `Fixtures`. HONEST LIMITS: (a) the packet fences the corpus to `*.md`, so the nine structured payload files carry that extension while holding raw payload text, not Markdown — the form field says so and the separation gate enforces it, but the directory listing shows `.md` on a unified diff; (b) `expectedBlockKinds` is a high-level inventory of strings, not an AST — P1.1 has not defined the semantic types yet and P2.x pins exact trees against these same files; (c) markers exist only for kinds with unambiguous syntax, so `paragraph`, `unknown`, `emphasis` and the structured prose kinds (`approval`, `question`, `error`, `notice`) are declared but not probed; (d) the two check targets each own a thin reader because they cannot link each other — the DATA is single-homed and gated, the ~15 lines of delta-splitting logic are not; (e) the one-home scan covers `Sources/` only, so a corpus copy parked under `docs/` or `scripts/` is not caught, and a drifted copy that also renamed the file escapes both rules by design (name+content, or bytes); (f) the chmod-000 DIRECTORY case has no live witness because SwiftPM refuses to build when a directory under `Sources/` is unreadable — only the unreadable-FILE path is witnessed, though both feed the same fail-closed assertion; (g) Core's `required` list names 14 of 19 ids on purpose — it is what Core consumes, and every other rename is already a two-place diff forced by the AgentContent parity gate (Codex round 3 agreed this leaves no hole); (h) fixture text is written, never captured, so it exercises shapes Continuum expects rather than a real provider's exact bytes; (i) no AppKit/UI work here, so no baseline could move. |
| `P0.5-compatibility-pipeline-harness.md` | done | this commit | 2026-07-29T02:14:00Z | Recovered the exact staged patch preserved after the original worker ended on provider DNS `ENOTFOUND`. Added the temporary `AgentTranscriptProjecting` migration seam and a compatibility snapshot over ordered ids/kinds/bodies/statuses, active-tool count and turn status without defining any P1 semantic-document type. The check replays 9 corpus-backed scripts through the shipped `PiEventTranslator → withThreadId → ManagedAgentTranscriptModel` path: prose/tool/prose order, clean and interrupted turn boundaries, user prompt, unfinished active tool, failed tool, reasoning separation, 5,000 streamed deltas, and notice deduplication. Tool command/cwd arguments remain absent while provider-streamed sentinel text survives verbatim. The future-projection comparator is exercised every run against a deliberately divergent status-dropping/turn-merging stub, so parity is not vacuous before P1.5 registers a second real projection. Verification: `swift build`, `swift run ContinuumRevivedAgentContentChecks`, and `swift run ContinuumRevivedCoreChecks` passed; Core reports 9 scripts, 1 real projection, privacy gate and divergent comparator. The active-tool floor now captures both directions: an unfinished tool is `.inProgress` with count 1/working, while completed and failed tools return to count 0. One final-code mutation witness was re-observed red: prefixing every compatibility row id failed the exact floor at exit 1 (`changed-assistant-1` vs `assistant-1`); log `~/.pi/agent-tile-ux-prep/20260728T220454/p05-negative-witnesses/01-changed-row-id.log`, and the staged source was restored byte-for-byte. Inventory changed only `ContinuumRevivedCoreChecks` 32 → 33; program guard remains green. The first matrix run exposed the known display-dependent glyph baseline debt with the external 1x display primary; no baseline was blessed or relaxed. Switching the built-in Retina panel to primary for this login session produced two consecutive 62/62 baseline passes, then `CONTINUUM_SKIP_SURFACE_CHECKS=1 ./scripts/run-matrix.sh </dev/null` passed at exit 0, artifact `qa-runs/20260729T023353Z/app-bundle/manifest.json`; final-tree full log `~/.pi/agent-tile-ux-prep/20260728T220454/p05-full-matrix-final-retina.log`. Luna medium read-only review accepted the implementation shape and requested a finalized ledger/evidence record. Sol medium re-review then caught the missing positive `activeToolCount == 1` snapshot; that blocking finding was fixed with the unfinished-tool script. Final Luna medium review returned APPROVE with no blocking findings; artifact `.pi/agent-runs/luna-code-reviewer-20260729T023552Z-edf57b/final.md` from coordinator cwd `/Users/dylan/Documents/personal`. Honest limits: the semantic projection does not exist until P1.5; exact old presentation is a temporary floor, not permanent product policy; display-independent glyph rendering remains unproven and overnight matrices require the Retina panel to stay primary. |
| `P1.1-document-schema.md` | done | this commit | 2026-07-29T03:15:29Z | Added the platform-neutral semantic vocabulary: validated `AgentNodeID`, extensible validated raw `AgentBlockKind` with 16 built-ins, document/entry/block/source-range values, 4 semantic roles, typed provenance and 6 lifecycle statuses, 15 typed built-in payload cases (including structured tool/plan/diff/request/error/notice values) plus a typed opaque fallback, 7 recursive inline forms, and a Codable/Sendable JSON-shaped opaque fallback with no `Any` or platform/view state. `swift build` and `swift run ContinuumRevivedAgentContentChecks` passed; the new schema leg proves compile-time Codable/Sendable constraints, exact mixed-document equality and stable sorted JSON bytes after round-trip, all built-in/future kinds, invalid kind/ID/range rejection, and checked Codable decoding. The existing source/compiler scans report 4 Foundation-only imports across 4 AgentContent files and no forbidden module. Negative witness: removed semantic-key syntax validation from the final implementation; the focused executable exited 1 with `invalid block kind syntax must be rejected` and listed six newly accepted invalid keys; artifact `tasks/iteration-001/negative-witness/invalid-kind.log`, then exact source bytes were restored and focused checks rerun green. Matrix inventory grew only `ContinuumRevivedAgentContentChecks` 5 → 6; no leg, floor, contrast, baseline, or I5 boundary changed. `CONTINUUM_SKIP_SURFACE_CHECKS=1 ./scripts/run-matrix.sh </dev/null` passed, artifact `qa-runs/20260729T030840Z/app-bundle/manifest.json`; only the runbook-authorized surface-rendering legs were headlessly deferred. Independent Luna medium read-only review initially reported a separator-reset bug, but the cited assignment does reset on every accepted scalar and the executable's future-kind assertions pass; a re-read approved that resolution. Review of the finalized ledger then correctly caught `16 typed payload families` as imprecise; this was corrected to `15 typed built-ins plus opaque fallback`, focused checks reran green, and final review returned `DECISION: APPROVE`, confirming scope, architecture, proof, inventory, and ledger honesty. Honest limits: this ticket defines values only—stable identity reconciliation, mutation validation, parser semantics, kind/payload consistency enforcement, and UI rendering remain later packets; opaque values may retain provider data in desktop memory but no new sync/storage path was added; source ranges are UTF-8 offsets and heading/list domain validation belongs to the parser/reducer tickets. |
| `P1.2-stable-node-identity.md` | done | this commit | 2026-07-29T05:35:00Z | Added `AgentNodeID` with deterministic stable-key children, persisted ordinal reconciliation, collision-safe encoding, validated Codable allocator state, and bounded derived scopes; added document-wide duplicate/path validation plus previous-snapshot validation and strict visible-change revision semantics (status changes count, source-range-only changes do not, overflow fails explicitly). Focused `swift run ContinuumRevivedAgentContentChecks` passed with stable-key/serialization/insertion, two-block streaming paragraph, duplicate paths, malformed previous snapshot, revision, and allocator-boundary assertions. Negative witness against final code: bypassed block duplicate validation; focused check failed `duplicate IDs must fail validation` (exit 1), artifact `tasks/iteration-002/negative-witness/duplicate-validation-bypass.log`; restored source and reran green. `swift build` passed; `CONTINUUM_SKIP_SURFACE_CHECKS=1 ./scripts/run-matrix.sh </dev/null` passed (artifact `qa-runs/20260729T035318Z/app-bundle/manifest.json`), with only the runbook-authorized surface legs headlessly deferred. Inventory changed only `ContinuumRevivedAgentContentChecks` 6 → 7; no gates/floors/contrast/baseline/I5 boundaries changed. Independent Sol medium read-only review required five rounds; all blocking findings were fixed (type-change crash, missing paragraph streaming proof, overflow behavior, child encoding/bounds, allocator Codable validation, previous-snapshot duplicates); final review returned `DECISION: APPROVE`. Honest limits: the child allocator intentionally rejects scopes over 320 UTF-8 bytes and keys over 128 bytes to keep encoded derived IDs within the 512-byte identity bound; future reducer/reconciler tickets still own assignment policy and parser integration.
| `P1.3-mutation-patch-vocabulary.md` | done | this commit | 2026-07-29T04:35:03Z | Added the six typed content mutations, explicit reducer error vocabulary, and validated stable-ID patches that advance document version exactly once, sort IDs deterministically, reject duplicate/contradictory changes, allow move+update, and represent semantic no-ops as empty node changes without inventing UI instructions. `swift run ContinuumRevivedAgentContentChecks` passed with 6 mutation forms, 7 explicit errors, Codable round-trip, Sendable constraints, version/overflow, ordering, duplicate, overlap, and empty-patch assertions; `swift build` passed; `CONTINUUM_SKIP_SURFACE_CHECKS=1 ./scripts/run-matrix.sh </dev/null` passed, artifact `qa-runs/20260729T043315Z/app-bundle/manifest.json`, with only the runbook-authorized surface legs deferred. Negative witness against final code: bypassing patch-ID sorting made focused checks fail `patch IDs must be deterministically sorted while allowing a moved node to update` (exit 1), artifact `tasks/iteration-001/negative-witness/unsorted-patch.log`; restored exact source and reran green. Inventory changed only `ContinuumRevivedAgentContentChecks` 7 → 8; no leg, floor, contrast, baseline, or I5 boundary changed. Independent Luna medium read-only review returned `DECISION: APPROVE`. Honest limits: P1.4 still owns applying mutations, reference/status validation, node revisions, lookup performance, and deriving minimal IDs; patch values are ephemeral list updates rather than persisted Codable document state. |
| `P1.4-document-reducer.md` | done | this commit | 2026-07-29T22:52:48Z | Added reconstructible open/finished entry lifecycle state and the pure indexed `AgentDocumentReducer`, covering all six mutations, explicit invalid transitions, minimal stable-ID patches, revision normalization, opaque future blocks, and bounded repeated upserts without full-document reindexing. `swift run ContinuumRevivedAgentContentChecks` and `swift build` passed. The required negative witness reintroduced a full-index rebuild and failed with 8,012,000 indexed nodes versus the 8,000-node bound; restoration reran green (`~/.pi/agent-tile-ux-runs/continuum-overnight/run-20260729T105023/tasks/iteration-001-P1.4-document-reducer.md/negative-witness.log`). The complete seven-path diff is byte-identical to `candidate-supervised.diff`; independent Luna medium adjudication returned `DECISION: APPROVE` with no blocking findings. Matrix evidence is split and recorded honestly: the first full run reached Component Lab after all prior legs passed, then failed only the known 1×-Main glyph baselines without source/baseline changes (`final-matrix.log`); a Retina-Main retry passed Component Lab and every subsequent leg through `--browser-target-blank-check`, then was interrupted during `--browser-download-check` (`final-matrix-retina.log`). No baseline was changed or blessed, and no UI/AppKit code changed. Inventory changed only `ContinuumRevivedAgentContentChecks` 8 → 9. |
| `P1.5-runtime-event-projection.md` | done | this commit | 2026-07-29T23:29:41Z | Added Core’s provider-neutral `AgentTranscriptProjection`, the one-way Core → AgentContent dependency, six-kind runtime-event mapping, stable provenance, typed command/error state, explicit thread filtering, and assistant/reasoning/turn stream boundaries without exposing raw tool arguments. Registered the semantic projection beside `ManagedAgentTranscriptModel` in the existing nine-script compatibility corpus; exact ordered snapshot parity now runs over both production projections and the divergent stub independently proves comparator liveness. Focused projection and compatibility flags pass; `swift build` passed; `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed with the display-dependent Component Lab baseline comparison loudly deferred to the supervised Retina-Main gate, artifact `qa-runs/20260729T232925Z/app-bundle/manifest.json`. Required final-code mutation witness temporarily mapped reasoning runs to assistant runs and failed `P1.5 assistant/reasoning runs must not merge across kind or turn boundaries`; the exact pre-mutation SHA-256 was restored and both focused checks reran green (`negative-witness-final-projection-complete.log`). Inventory changed only `ContinuumRevivedCoreChecks` 33 → 34. The initial worker correctly blocked on a missing compatibility-harness fence; coordinator commit `53a7aec` added only that directly related file. Complete final diff is byte-identical to the Luna-reviewed candidate; correction-only Luna medium adjudication returned `DECISION: APPROVE` with no remaining blockers. |
| `P1.6-local-user-notice-nodes.md` | done | this commit | 2026-07-29T23:48:55Z | Added explicit semantic APIs for caller-ID-stable local user prompts and Continuum notices without forging provider events. Prompts retain `.user` role and local prompt provenance; notices retain `.system` role, local notice provenance, typed notice bodies, and semantic heading titles. Duplicate prompt/notice IDs are idempotent no-ops, patch sequences are contiguous, compatibility APIs remain, and a seeded provider event history is unchanged by local nodes. Focused local-node checks, AgentContent checks, `swift build`, and `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260729T234842Z/app-bundle/manifest.json`, with display-dependent Component Lab baselines loudly deferred to the supervised Retina-Main gate. The required final-code witness disabled the prompt duplicate-ID guard, observed exit 133 with `duplicateBegin`, restored the exact SHA-256, and reran green (`negative-witness-final-local-nodes.log`). The authoritative inventory guard passes 282 records and records `ContinuumRevivedCoreChecks` 34 → 38. The complete candidate remained byte-identical through final checks. Luna medium correction-only adjudication resolved two evidence-reading errors and returned `DECISION: APPROVE` with no blockers. |
| `P1.7-unknown-node-forward-compat.md` | done | this commit | 2026-07-30T00:05:27Z | Harness-owned completion: focused worker checks, independent opposite-model review, swift build, and final matrix passed. Evidence: /Users/dylan/.pi/agent-tile-ux-runs/continuum-overnight/run-20260729T194934/tasks/iteration-001-P1.7-unknown-node-forward-compat.md. |
| `P1.8-content-diagnostics.md` | done | this commit | 2026-07-30T00:35:29Z | Added body-free Codable semantic-document diagnostics with versions, IDs, kinds, revisions, child/source-length/status summaries, patch counts, open entries, and exact-path invariant issues for duplicate IDs, invalid ranges, missing parents, and backwards revisions. The production AgentContent module exposes no body-bearing diagnostics API, logger, option, SPI, or package escape hatch. Privacy fixtures place prose, a private path, a local prompt, and a sentinel secret into real semantic fields and prove the encoded safe diagnostics omit all four. Focused AgentContent checks and `swift build` passed. Required mutations observed the duplicate-ID and dangling-parent assertions red and the unchecked invalid-range path trap before restoration; worker session evidence is under `tasks/iteration-002-P1.8-content-diagnostics.md/worker-session-*`. The complete diff, including both new files omitted by the original harness review, received Sol medium `DECISION: APPROVE` with no blockers. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed after coordinator commit `98333b4` corrected the flag to defer both display-dependent baseline entry points; artifact `qa-runs/20260730T003515Z/app-bundle/manifest.json`. No display configuration or baseline changed. Inventory changed only `ContinuumRevivedAgentContentChecks` 10 → 11. |
| `P1.9-card-compatibility-projection.md` | done | this commit | 2026-07-30T01:15:02Z | Replaced independent card-array mutation with a temporary one-way `AgentDocument` → `ManagedTranscriptCard` projection while preserving the legacy public APIs and all six card kinds. Rich inline marks flatten to readable labels without link destinations; provider item IDs, titles, concrete item kinds, and exact runtime statuses survive, including completed/failed/declined diff and error cases whose P1 payloads cannot encode status. A frozen pre-P1.9 reducer runs beside the semantic-backed model over all nine P0.5 scripts, including presentation fields, while P1.5’s direct semantic-vs-card capture remains content-only. Required final-code witness removed runtime status preservation, failed exit 1 on exact file-change status loss, restored the exact SHA-256, and reran projection plus compatibility checks green (`negative-witness-final-card-projection.log`). Complete diff includes the new projection file. `swift build` and the post-repair `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed, artifact `qa-runs/20260730T011413Z/app-bundle/manifest.json`; no display or baseline changed. Inventory changed only `ContinuumRevivedCoreChecks` 38 → 40. Luna medium final adjudication returned `DECISION: APPROVE` with no remaining blocker. |
| `P2.1-markdown-parser-seam.md` | done | this commit | 2026-07-30T01:47:44Z | Added the owned `AgentMarkupParsing` seam and exact reviewed `swift-markdown` 0.8.0 pin, with `Markdown` and its AST confined to one unconditional production adapter. Plain CommonMark becomes one stable semantic paragraph; valid 512-byte entry IDs receive deterministic compact child IDs; unsupported nonempty structure remains losslessly opaque with body-free owned diagnostics rather than disappearing. Manifest/import checks enforce the source, exact pin, dependency direction, and adapter-only import. The fence was narrowly expanded and committed first as `35ea98f` because P1.7’s isolated negative-witness package copies the whole AgentContent target and therefore needed the same compile-enforced dependency; no adjacent production or UX scope was added. The required final-code witness appended `!` to AST-derived text, exited 1 at the exact paragraph assertion, restored SHA-256 `6057b0954d01517ba79ced30335317eabad2ee51e53f64de61671f19b9f7c12f`, and reran AgentContent checks green (`negative-witness-final-markdown-parser.log`). Complete diff includes all three new files. `swift build` and `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T014658Z/app-bundle/manifest.json`, with no display or baseline changed. Inventory changed only `ContinuumRevivedAgentContentChecks` 11 → 12. Luna medium source review and final evidence adjudication both returned `DECISION: APPROVE`. |
| `P2.2-inline-markup-runs.md` | done | this commit | 2026-07-30T01:58:31Z | Harness-owned completion: focused worker checks, independent opposite-model review, swift build, and final matrix passed. Evidence: /Users/dylan/.pi/agent-tile-ux-runs/continuum-overnight/run-20260729T214814/tasks/iteration-001-P2.2-inline-markup-runs.md. |
| `P2.3-paragraph-heading-blocks.md` | done | this commit | 2026-07-30T02:15:10Z | Harness-owned completion: focused worker checks, independent opposite-model review, swift build, and final matrix passed. Evidence: /Users/dylan/.pi/agent-tile-ux-runs/continuum-overnight/run-20260729T214814/tasks/iteration-002-P2.3-paragraph-heading-blocks.md. |
| `P2.4-list-quote-rule-blocks.md` | done | this commit | 2026-07-30T02:38:13Z | Harness-owned completion: focused worker checks, independent opposite-model review, swift build, and final matrix passed. Evidence: /Users/dylan/.pi/agent-tile-ux-runs/continuum-overnight/run-20260729T214814/tasks/iteration-003-P2.4-list-quote-rule-blocks.md. |
| `P2.5-fenced-code-blocks.md` | done | this commit | 2026-07-30T03:39:52Z | Added first-class fenced and indented code payloads with normalized non-executable language metadata, explicit streaming completion, and stable identity when an open fence closes. After three autonomous repair rounds exposed exact-source edge cases, supervised recovery replaced ad hoc fence/container parsing with a coherent strategy: swift-markdown parses a normalized-line-ending shadow, cmark owns CommonMark de-indentation/container/tab semantics, and a UTF-8 physical-line map restores original LF/CR/CRLF/mixed endings without inventing an EOF newline. Exact checks cover long/embedded/invalid fences, empty fences, open fences, CR/CRLF/mixed and multiple CR-only blocks, quote/list nesting, allowed fence indentation, tabs/partial content indentation, attributes, and alignment fallback; unexpected alignment preserves semantic code and emits only body-free `markdown.code-source-alignment`. The required final-code witness changed the converted semantic kind to unknown, exited 1 at the exact first-class fenced-code assertion, restored SHA-256 `9f021952a68dc2992a733aa7d6086397a6cbbbda13f215bcd4db9ed3fb40ef28`, and reran AgentContent checks green (`negative-witness-final-fenced-code.log`). Sol medium approved the complete final candidate and evidence. `swift build` and `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T033842Z/app-bundle/manifest.json`, with no display or baseline changed. Inventory count was unchanged because P2.5 extended the existing parser check function. |
| `P2.6-link-policy.md` | done | this commit | 2026-07-30T04:06:58Z | Added semantic inline/autolinks that preserve destination, optional title, and readable label separately, plus a pure provider-neutral activation policy. HTTP(S)/mailto classify external, Continuum links internal, local/file/custom/javascript/data destinations remain visible/copyable but non-activatable, and malformed/whitespace/control/bad-percent destinations reject; no cwd resolution, opening, or authorization occurs in AgentContent, and renderers must recheck policy. Codable round-trip and body-free diagnostics remain intact. The fence was narrowly expanded and committed first as `5bdf0ea` because P2.2 intentionally treated links as unsupported until this ticket; that corpus now retains equally strong unsupported-inline coverage with image syntax. The required check copies exact final `AgentLink.swift`, mutates only file URLs from display-only to external, observes isolated exit 1 at the safety assertion, and leaves production SHA-256 `8560f4a515c9e9c90e6cf2424a5c27f0157c9f657ca8723ebd1ae8ffe4f7b643` unchanged (`negative-witness-final-link-policy.log`). Complete diff includes both new files. Luna medium approved source and final evidence. `swift build` and `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T040612Z/app-bundle/manifest.json`, with no display or baseline changed. Inventory changed only `ContinuumRevivedAgentContentChecks` 12 → 13. |
| `P2.7-partial-streaming-markdown.md` | done | this commit | 2026-07-30T04:26:56Z | Harness-owned completion: focused worker checks, independent opposite-model review, swift build, and final matrix passed. Evidence: /Users/dylan/.pi/agent-tile-ux-runs/continuum-overnight/run-20260730T000730/tasks/iteration-001-P2.7-partial-streaming-markdown.md. |
| `P2.8-ast-identity-reconciliation.md` | done | this commit | 2026-07-30T05:28:11Z | Added a provider-neutral semantic-tree identity reconciler with stable completed-prefix IDs/revisions, compatible open-tail evolution, deterministic collision-safe fresh IDs, recursive inserted-subtree patches, known moves, sorted minimal patch sets, and explicit recoverable revision/allocation overflow errors rather than traps. The append-only streaming contract resolves indistinguishable repeated-node growth by preserving existing occurrence order and allocating fresh tail identities; unique middle insertions cannot steal completed or edited-tail IDs. Real-newline repeated-paragraph/fenced-code fixtures prove recursive convergence for every scalar split and one-scalar growth through this reconciler itself. The required final-code witness redirected compatible matching from the parsed tail to the first sibling, exited 1 at `reconciliation must preserve stable IDs for an edited tail`, restored SHA-256 `38364df7b38266c36494a4566cf84af7734c2916277cf1960e7c3ecad571e04e`, and reran AgentContent checks green (`negative-witness-final-identity-reconciler.log`). Complete diff includes both new files. Sol medium approved source and final evidence. `swift build` and `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T052711Z/app-bundle/manifest.json`, with no display or baseline changed. Inventory changed only `ContinuumRevivedAgentContentChecks` 14 → 15. |
| `P2.9-parser-corpus-fuzz-performance.md` | done | this commit | 2026-07-30T06:37:29Z | Closed the parser phase with three deterministic gates. Ten shared markup fixtures now pass parse→Codable round-trip→plain-text projection with per-fixture ordered preservation of all canonical user Unicode scalars. A 500-seed malformed corpus checks every full normalized source—including suffix words, punctuation, symbols, emoji, combining marks, and multiplicity—for no body loss, duplicate IDs, nondeterminism, or chunk-dependent ASTs across 7,166 chunks. Five independent 5,000-delta coalesced runs each produced 149 parses under the 152 algorithmic ceiling and identical final ASTs; measured sorted samples were 0.057–0.058 s, p50 0.057 s under the 0.25 s headroom floor and worst 0.058 s under 1.0 s. The required negative witness copies and mutates exact production parser source to return empty blocks, executes the ordinary final corpus path (only recursive witness spawning is suppressed), and exits 1 at the normal `prose-plain` whole-body assertion; production parser SHA-256 remained `6ac9f129515c4d07d7b69647379d1df58e3945c9c52228e35fa03f5043684ae3` (`final-parser-gates-and-negative-witness.log`). Complete diff includes all three new check files. Luna medium approved source and final evidence. `swift build` and `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T063631Z/app-bundle/manifest.json`, with no display or baseline changed. Inventory changed only `ContinuumRevivedAgentContentChecks` 15 → 18. |
| `P3.1-renderer-registry.md` | done | this commit | 2026-07-30T07:04:54Z | Added the `@MainActor` semantic block renderer protocol with make/update/measure/accessibility hooks and an actions/tokens/appearance-only context that exposes no supervisor, provider, parser, or document owner. One frozen production registry registers 15 distinct renderer-owned built-in families plus one mandatory privacy-safe unknown fallback; duplicate kinds, renderer reuse, declared-kind mismatches, missing fallback, and late mutation use named errors. The supervisor gate proves all 16 fixture kinds resolve exactly once to unique instances, future kinds share the unknown fallback, and the new AgentTranscript path contains no `AgentBlockKind` switch. Live card migration remains intentionally deferred: P3.2 owns the first reusable registry host and P5.4 owns reversible live-tile wiring, avoiding premature dual visible transcripts. The required final-code witness changed the exact built-in cardinality assertion 16→17, exited 1 at the named fixture failure, restored SHA-256 `3768818cfb1881d71aebf1e86ece9d4ad2fbe3eef2c664894b84bbef5084ed80`, then rebuilt and reran the supervisor check green (`negative-witness-final-renderer-registry.log`). Complete diff includes both new files. Luna medium correction review and final adjudication approved with no blockers. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T070352Z/app-bundle/manifest.json`, with no display or baseline changed. No matrix inventory count changed because the registry assertions extend the existing supervisor leg. |
| `P3.2-reusable-block-host.md` | done | this commit | 2026-07-30T07:35:20Z | Added the reusable `AgentBlockHostView` and width-aware `AgentBlockMeasurementCache`. Same ID/kind revisions update the renderer view in place; ID or kind replacement creates a clean view, clears text/accessibility/hover/selection/disclosure state, and increments reuse identity. Renderer actions are capability-proxied through a host generation so closures retained by detached, kind-superseded, revision-superseded, or reset views become inert rather than invoking stale block actions. Measurement keys include ID, mutable semantic kind, revision, quantized width, appearance, and content-size policy. The geometry gate proves stable view identity, clean text and accessibility replacement, same-ID kind replacement, stale-action invalidation after ID/kind/reset transitions, and six isolated cache dimensions. The loop failed closed after three reviews; coordinator accepted the substantive kind/action findings and applied only the in-fence correction. The first compile-level witness attempt was rejected because it ran an old binary; the valid final-code behavior witness removed kind from both the real key type and construction, built the mutation successfully, then the real geometry gate exited 1 with cache/renderer 5 versus expected 6. It restored exact SHA-256 `67be5b59e6644d74ba84d5f6068810a714f9fe6acc7f98c020e5be243f4d25d6`, and restored build, geometry, and supervisor checks passed (`negative-witness-final-measurement-kind.log`). Luna medium correction review and final adjudication approved with no blockers. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T073415Z/app-bundle/manifest.json`, with no display or baseline changed. No matrix inventory count changed because assertions extend the existing geometry leg. |
| `P3.3-assistant-prose-renderer.md` | done | this commit | 2026-07-30T08:23:49Z | Added a borderless/fill-less `AssistantProseRenderer` for paragraph, heading, list, list-item, and quote families. It traverses only the semantic tree, emits native selectable wrapping text, preserves heading/list accessibility, uses primary-text/tile-body tokens in both appearances, and measures with the same 320pt reading inset used by layout. `AssistantProseView` uses flipped coordinates and a geometry assertion so document rows remain visually top-to-bottom rather than reversing under AppKit’s default coordinate system. Production bootstrap now replaces prose placeholders with the real renderer while all non-prose families remain deferred; default `AgentBlockHostView` checks prove that compiled path. This one-file fence expansion to `AgentBlockRendererRegistry.swift` was coordinator-adjudicated as the sole directly related P3.1 registration point after the harness correctly failed closed; no live-tile file changed. A mistaken nonexistent `--ui-appearance-check` launched the normal app loop and was terminated by PID only; the worker then ran the real `--ui-probe-check` green. The final-code witness changed the real reading inset to zero, built successfully, and the geometry gate exited 1 at the 320pt inset assertion; it restored exact SHA-256 `542137f5c8b960c5350943a89cc01dad590ec27221c95bc1b0bfeb6c40373e29`, then build, geometry, UI-probe, and supervisor checks passed (`negative-witness-final-reading-inset.log`). Luna medium source reviews and final adjudication approved with no blockers. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T082249Z/app-bundle/manifest.json`, with no display or baseline changed. No matrix inventory count changed because checks extend existing geometry/UI-probe legs. |
| `P3.4-user-prompt-renderer.md` | done | this commit | 2026-07-30T08:55:53Z | Added `UserPromptRenderer`: a full-readable-measure, left-aligned semantic user surface with quiet user-message fill, artifact radius, modest vertical inset, selectable multiline prose, no outline/title/status caption, and accessibility-only `You` authorship. The role-aware registry seam chooses it only for user prose and retains `AssistantProseRenderer` for assistant prose. After three bounded passes failed closed on integration, coordinator documented and made only two directly related call-site expansions: `AgentBlockHostView` now threads `AgentEntryRole` through apply/measure with `.assistant` compatibility defaults and replaces views on role change; `AgentBlockMeasurementCache` keys role so identical user/assistant blocks cannot share different heights. Geometry exercises the default production host, proves role-isolated cache entries, full-width 320pt multiline layout, user→assistant replacement, and non-color accessibility distinction. `final-scope-evidence.log` records changed paths and proves zero diff for `ManagedAgentTileNSView.swift` and `TranscriptCardViews.swift`; no live-tile migration occurred. The final-code witness right-aligned the real prose view at 72%, built successfully, and the geometry gate exited 1 at the leading-edge assertion; it restored exact SHA-256 `b219d9fda89fed5b8516d3bae9a340902a83e1eb948725b1bd21b19cf694d680`, then build, geometry, UI-probe, and supervisor checks passed (`negative-witness-final-user-prompt-width.log`). Luna medium source review and second final adjudication approved with no blockers. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T085335Z/app-bundle/manifest.json`, with no display or baseline changed. No matrix inventory count changed because checks extend existing geometry/UI-probe legs. |
| `P3.5-rich-inline-text-renderer.md` | done | this commit | 2026-07-30T09:29:03Z | Added native TextKit `RichInlineTextView` plus `AgentTextStyleResolver` for deterministic nested emphasis/strong/code styling, token-driven theme repaint without reparsing, safe/display-only link ranges, policy re-evaluation at activation, semantic action routing, and exact plain-text plus Markdown pasteboard representations for whole or arbitrary partial UTF-16 selections. Production integration replaces `AssistantProseView`’s temporary flattened labels with semantic rich rows while preserving paragraph/heading/list/list-item/quote traversal, row block IDs, heading typography/accessibility, ordering, and measurement parity; user prompts inherit it through their composed prose renderer. This one directly related `AssistantProseRenderer.swift` fence expansion was coordinator-documented before editing. `final-scope-evidence.log` proves zero diff for both live-tile files and no display/baseline changes. The final-code witness allowed `displayOnly` through the real activation guard, built successfully, and the UI probe exited 1 because the file link activated; it restored exact SHA-256 `890f7d5fc09372e50c1f70a10654789763ea897b2b0754a303157912727f0fd4`, then build, geometry, UI-probe, and supervisor checks passed (`negative-witness-final-unsafe-link.log`). Luna medium source review and final adjudication approved with no blockers. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T092709Z/app-bundle/manifest.json`, with no display or baseline changed. No matrix inventory count changed because checks extend existing UI-probe/geometry legs. |
| `P3.6-code-block-renderer.md` | done | this commit | 2026-07-30T10:01:50Z | Added production `CodeBlockRenderer` and native `CodeTextView`: exact selectable monospaced bytes, horizontal and capped vertical scrolling, subtle language/streaming labels outside code text, borderless accessible copy affordance, no execute action, in-place same-ID streaming revisions with selection preservation, token repainting, and a dependency-free `CodeHighlighting` seam. Highlighter output is sanitized with nonnegative, subtraction-based UTF-16 bounds checks; hostile negative-location, negative-length, one-past-end, and `Int.max` spans are discarded without overflow or text loss. The frozen production registry now resolves `.fencedCode` to the real renderer; this one directly related bootstrap expansion was coordinator-documented before editing. Geometry uses default `AgentBlockHostView`, proves exact indentation/trailing-newline copy, one-character view identity, 81-line dual-axis scrolling at 320pt, accessibility, inert controls, appearance, and production measurement. `final-scope-evidence.log` proves zero diff for live-tile files. The final-code witness changed the real height cap 320→640, built successfully, and the geometry gate exited 1 at measured 640; it restored exact SHA-256 `d8a4cbc25e6c2d7038b6f4c60f169f805a45401fd52ac191f8394903faa305dd`, then build, geometry, UI-probe, and supervisor checks passed (`negative-witness-final-code-height-cap.log`). Luna medium source review approved; final adjudication requested this bookkeeping update only. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T100002Z/app-bundle/manifest.json`, with no display or baseline changed. No matrix inventory count changed because checks extend existing geometry/UI-probe legs. |
| `P3.7-tool-command-renderer.md` | done | this commit | 2026-07-30T10:51:56Z | Added compact stateless production renderers for tool calls and command output: glyph-plus-text statuses, collapsed routine completion, expanded active/failure defaults, safe one-line titles/summaries, no raw tool arguments, exact selectable/copyable capped output, borderless keyboard-accessible disclosure/copy controls, and no semantic agent action for local disclosure. `DisclosureStateStore` keys explicit choices by `(agentID, blockID)` outside the document. To preserve P3.1’s actions/tokens/appearance-only context and global registry, the owner binds disclosure read/write, presentation revision, and layout invalidation capabilities inside `AgentRenderActions`; the host generation-gates every capability, evicts block measurements, and invalidates layout, while cache keys include presentation revision. Geometry toggles a live default host, observes owner invalidation/cache eviction, remeasures/resizes without clipping, and proves recreation/reuse persistence, other-block reset, same-ID cross-agent isolation, stale-view inertness, all six status presentations, exact/capped command output, and raw-argument privacy. P3.10—not this packet—owns the first `AgentTranscriptListView` binding; P5.4 owns live migration, so correction review rejected premature legacy-tile wiring. `final-scope-evidence.log` proves zero live-tile diff. The final-code witness made completed work expanded by default, built successfully, and geometry exited 1 at the routine-collapse assertion; it restored exact SHA-256 `b281146b1932d934211395a623b2cb2979e702ff0874de6d24f101fce4b66755`, then build, geometry, UI-probe, and supervisor checks passed (`negative-witness-final-completed-collapse.log`). Luna medium correction review approved. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T105137Z/app-bundle/manifest.json`, with no display or baseline changed. No matrix inventory count changed because checks extend existing geometry/UI-probe legs. |
| `P3.8-plan-diff-renderers.md` | done | this commit | 2026-07-30T11:56:15Z | Added distinct production plan and diff renderers plus the missing provider-neutral semantic vocabulary. Plans render explicit provider-owned nested steps with ordinal hierarchy and glyph-plus-text status; completed detail collapses passively, while active/pending detail stays visible, selectable, bounded, and read-only. Diffs render only explicit safe summaries/file display names and supplied unsigned counts, cap visible file rows, never parse/display compatibility raw diff text, never read disk, and expose a borderless keyboard-accessible `openDiff(blockID:)` semantic action only when available. Payload extensions retain old JSON decoding defaults; runtime projection carries only an explicit provider title as safe summary and invents no files/counts/steps. The frozen registry replaces only `.plan`/`.diff` placeholders. An isolated Component Lab fixture (not a new autonomous baseline) proves production resolution, distinct list/group accessibility roles, nested ordinals, completed-detail collapse, exact count aggregation, raw-text privacy, action gating after host reuse, bounds, and legacy decoding. `final-scope-evidence.log` proves zero live-tile diff and records the compile-enforced fence adjudication. The final-code witness temporarily displayed raw compatibility diff text, built successfully, and Component Lab exited 1 at the raw-text privacy assertion; exact SHA-256 `76c498041add6932c05d2bf978fd01f7ee37471167fcdd3abdf4870538ce7d99` was restored. Build, AgentContent, projection, geometry, UI-probe, and supervisor checks passed. A later direct Component Lab baseline comparison showed the known environment-dependent glyph drift (22/62 matched, 40 mismatched); no display or baseline changed or was blessed, and both display-dependent entry points remain owed to P3.12. Luna medium source review returned `DECISION: APPROVE`. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T115602Z/app-bundle/manifest.json`. No inventory count changed because checks extend existing Component Lab/projection legs. |
| `P3.9-interactive-error-unknown-renderers.md` | done | this commit | 2026-07-30T12:36:43Z | Completed production coverage for exceptional semantic blocks. Approval and question views render explicit semantic prompts and bounded custom choice buttons only when an unresolved payload carries an opaque provider `requestID`; responses route as request-keyed intents, request-less/resolved history stays readable and passive, and per-view choice generations plus host action generations make old buttons inert after revision, ID, kind, detach, or reuse. `AgentRequestPayload` adds optional request identity with backward decoding. Error views expose selectable bounded message/code plus block-scoped copy and recoverable-only retry; notices share the quiet presentation but never acquire controls. The mandatory fallback moved into its packet file and displays only validated `safeFallbackSummary`/kind; opaque debug labels, values, request-shaped JSON, Markdown checkboxes, sentinels, actions, and buttons remain absent from visible/accessibility/copy surfaces. Production registry replaces only approval/question/error/notice/fallback placeholders. Geometry uses default hosts at 320pt to prove request identity, custom keyboard controls, resolved/request-less passivity, old-button inertness, retry/copy, notice passivity, bounds, six exceptional states, legacy request decoding, and payload-blind unknown fallback. `final-scope-evidence.log` proves zero runtime projection/live-tile diff and documents the narrow compile seams. Final-code mutation displayed opaque `debugLabel`; mutated build succeeded, geometry exited 1 at the sentinel privacy assertion, and exact SHA-256 `de074128e4465c20c0942a5016b36d5ec4559d2374d235e81f1f125ea04d556c` restored before build, AgentContent, geometry, UI-probe, and supervisor checks passed (`negative-witness-final-opaque-privacy.log`). Luna medium source review approved with no blockers. First matrix reached final `git diff --check` and correctly failed on one trailing blank line introduced when moving the fallback; that whitespace was removed and the complete matrix reran green with autonomous display deferral, artifact `qa-runs/20260730T123631Z/app-bundle/manifest.json`. No display/baseline changed or was blessed; inventory count unchanged. |
| `P3.10-transcript-collection-list.md` | done | this commit | 2026-07-30T13:19:54Z | Harness-owned completion: focused worker checks, independent opposite-model review, swift build, and final matrix passed. Evidence: /Users/dylan/.pi/agent-tile-ux-runs/continuum-overnight/run-20260730T083823/tasks/iteration-001-P3.10-transcript-collection-list.md. |
| `P3.11-incremental-scroll-copy-accessibility.md` | done | this commit | 2026-07-30T14:23:43Z | Added a main-thread 30 Hz visual scheduler with synchronous final/detach flush, measured near-bottom policy, semantic top-ID/offset anchoring, selection-safe stationary updates, custom Jump to latest, responder-reachable block copy, and explicit ordered accessibility hosts. Every queued document/patch pair and producer version edge is validated before scheduling; visual coalescing retains only the latest valid semantic snapshot, compares it against last-painted stable IDs/roles/blocks, and invalidates only actually changed top-level rows—never fabricating a multi-version `AgentDocumentPatch` or remeasuring history. Reader insertion above preserves even a viewport starting in row spacing; active native transcript text selection prevents both bottom sticking and anchor restoration. Plain/Markdown copy avoids opaque/raw diff data, preserves nested plans and safe diff summaries, and matches native inline soft/hard-break, escaping, title, code-backtick, and fenced-newline semantics. Accessibility returns actual materialized block hosts in collection/document order and appends the visible Jump action, so host-owned headings, links, code, disclosure, and actions remain traversable. The loop stopped correctly after three reviews; coordinator recovery fixed the remaining patch-contract, history-wide invalidation, and absent-proof blockers. Geometry now exercises 5,000 valid sequential deltas as exactly 1 visual apply, 1 invalidated row, and bounded layout work; invalid-pair rejection; actual insert-above anchoring; real selected `NSTextView` stationarity; actual accessibility child order; Jump; and selected-block pasteboard formats. `final-scope-evidence.log` proves protected live/runtime paths unchanged. Final-code fault injection flushed every delta and exited 1 at `5000 visual applies`; the same SHA-256 `baf2116beef812bd4486fc019654f93070a21564adf37a09888d27a945ac70fc` then passed geometry, build, UI-probe, and supervisor checks (`negative-witness-final-visual-coalescing.log`). Sol medium correction review approved with no blockers. `CONTINUUM_SKIP_SURFACE_CHECKS=1 CONTINUUM_SKIP_UI_BASELINES=1 ./scripts/run-matrix.sh </dev/null` passed; artifact `qa-runs/20260730T142327Z/app-bundle/manifest.json`. No display/baseline changed or was blessed; inventory count unchanged. |
| `P3.12-transcript-supervised-review.md` | done | this commit | 2026-07-30T22:55:21Z | Added an isolated production-path semantic transcript review surface with mixed, long, active-tool, failed-tool, approval, and 320/480/640/900 pt states in both appearances. The first owner review rejected legacy shell concerns rather than the semantic architecture: harsh sidebar/inbox perimeter treatment and weak selection, raw composer/dropdowns, and an unclear needs-attention path. Those corrections are now locked into the existing P4.1/P4.7/P4.8/P4.10 and P5.1–P5.5 packets without a second program. Within P3.12, review evidence exposed and corrected only directly related presentation defects: multiline provider summaries/prompts wrap, statuses no longer ellipsize, active plan detail remains below its title, and the realistic provider request shows context plus equal-emphasis custom `Allow once`/`Decline` buttons without inferring recommendation from provider order or inventing Continuum policy. The owner then clarified composer scope and explicitly instructed `let's continue the loop pls with the same 15 min wakeup schedule`, approving passage of this supervised gate. On the unchanged Retina Main at 2×, three consecutive 16-shot transcript sets were byte-identical (`qa-runs/2026-07-30T224450Z/tour`, `...224457Z/tour`, `...224503Z/tour`); exactly those 16 approved images were copied to the queue-91 baseline directory, with no queue-90 baseline or display change. Required witness changed `Allow once` to `Allow`; Component Lab exited 1 at the exact contextual custom-request assertion, then restored `ComponentLab.swift` SHA-256 `b497d9b6effad85a1c21d0641bef8cb7548b4aa98e8d5ee269e01f690017ca08`. Final `swift build`, Component Lab 62/62, geometry, probe/appearance, pixel, contrast, supervisor, direct baseline 62/62, program guard, `git diff --check`, and matrix passed; matrix artifact `qa-runs/20260730T225251Z/app-bundle/manifest.json`. Protected live-tile/runtime paths remained at zero diff. Final Luna medium read-only review found no blocker and returned `DECISION: APPROVE`; artifact `.pi/agent-runs/luna-code-reviewer-20260730T225349Z-1aaebc/final.md`. |
| `P4.1-custom-composer-shell.md` | done | this commit | 2026-07-30T23:46:14Z | Added isolated `AgentComposerView` custom chrome over a native TextKit `ComposerTextView`: no Aqua bezel/background or popup, semantic composer fill and quiet idle boundary, explicit focus ring, placeholder, native text-area accessibility, selection, paste, undo, and a view-owned text/selection/revision binding. The document receives nonzero width/height, measures multiline TextKit content, caps the shell at eight visible lines, and retains internal scrolling; P4.2 still owns exhaustive width-sensitive growth behavior. UIProbe covers empty/focus/selection in both appearances, draft normalization/publishing, native paste-only undo, document overflow, and an executable stock-scroll-chrome witness rejected by the production assertion. Bounded run `run-20260730T190006` used Sol/Luna medium: Luna round 1 corrected undo evidence and missing witness; round 2 found zero-sized scrolling and the absent Component Lab fixture; worker pass 3 repaired geometry then correctly blocked because `ComponentLab.swift` was out of fence. Coordinator documented the narrow expansion before adding only `agent.composer.review` as a baseline-excluded `reviewSurface` plus deterministic self-check. No live tile, queue-90 baseline, provider, controls, or P4.3 key policy changed. Final `swift build`, `--ui-probe-check` (72 composer assertions), `--component-lab-check` (62/62 legacy baselines), `--agent-supervisor-check`, program guard, `git diff --check`, and matrix passed; artifact `qa-runs/20260730T234440Z/app-bundle/manifest.json`. Protected live-tile/runtime paths remained unchanged. Final Luna medium read-only review found no blocker and returned `DECISION: APPROVE`; artifact `.pi/agent-runs/luna-code-reviewer-20260730T233708Z-c53669/final.md`. Final source SHA-256: `AgentComposerView.swift` `4536134af3f31db05c732a91b7854495efd94f07996e80f57efee13d26ccd3b5`, `ComposerTextView.swift` `c5d26051b17485804bcc46d3a2c47cdf5c63ed712230a4da59ddd155fcb2b783`. |
| `P4.2-growing-text-layout.md` | pending | — | — | — |
| `P4.3-key-ime-undo-contract.md` | pending | — | — | — |
| `P4.4-per-agent-draft-store.md` | pending | — | — | — |
| `P4.5-prompt-history.md` | pending | — | — | — |
| `P4.6-send-stop-intent-state.md` | pending | — | — | — |
| `P4.7-custom-choice-popover.md` | pending | — | — | — |
| `P4.8-model-effort-controls.md` | pending | — | — | — |
| `P4.9-completion-query-providers.md` | pending | — | — | — |
| `P4.10-composer-supervised-review.md` | pending | — | — | — |
| `P5.1-agent-tile-header-shell.md` | pending | — | — | — |
| `P5.2-capability-driven-turn-states.md` | pending | — | — | — |
| `P5.3-provider-current-work-projection.md` | pending | — | — | — |
| `P5.4-live-tile-migration.md` | pending | — | — | — |
| `P5.5-final-supervised-acceptance.md` | pending | — | — | — |
