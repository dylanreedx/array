Walking the rollover failure end to end, one paragraph per shard generation, because a
streamed message of this length is the only honest way to measure delta coalescing.

Generation 119 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 120 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 121 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 122 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 123 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 124 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 125 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 126 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 127 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 128 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 129 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 130 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 131 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 132 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 133 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 134 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 135 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 136 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 137 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 138 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 139 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 140 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 141 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 142 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 143 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 144 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 145 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 146 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 147 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 148 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 149 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 150 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 151 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 152 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 153 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 154 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 155 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 156 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 157 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.

Generation 158 rolled after the open segment passed its byte budget, and the reader held the previous segment open until the new one was durable. The ingest queue and the retention timer both observed the roll, which is exactly the window where the duplicate generation appears once the two callers interleave.
