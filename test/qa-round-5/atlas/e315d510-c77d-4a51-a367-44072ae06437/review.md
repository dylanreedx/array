Visual review N/A: this task adds the in-process QA screenshot capture seam and verifies it by running the AppKit smoke harness with CONTINUUM_QA_CAPTURE enabled.

The captured PNGs were sanity-checked as evidence artifacts for the default-smoke flow, not calibrated as a product fixture score.
