Visual review: N/A for calibrated J= because this task fixes Cmd-K palette memory lifetime and uses the palette-leak-check perf gate rather than a deterministic visual scoring fixture.

The captured palette-leak-check PNGs were inspected as visual sanity artifacts for readable palette text, clipping, and overlap after replacing the NSPanel with an in-window palette view.
