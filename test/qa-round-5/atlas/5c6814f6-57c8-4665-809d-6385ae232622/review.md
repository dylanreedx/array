# Visual Review

N/A for this ticket. The target behavior is modifier-only AppKit event forwarding through Ghostty's key path, verified by the app smoke binary instead of calibrated screenshot scoring.
