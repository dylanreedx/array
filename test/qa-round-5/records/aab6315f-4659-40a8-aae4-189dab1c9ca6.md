## TL;DR
Verdict: pass
- Gate: GREEN `node scripts/check-package-targets.js`, GREEN `swift run ContinuumRevivedPerfChecks`, and review.md visual review N/A.

## What is broken
- The current package already registers `ContinuumRevivedPerfChecks`, but there was no dedicated guard that would fail when a future `Sources/*Checks/main.swift` directory is not registered as an executable target. Package.swift:40 scripts/check-package-targets.js:41
- The perf check adds unique coverage for required perf baseline keys, percentile interpolation, and forced finding title shape that `ContinuumRevivedCoreChecks` does not cover. Sources/ContinuumRevivedPerfChecks/main.swift:62 Sources/ContinuumRevivedPerfChecks/main.swift:66 Sources/ContinuumRevivedPerfChecks/main.swift:69

## Why
- The original perf harness commit added `Sources/ContinuumRevivedPerfChecks/main.swift` and the matching manifest target, so deleting the target file would remove active perf-baseline coverage rather than dead code. Sources/ContinuumRevivedPerfChecks/main.swift:58 Package.swift:40
- `ContinuumRevivedCoreChecks` covers core model and store behavior, while perf baseline schema and finding semantics live in the separate perf executable. Sources/ContinuumRevivedCoreChecks/main.swift:1 Sources/ContinuumRevivedPerfChecks/main.swift:64
- The package already treats executable checks as the regression suite, so the missing layer was a manifest/source drift check instead of a new XCTest target. Package.swift:30 scripts/check-package-targets.js:30

## Fix
- Added `scripts/check-package-targets.js` to scan `Package.swift` executable targets and `Sources/*Checks/main.swift` directories in both directions. scripts/check-package-targets.js:23 scripts/check-package-targets.js:30 scripts/check-package-targets.js:46
- The new gate fails when a check directory lacks `main.swift`, when a check directory lacks a matching executable target, or when the manifest registers a missing check executable. scripts/check-package-targets.js:38 scripts/check-package-targets.js:41 scripts/check-package-targets.js:51
- Kept `ContinuumRevivedPerfChecks` registered because the manifest target builds and runs the existing perf-baseline checks. Package.swift:40 Sources/ContinuumRevivedPerfChecks/main.swift:73

## Proof
- RED: `node scripts/check-package-targets.js` failed before the fix with `Cannot find module '/Users/dylan/Documents/personal/continuum-revived/scripts/check-package-targets.js'`.
- GREEN: `node scripts/check-package-targets.js` passed after the fix with `Package executable check targets passed`.
- GREEN: `./scripts/prepare-ghosttykit.sh` completed and linked `ThirdParty/GhosttyKit.xcframework`.
- GREEN: `swift build` completed with `Build complete!`.
- GREEN: `swift run ContinuumRevivedCoreChecks` completed with `ContinuumRevivedCoreChecks passed`.
- GREEN: `swift run ContinuumRevivedPaletteChecks` exited 0.
- GREEN: `swift run ContinuumRevivedPerfChecks` completed with `ContinuumRevivedPerfChecks passed`.
- GREEN: `CONTINUUM_SMOKE_TEST=1 .build/debug/continuum-revived` completed with the full smoke pass line including text, key, scroll, modifier, ime, persistence, canvas, multiTerminal, browser, midExit, note, and file.
- Visual review: N/A for calibrated J= because this task changes a package drift gate rather than UI rendering, recorded in test/qa-round-5/atlas/aab6315f-4659-40a8-aae4-189dab1c9ca6/review.md.
- Target fixture: test/qa-round-5/fixtures/fixture-file-text.
- Cross fixture: test/qa-round-5/fixtures/fixture-note-text.
- Visual sanity: test/qa-round-5/atlas/aab6315f-4659-40a8-aae4-189dab1c9ca6/fixture-package-targets/full-page.png and test/qa-round-5/atlas/aab6315f-4659-40a8-aae4-189dab1c9ca6/fixture-package-targets/bands/01.png.
- Visual sanity: test/qa-round-5/atlas/aab6315f-4659-40a8-aae4-189dab1c9ca6/fixture-package-targets/compare/01.png preserves a second atlas PNG reference for the N/A package gate.
- Readability: the cited atlas full-page and band captures retain readable text with no clipping and no overlap, while this package-target fix itself has no rendered UI.

## Gotchas
- Similar drift can recur for any future executable check directory under `Sources`, because SwiftPM only runs a check by name after the manifest exposes a matching executable target. scripts/check-package-targets.js:30 Package.swift:32
- The domain knowledge gap is that this project uses executable check targets as its regression suite, so `swift test` target discovery is not the source of truth for these checks. Package.swift:30 Sources/ContinuumRevivedCoreChecks/main.swift:5
- The rejected alternative was deleting `Sources/ContinuumRevivedPerfChecks`, because the file still verifies perf baseline schema, percentile math, and finding output semantics. Sources/ContinuumRevivedPerfChecks/main.swift:64 Sources/ContinuumRevivedPerfChecks/main.swift:66 Sources/ContinuumRevivedPerfChecks/main.swift:68
