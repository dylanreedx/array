## TL;DR
Verdict: pass
- Gate: GREEN `swift run ContinuumRevivedFileTreeChecks` and `swift build`; review.md visual review is N/A for this headless AppKit wiring pass.

## What is broken
- Persisted `.fileTree` tiles restored as descriptor placeholders, so the seeded smoke file-tree tile did not install a real file-tree outline view. Sources/ContinuumRevived/App/ContinuumApp.swift:163
- The app had scanner and view-model infrastructure, but no `FileTreeTileNSView` with an `NSSearchField`, `NSOutlineView`, loading state, empty state, error state, file spawn action, or preferred-editor context menu. Sources/ContinuumRevived/Canvas/FileTreeViewModel.swift:7

## Why
- The boot path delegated `.note` and `.file` tiles through `TileSpawner`, while `.fileTree` still bypassed `TileSpawner` and installed `DescriptorTileNSView`. Sources/ContinuumRevived/App/ContinuumApp.swift:419
- The view model previously swallowed scanner errors, so the UI layer had no data-layer signal for the required directory-read error state. Sources/ContinuumRevived/Canvas/FileTreeViewModel.swift:39
- The flat `FileTreeSnapshot.nodes` list needed a tree adapter before AppKit could render parent and child rows or keep ancestors visible during search. Sources/ContinuumRevived/Canvas/FileTreeOutlineModel.swift:18

## Fix
- Added `FileTreeTileNSView` with a 28pt monospaced search header, scroll-wrapped `NSOutlineView`, loading, empty, and error state swapping, 200ms search persistence, selection persistence, expansion persistence, double-click or Return file spawning, and the `Open in Preferred Editor` menu action. Sources/ContinuumRevived/Canvas/FileTreeTileNSView.swift:29 Sources/ContinuumRevived/Canvas/FileTreeTileNSView.swift:103 Sources/ContinuumRevived/Canvas/FileTreeTileNSView.swift:112 Sources/ContinuumRevived/Canvas/FileTreeTileNSView.swift:127
- Added `FileTreeOutlineModel` so filtering is tested outside AppKit and keeps ancestor directories for matched descendant files. Sources/ContinuumRevived/Canvas/FileTreeOutlineModel.swift:56 Sources/ContinuumRevivedFileTreeChecks/main.swift:60
- Added view-model error propagation and covered it with the file-tree executable check, which keeps the error state driven by scanner results instead of display-layer guessing. Sources/ContinuumRevived/Canvas/FileTreeViewModel.swift:8 Sources/ContinuumRevivedFileTreeChecks/main.swift:87
- Routed file-tree restore through `TileSpawner`, persisted `FileTreeTile` edits into `FileTreeState`, spawned normal file tiles from file rows, and launched the preferred editor before falling back to `NSWorkspace.shared.open`. Sources/ContinuumRevived/App/TileSpawner.swift:525 Sources/ContinuumRevived/App/TileSpawner.swift:538 Sources/ContinuumRevived/App/TileSpawner.swift:541 Sources/ContinuumRevived/App/TileSpawner.swift:550

## Proof
- RED: `swift run ContinuumRevivedFileTreeChecks` failed after adding the regression because `FileTreeOutlineModel` was not visible to the helper target.
- GREEN: `swift run ContinuumRevivedFileTreeChecks` passed after the helper target included `FileTreeOutlineModel` and the implementation covered filter and error branches.
- GREEN: `swift build` passed after `FileTreeTileNSView`, `TileSpawner`, and boot-loop wiring compiled into the app target.
- Visual review: test/qa-round-5/atlas/279e958b-73fe-4437-9d15-27f69d5e8616/review.md records N/A for calibrated J= because this pass was verified through headless AppKit build and executable seams.
- Target fixture: test/qa-round-5/fixtures/fixture-file-text.
- Cross fixture: test/qa-round-5/fixtures/fixture-empty-canvas.
- Visual sanity: test/qa-round-5/atlas/279e958b-73fe-4437-9d15-27f69d5e8616/fixture-file-tree-tile/full-page.png and test/qa-round-5/atlas/279e958b-73fe-4437-9d15-27f69d5e8616/fixture-file-tree-tile/bands/01.png.
- Readability: the tile uses a monospaced 13pt font for readable text in the search field and row labels, truncates row text instead of clipping, and separates the search header, outline, and centered state view to avoid overlap. Sources/ContinuumRevived/Canvas/FileTreeTileNSView.swift:141 Sources/ContinuumRevived/Canvas/FileTreeTileNSView.swift:151 Sources/ContinuumRevived/Canvas/FileTreeTileNSView.swift:186

## Gotchas
- A similar restore gap can recur for any tile kind that has a real `TileSpawner` install method but a boot-loop arm still installs `DescriptorTileNSView`. Sources/ContinuumRevived/App/ContinuumApp.swift:153 Sources/ContinuumRevived/App/TileSpawner.swift:525
- The domain knowledge gap is that file-tree search must filter the latest in-memory snapshot while preserving persisted expansion separately, so search should not mutate `expandedPaths` just to reveal matches. docs/superpowers/specs/2026-05-09-file-tree-tile-design.md:152 Sources/ContinuumRevived/Canvas/FileTreeTileNSView.swift:218
- One alternative was to build filtering directly inside `NSOutlineViewDataSource`, but that was rejected because the ancestor-retention behavior belongs in a pure adapter with executable coverage. Sources/ContinuumRevived/Canvas/FileTreeOutlineModel.swift:14 Sources/ContinuumRevivedFileTreeChecks/main.swift:57
