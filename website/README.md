# Array website

Astro 7 + Tailwind CSS 4 marketing site for Array.

## Development

```sh
pnpm install
pnpm dev
```

The local site runs at `http://localhost:4321`.

## Production build

```sh
pnpm build
pnpm preview
```

Static output is written to `dist/`.
